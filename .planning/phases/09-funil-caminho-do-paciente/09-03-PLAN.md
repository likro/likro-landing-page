---
phase: 09-funil-caminho-do-paciente
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified:
  - src/app/page.tsx
  - tests/landing/coherence.test.ts
  - src/sections/HowItWorks/index.tsx
  - src/sections/HowItWorks/HowItWorksHeader.tsx
  - src/sections/HowItWorks/HowItWorksStep.tsx
  - src/sections/HowItWorks/HowItWorksMiniMockup.tsx
  - src/content/how-it-works.ts
  - tests/content/how-it-works.test.ts
  - tests/sections/how-it-works-invariants.test.ts
autonomous: false
requirements: [FUNIL-06, FUNIL-01]
must_haves:
  truths:
    - "page.tsx imports Funnel (not HowItWorks) and renders it wrapped in TrackSection section=\"funnel\" between Product and Proof"
    - "HowItWorks is fully gone: section dir, content module, and its 2 test files deleted"
    - "coherence.test.ts asserts the order Hero → Pain → Bridge → Product → Funnel → Proof and FAILS (not skips) if it regresses"
    - "coherence.test.ts no longer bans @/components/motion for the Funnel dir (Funnel legitimately uses primitives)"
    - "Full suite + build are green after the swap"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Funnel wired between Product and Proof; HowItWorks removed"
      contains: "section=\"funnel\""
    - path: "tests/landing/coherence.test.ts"
      provides: "Updated order gate (Product → Funnel → Proof)"
      contains: "Funnel"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/sections/Funnel/index.tsx"
      via: "import { Funnel } + <TrackSection section=\"funnel\">"
      pattern: "from \"@/sections/Funnel\""
    - from: "tests/landing/coherence.test.ts"
      to: "page.tsx section order"
      via: "order regex assertion"
      pattern: "<Product[\\s\\S]*?<Funnel[\\s\\S]*?<Proof"
---

<objective>
Swap HowItWorks out for the Funnel: wire `<Funnel>` into page.tsx (TrackSection parity), update the coherence test to the new order AND stop it from banning the motion primitives Funnel legitimately uses, then delete HowItWorks (section + content + its 2 test files). Build-safe ordering ensures the page never has a dangling import.

Purpose: FUNIL-06 — there must not be two similar sequential sections; HowItWorks is absorbed by the Funil chapter. The page's dark-bookend rhythm places Funil (dark) where HowItWorks (light) was, between Product and Proof.

Output: page.tsx rewired, coherence.test.ts updated, HowItWorks fully removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-funil-caminho-do-paciente/09-CONTEXT.md
@.planning/phases/09-funil-caminho-do-paciente/09-RESEARCH.md
@src/app/page.tsx
@tests/landing/coherence.test.ts

<interfaces>
<!-- Exact current state the executor edits. -->

page.tsx currently (lines 7, 31-33): imports `{ HowItWorks } from "@/sections/HowItWorks"` and renders
`<TrackSection section="how"><HowItWorks /></TrackSection>` between the Product block (28-30) and Proof block (34-36).
Replace with `import { Funnel } from "@/sections/Funnel"` and `<TrackSection section="funnel"><Funnel /></TrackSection>` in the SAME position. The Funnel section is a named export — confirm `src/sections/Funnel/index.tsx` exports `Funnel` (named export, matching the other sections).

coherence.test.ts current state:
- line 21: `const SECTION_NAMES = ["Pain", "Bridge", "Product", "HowItWorks", "Proof"] as const;`
- Test 1 (line 53-89): import-order, builds `expected = ["Hero", ...SECTION_NAMES]`, skipIf(!allSectionDirsExist()).
- Test 2 (line 94-105): JSX-order regex `/<Hero[\s\S]*?<Pain[\s\S]*?<Bridge[\s\S]*?<Product[\s\S]*?<HowItWorks[\s\S]*?<Proof/`, skipIf(!allSectionDirsExist()).
- Test 3 (line 110): zero `priority` in section files — KEEP (Funnel has no priority either).
- **Test 4 (line 136-156): bans `@/components/motion` imports in ALL section dirs.** ← THIS IS THE TRAP. Funnel REQUIRES `@/components/motion` (it uses the frozen primitives by design — FUNIL-01). If Funnel is added to SECTION_NAMES naively, Test 4 fails. Must scope Test 4 to exclude Funnel.
- `allSectionDirsExist()` (line 41) checks every SECTION_DIRS entry exists → after deleting HowItWorks/, if HowItWorks is still in SECTION_NAMES the order tests SKIP (false green — RESEARCH Pitfall 6).

Files to DELETE (RESEARCH inventory, VERIFIED):
- `src/sections/HowItWorks/` entire dir: index.tsx, HowItWorksHeader.tsx, HowItWorksStep.tsx, HowItWorksMiniMockup.tsx
- `src/content/how-it-works.ts`
- `tests/content/how-it-works.test.ts`
- `tests/sections/how-it-works-invariants.test.ts`

Reference-only stale comments (OPTIONAL cosmetic, non-breaking — update if convenient):
- src/sections/Proof/ProofBackground.tsx:4,15 and Proof/index.tsx:7 ("flui com HowItWorks acima")
- src/components/analytics/TrackSection.tsx:8 (example list)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Funnel into page.tsx (replace HowItWorks block) + update coherence.test.ts</name>
  <files>src/app/page.tsx, tests/landing/coherence.test.ts</files>
  <read_first>
    - src/app/page.tsx (current import line 7 + render block lines 31-33)
    - tests/landing/coherence.test.ts (SECTION_NAMES line 21, Test 2 regex line 98, Test 4 line 136-156)
    - src/sections/Funnel/index.tsx (confirm named export `Funnel`)
    - .planning/phases/09-funil-caminho-do-paciente/09-RESEARCH.md (§Runtime State Inventory — build-safe ordering)
  </read_first>
  <action>
    Do these edits BEFORE deleting anything (Task 2), so the build never has a dangling import.

    **page.tsx:**
    - Replace `import { HowItWorks } from "@/sections/HowItWorks";` (line 7) with `import { Funnel } from "@/sections/Funnel";`.
    - Replace the block
      ```tsx
      <TrackSection section="how">
        <HowItWorks />
      </TrackSection>
      ```
      with
      ```tsx
      <TrackSection section="funnel">
        <Funnel />
      </TrackSection>
      ```
      Keep its position between the Product block and the Proof block (Product → Funnel → Proof). Do NOT wrap Funnel in its own TrackSection inside the section — TrackSection stays in page.tsx (analytics parity with the other sections; the section component does NOT self-track).

    **tests/landing/coherence.test.ts:**
    - Line 21: change `SECTION_NAMES` to `["Pain", "Bridge", "Product", "Funnel", "Proof"] as const;` (drop HowItWorks, add Funnel in its position). This makes `allSectionDirsExist()` check the NEW set, so the order tests run (not skip) after HowItWorks/ is deleted.
    - Test 1 import-order: builds `expected` from SECTION_NAMES automatically — no change needed beyond line 21 (it will now expect `@/sections/Funnel`). Update the test `it(...)` title string to `Hero → Pain → Bridge → Product → Funnel → Proof`.
    - Test 2 JSX-order regex (line 98): change to `/<Hero[\s\S]*?<Pain[\s\S]*?<Bridge[\s\S]*?<Product[\s\S]*?<Funnel[\s\S]*?<Proof/` and update its title + error message to the new order.
    - **Test 4 (motion-import ban) — scope it to exclude Funnel.** The Funnel section legitimately imports `@/components/motion` (FUNIL-01: it MUST use the frozen primitives). The original narrative sections (Pain/Bridge/Product/Proof) still must NOT import motion libs (they use useInView + CSS keyframes per NARR-06). Fix: in Test 4, build the file list from the original narrative dirs only — e.g. `const NARRATIVE_NO_MOTION = SECTION_NAMES.filter((s) => s !== "Funnel")` and walk those dirs, OR keep a separate explicit const `["Pain","Bridge","Product","Proof"]` for this test. Add a code comment explaining: "Funnel is excluded — it consumes the Phase 2 motion primitives via @/components/motion by design (FUNIL-01); its motion-import discipline is gated by tests/sections/funnel-invariants.test.ts instead." Keep the regex and the rest of Test 4 unchanged for the narrative-four.
    - Update the file's top JSDoc to mention the order is now `Hero → Pain → Bridge → Product → Funnel → Proof` and that Funnel is the dark chapter replacing HowItWorks.
  </action>
  <verify>
    <automated>npx vitest run tests/landing/coherence.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "from \"@/sections/Funnel\"" src/app/page.tsx` matches; `grep -n "HowItWorks" src/app/page.tsx` returns nothing
    - `grep -n "section=\"funnel\"" src/app/page.tsx` matches; `grep -n "section=\"how\"" src/app/page.tsx` returns nothing
    - `grep -n "\"Funnel\"" tests/landing/coherence.test.ts` shows Funnel in SECTION_NAMES
    - `grep -n "<Product\[" tests/landing/coherence.test.ts` shows the regex now references `<Funnel`
    - Test 4 in coherence.test.ts walks only Pain/Bridge/Product/Proof (Funnel excluded with an explanatory comment)
    - `npx vitest run tests/landing/coherence.test.ts` exits 0 (the order tests RUN, not skip, and pass); `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>page.tsx renders Funnel between Product and Proof with TrackSection parity; coherence test enforces the new order and no longer bans Funnel's primitive imports.</done>
</task>

<task type="auto">
  <name>Task 2: Delete HowItWorks — section dir, content module, and its 2 test files</name>
  <files>src/sections/HowItWorks/index.tsx, src/sections/HowItWorks/HowItWorksHeader.tsx, src/sections/HowItWorks/HowItWorksStep.tsx, src/sections/HowItWorks/HowItWorksMiniMockup.tsx, src/content/how-it-works.ts, tests/content/how-it-works.test.ts, tests/sections/how-it-works-invariants.test.ts</files>
  <read_first>
    - .planning/phases/09-funil-caminho-do-paciente/09-RESEARCH.md (§Runtime State Inventory — files to DELETE, build-safe ordering)
    - src/app/page.tsx (confirm Task 1 already removed all HowItWorks references — must be done first)
  </read_first>
  <action>
    Now that page.tsx no longer imports HowItWorks (Task 1), delete the absorbed module entirely (CONTEXT D-8). Delete:
    - `src/sections/HowItWorks/index.tsx`
    - `src/sections/HowItWorks/HowItWorksHeader.tsx`
    - `src/sections/HowItWorks/HowItWorksStep.tsx`
    - `src/sections/HowItWorks/HowItWorksMiniMockup.tsx`
    - the now-empty `src/sections/HowItWorks/` directory itself
    - `src/content/how-it-works.ts`
    - `tests/content/how-it-works.test.ts` (gates the deleted content module — RESEARCH)
    - `tests/sections/how-it-works-invariants.test.ts` (gates the deleted dir — RESEARCH)

    Use `git rm` (or filesystem delete + stage) for all 8 items. After deletion, search for any remaining references to `HowItWorks` or `how-it-works` across `src/` and `tests/` — there must be none that break the build/tests (the stale JSDoc comments in Proof/TrackSection are non-breaking prose; OPTIONAL: update them to say "Funnel" instead of "HowItWorks" for cleanliness, but they do not block).

    Run the full suite + build to confirm nothing dangles. The `"how"` member of the `WhatsAppLocation` union (src/lib/whatsapp.ts) is now unused but HARMLESS — leave it (removing it is optional cleanup out of scope; the Funil adds NO CTA per CONTEXT D-2, so do NOT add a `"funnel"` location).
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/sections/HowItWorks` fails (dir removed)
    - `ls src/content/how-it-works.ts` fails
    - `ls tests/content/how-it-works.test.ts tests/sections/how-it-works-invariants.test.ts` both fail
    - `grep -rn "HowItWorks\|how-it-works" src/ tests/` returns nothing build/test-breaking (only acceptable: none, or already-updated prose comments)
    - `npm test` exits 0 (full suite green, including coherence with the new order RUNNING not skipping)
    - `npm run build` succeeds
  </acceptance_criteria>
  <done>HowItWorks is fully absorbed/removed (8 items deleted); the full suite + build are green with Funnel in its place.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Lenny visual verification — traversal, climax, reduced-motion, mobile</name>
  <files>src/sections/Funnel/index.tsx</files>
  <action>Claude FIRST runs the mandatory pipeline from CLAUDE.md before handing to Lenny: (1) invoke the `requesting-code-review` skill / code-reviewer on the Phase 9 diff and fix any real issue; (2) use Playwright MCP to drive the dev server through the Funnel section (desktop @1536×730 scroll 0→100%, OS reduced-motion toggle, mobile ≤639px viewport), checking the browser console for JS errors and screenshotting the traversal/climax/reduced/mobile states. THEN present the results to Lenny and pause for his confirmation against the criteria in &lt;how-to-verify&gt;. This task gates phase completion (FUNIL-01/03/04/05 are scroll-driven and not headlessly assertable).</action>
  <what-built>
    The Funnel chapter is live in the page (Product → Funnel → Proof), HowItWorks removed. Desktop traversal (Marina walking 4 columns, neutral active lighting, purple climax), mobile rail + focused card (~420svh), and the reduced-motion pre-assembled final state are all implemented. The full automated suite + build are green.
  </what-built>
  <how-to-verify>
    Claude runs the pipeline FIRST (per CLAUDE.md): code review on the diff, then Playwright MCP. Then Lenny confirms:
    1. Run dev server; scroll through the Funnel section on desktop @1536×730. Confirm: Marina's card walks left→right through "Chegou agora" → "Em atendimento" → "Escolhendo horário" → "Consulta marcada"; the active column lights (neutral white, NO purple); ghost cards stay dimmed behind; the beat text swaps at each step; it reads as "uma pessoa caminhando", matching the approved prototype rhythm.
    2. Confirm the climax: arrival in column 4 ignites purple (border + glow + card ring + confirmation tag "Consulta confirmada · quinta, 14h" + Check seal). Confirm purple is ABSENT in columns 1–3.
    3. Toggle OS reduced-motion (Windows Animations off / macOS Reduce Motion). Reload. Confirm: Marina already in column 4, purple lit, seal visible, NO travel, NO ~5 screens of dead scroll.
    4. Mobile viewport (≤639px, e.g. iPhone width in Playwright + a real phone if available): confirm the 4-step rail + one large readable centered card crossfading content; climax ignites purple on the final chip + card; no 4-column squash.
    5. Confirm zero metric/KPI/%/dashboard anywhere; no legend under the board; no CTA in the section.
  </how-to-verify>
  <verify>Manual — Lenny confirms in the browser after Claude's code-review + Playwright pass. No headless automated command (scroll-driven visual behavior).</verify>
  <done>Lenny types "approved" (or his issues are addressed and re-verified): traversal reads as a person walking, climax purple is felt and reserved to column 4, reduced-motion shows the pre-assembled final state with no dead scroll, mobile rail is legible at ~420svh.</done>
  <resume-signal>Type "approved" or describe issues (e.g. "climax purple too subtle", "mobile card text clipped", "traversal pace too fast").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | This plan is a section swap + file deletion in a static marketing page. No inputs, no auth, no data, no third-party script. The `section_view` analytics event for "funnel" reuses the already-vetted Phase 6 TrackSection (no PII, no new payload field). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Information disclosure | `section_view` analytics payload for "funnel" | accept | Payload is a section name string only; no PII; identical shape to the existing 6 sections vetted in Phase 6. No new analytics surface. |
| T-09-07 | Tampering | Deleting HowItWorks could break the build mid-way | mitigate | Build-safe ordering: page.tsx rewired (Task 1) BEFORE deletion (Task 2); full suite + build run after deletion to catch any dangling reference. |

No high-severity security threats. ASVS L1 surface nil (no inputs, no auth, no data fetch). Stated explicitly per security_enforcement honesty requirement.
</threat_model>

<verification>
- `npm test` exits 0 (full suite, coherence order tests RUNNING with Product → Funnel → Proof)
- `npm run build` succeeds
- `grep -rn "HowItWorks\|how-it-works" src/ tests/` returns nothing build/test-breaking
- Manual (Task 3 checkpoint): Lenny approves traversal, climax, reduced-motion, mobile
</verification>

<success_criteria>
- Funnel wired between Product and Proof with TrackSection parity; HowItWorks fully removed
- coherence.test.ts enforces the new order and excludes Funnel from the motion-import ban
- Full suite + build green
- Lenny approves the live experience
</success_criteria>

<output>
After completion, create `.planning/phases/09-funil-caminho-do-paciente/09-03-SUMMARY.md`
</output>
