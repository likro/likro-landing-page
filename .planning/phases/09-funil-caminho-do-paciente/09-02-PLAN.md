---
phase: 09-funil-caminho-do-paciente
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/sections/Funnel/index.tsx
  - src/sections/Funnel/FunnelHead.tsx
  - src/sections/Funnel/FunnelBoard.tsx
  - src/sections/Funnel/FunnelColumn.tsx
  - src/sections/Funnel/MarinaCard.tsx
  - src/sections/Funnel/GhostCard.tsx
  - src/sections/Funnel/FunnelClosing.tsx
  - src/sections/Funnel/FunnelRail.tsx
autonomous: true
requirements: [FUNIL-01, FUNIL-02, FUNIL-03, FUNIL-04, FUNIL-05, FUNIL-COPY]
must_haves:
  truths:
    - "Marina's card travels left→right across 4 columns as the user scrolls; the nearest column lights (neutral), others dim, ghosts stay dimmed"
    - "The card's beat content (channel + human moment) swaps through the 4 steps as the active step changes"
    - "Arrival in column 4 ignites purple (border + glow + ring + confirmation tag) — the felt 'it happened' beat"
    - "Purple appears ONLY at the climax — columns 1–3 use neutral highlight, never purple"
    - "prefers-reduced-motion renders the journey pre-assembled at the final state (Marina in column 4, purple lit, seal visible) with no scrub and no dead scroll"
    - "On mobile (≤639px) the board becomes a 4-step rail + one large centered card whose content crossfades; stage length ~420svh"
    - "The section uses ONLY @/components/motion primitives + the sanctioned useTransform/motion render-prop exception; zero useScroll/useMotionValue direct imports"
  artifacts:
    - path: "src/sections/Funnel/index.tsx"
      provides: "Section root: ScrollScene>StickyStage, tier branch, reduced final-state"
      min_lines: 40
    - path: "src/sections/Funnel/FunnelBoard.tsx"
      provides: "4 columns + ghosts + MarinaCard, progress-derived activeIndex + isClimax"
    - path: "src/sections/Funnel/MarinaCard.tsx"
      provides: "translateX-driven protagonist with beat crossfade + .win climax + seal"
    - path: "src/sections/Funnel/FunnelRail.tsx"
      provides: "Mobile 4-step rail with active chip + centered card content"
  key_links:
    - from: "src/sections/Funnel/index.tsx"
      to: "@/components/motion ScrollScene + StickyStage"
      via: "frozen primitive import"
      pattern: "from \"@/components/motion\""
    - from: "src/sections/Funnel/index.tsx"
      to: "src/content/funnel.ts FUNNEL_COPY"
      via: "import"
      pattern: "FUNNEL_COPY"
    - from: "src/sections/Funnel/MarinaCard.tsx"
      to: "progress MotionValue (useTransform translateX)"
      via: "render-prop derived x offset"
      pattern: "useTransform"
---

<objective>
Build the Funil section — a dark cinematic Kanban traversal where Marina's card walks from "Chegou agora" to "Consulta marcada" as the user scrolls, with a purple climax on arrival. Desktop = full 4-column board; mobile = "1 column in focus + rail"; reduced-motion = pre-assembled final state. All motion via the Phase 2 frozen primitives + the sanctioned render-prop exception.

Purpose: this is the craft-heavy heart of the chapter — the felt sense of ONE person walking to their appointment (not a dashboard). The prototype (`funil-proto.html`) is the approved visual; this ports its idea onto the production stack, honoring the UI-SPEC contract exactly.

Output: 8 files under `src/sections/Funnel/` implementing the full traversal, climax, mobile rail, and reduced-motion paths. NOT yet wired into page.tsx (Plan 03 does the removal + integration).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-funil-caminho-do-paciente/09-CONTEXT.md
@.planning/phases/09-funil-caminho-do-paciente/09-UI-SPEC.md
@.planning/phases/09-funil-caminho-do-paciente/09-RESEARCH.md
@src/content/funnel.ts
@src/components/motion/README.md
@src/app/dev/sticky/page.tsx
@src/sections/Hero/index.tsx
@funil-proto.html

<interfaces>
<!-- Frozen primitive contracts the executor consumes directly. No exploration needed. -->

From @/components/motion (barrel — the ONLY allowed motion import surface):
```ts
// ScrollScene: emits MotionValue<number> 0→1
<ScrollScene
  offset={["start start", "end end"]}   // [string,string], default ["start end","end start"]
  as="section" | "div" | "article"      // default "section"; aria-* NOT typed → wrap with outer <section>
  className={string}
>
  {(progress: MotionValue<number>) => ReactNode}
</ScrollScene>

// StickyStage: structural pin via position:sticky + svh
<StickyStage length={`${number}svh`} className={string}>{children}</StickyStage>
// length is template-literal typed `${number}svh` — pass a literal const:
//   const LEN = tier === "mobile" ? ("420svh" as const) : ("560svh" as const)
```

Sanctioned render-prop exception (MOTION-05, README §Exceção controlada) — the ONLY place these may be imported, and ONLY these:
```ts
import { useTransform, motion } from "motion/react";       // allowed
import { useMotionValueEvent, type MotionValue } from "motion/react"; // allowed (discrete index)
// FORBIDDEN even inside the section: useScroll, useMotionValue (Hero workaround anti-pattern, RESEARCH Pitfall 1)
```

From @/hooks/use-device-tier:
```ts
export type DeviceTier = "reduced" | "mobile" | "tablet" | "desktop";
export function useDeviceTier(): DeviceTier;
// SSR-safe: returns "desktop" during hydration, corrects post-mount via matchMedia.
// "reduced" covers both prefers-reduced-motion AND slow-2g/2g (so one branch satisfies FUNIL-04 + PERF-09).
```

From @/components/ui/container: `<Container className>` layout wrapper.
From @/lib/utils: `cn(...classes)` clsx+tailwind-merge.
From lucide-react: `import { Check } from "lucide-react"` — the ONLY icon allowed (UI-SPEC §Design System).

New tokens (Plan 01, now in globals.css): `bg-funnel-stage` (#0d0e16), `bg-funnel-column` (#13141f), `border-funnel-column-line` (#1e2030).

Proven sub-range technique to mirror — /dev/sticky StageB segments ONE progress into 4 windows (op1..op4, y1..y4) via useTransform. The Funil's 4 beats use the exact same pattern.

Hero reduced-motion collapse pattern (Hero/index.tsx): `const reduced = tier === "reduced"; className={reduced ? "relative min-h-svh ..." : "relative min-h-[...svh] ..."}` — render final static values instead of progress-driven ones. Hero never setStates per frame; climax ramp `useTransform(progress,[0.6,0.8],[0,1])` is the model for the Funil's `useTransform(progress,[0.82,0.95],[0,1])` ignition.
</interfaces>

<skills>
Per Lenny's explicit request, invoke frontend craft skills during this build (available globally): **frontend-design** (authored, anti-generic-AI aesthetic) for the board/card composition; **impeccable** (motion/polish premium) for the traversal feel + climax ignition timing; **taste-skill** (anti-slop pre-flight) before declaring done. The prototype is the approved visual target — match its rhythm ("uma pessoa caminhando", ~6s easeInOut), do not invent a new look.
</skills>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Static board pieces — FunnelHead, FunnelColumn, GhostCard, MarinaCard (presentational), FunnelClosing</name>
  <read_first>
    - src/content/funnel.ts (FUNNEL_COPY — the source of every string)
    - .planning/phases/09-funil-caminho-do-paciente/09-UI-SPEC.md (§Typography, §Color, §Spacing Scale, §Copywriting Contract — the binding visual contract)
    - funil-proto.html (visual target: column styling, card styling, ghost opacity, seal)
    - src/sections/Bridge/index.tsx (dark-section composition + rgba glow escape-hatch pattern)
    - tests/brand-lock.test.ts (purple discipline: rgba(124,58,237,…) allowed, #7c3aed hex forbidden)
  </read_first>
  <action>
    Build the presentational (non-motion-driven) pieces. All copy from `FUNNEL_COPY` — ZERO hard-coded PT-BR strings in JSX (the invariant test greps for this). Typography per UI-SPEC §Typography (Inter, weights 400/500 ONLY — never 600; the 4-size scale: 11px micro, 14px body/head, `clamp(24px,3.4vw,40px)` headline, `clamp(20px,2.6vw,30px)` closing). Colors per UI-SPEC §Color tokens (`text-on-dark-primary` #F5F7FA, `text-on-dark-secondary` #B6BDC9, `text-on-dark-muted` #7F8896, `accent-on-dark` #a78bfa for purple TEXT). NO `motion.*` here — these are static; motion wiring is Task 2.

    - **FunnelHead.tsx**: `<p>` eyebrow (`FUNNEL_COPY.eyebrow`, uppercase, `tracking-[0.14em]`, 14px, weight 500, `text-accent-on-dark` ~85% opacity) above `<h2 id="funnel-headline">` (`FUNNEL_COPY.headline`, `clamp(24px,3.4vw,40px)`, weight 500, line-height 1.12, tracking -0.02em, `text-on-dark-primary`). The eyebrow is a `<p>`/`<span>`, NOT a heading.
    - **FunnelColumn.tsx**: one column. Props `{ head: string; state: "resting" | "active" | "win"; children?: ReactNode }`. Resting: `bg-funnel-column` fill, `border-funnel-column-line` border, head text `text-on-dark-muted` (14px, weight 500, `tracking-[0.02em]`). Active (neutral — NO purple per UI-SPEC §Color / CONTEXT D-3): border lifts to `border-[rgba(255,255,255,0.18)]`, head text → `text-on-dark-primary`, active dot → `text-on-dark-secondary`. Win (purple climax ONLY): border `border-[rgba(124,58,237,0.85)]`, fill `bg-[linear-gradient(rgba(124,58,237,0.14),rgba(124,58,237,0.03))]`, glow `shadow-[0_24px_60px_-20px_rgba(124,58,237,0.55)]`, dot soft-violet with `shadow-[0_0_10px_rgba(124,58,237,…)]`. Column inner padding `p-4` (16px). Use `cn()` to compose state variants.
    - **GhostCard.tsx**: static dimmed placeholder, `opacity-[0.32]`, fill `#0f1019`, line `#181a26`, never moves/animates. A few generic dimmed cards behind Marina proving she is the story. No copy (or a non-PT-BR placeholder dot). These are decorative — `aria-hidden`.
    - **MarinaCard.tsx (presentational shell only this task)**: protagonist card, fixed width `w-[236px]` desktop. Renders `FUNNEL_COPY.protagonist` as the name (14px, weight 500, primary), the current beat `channel` (11px, weight 400, secondary) + `moment` (14px, weight 400, `#c9cbd9`). A `.win` variant: border `border-[rgba(157,107,240,0.8)]`, ring `ring-[rgba(124,58,237,0.4)]`, violet shadow, plus a confirmation tag row: `<Check>` icon (lucide, `accent-primary` dot) + `FUNNEL_COPY.seal` text (11px, weight 500, `text-accent-on-dark`). Accept beat content + `win` as props so Task 2 can drive them. Card vertical pin is a STATIC offset (prototype `top:120px`) — set via a static class/style, never animated. NO em-dash / staccato in any visible text (all comes from content anyway).
    - **FunnelClosing.tsx**: closing statement, `clamp(20px,2.6vw,30px)`, weight 500, line-height 1.3, `text-on-dark-primary`; render `FUNNEL_COPY.closing.lead` + an accent `<span class="text-accent-on-dark">{closing.accent}</span>` + `closing.tail`. NO CTA (CONTEXT D-2). NO legend/caption anywhere (CONTEXT D-7).

    Use `rgba(124,58,237,…)` literals for the purple climax surfaces (the brand-lock escape hatch — Bridge/Hero do this). NEVER write `#7c3aed` in these files. Animate nothing here.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/funnel-invariants.test.ts tests/brand-lock.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/sections/Funnel/{FunnelHead,FunnelColumn,GhostCard,MarinaCard,FunnelClosing}.tsx` all exist
    - `grep -r "#7c3aed" src/sections/Funnel` returns nothing (purple is rgba only)
    - `grep -rn "Você vê cada paciente\|Consulta confirmada\|Chegou agora" src/sections/Funnel/*.tsx` returns nothing (copy comes from FUNNEL_COPY, not inline)
    - `grep -rE "font-semibold|font-\[600\]|weight: 600|600\b" src/sections/Funnel/*.tsx` returns nothing for font weight (only 400/500 allowed)
    - `grep -r "from \"lucide-react\"" src/sections/Funnel` shows only `Check` imported
    - `npx vitest run tests/sections/funnel-invariants.test.ts tests/brand-lock.test.ts` exits 0; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>All static board pieces render from FUNNEL_COPY with correct tokens/typography; no purple hex, no inline copy, brand-lock + invariants green.</done>
</task>

<task type="auto">
  <name>Task 2: Motion wiring — FunnelBoard traversal + climax (desktop) via ScrollScene render-prop</name>
  <read_first>
    - src/app/dev/sticky/page.tsx (StageB — the exact 4-window useTransform sub-range technique: op1..op4 / y1..y4)
    - src/sections/Hero/index.tsx (lines ~50-72, ~161 — no-setState-per-frame discipline, climax ramp useTransform(progress,[0.6,0.8],[0,1]))
    - src/components/motion/README.md (§Regra de import — render-prop exception scope)
    - funil-proto.html (lines ~218-251 — seg=p*3 leg logic, per-leg easeInOut, active-index guard `if (moment.dataset.cur != active)`)
    - .planning/phases/09-funil-caminho-do-paciente/09-UI-SPEC.md (§Layout & Motion Contract)
  </read_first>
  <action>
    Build **FunnelBoard.tsx** = the desktop 4-column board driven by `progress: MotionValue<number>` (passed in from index.tsx's ScrollScene render-prop). This file MAY import `useTransform`/`motion`/`useMotionValueEvent`/`type MotionValue` from `motion/react` (the sanctioned exception) — it is rendered INSIDE the render-prop. Do NOT import `useScroll`/`useMotionValue`.

    Layout: a centered board, `max-width: 1240px`, `gap-5` (20px desktop), `px-8` (32px), 4 `FunnelColumn`s in a CSS grid `grid-cols-4`. GhostCards scattered statically in columns. The MarinaCard is absolutely positioned over the board and translated horizontally.

    **Horizontal travel (translateX, NEVER left — MOTION-08):** the card travels through 4 column centers. Compute column centers as fractions of board width (12.5% → 37.5% → 62.5% → 87.5%) — use a measured board-width ref updated on resize (debounce ~150ms like use-line-grouping) so it survives resize/tablet, OR a fraction-based translateX that scales with the container (RESEARCH Pitfall 2 — fraction-based is stable because the board is fixed max-width centered). Derive x via a 7-point useTransform that plateaus at each column center to give the "deliberate footstep" feel Lenny approved:
    ```ts
    const x = useTransform(progress, [0, 0.16, 0.33, 0.5, 0.66, 0.83, 1], [x0, x1, x1, x2, x2, x3, x3]);
    ```
    Apply via `<motion.div style={{ x }}>` wrapping MarinaCard. Per-leg easeInOut feel matches the prototype's symmetric ease (the plateaus approximate it; tune against the prototype during build — the 560svh length produces the approved ~6s pace).

    **Active index (discrete, NOT per-frame setState — TPRF-04):** derive `activeIndex` via `useTransform(progress, [0, 0.33, 0.66, 1], [0,1,2,3])`, subscribe once with `useMotionValueEvent(idx, "change", ...)` and `setState` ONLY when the rounded index actually changes (4 state changes total — the faithful port of the prototype's `dataset.cur != active` guard). Use `activeIndex` to set each column's `state` ("active" for the nearest, "resting" otherwise; "win" for column 4 once climax).

    **Beat content swap (recommended: zero React state):** render all 4 beats stacked in MarinaCard and crossfade with 4 `useTransform` opacity ramps (windows mirroring StageB's op1..op4) — cleaner for MOTION-08, no churn. Pass the per-beat opacity MotionValues into MarinaCard.

    **Climax ignition (FUNIL-03):** drive column-4 `.win` border/glow, MarinaCard `.win` ring/border, and the confirmation tag (`opacity 0→1`, `translateY 4px→0`) from `useTransform(progress, [0.82, 0.95], [0, 1])` (mirroring Hero's resolveOpacity). `isClimax = true` once progress ≥ ~0.9. Purple appears ONLY here — columns 1–3 stay neutral.

    **Atmosphere:** the stage's 6% purple top-radial wash (`radial-gradient(120% 80% at 50% -10%, rgba(124,58,237,0.06), transparent 60%)`) lives on the stage container (set in index.tsx Task 3) — NOT a board concern. Transform/opacity only; no layout-triggering props; no autonomous RAF.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/funnel-invariants.test.ts tests/sections/funnel-no-metric.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/sections/Funnel/FunnelBoard.tsx` exists
    - `grep -n "useScroll\|useMotionValue\b" src/sections/Funnel/FunnelBoard.tsx` returns nothing (only useTransform/useMotionValueEvent/motion/MotionValue allowed)
    - `grep -nE "(left|top):|left-\[|\bleft=" src/sections/Funnel/FunnelBoard.tsx` shows no ANIMATED left/top (translateX/`x` only; static `top` offset on a class is acceptable per README note)
    - `grep -n "useTransform(progress, \[0.82" src/sections/Funnel/FunnelBoard.tsx` (or equivalent climax ramp ~0.82–0.95) shows the climax ignition window
    - `npx vitest run tests/sections/funnel-invariants.test.ts tests/sections/funnel-no-metric.test.ts` exits 0; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>FunnelBoard renders the 4-column traversal: card walks via translateX, active column lights neutral, beats crossfade, column 4 + card ignite purple at the climax. No useScroll/useMotionValue, no animated left/top.</done>
</task>

<task type="auto">
  <name>Task 3: Section root index.tsx — ScrollScene>StickyStage shell, tier branch (mobile rail), reduced final-state</name>
  <read_first>
    - src/sections/Hero/index.tsx (reduced-motion collapse: min-h-[200svh] → min-h-svh; final static render)
    - src/app/dev/sticky/page.tsx (ScrollScene offset + StickyStage length nesting)
    - .planning/phases/09-funil-caminho-do-paciente/09-UI-SPEC.md (§Layout & Motion Contract, §Reduced Motion, §Responsive, §Accessibility)
    - src/hooks/use-device-tier.ts (tier values + SSR behavior)
    - src/content/funnel.ts (FUNNEL_COPY for the rail + final-state)
  </read_first>
  <action>
    > CONTEXT-BUDGET NOTE (heaviest task in the phase, knowingly accepted — not split): this single task carries the ScrollScene/StickyStage shell wiring, the reduced-motion final-state path, the desktop board hookup, AND the net-new mobile `FunnelRail.tsx` (no prototype reference — the only invented DOM), then closes with a full `npm run build`. Budget context accordingly: build `index.tsx` + the reduced/desktop wiring to a green `typecheck` FIRST, commit, THEN build `FunnelRail.tsx`, THEN run `npm run build`. If context tightens, the FunnelRail is the natural seam to pause/commit at before the build.

    Build **index.tsx** (`"use client"`) as the section root + **FunnelRail.tsx** (mobile branch).

    **index.tsx structure:**
    ```tsx
    const tier = useDeviceTier();
    const reduced = tier === "reduced";
    const length = tier === "mobile" ? ("420svh" as const) : ("560svh" as const);
    ```
    Outer `<section id="funnel" aria-labelledby="funnel-headline" className="relative bg-funnel-stage ...">` (aria-labelledby is NOT a typed ScrollScene prop — keep it on an OUTER `<section>` wrapping the ScrollScene, per RESEARCH note). Apply the 6% purple top-radial atmosphere as an `aria-hidden` absolutely-positioned div with inline `background: radial-gradient(120% 80% at 50% -10%, rgba(124,58,237,0.06), transparent 60%)` (rgba — brand-lock safe).

    **Reduced path (FUNIL-04):** if `reduced`, render a single-viewport (`min-h-svh`, NO StickyStage scroll distance OR `length="100svh"`) with FunnelHead + FunnelBoard rendered at the FINAL state: `activeIndex = 3`, `isClimax = true`, Marina statically in column 4, `.win` purple lit, confirmation tag + seal visible, column 4 active. No ScrollScene scrub, no travel, no dead scroll (collapse exactly like Hero collapses min-h-[200svh]→min-h-svh). Then FunnelClosing. This is the a11y-complete fallback — the full story present without motion.

    **Animated path (desktop + tablet + mobile):** 
    ```tsx
    <ScrollScene as="div" offset={["start start", "end end"]} className="relative">
      {(progress) => (
        <StickyStage length={length}>
          {/* head (static, top of stage) + (tier==="mobile" ? <FunnelRail progress=…/> : <FunnelBoard progress=…/>) + closing region */}
        </StickyStage>
      )}
    </ScrollScene>
    ```
    Same `progress`, same derived activeIndex/isClimax; only the inner component + length differ by tier (FUNIL-05 — no component duplication). FunnelHead renders once at the top of the pinned stage. The closing statement appears at/after the climax (drive its reveal with a late `useTransform(progress,[0.9,1],[0,1])` opacity, or place it below the stage — match the prototype's flow; keep it readable).

    **FunnelRail.tsx (mobile — net-new DOM, RESEARCH open Q2):** receives `progress`. Renders a horizontal rail of 4 step chips (from `FUNNEL_COPY.steps[i].head`) pinned at top; the active chip lights via the same `activeIndex` derivation; a position marker advances along the rail via a `translateX` `useTransform`. Below: ONE large centered MarinaCard (`w-[170px]`→up to ~280–300px readable) whose `channel`+`moment` crossfade through the 4 beats (same opacity-ramp technique as the board). Climax ignites purple on the final chip + the card (`useTransform(progress,[0.82,0.95],[0,1])`). This preserves "the person walking" + left→right progression without 4-column squash (the "board miniaturizado" alternative is REJECTED — do not build it). May import the render-prop exception (it is inside the ScrollScene tree).

    **SSR/CLS:** `useDeviceTier` returns "desktop" during hydration then corrects; the section is below the fold (no LCP). Reserve stage height in `svh` regardless of tier so the tier swap causes no layout shift. No `priority` images. Transform/opacity only.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/funnel-invariants.test.ts tests/sections/funnel-no-metric.test.ts tests/content/funnel.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/sections/Funnel/index.tsx src/sections/Funnel/FunnelRail.tsx` exist
    - `grep -n "from \"@/components/motion\"" src/sections/Funnel/index.tsx` shows ScrollScene + StickyStage imported from the barrel
    - `grep -n "420svh\|560svh" src/sections/Funnel/index.tsx` shows the tier-branched length
    - `grep -n "reduced\|tier === \"reduced\"" src/sections/Funnel/index.tsx` shows the reduced-motion branch
    - `grep -n "aria-labelledby=\"funnel-headline\"" src/sections/Funnel/index.tsx` matches on an outer `<section>`
    - `grep -rn "useScroll\|useMotionValue\b" src/sections/Funnel/` returns nothing
    - `grep -rE "min-h-\[?100vh|h-screen|: *[0-9]+vh\b|-\[[0-9]+vh\]" src/sections/Funnel/` returns nothing (svh only)
    - `npx vitest run tests/sections/funnel-invariants.test.ts tests/sections/funnel-no-metric.test.ts tests/content/funnel.test.ts` exits 0; `npm run typecheck` exits 0; `npm run build` succeeds
  </acceptance_criteria>
  <done>The section composes the frozen primitives, branches mobile rail vs desktop board by tier (single component, 420/560svh), and renders a pre-assembled final state under reduced-motion with no dead scroll. Build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | The Funil is a scroll-driven, content-only illustration. No inputs, no data fetch, no auth, no persistence, no third-party script. Scroll position is read-only client state via the frozen primitives. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering / Injection | Beat/seal strings rendered into DOM | accept | All strings are static author-controlled literals from FUNNEL_COPY; React escapes by default; no `dangerouslySetInnerHTML`. No injection surface. |
| T-09-04 | Denial of Service | Scroll-driven transforms on the client | mitigate | Transform/opacity only, no autonomous RAF, no per-frame setState (TPRF-04 discipline ported from Hero); reduced/slow-2g tier collapses to static final state — protects low-end devices. |
| T-09-05 | (a11y, not security) | Color-only climax signalling | mitigate | The confirmation tag carries meaning textually ("Consulta confirmada · quinta, 14h" + Check), not via purple alone (UI-SPEC §Accessibility). |

No high-severity security threats. ASVS L1: no inputs (V5 N/A), no auth (V2/V3 N/A), no data fetch. The only real risks are performance/a11y, mitigated above. Stated explicitly per security_enforcement honesty requirement.
</threat_model>

<verification>
- `npx vitest run tests/sections/funnel-invariants.test.ts tests/sections/funnel-no-metric.test.ts tests/content/funnel.test.ts tests/brand-lock.test.ts` exits 0
- `npm run typecheck` exits 0
- `npm run build` succeeds
- Manual (deferred to Plan 03 checkpoint / verify-work): Playwright @1536×730 screenshots at 0/25/50/75/100% scroll show Marina traversing 4 columns, active lighting, purple ONLY at climax; OS reduced-motion toggle shows pre-assembled final state; mobile viewport shows rail + single card at ~420svh.
</verification>

<success_criteria>
- Marina traverses 4 columns on scroll; active column lights neutral, ghosts dim, beats crossfade
- Purple ignites ONLY at the column-4 climax (border + glow + ring + seal)
- Reduced-motion delivers the pre-assembled final state with no dead scroll
- Mobile renders the rail + focused card at ~420svh via the same component
- Section uses only frozen primitives + the sanctioned render-prop exception; build + all funnel tests green
</success_criteria>

<output>
After completion, create `.planning/phases/09-funil-caminho-do-paciente/09-02-SUMMARY.md`
</output>
