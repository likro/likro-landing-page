---
phase: 06-analytics-instrumentation-pass
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - src/app/page.tsx
  - .planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md
autonomous: true
requirements: [TRACK-04, TRACK-06]
must_haves:
  truths:
    - "Cada seca narrativa em page.tsx esta envolvida por TrackSection e dispara section_view"
    - "ScrollDepthTracker esta montado uma unica vez em page.tsx"
    - "Existe exatamente um <GoogleAnalytics> e zero scripts GA manuais (TRACK-06 verificado)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Wiring de section_view (TrackSection) e scroll_depth (ScrollDepthTracker)"
      contains: "ScrollDepthTracker"
    - path: ".planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md"
      provides: "Grep test de TRACK-06 (GA4 SPA — single GoogleAnalytics)"
      contains: "GoogleAnalytics"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/analytics/TrackSection.tsx"
      via: "import { TrackSection } + wrap de cada secao"
      pattern: "TrackSection section="
    - from: "src/app/page.tsx"
      to: "src/components/analytics/ScrollDepthTracker.tsx"
      via: "import + mount unico"
      pattern: "<ScrollDepthTracker"
---

<objective>
Conectar os componentes de tracking criados no Plan 02 à página: envolver cada seção narrativa em `<TrackSection>` e montar `<ScrollDepthTracker />` uma vez em `page.tsx`. Verificar TRACK-06 (GA4 SPA — um único `<GoogleAnalytics>`) via grep test documentado em `06-VALIDATION.md`.

Purpose: fechar a Parte A da Phase 6 — todos os eventos `section_view`/`scroll_depth` passam a disparar de fato; TRACK-06 fica verificado por grep.
Output: `page.tsx` com wiring completo; `06-VALIDATION.md` com a verificação de TRACK-06.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md
@.planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md
@.planning/phases/06-analytics-instrumentation-pass/06-02-SUMMARY.md
@src/app/page.tsx
@src/components/providers/analytics-provider.tsx

<interfaces>
<!-- Componentes criados no Plan 02 — consumir diretamente. -->

```typescript
// src/components/analytics/TrackSection.tsx
export function TrackSection({ section, children }: { section: string; children: React.ReactNode }): React.ReactElement;

// src/components/analytics/ScrollDepthTracker.tsx
export function ScrollDepthTracker(): null;
```

`page.tsx` atual (server component) renderiza, dentro de `<main>`:
`<Hero />`, `<Pain />`, `<Bridge />`, `<Product />`, `<HowItWorks />`, `<Proof />`, `<Form />`.

Section keys decididas (decisão de planner — alinhar com os `id`s de seção existentes onde houver):
`hero`, `pain`, `bridge`, `product`, `how`, `proof`, `form`.
(IDs reais no DOM: Pain `id="pain"`, Bridge `id="bridge"`, Product `id="produto"`, HowItWorks `id="how-it-works"`, Proof `id="proof"`, Form `id` de FORM_COPY.sectionId, Hero sem id. As keys de analytics acima são o vocabulário do payload `section`, independentes do `id` do DOM — manter curtas e estáveis.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire TrackSection + ScrollDepthTracker em page.tsx</name>
  <read_first>
    - src/app/page.tsx (estado atual — server component, 7 seções dentro de <main>)
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Pattern 2 — montagem em page.tsx, não layout.tsx; Open Question 1 — incluir as 7 seções)
    - .planning/phases/06-analytics-instrumentation-pass/06-02-SUMMARY.md (decisão display:contents vs div no TrackSection)
    - src/components/analytics/TrackSection.tsx
    - src/components/analytics/ScrollDepthTracker.tsx
  </read_first>
  <files>src/app/page.tsx</files>
  <action>
    Editar `src/app/page.tsx` (continua server component — `TrackSection` e `ScrollDepthTracker` são client components renderizáveis a partir de um server component, sem tornar `page.tsx` client):

    1. Adicionar imports:
       `import { TrackSection } from "@/components/analytics/TrackSection";`
       `import { ScrollDepthTracker } from "@/components/analytics/ScrollDepthTracker";`

    2. Envolver CADA uma das 7 seções dentro de `<main>` com `<TrackSection section="...">`, mapeando:
       - `<Hero />` → `<TrackSection section="hero"><Hero /></TrackSection>`
       - `<Pain />` → `<TrackSection section="pain"><Pain /></TrackSection>`
       - `<Bridge />` → `<TrackSection section="bridge"><Bridge /></TrackSection>`
       - `<Product />` → `<TrackSection section="product"><Product /></TrackSection>`
       - `<HowItWorks />` → `<TrackSection section="how"><HowItWorks /></TrackSection>`
       - `<Proof />` → `<TrackSection section="proof"><Proof /></TrackSection>`
       - `<Form />` → `<TrackSection section="form"><Form /></TrackSection>`

    3. Montar `<ScrollDepthTracker />` UMA vez, após `</main>` e antes/depois de `<FloatingWhatsApp />` (componente headless, ordem irrelevante). NÃO montar no `layout.tsx` (layout cobre `/privacy`).

    4. NÃO editar nenhum arquivo em `src/sections/`. NÃO mover `<Header>`/`<Footer>`/`<FloatingWhatsApp>`. Manter a estrutura `<Header /> <main>...</main> <Footer /> <FloatingWhatsApp />` intacta, apenas adicionando os wrappers e o tracker.

    Atenção: se o SUMMARY do Plan 02 indicar que `TrackSection` usa `<div>` comum (não `display:contents`), confirmar que envolver `<Hero />` (que tem `min-h-svh`) num `<div>` extra não quebra o layout full-height — se quebrar, o `TrackSection` deve usar `display:contents`; reportar ao Plan 02 não é possível nesta wave, então registrar no SUMMARY como observação e, se necessário, ajustar `TrackSection.tsx` aqui mesmo (é o único caso em que esta task pode tocar `TrackSection.tsx` — documentar).
  </action>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contém `import { TrackSection }` e `import { ScrollDepthTracker }`.
    - Grep `TrackSection section=` em `page.tsx` retorna exatamente 7 ocorrências.
    - Grep `<ScrollDepthTracker` em `page.tsx` retorna exatamente 1 ocorrência.
    - `npm test` (suite completa) verde — zero regressão.
    - `npm run typecheck` e `npm run lint` limpos.
    - `npm run build` conclui sem erro (page.tsx continua server component válido).
  </acceptance_criteria>
  <done>section_view e scroll_depth wired; página dispara os dois eventos em runtime.</done>
</task>

<task type="auto">
  <name>Task 2: Verificar TRACK-06 (GA4 SPA) e finalizar VALIDATION.md</name>
  <read_first>
    - src/components/providers/analytics-provider.tsx (confirmar: um único <GoogleAnalytics>, zero gtag manual)
    - .planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md (preencher Per-Task Verification Map + Wave 0 + Sign-Off)
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Pitfall 4 — double-fire; Open Question 2 — grep test)
  </read_first>
  <files>.planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md</files>
  <action>
    TRACK-06 não precisa de código novo (já resolvido na Phase 1 via `<GoogleAnalytics>`). Verificar e documentar:

    1. Verificação por grep (rodar e registrar o resultado no VALIDATION.md):
       - `grep -rn "GoogleAnalytics" src/` deve mostrar exatamente UMA renderização de `<GoogleAnalytics ...>` (em `analytics-provider.tsx`) + a linha de `import`. Confirmar que não há um segundo `<GoogleAnalytics>` em outro arquivo.
       - `grep -rn "page_view" src/` deve retornar ZERO chamadas manuais a `gtag('event','page_view')` ou similar.
       - `grep -rn "gtag(" src/` — a única ocorrência aceitável é dentro de `track()` em `src/lib/analytics.ts` (`window.gtag?.("event", event, ...)`); zero scripts GA manuais montados além do `<GoogleAnalytics>`.
       Se qualquer verificação falhar (segundo mecanismo GA encontrado) → isso é um bug de double-fire; corrigir removendo o mecanismo manual e registrar. Esperado: tudo passa (código atual já está correto per RESEARCH §Pitfall 4).

    2. Atualizar `06-VALIDATION.md`:
       - Preencher a tabela "Per-Task Verification Map" com as linhas reais: Plan 01 (Wave 0 tests), Plan 02 Tasks 1-3, Plan 03 Task 1, com seus comandos `npm test -- --run <pattern>`.
       - Marcar os 3-4 itens de "Wave 0 Requirements" como concluídos (`[x]`).
       - Adicionar uma subseção "TRACK-06 — Verificação GA4 SPA" com os 3 comandos grep acima e o resultado (PASS/FAIL) registrado como bloco de texto.
       - No "Validation Sign-Off": marcar os itens da Parte A concluídos; setar `nyquist_compliant: true` e `wave_0_complete: true` no frontmatter; o item de Parte B permanece pendente (referenciar `06-HUMAN-UAT.md` do Plan 04).
  </action>
  <verify>
    <automated>npm test && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `06-VALIDATION.md` tem a tabela "Per-Task Verification Map" preenchida (sem `TBD`) e a subseção "TRACK-06 — Verificação GA4 SPA" com resultado PASS registrado.
    - Frontmatter de `06-VALIDATION.md`: `nyquist_compliant: true`, `wave_0_complete: true`.
    - `grep -rn "GoogleAnalytics" src/` confirma um único `<GoogleAnalytics>` montado.
    - `npm test` verde + `npm run lint` limpo.
  </acceptance_criteria>
  <done>TRACK-06 verificado e documentado; VALIDATION.md finalizado para a Parte A.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → GA4 | `page_view` e eventos custom enviados ao GA4 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Repudiation / data integrity | GA4 double-fire de page_view | mitigate | Grep test (Task 2) confirma um único `<GoogleAnalytics>` e zero `gtag('event','page_view')` manual — fonte única de page_view. |
| T-06-08 | Information Disclosure | wiring de section_view em page.tsx | accept | `section` keys são literais no `page.tsx` (`hero`/`pain`/...), nunca input de usuário — sem superfície. |
</threat_model>

<verification>
- `npm test` — suite completa verde.
- `npm run typecheck` — limpo.
- `npm run lint` — limpo.
- `npm run build` — conclui sem erro.
- Grep `TrackSection section=` em `page.tsx` = 7; `<ScrollDepthTracker` = 1.
- Grep `GoogleAnalytics` em `src/` — uma única renderização.
</verification>

<success_criteria>
- 7 seções envolvidas por `<TrackSection>`; `<ScrollDepthTracker />` montado 1x em `page.tsx`.
- TRACK-06 verificado por grep e documentado no VALIDATION.md.
- VALIDATION.md finalizado para a Parte A; `nyquist_compliant: true`.
- Zero regressão (suite completa verde, build OK).
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-analytics-instrumentation-pass/06-03-SUMMARY.md`. Registrar: resultado dos 3 greps de TRACK-06; se o `TrackSection` precisou de ajuste (display:contents) por causa do `min-h-svh` do Hero.
</output>
