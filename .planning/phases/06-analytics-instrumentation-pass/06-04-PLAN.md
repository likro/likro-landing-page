---
phase: 06-analytics-instrumentation-pass
plan: 04
type: execute
wave: 3
depends_on: [02]
files_modified:
  - .planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md
autonomous: false
requirements: [TRACK-05, TRACK-07]
user_setup:
  - service: meta-pixel
    why: "Verificacao de eventos em Pixel Test Events (Parte B)"
    env_vars:
      - name: NEXT_PUBLIC_META_PIXEL_ID
        source: "Meta Business → Events Manager → Pixel → Settings"
  - service: ga4
    why: "Verificacao de eventos em GA4 DebugView (Parte B)"
    env_vars:
      - name: NEXT_PUBLIC_GA4_ID
        source: "GA4 → Admin → Data Streams → Web stream → Measurement ID (G-XXXX)"
  - service: microsoft-clarity
    why: "Verificacao de PII masking em gravacao real (Parte B)"
    env_vars:
      - name: NEXT_PUBLIC_CLARITY_ID
        source: "Clarity → Settings → Setup → Project ID"
must_haves:
  truths:
    - "Existe um documento 06-HUMAN-UAT.md com o checklist B1-B8 de verificacao cross-dashboard"
    - "O checkpoint comunica ao Lenny que a Parte B esta bloqueada ate as 3 contas existirem"
  artifacts:
    - path: ".planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md"
      provides: "Checklist guiado de verificacao Meta/GA4/Clarity (Parte B)"
      contains: "B1"
  key_links: []
---

<objective>
Documentar a Parte B da Phase 6 — a verificação cross-dashboard (Meta Pixel Test Events, GA4 DebugView, Clarity recordings) e a validação real de PII masking. Esta parte está BLOQUEADA: depende das 3 contas de analytics (Meta/GA4/Clarity) que o Lenny ainda não criou. Não é uma task de código autônoma — é a criação de um documento de UAT humano + um checkpoint que informa o estado da phase.

Purpose: fechar formalmente TRACK-07 (verificação) e a parte real de TRACK-05 (PII masking validado em sessão) como itens DEFERIDOS e documentados, sem bloquear a conclusão da Parte A. Decisão LOCKED do Lenny (CONTEXT.md 2026-05-20): planejar agora, executar a verificação quando as contas existirem.
Output: `06-HUMAN-UAT.md` + checkpoint humano explicando que a phase chega a "Parte A completa, Parte B pendente".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-instrumentation-pass/06-CONTEXT.md
@.planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md
@.planning/phases/06-analytics-instrumentation-pass/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar 06-HUMAN-UAT.md (checklist Parte B)</name>
  <read_first>
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Validation Architecture → "Part B — HUMAN-UAT" — checklist B1-B8 completo; §Environment Availability)
    - .planning/phases/06-analytics-instrumentation-pass/06-CONTEXT.md (§deferred — Parte B bloqueada; §specifics — HUMAN-UAT proposto)
    - .planning/phases/05-conversion/05-HUMAN-UAT.md (precedente de formato de HUMAN-UAT no projeto, se existir — buscar com Glob)
  </read_first>
  <files>.planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md</files>
  <action>
    Criar `.planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md` em pt-BR. Seguir o formato de `05-HUMAN-UAT.md` se existir no projeto (buscar via Glob `.planning/phases/**/05-HUMAN-UAT.md`); caso não exista, usar a estrutura abaixo.

    Conteúdo obrigatório:

    1. **Frontmatter**: `phase: 06-analytics-instrumentation-pass`, `type: human-uat`, `status: blocked`, `blocked_on: "contas Meta Pixel + GA4 + Clarity inexistentes"`.

    2. **Seção "Status"**: deixar explícito que a Parte A (implementação de `section_view`/`scroll_depth` + `data-clarity-mask` + testes unitários) está COMPLETA e verificada; a Parte B (este documento) está BLOQUEADA até o pré-requisito ser cumprido.

    3. **Seção "Pré-requisito (Lenny)"** — passos antes de executar o checklist:
       - Criar conta Meta Business + Pixel → obter `NEXT_PUBLIC_META_PIXEL_ID`.
       - Criar propriedade GA4 → obter `NEXT_PUBLIC_GA4_ID` (formato `G-XXXXXXX`).
       - Criar projeto Microsoft Clarity → obter `NEXT_PUBLIC_CLARITY_ID`.
       - Configurar os 3 IDs em Vercel env vars (Production + Preview + Development) → redeploy.
       Mencionar que pode ser sessão guiada separada, igual foi feito com Resend/GCP.

    4. **Seção "Checklist de Verificação"** — tabela com as 8 linhas B1-B8 EXATAMENTE como em 06-RESEARCH.md §Validation Architecture → "Checklist HUMAN-UAT proposto" (colunas: #, Verificação, Ferramenta, Esperado, Resultado [a preencher]). Reproduzir B1-B8 com fidelidade:
       - B1: `whatsapp_click` → Meta Pixel Test Events → evento `Contact` com `eventID`.
       - B2: `form_submit_success` → Meta Pixel Test Events → evento `Lead` com `eventID`.
       - B3: `section_view`/`scroll_depth` → Meta Pixel Test Events → custom events com `eventID` e payload (`section`/`depth`).
       - B4: eventos custom + `page_view` → GA4 DebugView → todos listados.
       - B5: zero double-fire → GA4 DebugView + Network tab → um `page_view` por reload.
       - B6: `section_view` não em burst → GA4 DebugView → aparece conforme o scroll, não no load.
       - B7: PII masking → Clarity Recordings → nome/WhatsApp/email mascarados (blocos cinza) na gravação de quem preencheu o form.
       - B8: eventos custom no Clarity → Clarity Dashboard → `section_view`/`scroll_depth` registrados.

    5. **Seção "Notas"**: Clarity demora ~30 min para processar gravações/settings; masking é client-side (conteúdo mascarado nunca chega aos servidores). `event_id` aparece como `eventID` no payload do Pixel (retrofit Meta CAPI futuro = zero-cost).

    NÃO criar nenhum arquivo de código. NÃO configurar env vars (Lenny faz). Este é um documento de planejamento de UAT.
  </action>
  <verify>
    <automated>test -f .planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md && grep -q "B8" .planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `06-HUMAN-UAT.md` existe com frontmatter `status: blocked`.
    - O documento contém as 8 linhas B1-B8 do checklist e a seção de pré-requisito com os 3 serviços.
    - Grep `B1` ... `B8` no arquivo retorna as 8 entradas.
    - Idioma pt-BR.
  </acceptance_criteria>
  <done>Documento de UAT da Parte B criado e bloqueado.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Parte B deferida, confirmar fechamento da Parte A</name>
  <action>Pausar a execução e apresentar ao Lenny o estado da Phase 6: Parte A completa e verificada por testes, Parte B documentada em 06-HUMAN-UAT.md e bloqueada até as 3 contas de analytics existirem. Aguardar o sinal de retomada.</action>
  <files>.planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md</files>
  <verify>Checkpoint humano — sem comando automatizado; Lenny revisa 06-HUMAN-UAT.md e responde.</verify>
  <done>Lenny respondeu ao checkpoint; status da Phase 6 (Parte A fechada, Parte B pendente) confirmado.</done>
  <what-built>
    Parte A da Phase 6 implementada e verificada por testes automatizados:
    - `useSectionView` hook + `TrackSection` wrapper → `section_view` dispara por seção (7 seções).
    - `<ScrollDepthTracker />` → `scroll_depth` dispara nos marcos 25/50/75/100.
    - `data-clarity-mask` confirmado no `<form>` e `<section>` do form (TRACK-05 Parte A).
    - TRACK-06 verificado por grep (um único `<GoogleAnalytics>`, zero double-fire).
    - `06-HUMAN-UAT.md` criado documentando a Parte B (TRACK-07 + TRACK-05 real).
  </what-built>
  <how-to-verify>
    1. Revisar `.planning/phases/06-analytics-instrumentation-pass/06-HUMAN-UAT.md` — confirmar que o checklist B1-B8 está claro e executável quando as contas existirem.
    2. Confirmar o entendimento: a Phase 6 fecha com status "Parte A completa, Parte B pendente". A Parte B (verificação nos 3 dashboards) só roda depois que você criar as contas Meta Pixel + GA4 + Clarity e configurar `NEXT_PUBLIC_META_PIXEL_ID` / `NEXT_PUBLIC_GA4_ID` / `NEXT_PUBLIC_CLARITY_ID` na Vercel.
    3. Decidir: criar as 3 contas agora (sessão guiada, igual Resend/GCP) OU deixar a Parte B como pendência conhecida e seguir para a Phase 7.
  </how-to-verify>
  <resume-signal>Digite "parte A aprovada" para fechar a Phase 6 com a Parte B pendente, ou descreva ajustes no HUMAN-UAT.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| usuário → form (PII) | nome/WhatsApp/email no form podem aparecer em gravação Clarity |
| browser → vendores | eventos enviados a Meta/GA4/Clarity — verificação confirma corretude |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-09 | Information Disclosure | gravação Clarity com PII | mitigate (verificação pendente) | `data-clarity-mask` já aplicado (Plan 02); checklist B7 valida em gravação real quando a conta existir. Até lá, mitigação é por código + verificação documentada como pendente. |
| T-06-10 | Compliance | scripts de tracking sem consentimento LGPD | accept | Out of scope desta phase — cookie banner LGPD deferido para Phase 7/backlog. Dívida conhecida documentada. |
</threat_model>

<verification>
- `06-HUMAN-UAT.md` existe, `status: blocked`, contém B1-B8 e os 3 pré-requisitos de conta.
- Checkpoint humano apresentado e resolvido.
</verification>

<success_criteria>
- `06-HUMAN-UAT.md` criado e completo (Parte B documentada como bloqueada).
- Lenny confirma o fechamento da Phase 6 com status "Parte A completa, Parte B pendente".
- Zero código tocado neste plano.
</success_criteria>

<output>
Após o checkpoint, criar `.planning/phases/06-analytics-instrumentation-pass/06-04-SUMMARY.md`. Registrar: a decisão do Lenny no checkpoint (criar contas agora vs deferir); o status final da Phase 6.
</output>
