---
phase: 06-analytics-instrumentation-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/hooks/use-section-view.test.tsx
  - tests/components/scroll-depth-tracker.test.tsx
  - tests/analytics/clarity-mask.test.tsx
autonomous: true
requirements: [TRACK-04, TRACK-05]
must_haves:
  truths:
    - "A suite de testes contém specs RED para section_view, scroll_depth e clarity-mask"
    - "Rodar a suite mostra os 3 test files falhando por modulo/comportamento ausente (RED esperado)"
  artifacts:
    - path: "tests/hooks/use-section-view.test.tsx"
      provides: "Spec de section_view — fire-once, section correto, sem re-disparo"
      contains: "useSectionView"
    - path: "tests/components/scroll-depth-tracker.test.tsx"
      provides: "Spec de scroll_depth — marcos 25/50/75/100, dedup, no-op nao-rolavel"
      contains: "ScrollDepthTracker"
    - path: "tests/analytics/clarity-mask.test.tsx"
      provides: "Spec de PII masking — data-clarity-mask no Form section e no form do LeadForm"
      contains: "data-clarity-mask"
  key_links:
    - from: "tests/hooks/use-section-view.test.tsx"
      to: "@/hooks/use-section-view"
      via: "import do hook ainda inexistente"
      pattern: "from .@/hooks/use-section-view."
    - from: "tests/components/scroll-depth-tracker.test.tsx"
      to: "@/components/analytics/ScrollDepthTracker"
      via: "import do componente ainda inexistente"
      pattern: "ScrollDepthTracker"
---

<objective>
Criar os test files da Wave 0 da Phase 6 — specs RED que definem o contrato de `useSectionView`, `<ScrollDepthTracker />` e do PII masking do form. Estes testes DEVEM falhar ao final deste plano (RED): os módulos `@/hooks/use-section-view` e `@/components/analytics/ScrollDepthTracker` ainda não existem; o teste de clarity-mask pode passar parcialmente (os atributos já existem da Phase 5) mas o arquivo de teste em si não existe ainda.

Purpose: estabelecer a verificação automatizável ANTES da implementação (Nyquist — Plan 02 implementa contra estes contratos). Cobre TRACK-04 (eventos `section_view`/`scroll_depth`) e TRACK-05 Parte A (masking verificado por render).
Output: 3 test files em `tests/`. Nenhum código de produção tocado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md
@.planning/phases/06-analytics-instrumentation-pass/06-VALIDATION.md
@tests/setup.ts
@src/lib/analytics.ts
@src/sections/Form/index.tsx
@src/sections/Form/LeadForm.tsx

<interfaces>
<!-- Contratos que Plan 02 vai implementar. Os testes DEVEM ser escritos contra estas assinaturas exatas. -->

Hook a ser criado (Plan 02) — `src/hooks/use-section-view.ts`:
```typescript
export function useSectionView<T extends Element = HTMLElement>(
  section: string,
  opts?: { threshold?: number },
): React.RefObject<T | null>;
```
Comportamento: ao primeiro `isIntersecting`, chama `track("section_view", { section })` UMA vez e desconecta o observer. NÃO consulta `prefers-reduced-motion`.

Componente a ser criado (Plan 02) — `src/components/analytics/ScrollDepthTracker.tsx`:
```typescript
export function ScrollDepthTracker(): null;
```
Comportamento: monta listener `scroll` passivo, throttle via `requestAnimationFrame`, calcula `pct = scrollY / (scrollHeight - innerHeight) * 100`, dispara `track("scroll_depth", { depth })` ao cruzar cada marco de `[25, 50, 75, 100]`, cada marco UMA vez via `Set`. Marco 100 usa tolerância `pct >= 99`. No-op quando `scrollHeight - innerHeight <= 0`.

Função de tracking — `src/lib/analytics.ts` (JÁ EXISTE, não tocar):
```typescript
export function track(event: AnalyticsEvent, payload?: Record<string, unknown>): void;
// AnalyticsEvent inclui "section_view" e "scroll_depth"
```

PII masking (JÁ EXISTE da Phase 5 — só verificar):
- `src/sections/Form/index.tsx` → `<section id={FORM_COPY.sectionId} data-clarity-mask="true">`
- `src/sections/Form/LeadForm.tsx` → `<form>` root com `data-clarity-mask` (confirmar via Read durante execução)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Spec RED de useSectionView (section_view)</name>
  <read_first>
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Code Examples — "Testar useSectionView com IntersectionObserver mockado"; §Pitfall 3)
    - tests/setup.ts (stub global de IntersectionObserver — testes que controlam o observer sobrescrevem no beforeEach)
    - tests/hooks/use-in-view.test.ts (precedente de teste de hook IO no projeto)
    - src/lib/analytics.ts (assinatura de track + tipo AnalyticsEvent)
  </read_first>
  <files>tests/hooks/use-section-view.test.tsx</files>
  <action>
    Criar `tests/hooks/use-section-view.test.tsx` cobrindo o contrato de `useSectionView(section)` de `@/hooks/use-section-view` (módulo ainda inexistente — import vai falhar, RED esperado).

    Padrão de teste (research recomenda componente de teste wrapper, NÃO manipulação manual de ref):
    - `vi.mock("@/lib/analytics", () => ({ track: vi.fn() }))` e importar `track` mockado.
    - No `beforeEach`, sobrescrever `globalThis.IntersectionObserver` com uma classe stub que captura o callback (`let ioCallback`) e expõe `observe`/`disconnect`/`unobserve` como `vi.fn()`. Limpar com `vi.clearAllMocks()`.
    - Componente de teste: `function Probe({ section }: { section: string }) { const ref = useSectionView<HTMLElement>(section); return <section ref={ref} />; }` — renderizar via `render(<Probe section="pain" />)` para que o jsdom anexe o nó real e o `useEffect` capture `ref.current`.

    Casos obrigatórios:
    1. `it("dispara section_view uma vez ao entrar no viewport")` — render `<Probe section="pain" />`, invocar `ioCallback([{ isIntersecting: true }], observerStub)`, asserir `expect(track).toHaveBeenCalledWith("section_view", { section: "pain" })`.
    2. `it("nao re-dispara em re-intersecao")` — após o caso acima, invocar `ioCallback` de novo com `isIntersecting: true`, asserir `expect(track).toHaveBeenCalledTimes(1)`.
    3. `it("nao dispara enquanto a secao nao intersecta")` — render `<Probe />`, invocar `ioCallback([{ isIntersecting: false }], observerStub)`, asserir `expect(track).not.toHaveBeenCalled()`.
    4. `it("usa o section name passado no payload")` — render `<Probe section="bridge" />`, intersectar, asserir payload `{ section: "bridge" }`.
    5. `it("desconecta o observer apos o primeiro disparo")` — após intersectar, asserir `expect(disconnectSpy).toHaveBeenCalled()`.
  </action>
  <verify>
    <automated>npm test -- --run use-section-view</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/hooks/use-section-view.test.tsx` existe e contém os 5 `it(...)` listados.
    - `npm test -- --run use-section-view` executa e o arquivo FALHA (RED) — falha esperada por `@/hooks/use-section-view` não existir (erro de resolução de módulo), NÃO por erro de sintaxe do teste.
    - Grep `useSectionView` no arquivo retorna >= 6 ocorrências (import + 5 usos).
  </acceptance_criteria>
  <done>Spec de section_view escrita, RED confirmado por módulo ausente.</done>
</task>

<task type="auto">
  <name>Task 2: Spec RED de ScrollDepthTracker (scroll_depth)</name>
  <read_first>
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Code Examples — "Testar <ScrollDepthTracker />"; §Pitfall 1 e §Pitfall 2 — stub de geometria jsdom)
    - tests/setup.ts
    - src/lib/analytics.ts
  </read_first>
  <files>tests/components/scroll-depth-tracker.test.tsx</files>
  <action>
    Criar `tests/components/scroll-depth-tracker.test.tsx` cobrindo `<ScrollDepthTracker />` de `@/components/analytics/ScrollDepthTracker` (módulo ainda inexistente — RED esperado).

    Setup obrigatório (jsdom não tem geometria de scroll — ver Pitfall 2):
    - `vi.mock("@/lib/analytics", () => ({ track: vi.fn() }))`.
    - Em cada teste, antes do `render`: stub das propriedades de geometria —
      `Object.defineProperty(document.documentElement, "scrollHeight", { value: 4000, configurable: true })`,
      `Object.defineProperty(window, "innerHeight", { value: 1000, configurable: true })`,
      `Object.defineProperty(window, "scrollY", { value: 0, writable: true, configurable: true })`.
    - `vi.stubGlobal("requestAnimationFrame", (cb) => { cb(0); return 0; })` — RAF síncrono.
    - Disparar scroll: setar `(window as any).scrollY = N` e `window.dispatchEvent(new Event("scroll"))`.
    - `afterEach(() => { vi.unstubAllGlobals(); vi.clearAllMocks(); })`.

    Casos obrigatórios:
    1. `it("dispara scroll_depth 25 e 50 ao rolar ~60%")` — scrollHeight 4000, innerHeight 1000 (scrollable=3000), `scrollY=1800` (60%), dispatch scroll → `expect(track).toHaveBeenCalledWith("scroll_depth", { depth: 25 })` e `{ depth: 50 }`; `expect(track).not.toHaveBeenCalledWith("scroll_depth", { depth: 75 })`.
    2. `it("nao duplica marco ja disparado")` — rolar a 60% duas vezes (dois dispatch scroll), asserir que `track` com `{ depth: 25 }` foi chamado exatamente 1 vez (`track.mock.calls.filter(c => c[1]?.depth === 25).length === 1`).
    3. `it("dispara 100 com tolerancia pct >= 99")` — `scrollY=2970` (2970/3000 = 99%), dispatch scroll → `expect(track).toHaveBeenCalledWith("scroll_depth", { depth: 100 })`.
    4. `it("no-op quando a pagina nao e rolavel")` — `scrollHeight=800`, `innerHeight=1000` (scrollable negativo), dispatch scroll → `expect(track).not.toHaveBeenCalled()`.
    5. `it("checa estado inicial no mount sem scroll event")` — `scrollY=3000` ANTES do render (página restaurada no fim), `render(<ScrollDepthTracker />)` sem dispatch → `track` foi chamado com `{ depth: 100 }` (o componente computa no mount).
  </action>
  <verify>
    <automated>npm test -- --run scroll-depth-tracker</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/components/scroll-depth-tracker.test.tsx` existe com os 5 `it(...)` listados.
    - `npm test -- --run scroll-depth-tracker` executa e FALHA (RED) por `@/components/analytics/ScrollDepthTracker` não existir.
    - O arquivo contém `vi.stubGlobal("requestAnimationFrame"` e os 3 `Object.defineProperty` de geometria.
  </acceptance_criteria>
  <done>Spec de scroll_depth escrita, RED confirmado por módulo ausente.</done>
</task>

<task type="auto">
  <name>Task 3: Spec de PII masking (clarity-mask)</name>
  <read_first>
    - .planning/phases/06-analytics-instrumentation-pass/06-RESEARCH.md (§Code Examples — "Verificar data-clarity-mask por render")
    - src/sections/Form/index.tsx (section wrapper com data-clarity-mask)
    - src/sections/Form/LeadForm.tsx (confirmar se o <form> root tem data-clarity-mask — ler o arquivo inteiro)
    - src/content/form.ts (FORM_COPY.sectionId — para o seletor)
  </read_first>
  <files>tests/analytics/clarity-mask.test.tsx</files>
  <action>
    Criar `tests/analytics/clarity-mask.test.tsx` (criar o diretório `tests/analytics/` se não existir).

    Este teste verifica TRACK-05 Parte A: os atributos `data-clarity-mask="true"` já entregues na Phase 5 estão presentes. PODE passar GREEN imediatamente (diferente das Tasks 1 e 2) — é um teste de regressão, não RED. Se algum atributo NÃO estiver presente, o teste falha e Plan 02 corrige.

    Casos obrigatórios:
    1. `it("Form section wrapper tem data-clarity-mask=true")` — `import { Form } from "@/sections/Form"`, `const { container } = render(<Form />)`, localizar a `<section>` (via `container.querySelector("section[data-clarity-mask]")` ou pelo id de `FORM_COPY.sectionId`), asserir `expect(section).toHaveAttribute("data-clarity-mask", "true")`.
    2. `it("o <form> do LeadForm tem data-clarity-mask=true")` — `import { LeadForm } from "@/sections/Form/LeadForm"`, `render(<LeadForm />)`, localizar o `<form>` (`container.querySelector("form")`), asserir `toHaveAttribute("data-clarity-mask", "true")`. Se durante o `read_first` você confirmar que o `<form>` NÃO tem o atributo, ainda assim escreva este teste asserindo a presença — ele será RED e Plan 02 adicionará o atributo (defense in depth, decisão LOCKED do CONTEXT).
    3. `it("inputs de nome e WhatsApp estao dentro do container mascarado")` — render `<Form />`, confirmar que os inputs (por `name`/`label`) são descendentes do nó com `data-clarity-mask` (`maskedNode.contains(input)`).

    Notas: `LeadForm` é `"use client"` e usa `react-hook-form` — renderizar isolado em jsdom funciona (precedente: tests/sections da Phase 5). Se o render exigir mock de `server-only`, o `tests/setup.ts` já cobre.
  </action>
  <verify>
    <automated>npm test -- --run clarity-mask</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/analytics/clarity-mask.test.tsx` existe com os 3 `it(...)` listados.
    - `npm test -- --run clarity-mask` executa sem erro de sintaxe/resolução.
    - Casos 1 e 3 passam GREEN (atributo da Phase 5 presente). Caso 2 passa GREEN se o `<form>` já tem o atributo, ou fica RED documentado se não tiver — registrar o resultado no SUMMARY para Plan 02.
  </acceptance_criteria>
  <done>Spec de masking escrita; estado (GREEN/RED por atributo) registrado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → vendor (GA4/Pixel/Clarity) | payloads de analytics saem do browser para terceiros |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | testes de analytics | accept | Test files não enviam dados reais; `track` é mockado. Sem superfície. |
| T-06-02 | Tampering | scroll geometry stubs em jsdom | accept | Stubs vivem só no ambiente de teste; não afetam produção. |
</threat_model>

<verification>
- `npm test -- --run use-section-view` — arquivo existe, RED por módulo ausente.
- `npm test -- --run scroll-depth-tracker` — arquivo existe, RED por módulo ausente.
- `npm test -- --run clarity-mask` — arquivo existe, executa; estado GREEN/RED registrado.
- `npm run typecheck` — sem erros de tipo nos test files (imports de tipo válidos mesmo com módulo de runtime ausente exigem que o teste seja escrito de forma que typecheck não quebre; se necessário usar `// @ts-expect-error` no import do módulo inexistente com comentário explicativo).
</verification>

<success_criteria>
- 3 test files criados em `tests/`.
- section_view e scroll_depth specs em estado RED (módulo de produção ainda não existe) — comportamento esperado e correto para Wave 0.
- clarity-mask spec executável; estado documentado.
- Zero arquivo de produção (`src/`) modificado.
</success_criteria>

<output>
Após completar, criar `.planning/phases/06-analytics-instrumentation-pass/06-01-SUMMARY.md`. Registrar explicitamente: o estado RED/GREEN de cada arquivo, e se o `<form>` do `LeadForm` tem ou não `data-clarity-mask` (input crítico para Plan 02 Task de masking).
</output>
