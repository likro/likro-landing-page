---
phase: 01-foundations-design-system
plan: 04
type: execute
wave: 2
depends_on: [01, 02, 03]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/label.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/sheet.tsx
  - src/components/ui/sonner.tsx
  - src/components/ui/container.tsx
  - src/components/ui/headline.tsx
  - src/components/ui/whatsapp-cta.tsx
  - src/components/ui/whatsapp-icon.tsx
  - src/components/providers/analytics-provider.tsx
  - src/components/providers/smooth-scroll-provider.tsx
  - src/components/providers/motion-config-provider.tsx
  - src/app/layout.tsx
  - src/app/dev/page.tsx
  - src/app/opengraph-image.tsx
  - src/app/icon.tsx
  - tests/components/ui/whatsapp-cta.test.tsx
  - tests/providers/smooth-scroll.test.tsx
  - tests/providers/analytics.test.tsx
  - tests/app/layout-providers.test.tsx
  - tests/app/dev-route.test.tsx
  - docs/BRAND.md
  # W3 fix (checker revision): migrate whatsapp.ts to use lib/env (Plan 04 owns the refactor since lib/env.ts comes from Plan 03 in same Wave 1)
  - src/lib/whatsapp.ts
  - tests/lib/whatsapp.test.ts
autonomous: true
requirements: [FOUND-07, FOUND-08, FOUND-09, FOUND-10, FOUND-12, TRACK-03]

must_haves:
  truths:
    - "App renderiza em http://localhost:3000 com provider tree completo (AnalyticsProvider > SmoothScrollProvider > MotionConfigProvider > children > Toaster)"
    - "SmoothScrollProvider retorna children sem <ReactLenis> wrapper quando useReducedMotion()=true (FOUND-08)"
    - "SmoothScrollProvider passa syncTouch: false (NÃO smoothTouch) ao Lenis (orchestrator directive #1)"
    - "AnalyticsProvider monta <Script id='meta-pixel' strategy='afterInteractive'> e <Script id='ms-clarity' strategy='afterInteractive'> quando env vars presentes; NÃO monta quando ausentes (graceful)"
    - "Clarity Script id é 'ms-clarity', NÃO 'clarity' (Pitfall E)"
    - "<WhatsAppCta location='hero'/> ao clicar: chama track('whatsapp_click', {location:'hero'}), espera 250ms, então window.open(buildWhatsAppUrl(WHATSAPP_MESSAGES.hero, 'hero'))"
    - "<WhatsAppCta> mostra spinner Loader2 enquanto loading=true e desabilita botão (D-10)"
    - "/dev route renderiza showcase em next dev e retorna 404 em production build (process.env.NODE_ENV check estaticamente eliminado por Webpack/SWC — orchestrator directive #7)"
    - "metadata em app/layout.tsx exporta title.template='%s · Likro', description, openGraph (locale=pt_BR, type=website, images), twitter card=summary_large_image, robots, manifest, viewport.themeColor=#0a0a0b (NÃO o roxo)"
    - "next build gera bundle de prod sem erros e sem 'use client' no layout root"
  artifacts:
    - path: "src/components/providers/smooth-scroll-provider.tsx"
      provides: "SmoothScrollProvider client component que wrappa <ReactLenis root> com syncTouch: false; skip init em prefers-reduced-motion"
      exports: ["SmoothScrollProvider"]
      contains: "syncTouch: false"
    - path: "src/components/providers/motion-config-provider.tsx"
      provides: "MotionConfigProvider client component que aplica reducedMotion='user' globalmente"
      exports: ["MotionConfigProvider"]
      contains: "reducedMotion=\"user\""
    - path: "src/components/providers/analytics-provider.tsx"
      provides: "AnalyticsProvider client component com <GoogleAnalytics> + Pixel <Script> + Clarity <Script>; graceful se env var missing"
      exports: ["AnalyticsProvider"]
      contains: "id=\"ms-clarity\""
    - path: "src/components/ui/whatsapp-cta.tsx"
      provides: "<WhatsAppCta variant location label children/> wrapper de Button + buildWhatsAppUrl + track + 250ms loading"
      exports: ["WhatsAppCta"]
      contains: "track(\"whatsapp_click\""
    - path: "src/components/ui/whatsapp-icon.tsx"
      provides: "WhatsApp SVG oficial como componente, currentColor"
      exports: ["WhatsAppIcon"]
    - path: "src/components/ui/container.tsx"
      provides: "<Container> wrapper para max-w consistente (max-w-7xl mx-auto px-4 sm:px-6 lg:px-8)"
      exports: ["Container"]
    - path: "src/components/ui/headline.tsx"
      provides: "<Headline as='h1'|'h2'|'h3' size='hero'|'section'|'sub'> componente para hierarquia tipográfica brand book"
      exports: ["Headline"]
    - path: "src/app/layout.tsx"
      provides: "Root layout RSC com metadata FOUND-10 completa, viewport, html lang=pt-BR, provider tree exato AnalyticsProvider>SmoothScrollProvider>MotionConfigProvider>children + <Toaster>"
      contains: "AnalyticsProvider"
    - path: "src/app/dev/page.tsx"
      provides: "Server Component que verifica process.env.NODE_ENV === 'production' e chama notFound(); senão renderiza <DevShowcase> com atoms"
      contains: "notFound()"
    - path: "src/app/opengraph-image.tsx"
      provides: "OG image dinâmica via next/og 1200x630 com logo Likro + tagline (placeholder Phase 1; Phase 7 polish final)"
    - path: "src/app/icon.tsx"
      provides: "Favicon dinâmico via next/og baseado no logo Likro"
    - path: "docs/BRAND.md"
      provides: "Documentação leve do brand-lock (D-17 redefinido — orchestrator directive #2): regra do roxo + 4 extremos cromáticos + checklist de code review"
      contains: "## Brand Lock"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/providers/{analytics,smooth-scroll,motion-config}-provider.tsx"
      via: "Imports + JSX nesting na ordem AnalyticsProvider > SmoothScrollProvider > MotionConfigProvider > children"
      pattern: "AnalyticsProvider[\\s\\S]*SmoothScrollProvider[\\s\\S]*MotionConfigProvider"
    - from: "src/components/ui/whatsapp-cta.tsx"
      to: "src/lib/whatsapp.ts + src/lib/analytics.ts + src/content/whatsapp.ts"
      via: "Imports buildWhatsAppUrl, track, WHATSAPP_MESSAGES — único componente que toca esses três pontos"
      pattern: "import.*buildWhatsAppUrl.*from.*@/lib/whatsapp"
    - from: "src/components/providers/smooth-scroll-provider.tsx"
      to: "lenis/react ReactLenis + motion/react useReducedMotion"
      via: "syncTouch: false (1.x API, NÃO smoothTouch)"
      pattern: "syncTouch:\\s*false"
    - from: "src/app/dev/page.tsx"
      to: "next/navigation notFound + process.env.NODE_ENV"
      via: "Build-time elimination — Webpack/SWC inlines NODE_ENV; if branch é dead-code-eliminated em prod"
      pattern: "process\\.env\\.NODE_ENV.*production"
---

<objective>
Wave 2 monta o que Wave 0+1 prepararam: provider tree completo (AnalyticsProvider > SmoothScrollProvider > MotionConfigProvider > Toaster) integrado em `app/layout.tsx`, atoms shadcn customizados (Button/Card/Input/Textarea/Label/Dialog/Sheet/Sonner) + 3 atoms próprios (Container, Headline, WhatsAppCta + WhatsAppIcon), metadata completa FOUND-10, OG image + favicon, rota `/dev` com showcase dos atoms, e a doc leve BRAND.md fechando a 3ª camada do brand-lock (orchestrator directive #2).

Purpose: Esta plan fecha a Phase 1 — o repo fica pronto para Phase 2 (motion primitives) e Phase 3 (Hero) começarem. Todos os mecanismos de disciplina visual + WhatsApp + tracking estão wired e testados. `/dev` showcase serve como sandbox visual onde Lenny aprova feel premium dos atoms (D-02, D-03, D-04, D-05).

Output: 11 atoms (.tsx), 3 providers (.tsx), layout completo, /dev page, OG/favicon, BRAND.md, 5 test files (cobrindo provider order + smooth-scroll skip + analytics scripts presentes + WhatsAppCta integration + dev route guard).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@.planning/phases/01-foundations-design-system/01-01-SUMMARY.md
@.planning/phases/01-foundations-design-system/01-02-SUMMARY.md
@.planning/phases/01-foundations-design-system/01-03-SUMMARY.md
@src/lib/whatsapp.ts
@src/lib/analytics.ts
@src/lib/env.ts
@src/lib/utils.ts
@src/hooks/use-device-tier.ts
@src/content/whatsapp.ts
@src/app/globals.css

<interfaces>
<!-- APIs consumidas (todas já implementadas em Plans 01-03): -->

```ts
// src/lib/whatsapp.ts
export function buildWhatsAppUrl(message: string, location: WhatsAppLocation): string;
export type WhatsAppLocation = "hero"|"pain"|"product"|"how"|"proof"|"footer"|"floating";

// src/lib/analytics.ts
export function track(event: AnalyticsEvent, payload?: Record<string, unknown>): void;
export type AnalyticsEvent = "whatsapp_click"|"cta_click"|...;

// src/lib/env.ts
export const env: { NEXT_PUBLIC_WA_NUMBER, NEXT_PUBLIC_GA4_ID, NEXT_PUBLIC_META_PIXEL_ID, NEXT_PUBLIC_CLARITY_ID };

// src/lib/utils.ts
export function cn(...inputs: ClassValue[]): string;

// src/hooks/use-device-tier.ts
export function useDeviceTier(): "reduced"|"mobile"|"tablet"|"desktop";

// src/content/whatsapp.ts
export const WHATSAPP_MESSAGES: Record<WhatsAppLocation, string>;
```

shadcn primitives (instalados nesta plan via CLI):
- Button, Card{Header,Title,Description,Content,Footer}, Input, Textarea, Label, Dialog{Trigger,Content,Header,Title,Description,Footer}, Sheet{Trigger,Content,Header,Title,Description}, Toaster (sonner)

Tailwind classes disponíveis (de Plan 01 globals.css):
- bg-accent-primary, bg-accent-hover, bg-accent-glow
- bg-surface-{dark,darker,light,lighter}
- text-text-{primary,secondary,muted}, text-text-on-dark-{primary,secondary,muted}
- border-border-{subtle,default}, border-border-on-dark-{subtle,default}
- bg-neutral-{50..900}
- font-sans, rounded-md (10px), rounded-lg (12px)
- transition-* com --ease-premium-out via arbitrary value [cubic-bezier(0.16,1,0.3,1)]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn add atoms + customização Linear/Stripe + atoms próprios (Container, Headline, WhatsAppIcon, WhatsAppCta) + tests integration</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/input.tsx, src/components/ui/textarea.tsx, src/components/ui/label.tsx, src/components/ui/dialog.tsx, src/components/ui/sheet.tsx, src/components/ui/sonner.tsx, src/components/ui/container.tsx, src/components/ui/headline.tsx, src/components/ui/whatsapp-icon.tsx, src/components/ui/whatsapp-cta.tsx, tests/components/ui/whatsapp-cta.test.tsx, src/lib/whatsapp.ts, tests/lib/whatsapp.test.ts</files>
  <read_first>
    - components.json (criado em Plan 01 — confirma Tailwind v4 mode)
    - src/app/globals.css (Plan 01 — tokens disponíveis)
    - src/lib/utils.ts (Plan 01 — cn helper)
    - src/lib/whatsapp.ts (Plan 02 — buildWhatsAppUrl)
    - src/lib/analytics.ts (Plan 03 — track)
    - src/content/whatsapp.ts (Plan 02 — WHATSAPP_MESSAGES)
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Atoms UI — Pattern shadcn customizado, snippet completo do <WhatsAppCta>)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (D-01, D-02, D-03 Linear+Stripe, D-04 micro-states, D-08, D-09, D-10, D-11)
  </read_first>
  <action>
    1. **Adicionar atoms shadcn** (D-01) via CLI — comando único:
       ```
       npx --yes shadcn@latest add button card input textarea label dialog sheet sonner
       ```
       Aceitar overwrites. CLI vai criar arquivos em `src/components/ui/` baseado em `components.json`.

    2. **Customizar Button** (`src/components/ui/button.tsx`) — substituir variants default por brand book Likro (D-02, D-03 Linear+Stripe feel, D-04 micro-states). Manter API cva existente, ajustar apenas as classes:

       ```tsx
       import * as React from "react";
       import { Slot } from "@radix-ui/react-slot";
       import { cva, type VariantProps } from "class-variance-authority";
       import { cn } from "@/lib/utils";

       const buttonVariants = cva(
         // Base: refinement Linear (peso visual leve, foco bem resolvido) + Stripe (profundidade sutil)
         "inline-flex items-center justify-center gap-2 whitespace-nowrap rounded-md text-sm font-medium " +
           "transition-all duration-200 [transition-timing-function:cubic-bezier(0.16,1,0.3,1)] " +
           "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-primary focus-visible:ring-offset-2 " +
           "disabled:pointer-events-none disabled:opacity-50 " +
           "[&_svg]:pointer-events-none [&_svg]:size-4 [&_svg]:shrink-0",
         {
           variants: {
             variant: {
               // Primary: roxo accent CTA principal (Stripe-like depth)
               default:
                 "bg-accent-primary text-white shadow-sm hover:bg-accent-hover hover:shadow-md active:translate-y-px",
               // Secondary: outline com texto roxo (Linear-like minimal)
               secondary:
                 "border border-accent-primary bg-transparent text-accent-primary hover:bg-accent-primary/5",
               // Ghost: sem fundo, hover sutil
               ghost: "hover:bg-neutral-100 text-text-primary",
               // Outline: border neutral
               outline:
                 "border border-border-default bg-surface-lighter text-text-primary hover:bg-neutral-50",
               // Destructive
               destructive: "bg-red-600 text-white hover:bg-red-700",
               // Link
               link: "text-accent-primary underline-offset-4 hover:underline",
             },
             size: {
               default: "h-10 px-4 py-2",
               sm: "h-9 rounded-md px-3",
               lg: "h-12 rounded-md px-6 text-base",
               icon: "size-10",
             },
           },
           defaultVariants: { variant: "default", size: "default" },
         },
       );

       export interface ButtonProps
         extends React.ButtonHTMLAttributes<HTMLButtonElement>,
           VariantProps<typeof buttonVariants> {
         asChild?: boolean;
       }

       const Button = React.forwardRef<HTMLButtonElement, ButtonProps>(
         ({ className, variant, size, asChild = false, ...props }, ref) => {
           const Comp = asChild ? Slot : "button";
           return (
             <Comp
               className={cn(buttonVariants({ variant, size }), className)}
               ref={ref}
               {...props}
             />
           );
         },
       );
       Button.displayName = "Button";

       export { Button, buttonVariants };
       ```

       Se `@radix-ui/react-slot` não estiver instalado (shadcn CLI já adiciona como peer): `npm install @radix-ui/react-slot`.

    3. **Customizar Card** (`src/components/ui/card.tsx`) — refinement Linear/Stripe: border-1 + shadow sutil:

       Manter export shape `Card`, `CardHeader`, `CardTitle`, `CardDescription`, `CardContent`, `CardFooter` (shadcn default API). Substituir classes raiz para:
       - Card: `rounded-lg border border-border-subtle bg-surface-lighter shadow-sm`
       - Em contexto dark: classes específicas via prop variant futura (não bloqueia Phase 1).

    4. **Customizar Input/Textarea** — Linear-like minimal: border-bottom em foco em vez de full ring:
       - Input: `flex h-10 w-full rounded-md border border-border-default bg-surface-lighter px-3 py-2 text-sm transition-colors focus-visible:outline-none focus-visible:border-accent-primary focus-visible:ring-1 focus-visible:ring-accent-primary disabled:opacity-50`
       - Textarea: similar, min-h-20.

    5. **Customizar Label** — usar text-text-primary em vez de neutral default.

    6. **Customizar Sonner** (`src/components/ui/sonner.tsx`) — confirmar export `Toaster` que aceita props standard sonner (richColors, position). Sem mudanças visuais críticas em Phase 1; Phase 4+ refinará.

    7. **Dialog/Sheet** — manter shadcn default (Radix sob capô). Customização visual fica para fases que consumirem (Phase 5 form em Sheet?).

    8. **Criar `src/components/ui/container.tsx`** (NÃO-shadcn, próprio):
       ```tsx
       import * as React from "react";
       import { cn } from "@/lib/utils";

       interface ContainerProps extends React.HTMLAttributes<HTMLDivElement> {
         as?: "div" | "section" | "main" | "article";
       }

       export function Container({
         as: Component = "div",
         className,
         ...props
       }: ContainerProps) {
         return (
           <Component
             className={cn("mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8", className)}
             {...props}
           />
         );
       }
       ```

    9. **Criar `src/components/ui/headline.tsx`** — hierarquia tipográfica brand book:
       ```tsx
       import * as React from "react";
       import { cn } from "@/lib/utils";

       type HeadlineSize = "hero" | "section" | "sub";
       type HeadlineAs = "h1" | "h2" | "h3";

       interface HeadlineProps extends React.HTMLAttributes<HTMLHeadingElement> {
         as?: HeadlineAs;
         size?: HeadlineSize;
       }

       const SIZE_CLASSES: Record<HeadlineSize, string> = {
         hero: "text-4xl sm:text-5xl lg:text-7xl font-bold tracking-tight",
         section: "text-3xl sm:text-4xl lg:text-5xl font-bold tracking-tight",
         sub: "text-xl sm:text-2xl font-semibold",
       };

       export function Headline({
         as: Component = "h2",
         size = "section",
         className,
         ...props
       }: HeadlineProps) {
         return (
           <Component
             className={cn("font-sans text-text-primary", SIZE_CLASSES[size], className)}
             {...props}
           />
         );
       }
       ```

    10. **Criar `src/components/ui/whatsapp-icon.tsx`** (D-11 — SVG oficial, currentColor):
        ```tsx
        import * as React from "react";

        type Props = React.SVGAttributes<SVGSVGElement>;

        export function WhatsAppIcon(props: Props) {
          return (
            <svg
              viewBox="0 0 24 24"
              fill="currentColor"
              xmlns="http://www.w3.org/2000/svg"
              aria-hidden="true"
              {...props}
            >
              <path d="M17.472 14.382c-.297-.149-1.758-.867-2.03-.967-.273-.099-.471-.148-.67.15-.197.297-.767.966-.94 1.164-.173.199-.347.223-.644.075-.297-.15-1.255-.463-2.39-1.475-.883-.788-1.48-1.761-1.653-2.059-.173-.297-.018-.458.13-.606.134-.133.298-.347.446-.52.149-.174.198-.298.298-.497.099-.198.05-.371-.025-.52-.075-.149-.669-1.612-.916-2.207-.242-.579-.487-.5-.669-.51-.173-.008-.371-.01-.57-.01-.198 0-.52.074-.792.372-.272.297-1.04 1.016-1.04 2.479 0 1.462 1.065 2.875 1.213 3.074.149.198 2.096 3.2 5.077 4.487.709.306 1.262.489 1.694.625.712.227 1.36.195 1.871.118.571-.085 1.758-.719 2.006-1.413.248-.694.248-1.289.173-1.413-.074-.124-.272-.198-.57-.347zm-5.421 7.403h-.004a9.87 9.87 0 01-5.031-1.378l-.361-.214-3.741.982.998-3.648-.235-.374a9.86 9.86 0 01-1.51-5.26c.001-5.45 4.436-9.884 9.888-9.884 2.64 0 5.122 1.03 6.988 2.898a9.825 9.825 0 012.893 6.994c-.003 5.45-4.437 9.884-9.885 9.884zm8.413-18.297A11.815 11.815 0 0012.05 0C5.495 0 .16 5.335.157 11.892c0 2.096.547 4.142 1.588 5.945L.057 24l6.305-1.654a11.882 11.882 0 005.683 1.448h.005c6.554 0 11.89-5.335 11.893-11.893a11.821 11.821 0 00-3.48-8.413z" />
            </svg>
          );
        }
        ```

    11. **Criar `src/components/ui/whatsapp-cta.tsx`** (D-08, D-09, D-10, D-11 — RESEARCH §Atoms UI snippet):
        ```tsx
        "use client";
        import { useState } from "react";
        import { Loader2 } from "lucide-react";
        import { Button } from "@/components/ui/button";
        import { WhatsAppIcon } from "@/components/ui/whatsapp-icon";
        import { buildWhatsAppUrl, type WhatsAppLocation } from "@/lib/whatsapp";
        import { WHATSAPP_MESSAGES } from "@/content/whatsapp";
        import { track } from "@/lib/analytics";
        import { cn } from "@/lib/utils";

        type Variant = "primary" | "secondary" | "floating" | "inline";

        interface Props {
          variant?: Variant;
          location: WhatsAppLocation;
          label?: string;
          className?: string;
          children?: React.ReactNode;
        }

        export function WhatsAppCta({
          variant = "primary",
          location,
          label,
          className,
          children,
        }: Props) {
          const [loading, setLoading] = useState(false);

          const handleClick = async () => {
            setLoading(true);
            // D-10: track ANTES do open garante envio antes de unload
            track("whatsapp_click", { location });
            await new Promise((r) => setTimeout(r, 250));
            const url = buildWhatsAppUrl(WHATSAPP_MESSAGES[location], location);
            window.open(url, "_blank", "noopener,noreferrer");
            setLoading(false);
          };

          // Variant → Button variant + className extras
          const variantProps =
            variant === "primary"
              ? { variant: "default" as const }
              : variant === "secondary"
                ? { variant: "secondary" as const }
                : variant === "floating"
                  ? {
                      variant: "default" as const,
                      size: "icon" as const,
                      className: cn(
                        "fixed bottom-4 right-4 z-50 size-14 rounded-full shadow-lg",
                        "[padding-bottom:env(safe-area-inset-bottom)]",
                      ),
                    }
                  : {
                      variant: "link" as const,
                      className: "h-auto p-0",
                    };

          return (
            <Button
              {...variantProps}
              onClick={handleClick}
              disabled={loading}
              data-testid="whatsapp-cta"
              data-location={location}
              className={cn(variantProps.className, className)}
              aria-label={label ?? "Falar no WhatsApp"}
            >
              {loading ? (
                <Loader2 className="size-4 animate-spin" />
              ) : (
                <WhatsAppIcon className="size-4" />
              )}
              {variant !== "floating" && (children ?? label ?? "Falar no WhatsApp")}
            </Button>
          );
        }
        ```

    12. **Criar `tests/components/ui/whatsapp-cta.test.tsx`** (FOUND-09 + integration com track):
        ```tsx
        import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
        import { render, screen } from "@testing-library/react";
        import userEvent from "@testing-library/user-event";
        import { WhatsAppCta } from "@/components/ui/whatsapp-cta";

        // Mock track
        const trackMock = vi.fn();
        vi.mock("@/lib/analytics", () => ({
          track: (...args: unknown[]) => trackMock(...args),
        }));

        describe("<WhatsAppCta>", () => {
          beforeEach(() => {
            trackMock.mockClear();
            process.env.NEXT_PUBLIC_WA_NUMBER = "5511999999999";
            window.open = vi.fn();
          });

          afterEach(() => {
            vi.restoreAllMocks();
          });

          it("renders default label 'Falar no WhatsApp'", () => {
            render(<WhatsAppCta location="hero" />);
            expect(screen.getByTestId("whatsapp-cta")).toHaveTextContent("Falar no WhatsApp");
          });

          it("renders custom label when provided", () => {
            render(<WhatsAppCta location="footer" label="Quero conversar" />);
            expect(screen.getByTestId("whatsapp-cta")).toHaveTextContent("Quero conversar");
          });

          it("calls track('whatsapp_click', {location}) on click", async () => {
            const user = userEvent.setup();
            render(<WhatsAppCta location="pain" />);
            await user.click(screen.getByTestId("whatsapp-cta"));
            expect(trackMock).toHaveBeenCalledWith("whatsapp_click", { location: "pain" });
          });

          it("opens wa.me URL after 250ms loading window (window.open called)", async () => {
            vi.useFakeTimers({ shouldAdvanceTime: true });
            const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
            render(<WhatsAppCta location="hero" />);
            await user.click(screen.getByTestId("whatsapp-cta"));
            await vi.advanceTimersByTimeAsync(300);
            expect(window.open).toHaveBeenCalledWith(
              expect.stringMatching(/^https:\/\/wa\.me\/5511999999999\?text=/),
              "_blank",
              "noopener,noreferrer",
            );
            vi.useRealTimers();
          });

          it("disables button while loading", async () => {
            const user = userEvent.setup();
            render(<WhatsAppCta location="hero" />);
            const btn = screen.getByTestId("whatsapp-cta") as HTMLButtonElement;
            await user.click(btn);
            expect(btn.disabled).toBe(true);
          });

          it("data-location attribute matches prop", () => {
            render(<WhatsAppCta location="floating" />);
            expect(screen.getByTestId("whatsapp-cta")).toHaveAttribute("data-location", "floating");
          });
        });
        ```

    13. **W3 fix (checker revision) — Migração de `src/lib/whatsapp.ts` para consumir `@/lib/env`** (Wave 2 owns this porque lib/env.ts vem de Plan 03 no mesmo Wave 1 que Plan 02 — não dava pra Plan 02 importar lib/env enquanto era criado em paralelo):

        **Por que aqui e não na Plan 02:** Plan 02 e Plan 03 rodam em paralelo (mesmo Wave 1), então whatsapp.ts foi criado lendo `process.env.NEXT_PUBLIC_WA_NUMBER` direto. Agora que Plan 04 está em Wave 2 (depende de [01,02,03]), `lib/env.ts` existe e a inconsistência (resto do código usa env wrapper, whatsapp.ts não) precisa ser sanada antes de qualquer atom consumir o helper.

        a) **Refatorar `src/lib/whatsapp.ts`** — substituir o acesso direto a process.env:

           Localizar este bloco no arquivo:
           ```ts
           const phone = process.env.NEXT_PUBLIC_WA_NUMBER;
           ```

           Substituir por:
           ```ts
           import { env } from "@/lib/env";
           // ... (no topo do arquivo, após o JSDoc/comentários)

           // ... e dentro de buildWhatsAppUrl:
           const phone = env.NEXT_PUBLIC_WA_NUMBER;
           ```

           Adicionar o `import { env } from "@/lib/env";` no topo do arquivo (após o JSDoc, antes da declaração de FORBIDDEN_HOSTS). O resto da função permanece idêntico — checks de undefined, regex de validação, encodeURIComponent, FORBIDDEN_HOSTS guards, mensagens de erro, fallback dev placeholder — TUDO inalterado.

        b) **Refatorar `tests/lib/whatsapp.test.ts`** — substituir as manipulações de `process.env` por mock de `@/lib/env`:

           Substituir o bloco `beforeEach` / `afterEach` que mexe em `process.env.NEXT_PUBLIC_WA_NUMBER` por:

           ```ts
           import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

           // Mock @/lib/env — controlamos NEXT_PUBLIC_WA_NUMBER via mockEnv.
           const mockEnv = { NEXT_PUBLIC_WA_NUMBER: "5511999999999" as string | undefined };
           vi.mock("@/lib/env", () => ({
             get env() {
               return mockEnv;
             },
           }));

           import { buildWhatsAppUrl } from "@/lib/whatsapp";

           describe("buildWhatsAppUrl", () => {
             beforeEach(() => {
               mockEnv.NEXT_PUBLIC_WA_NUMBER = "5511999999999";
             });

             afterEach(() => {
               mockEnv.NEXT_PUBLIC_WA_NUMBER = "5511999999999";
             });

             // ... (todos os 8 it(...) tests permanecem com o mesmo corpo,
             // EXCETO os que sobrescreviam process.env, que agora sobrescrevem mockEnv)
           });
           ```

           Para os tests que mudam o phone:
           - `process.env.NEXT_PUBLIC_WA_NUMBER = "+55 11 99999-9999";` → `mockEnv.NEXT_PUBLIC_WA_NUMBER = "+55 11 99999-9999";`
           - `process.env.NEXT_PUBLIC_WA_NUMBER = "55119999";` → `mockEnv.NEXT_PUBLIC_WA_NUMBER = "55119999";`
           - `process.env.NEXT_PUBLIC_WA_NUMBER = "551299999999";` → `mockEnv.NEXT_PUBLIC_WA_NUMBER = "551299999999";`
           - `delete process.env.NEXT_PUBLIC_WA_NUMBER;` → `mockEnv.NEXT_PUBLIC_WA_NUMBER = undefined;`

           Manter os 8 `it(...)` cases com mesma asserção de comportamento.

        c) **Rodar verificações:**
           ```
           grep -rEn "process\.env\.NEXT_PUBLIC_WA_NUMBER" src/lib/
           ```
           **Deve retornar 0 matches** (zero ocorrências em src/lib/ — toda leitura agora passa por env wrapper).

           ```
           npx vitest run tests/lib/whatsapp.test.ts
           ```
           **Deve passar todos os 8 tests** (comportamento inalterado, só a fonte do env mudou).

        d) **Por que isto importa (key_links integrity):** Sem este refactor, ficaria a inconsistência: `lib/whatsapp.ts` lê `process.env` direto, mas `lib/analytics.ts` (Plan 03) e providers (Plan 04) consomem via `env` wrapper. Inconsistência convida bugs futuros (ex: Phase 5 adiciona env validation no wrapper — whatsapp.ts não pega). Single source of truth para env vars = `lib/env.ts`.
  </action>
  <verify>
    <automated>npx vitest run tests/components/ui/whatsapp-cta.test.tsx tests/lib/whatsapp.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 8 atoms shadcn em `src/components/ui/`: button.tsx, card.tsx, input.tsx, textarea.tsx, label.tsx, dialog.tsx, sheet.tsx, sonner.tsx
    - 4 atoms próprios: container.tsx, headline.tsx, whatsapp-icon.tsx, whatsapp-cta.tsx
    - `src/components/ui/button.tsx` contains `bg-accent-primary` and `cubic-bezier(0.16,1,0.3,1)` and `focus-visible:ring-accent-primary`
    - `src/components/ui/whatsapp-cta.tsx` contains `track("whatsapp_click"`, `buildWhatsAppUrl(WHATSAPP_MESSAGES[location], location)`, `setTimeout(r, 250)`, `WhatsAppIcon`, `Loader2`
    - `src/components/ui/whatsapp-cta.tsx` starts with `"use client"`
    - `src/components/ui/whatsapp-cta.tsx` has variant 'floating' with `env(safe-area-inset-bottom)`
    - `tests/components/ui/whatsapp-cta.test.tsx` exists with 6 test cases
    - All 6 tests pass: `npx vitest run tests/components/ui/whatsapp-cta.test.tsx` exits 0
    - `npx tsc --noEmit` exits 0
    - Grep verification: `grep -rEn "buildWhatsAppUrl" src/` returns matches in src/lib/whatsapp.ts (definition) AND src/components/ui/whatsapp-cta.tsx (single consumer)
    - **W3 fix — lib/whatsapp.ts uses lib/env wrapper:**
      - `grep -rEn "process\.env\.NEXT_PUBLIC_WA_NUMBER" src/lib/` returns **0 matches** (zero — env wrapper é única fonte agora)
      - `src/lib/whatsapp.ts` contains literal `import { env } from "@/lib/env"`
      - `src/lib/whatsapp.ts` contains literal `env.NEXT_PUBLIC_WA_NUMBER` (NOT `process.env.NEXT_PUBLIC_WA_NUMBER`)
      - `tests/lib/whatsapp.test.ts` contains literal `vi.mock("@/lib/env"` (mock substitui a leitura direta de process.env)
      - `tests/lib/whatsapp.test.ts` contains literal `mockEnv.NEXT_PUBLIC_WA_NUMBER` (sobrescritas por test)
      - `npx vitest run tests/lib/whatsapp.test.ts` exits 0 (todos os 8 tests da Plan 02 continuam verdes — comportamento inalterado)
  </acceptance_criteria>
  <done>11 atoms criados/customizados; WhatsAppCta integra helper + track + UX 250ms loading; 6 tests verdes; brand book honrado nos atoms.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 3 Providers + integração no layout root + tests + metadata FOUND-10 + OG/favicon + /dev page (FOUND-12) + docs/BRAND.md</name>
  <!-- W2 fix (checker revision): tdd="false" porque os tests desta task são structure assertions (fs.readFileSync + regex sobre source files), NÃO TDD red→green. Eles verificam ordem do provider tree, presença do guard NODE_ENV, e invariantes como Lenis syncTouch lendo o source. WhatsAppCta integration test (Task 1) é o único realmente testando comportamento runtime. -->
  <files>src/components/providers/analytics-provider.tsx, src/components/providers/smooth-scroll-provider.tsx, src/components/providers/motion-config-provider.tsx, src/app/layout.tsx, src/app/dev/page.tsx, src/app/opengraph-image.tsx, src/app/icon.tsx, tests/providers/smooth-scroll.test.tsx, tests/providers/analytics.test.tsx, tests/app/layout-providers.test.tsx, tests/app/dev-route.test.tsx, docs/BRAND.md</files>
  <read_first>
    - src/app/layout.tsx (estado atual de Plan 01 — só Inter + metadata stub; vai ser substituído)
    - src/lib/env.ts (Plan 03 — env wrapper)
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Pattern 2 SmoothScrollProvider, §Pattern 3 MotionConfigProvider, §Pattern 4 AnalyticsProvider, §Pattern 5 Provider Tree, §Pattern 9 /dev Route Guard)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (D-06, D-07, D-17 docs leve)
    - orchestrator directives #1 (syncTouch), #6 (threat model — Clarity id, /dev guard), #7 (NODE_ENV build-time inline)
  </read_first>
  <action>
    1. **`src/components/providers/motion-config-provider.tsx`** (RESEARCH §Pattern 3):
       ```tsx
       "use client";
       import { MotionConfig } from "motion/react";
       import type { PropsWithChildren } from "react";

       export function MotionConfigProvider({ children }: PropsWithChildren) {
         return <MotionConfig reducedMotion="user">{children}</MotionConfig>;
       }
       ```

    2. **`src/components/providers/smooth-scroll-provider.tsx`** (RESEARCH §Pattern 2 — usar `syncTouch: false` per orchestrator directive #1):
       ```tsx
       "use client";
       import { ReactLenis } from "lenis/react";
       import { useReducedMotion } from "motion/react";
       import type { PropsWithChildren } from "react";

       /**
        * FOUND-08: Lenis 1.x renomeou smoothTouch → syncTouch.
        * Use `syncTouch: false` (smoothTouch é silenciosamente ignorado em 1.x).
        * Skip init em prefers-reduced-motion → native scroll.
        */
       export function SmoothScrollProvider({ children }: PropsWithChildren) {
         const reduced = useReducedMotion();

         if (reduced) return <>{children}</>;

         return (
           <ReactLenis
             root
             options={{
               lerp: 0.1,
               duration: 1.1,
               smoothWheel: true,
               syncTouch: false,
               easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
             }}
           >
             {children}
           </ReactLenis>
         );
       }
       ```

    3. **`src/components/providers/analytics-provider.tsx`** (RESEARCH §Pattern 4 — Pixel + Clarity + GA4 via @next/third-parties; Clarity id = "ms-clarity" per Pitfall E):
       ```tsx
       "use client";
       import Script from "next/script";
       import { GoogleAnalytics } from "@next/third-parties/google";
       import type { PropsWithChildren } from "react";
       import { env } from "@/lib/env";

       export function AnalyticsProvider({ children }: PropsWithChildren) {
         return (
           <>
             {children}

             {/* GA4 — handles route changes corretamente via @next/third-parties */}
             {env.NEXT_PUBLIC_GA4_ID && <GoogleAnalytics gaId={env.NEXT_PUBLIC_GA4_ID} />}

             {/* Meta Pixel */}
             {env.NEXT_PUBLIC_META_PIXEL_ID && (
               <Script
                 id="meta-pixel"
                 strategy="afterInteractive"
                 dangerouslySetInnerHTML={{
                   __html: `
                     !function(f,b,e,v,n,t,s)
                     {if(f.fbq)return;n=f.fbq=function(){n.callMethod?
                     n.callMethod.apply(n,arguments):n.queue.push(arguments)};
                     if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
                     n.queue=[];t=b.createElement(e);t.async=!0;
                     t.src=v;s=b.getElementsByTagName(e)[0];
                     s.parentNode.insertBefore(t,s)}(window, document,'script',
                     'https://connect.facebook.net/en_US/fbevents.js');
                     fbq('init', '${env.NEXT_PUBLIC_META_PIXEL_ID}');
                     fbq('track', 'PageView');
                   `,
                 }}
               />
             )}

             {/* Microsoft Clarity — id "ms-clarity" (NÃO "clarity" — Pitfall E) */}
             {env.NEXT_PUBLIC_CLARITY_ID && (
               <Script
                 id="ms-clarity"
                 strategy="afterInteractive"
                 dangerouslySetInnerHTML={{
                   __html: `
                     (function(c,l,a,r,i,t,y){
                       c[a]=c[a]||function(){(c[a].q=c[a].q||[]).push(arguments)};
                       t=l.createElement(r);t.async=1;t.src="https://www.clarity.ms/tag/"+i;
                       y=l.getElementsByTagName(r)[0];y.parentNode.insertBefore(t,y);
                     })(window, document, "clarity", "script", "${env.NEXT_PUBLIC_CLARITY_ID}");
                   `,
                 }}
               />
             )}
           </>
         );
       }
       ```

       NOTA SOBRE TEMPLATE LITERALS NO env: como esta string é avaliada em build (env é resolvido em runtime do servidor mas process.env.NEXT_PUBLIC_* é inlined pelo Webpack), o `${env.NEXT_PUBLIC_META_PIXEL_ID}` é literal-substituted. Isso é seguro porque o env var é controlado pelo deploy, não user input — mas threat model T-01-01 cobre.

    4. **Substituir COMPLETAMENTE `src/app/layout.tsx`** (RESEARCH §Pattern 5 — provider tree exata + metadata FOUND-10 completa):
       ```tsx
       import type { Metadata, Viewport } from "next";
       import { Inter } from "next/font/google";
       import { AnalyticsProvider } from "@/components/providers/analytics-provider";
       import { SmoothScrollProvider } from "@/components/providers/smooth-scroll-provider";
       import { MotionConfigProvider } from "@/components/providers/motion-config-provider";
       import { Toaster } from "@/components/ui/sonner";
       import "./globals.css";

       const inter = Inter({
         subsets: ["latin"],
         weight: ["400", "500", "700"],
         display: "swap",
         variable: "--font-inter",
         adjustFontFallback: true,
       });

       export const metadata: Metadata = {
         metadataBase: new URL("https://likro.com.br"),
         title: {
           default: "Likro — Operação comercial moderna para clínicas",
           template: "%s · Likro",
         },
         description:
           "CRM, atendimento multicanal e automação com IA — feito para clínicas e estéticas brasileiras.",
         openGraph: {
           type: "website",
           locale: "pt_BR",
           url: "https://likro.com.br",
           siteName: "Likro",
           title: "Likro — Operação comercial moderna para clínicas",
           description:
             "CRM, atendimento multicanal e automação com IA — feito para clínicas e estéticas brasileiras.",
           images: [{ url: "/opengraph-image", width: 1200, height: 630, alt: "Likro" }],
         },
         twitter: {
           card: "summary_large_image",
           title: "Likro",
           description: "Operação comercial moderna para clínicas e estéticas.",
         },
         robots: { index: true, follow: true },
         manifest: "/manifest.webmanifest",
       };

       export const viewport: Viewport = {
         themeColor: "#0a0a0b",
         colorScheme: "light dark",
         width: "device-width",
         initialScale: 1,
       };

       /**
        * FOUND-07: Provider tree em ORDEM EXATA — não trocar.
        *   AnalyticsProvider (fora — não re-renderiza no scroll)
        *     > SmoothScrollProvider (Lenis singleton, RAF único)
        *       > MotionConfigProvider (reducedMotion="user" global)
        *         > children
        *         > Toaster (overlay sonner)
        */
       export default function RootLayout({ children }: { children: React.ReactNode }) {
         return (
           <html lang="pt-BR" className={inter.variable}>
             <body>
               <AnalyticsProvider>
                 <SmoothScrollProvider>
                   <MotionConfigProvider>
                     {children}
                     <Toaster richColors position="bottom-right" />
                   </MotionConfigProvider>
                 </SmoothScrollProvider>
               </AnalyticsProvider>
             </body>
           </html>
         );
       }
       ```

    5. **`src/app/dev/page.tsx`** (RESEARCH §Pattern 9 + orchestrator directive #7 — NODE_ENV check é build-time inline):
       ```tsx
       import { notFound } from "next/navigation";
       import { Container } from "@/components/ui/container";
       import { Headline } from "@/components/ui/headline";
       import { Button } from "@/components/ui/button";
       import {
         Card,
         CardContent,
         CardDescription,
         CardHeader,
         CardTitle,
       } from "@/components/ui/card";
       import { Input } from "@/components/ui/input";
       import { Label } from "@/components/ui/label";
       import { WhatsAppCta } from "@/components/ui/whatsapp-cta";

       /**
        * FOUND-12: rota interna de showcase. Em produção, retorna 404.
        * process.env.NODE_ENV é estaticamente substituído por Webpack/SWC em build,
        * permitindo dead-code-elim do conteúdo (orchestrator directive #7).
        *
        * Esta é Server Component — sem "use client".
        */
       export default function DevPage() {
         if (process.env.NODE_ENV === "production") {
           notFound();
         }

         return (
           <main className="min-h-dvh bg-surface-light py-12">
             <Container>
               <Headline as="h1" size="hero">
                 Likro · /dev
               </Headline>
               <p className="mt-2 text-text-muted">
                 Showcase interno — disponível apenas em desenvolvimento.
               </p>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Atoms · Buttons
                 </Headline>
                 <div className="flex flex-wrap gap-3">
                   <Button>Default (primary)</Button>
                   <Button variant="secondary">Secondary</Button>
                   <Button variant="outline">Outline</Button>
                   <Button variant="ghost">Ghost</Button>
                   <Button variant="link">Link</Button>
                   <Button disabled>Disabled</Button>
                 </div>
               </section>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Atoms · WhatsAppCta variants
                 </Headline>
                 <div className="flex flex-wrap gap-3">
                   <WhatsAppCta variant="primary" location="hero" />
                   <WhatsAppCta variant="secondary" location="pain" label="Tirar dúvida" />
                   <WhatsAppCta variant="inline" location="footer" label="Falar no WhatsApp" />
                 </div>
                 {/* Floating fica em fixed bottom-right via CSS */}
                 <WhatsAppCta variant="floating" location="floating" label="Abrir WhatsApp" />
               </section>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Atoms · Card
                 </Headline>
                 <Card className="max-w-md">
                   <CardHeader>
                     <CardTitle>Card title</CardTitle>
                     <CardDescription>Subtítulo descritivo do card.</CardDescription>
                   </CardHeader>
                   <CardContent>
                     <p className="text-sm text-text-secondary">
                       Conteúdo do card. Linear+Stripe feel — border subtle, shadow sutil.
                     </p>
                   </CardContent>
                 </Card>
               </section>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Atoms · Input + Label
                 </Headline>
                 <div className="max-w-md space-y-3">
                   <div>
                     <Label htmlFor="dev-input">Nome</Label>
                     <Input id="dev-input" placeholder="Seu nome" />
                   </div>
                 </div>
               </section>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Motion Primitives (Phase 2)
                 </Headline>
                 <p className="text-text-muted">Será populado na Phase 2.</p>
               </section>

               <section className="mt-12 space-y-6">
                 <Headline as="h2" size="section">
                   Sections (Phase 3+)
                 </Headline>
                 <p className="text-text-muted">Será populado a partir da Phase 3.</p>
               </section>
             </Container>
           </main>
         );
       }
       ```

    6. **`src/app/opengraph-image.tsx`** (Phase 1 placeholder dinâmico via next/og — Phase 7 polish final):
       ```tsx
       import { ImageResponse } from "next/og";

       export const runtime = "edge";
       export const alt = "Likro — Operação comercial moderna para clínicas";
       export const size = { width: 1200, height: 630 };
       export const contentType = "image/png";

       export default function OpengraphImage() {
         return new ImageResponse(
           (
             <div
               style={{
                 fontSize: 80,
                 background: "#0a0a0b",
                 color: "#fafafa",
                 width: "100%",
                 height: "100%",
                 display: "flex",
                 flexDirection: "column",
                 justifyContent: "center",
                 alignItems: "center",
                 padding: 80,
               }}
             >
               <div style={{ fontWeight: 700, letterSpacing: -2 }}>Likro</div>
               <div
                 style={{
                   fontSize: 32,
                   marginTop: 24,
                   color: "#a1a1aa",
                   textAlign: "center",
                 }}
               >
                 Operação comercial moderna para clínicas
               </div>
             </div>
           ),
           { ...size },
         );
       }
       ```

    7. **`src/app/icon.tsx`** (favicon dinâmico, simples — Phase 7 polish):
       ```tsx
       import { ImageResponse } from "next/og";

       export const runtime = "edge";
       export const size = { width: 32, height: 32 };
       export const contentType = "image/png";

       export default function Icon() {
         return new ImageResponse(
           (
             <div
               style={{
                 fontSize: 24,
                 background: "#7c3aed",
                 color: "white",
                 width: "100%",
                 height: "100%",
                 display: "flex",
                 alignItems: "center",
                 justifyContent: "center",
                 fontWeight: 700,
                 borderRadius: 6,
               }}
             >
               L
             </div>
           ),
           { ...size },
         );
       }
       ```

    8. **`docs/BRAND.md`** (orchestrator directive #2 — terceira camada do brand-lock):

       > **Nota I2 (revisão checker):** docs/BRAND.md intencionalmente OVERRIDE a CONTEXT.md D-17 ("sem doc separada de brand") por orchestrator directive #2 — é a terceira camada de defesa do brand-lock (token absence + grep test + regra humana legível). O override é deliberado: D-17 propunha não ter doc, mas o orchestrator concluiu que tokens ausentes + grep test ainda admitem brechas culturais (ex: hard-coded #7c3aed em CSS arbitrary value, gradient com cores literais) que só code review com checklist humano pega. Esta doc é mínima (~80 linhas), focada em regras enforceable, não brand book completo.

       ```markdown
       # Likro — Brand Lock

       ## Brand Lock

       O brand book Likro é codificado mecanicamente em `src/app/globals.css` via Tailwind v4 `@theme`. Esta doc explica as regras culturais que NÃO são automaticamente forçadas — code review é a defesa final.

       ## Regra do roxo

       Roxo `#7C3AED` (`accent-primary`) é DESTAQUE, nunca PROTAGONISTA.

       **Permitido em:**
       - CTAs (botões, links de destaque)
       - Badges, chips, pills
       - Ícones ativos
       - Focus rings (`ring-accent-primary`)
       - Dots/indicators
       - Texto de destaque pontual (palavra ou frase curta)

       **Proibido em:**
       - Fundos de seção inteira (`<section className="bg-accent-primary">` é VETO)
       - Cards inteiros grandes
       - Gradients ocupando viewport
       - Header/Footer completos
       - Modal backgrounds

       **Tokens disponíveis:** apenas `accent-primary`, `accent-hover`, `accent-glow`. Não existem `accent-50/100/200/.../900`.

       **Defesa em 3 camadas:**
       1. **Tokens não declarados** — `globals.css` declara apenas os 3 acima. Tailwind v4 não auto-gera shades para tokens isolados (Plan 01).
       2. **Grep test em CI** — `tests/brand-lock.test.ts` falha CI se encontrar `bg-accent-NN`, `text-accent-NN`, `border-accent-NN`, `from-accent-NN`, `to-accent-NN`, `via-accent-NN`, `ring-accent-NN`, `outline-accent-NN`, `decoration-accent-NN` (Plan 03).
       3. **Code review** — esta doc + checklist abaixo (Plan 04).

       ## 4 extremos cromáticos a EVITAR (D-05)

       - **Branco demais** — sem personalidade, sensação de página vazia
       - **Preto demais** — pesado, cansativo, lembra dev-tool
       - **Glow demais** — vira "AI SaaS template" genérico
       - **Minimalismo frio extremo** — clínico, sem emoção, sem calor

       Equilíbrio editorial: alternation de seções `surface-dark` ↔ `surface-light`, com glows sutis (`accent-glow` rgba 0.18) apenas onde intencional. Translúcidos e sombras discretos, nunca chamativos.

       ## Code review checklist (rodar em todo PR que toque .tsx/.ts/.css)

       - [ ] Nenhuma classe `bg-accent-NN`, `text-accent-NN`, etc com shade numérico (CI já enforce)
       - [ ] Nenhum `<section>`, `<header>`, `<main>` com `bg-accent-primary` como fundo principal
       - [ ] Nenhum gradient `from-accent-* to-accent-*` ocupando viewport
       - [ ] Nenhuma cor hard-coded `#7c3aed`, `#6d28d9` em JSX/CSS — usar tokens
       - [ ] Nenhuma fonte importada além da Inter (3 pesos)
       - [ ] Nenhum motion direto em arquivo de seção (Phase 4+) — só primitivas em `components/motion/`
       - [ ] Nenhuma chamada direta a `window.fbq`, `window.gtag`, `window.clarity` — sempre via `track()` em `lib/analytics.ts`
       - [ ] Nenhuma URL WhatsApp construída fora de `lib/whatsapp.ts`
       - [ ] Nenhum `web.whatsapp.com` ou `api.whatsapp.com` em qualquer lugar do src/

       ## Tipografia

       Inter (Google Fonts, self-hosted via `next/font`). 3 pesos APENAS:
       - 400 (Regular) — body
       - 500 (Medium) — emphasis
       - 700 (Bold) — headlines

       ## Border radius

       - `rounded-md` = 10px (default — buttons, inputs, badges)
       - `rounded-lg` = 12px (cards, modals)
       - Nada acima de 12px sem aprovação.
       ```

    9. **Tests dos providers + layout + dev route:**

       > **Nota W2 (revisão checker):** Os tests de `tests/app/layout-providers.test.tsx` e `tests/app/dev-route.test.tsx` são **structure assertions** (fs.readFileSync + regex sobre o source), não TDD red→green. Eles verificam invariantes lendo o arquivo fonte: ordem do provider tree, ausência de "use client" no root layout, presença do guard `process.env.NODE_ENV === "production"` em /dev. Os tests de `tests/providers/smooth-scroll.test.tsx` e `tests/providers/analytics.test.tsx` SÃO runtime tests reais com mocks (verificam render output). Por isto a task carrega tdd="false" no header — mistura de runtime + structure asserts, não ciclo red→green puro.

       **`tests/providers/smooth-scroll.test.tsx`** (FOUND-08 — skip init em reduced):
       ```tsx
       import { describe, expect, it, vi } from "vitest";
       import { render } from "@testing-library/react";
       import { SmoothScrollProvider } from "@/components/providers/smooth-scroll-provider";

       const useReducedMotionMock = vi.fn();
       vi.mock("motion/react", () => ({
         useReducedMotion: () => useReducedMotionMock(),
       }));

       vi.mock("lenis/react", () => ({
         ReactLenis: ({ children, options }: any) => (
           <div data-testid="react-lenis" data-sync-touch={String(options.syncTouch)}>
             {children}
           </div>
         ),
       }));

       describe("<SmoothScrollProvider>", () => {
         it("returns children WITHOUT ReactLenis when reduced motion is active", () => {
           useReducedMotionMock.mockReturnValue(true);
           const { queryByTestId, getByText } = render(
             <SmoothScrollProvider>
               <span>child</span>
             </SmoothScrollProvider>,
           );
           expect(queryByTestId("react-lenis")).toBeNull();
           expect(getByText("child")).toBeInTheDocument();
         });

         it("wraps children in ReactLenis with syncTouch:false when motion enabled", () => {
           useReducedMotionMock.mockReturnValue(false);
           const { getByTestId } = render(
             <SmoothScrollProvider>
               <span>child</span>
             </SmoothScrollProvider>,
           );
           const wrapper = getByTestId("react-lenis");
           expect(wrapper).toBeInTheDocument();
           expect(wrapper.getAttribute("data-sync-touch")).toBe("false");
         });
       });
       ```

       **`tests/providers/analytics.test.tsx`** (TRACK-03):
       ```tsx
       import { describe, expect, it, vi, beforeEach } from "vitest";
       import { render } from "@testing-library/react";

       // Mock @next/third-parties before importing provider
       vi.mock("@next/third-parties/google", () => ({
         GoogleAnalytics: ({ gaId }: { gaId: string }) => (
           <div data-testid="ga4" data-ga-id={gaId} />
         ),
       }));

       vi.mock("next/script", () => ({
         default: ({ id, strategy, dangerouslySetInnerHTML }: any) => (
           <div
             data-testid={`script-${id}`}
             data-strategy={strategy}
             data-html={dangerouslySetInnerHTML?.__html ?? ""}
           />
         ),
       }));

       describe("<AnalyticsProvider>", () => {
         beforeEach(() => {
           process.env.NEXT_PUBLIC_GA4_ID = "G-TEST123";
           process.env.NEXT_PUBLIC_META_PIXEL_ID = "1234567890";
           process.env.NEXT_PUBLIC_CLARITY_ID = "abcd1234";
           vi.resetModules();
         });

         it("mounts GA4, Pixel and Clarity scripts when env vars present", async () => {
           const { AnalyticsProvider } = await import(
             "@/components/providers/analytics-provider"
           );
           const { getByTestId } = render(
             <AnalyticsProvider>
               <span>child</span>
             </AnalyticsProvider>,
           );
           expect(getByTestId("ga4")).toBeInTheDocument();
           expect(getByTestId("script-meta-pixel")).toBeInTheDocument();
           expect(getByTestId("script-meta-pixel").getAttribute("data-strategy")).toBe(
             "afterInteractive",
           );
           expect(getByTestId("script-ms-clarity")).toBeInTheDocument();
           expect(getByTestId("script-ms-clarity").getAttribute("data-strategy")).toBe(
             "afterInteractive",
           );
         });

         it("uses id='ms-clarity' (NOT 'clarity') to avoid window.clarity conflict (Pitfall E)", async () => {
           const { AnalyticsProvider } = await import(
             "@/components/providers/analytics-provider"
           );
           const { getByTestId, queryByTestId } = render(
             <AnalyticsProvider>
               <span>child</span>
             </AnalyticsProvider>,
           );
           expect(getByTestId("script-ms-clarity")).toBeInTheDocument();
           expect(queryByTestId("script-clarity")).toBeNull();
         });

         it("does not mount any vendor script when env vars missing (graceful degradation)", async () => {
           delete process.env.NEXT_PUBLIC_GA4_ID;
           delete process.env.NEXT_PUBLIC_META_PIXEL_ID;
           delete process.env.NEXT_PUBLIC_CLARITY_ID;
           vi.resetModules();
           const { AnalyticsProvider } = await import(
             "@/components/providers/analytics-provider"
           );
           const { queryByTestId } = render(
             <AnalyticsProvider>
               <span>child</span>
             </AnalyticsProvider>,
           );
           expect(queryByTestId("ga4")).toBeNull();
           expect(queryByTestId("script-meta-pixel")).toBeNull();
           expect(queryByTestId("script-ms-clarity")).toBeNull();
         });
       });
       ```

       **`tests/app/layout-providers.test.tsx`** (FOUND-07 — provider order):

       Como testar a ordem do provider tree em layout.tsx (RSC) é tricky em Vitest puro: usamos snapshot/text test do provider stack via mock. Implementação simplificada:
       ```tsx
       import { describe, expect, it } from "vitest";
       import fs from "node:fs";
       import path from "node:path";

       describe("app/layout.tsx — provider tree order", () => {
         const layoutPath = path.resolve(__dirname, "../../src/app/layout.tsx");
         const source = fs.readFileSync(layoutPath, "utf-8");

         it("imports all 3 providers + Toaster", () => {
           expect(source).toMatch(/import\s*\{\s*AnalyticsProvider\s*\}/);
           expect(source).toMatch(/import\s*\{\s*SmoothScrollProvider\s*\}/);
           expect(source).toMatch(/import\s*\{\s*MotionConfigProvider\s*\}/);
           expect(source).toMatch(/import\s*\{\s*Toaster\s*\}/);
         });

         it("nests providers in EXACT order: Analytics > SmoothScroll > MotionConfig > children > Toaster", () => {
           // Find the JSX tree (between <body> and </body>)
           const bodyMatch = source.match(/<body>([\s\S]+?)<\/body>/);
           expect(bodyMatch).not.toBeNull();
           const body = bodyMatch![1];
           const idxAnalytics = body.indexOf("<AnalyticsProvider>");
           const idxSmoothScroll = body.indexOf("<SmoothScrollProvider>");
           const idxMotionConfig = body.indexOf("<MotionConfigProvider>");
           const idxToaster = body.indexOf("<Toaster");
           const idxClosingMotion = body.indexOf("</MotionConfigProvider>");
           expect(idxAnalytics).toBeGreaterThanOrEqual(0);
           expect(idxSmoothScroll).toBeGreaterThan(idxAnalytics);
           expect(idxMotionConfig).toBeGreaterThan(idxSmoothScroll);
           expect(idxToaster).toBeGreaterThan(idxMotionConfig);
           expect(idxToaster).toBeLessThan(idxClosingMotion);
         });

         it("html has lang='pt-BR' and inter.variable className", () => {
           expect(source).toMatch(/lang=["']pt-BR["']/);
           expect(source).toMatch(/className=\{inter\.variable\}/);
         });

         it("does NOT have 'use client' directive in root layout", () => {
           expect(source.startsWith('"use client"')).toBe(false);
           expect(source.startsWith("'use client'")).toBe(false);
         });

         it("metadata exports title.template, openGraph locale=pt_BR, twitter card, robots", () => {
           expect(source).toMatch(/template:\s*["']%s\s*·\s*Likro["']/);
           expect(source).toMatch(/locale:\s*["']pt_BR["']/);
           expect(source).toMatch(/card:\s*["']summary_large_image["']/);
           expect(source).toMatch(/manifest:\s*["']\/manifest\.webmanifest["']/);
         });

         it("viewport themeColor is #0a0a0b (surface-dark, NOT roxo)", () => {
           expect(source).toMatch(/themeColor:\s*["']#0a0a0b["']/);
         });
       });
       ```

       **`tests/app/dev-route.test.tsx`** (FOUND-12 — guard via NODE_ENV):
       ```tsx
       import { describe, expect, it } from "vitest";
       import fs from "node:fs";
       import path from "node:path";

       describe("/dev route guard (FOUND-12)", () => {
         const devPath = path.resolve(__dirname, "../../src/app/dev/page.tsx");
         const source = fs.readFileSync(devPath, "utf-8");

         it("imports notFound from next/navigation", () => {
           expect(source).toMatch(/import\s*\{\s*notFound\s*\}\s*from\s*["']next\/navigation["']/);
         });

         it("checks process.env.NODE_ENV === 'production' and calls notFound()", () => {
           expect(source).toMatch(/process\.env\.NODE_ENV\s*===\s*["']production["']/);
           expect(source).toMatch(/notFound\(\)/);
         });

         it("is a Server Component (NO 'use client' directive)", () => {
           expect(source.startsWith('"use client"')).toBe(false);
           expect(source.startsWith("'use client'")).toBe(false);
         });
       });
       ```

    10. Rodar `npx vitest run` — TODOS os tests da Phase 1 (Plan 01-04) verdes.

    11. Rodar `npm run build` — completa sem erros.

    12. Rodar `npm run dev` e visitar:
        - http://localhost:3000 → ver placeholder Likro page
        - http://localhost:3000/dev → ver showcase de atoms
  </action>
  <verify>
    <automated>npx vitest run &amp;&amp; npm run build &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - 3 providers em `src/components/providers/`: analytics-provider.tsx, smooth-scroll-provider.tsx, motion-config-provider.tsx
    - `src/components/providers/smooth-scroll-provider.tsx` contains literal `syncTouch: false` (NOT smoothTouch)
    - `src/components/providers/smooth-scroll-provider.tsx` returns `<>{children}</>` (no ReactLenis) when useReducedMotion() is true
    - `src/components/providers/analytics-provider.tsx` contains `id="ms-clarity"` (NOT `id="clarity"`)
    - `src/components/providers/analytics-provider.tsx` contains `strategy="afterInteractive"` for both meta-pixel and ms-clarity Script tags
    - `src/components/providers/motion-config-provider.tsx` contains `reducedMotion="user"`
    - `src/app/layout.tsx` contains EXACT JSX nesting `<AnalyticsProvider>...<SmoothScrollProvider>...<MotionConfigProvider>...{children}...<Toaster ... /></MotionConfigProvider></SmoothScrollProvider></AnalyticsProvider>`
    - `src/app/layout.tsx` does NOT start with `"use client"` or `'use client'`
    - `src/app/layout.tsx` contains `template: "%s · Likro"`, `locale: "pt_BR"`, `card: "summary_large_image"`, `manifest: "/manifest.webmanifest"`, `themeColor: "#0a0a0b"`
    - `src/app/dev/page.tsx` contains `process.env.NODE_ENV === "production"` and `notFound()` and does NOT start with `"use client"`
    - `src/app/opengraph-image.tsx` exists, exports `runtime = "edge"`, `size = { width: 1200, height: 630 }`
    - `src/app/icon.tsx` exists, exports `runtime = "edge"`, `size = { width: 32, height: 32 }`
    - `docs/BRAND.md` exists, contains literal headings `## Brand Lock`, `## Regra do roxo`, `## 4 extremos cromáticos a EVITAR`, `## Code review checklist`
    - `tests/providers/smooth-scroll.test.tsx`, `tests/providers/analytics.test.tsx`, `tests/app/layout-providers.test.tsx`, `tests/app/dev-route.test.tsx` exist
    - `npx vitest run` exits 0 (all Phase 1 tests across 4 plans pass — expected ~40+ tests total)
    - `npm run build` exits 0 (production build OK)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <verify type="manual">
    <!-- I1 fix (checker revision): manual checks separados de acceptance_criteria automatizados.
         Estes precisam ser executados pelo Lenny (ou Claude com Playwright MCP per CLAUDE.md global rules)
         e o resultado documentado em 01-04-SUMMARY.md. -->
    - `npm run dev` → visitar http://localhost:3000/dev → confirma que showcase renderiza (Buttons, WhatsAppCta variants, Card, Input)
    - `npm run dev` → visitar http://localhost:3000/ → confirma placeholder Likro page renderiza
    - `npm run build && npm start` → visitar http://localhost:3000/dev → confirma que retorna 404 page (NÃO o showcase) — NODE_ENV=production guard funciona em runtime real, não só nos tests de structure
    - Em `npm run dev`, abrir React DevTools → confirmar provider tree visível na ordem exata: AnalyticsProvider > SmoothScrollProvider > MotionConfigProvider > children > Toaster
    - Em `npm run dev` com env vars set (NEXT_PUBLIC_GA4_ID, NEXT_PUBLIC_META_PIXEL_ID, NEXT_PUBLIC_CLARITY_ID), inspecionar DOM → confirmar `<script id="meta-pixel">` e `<script id="ms-clarity">` presentes
    - Em `npm run dev` SEM env vars (deletar do .env.local), inspecionar DOM → confirmar que NENHUM dos 3 scripts (ga4, meta-pixel, ms-clarity) aparece — graceful degradation funciona
  </verify>
  <done>Provider tree completo wired; metadata FOUND-10 completa; OG/favicon dinâmicos; /dev guard validado; BRAND.md fechando defesa do brand-lock; ~10 tests novos verdes; build de prod OK; manual /dev em prod=404 confirmado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → analytics vendors | window.fbq/gtag/clarity scripts carregados afterInteractive — não bloqueiam hydration. |
| build env → bundle | NEXT_PUBLIC_* env vars são INLINED em build (substituídos como string literal). Risco mínimo de XSS porque vars são controladas pelo deploy, não user input. |
| route boundary | /dev é Server Component com check NODE_ENV em build. Em prod, notFound() retorna 404. |
| WhatsApp deeplink | text param é encodeURIComponent (Plan 02 helper). WhatsAppCta sempre passa via WHATSAPP_MESSAGES (constants), nunca user input em Phase 1. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-14 | I (Information Disclosure) | /dev route | mitigate | NODE_ENV inline + notFound() em prod. Tested via tests/app/dev-route.test.tsx. Manual check em SUMMARY confirma 404 em next start. NOTA: Pitfall G — JSX abaixo do if é shipado no chunk, mas Phase 1 showcase é leve (~20KB) e usuário recebe 404 antes de carregar. Phase 2 deve mover showcase para arquivo dynamic-imported se ele crescer. |
| T-01-15 | T (Tampering) | Analytics scripts strategy | mitigate | strategy="afterInteractive" + dangerouslySetInnerHTML com env vars (não user input). Vars são build-time e controladas pelo deploy. |
| T-01-16 | I | Clarity ID conflict | mitigate | id="ms-clarity" (Pitfall E). Tested em tests/providers/analytics.test.tsx que verifica queryByTestId('script-clarity') === null. |
| T-01-17 | E (Elevation of Privilege) | WhatsAppCta deeplink | mitigate | text param é constante de WHATSAPP_MESSAGES (Phase 1) + encodeURIComponent (Plan 02). Phase 5 que adiciona form input deve revisar este boundary. |
| T-01-18 | D | Lenis on iOS Safari | mitigate | syncTouch: false (orchestrator directive #1) — touch usa native scroll, evita jank em sticky. Validação real-device fica em Phase 2 com primitivas de motion. |
| T-01-19 | I | "use client" leak em layout | mitigate | Test verifica que layout.tsx NÃO começa com "use client" — root é Server Component, providers são client (RSC boundary correto). Anti-pattern documentado. |
</threat_model>

<verification>
- ALL Phase 1 tests pass: `npx vitest run` (Plan 01: 3 cn tests; Plan 02: 8 whatsapp tests; Plan 03: 8+5+9 = 22 tests; Plan 04: 6+2+3+5+3 = 19 tests; total ~52 tests)
- `npm run build` exits 0
- `npx tsc --noEmit` exits 0
- `npm run lint` exits 0
- Manual: `npm run dev` → http://localhost:3000 OK + /dev OK
- Manual: `npm run build && npm start` → /dev returns 404
- DevTools inspect em dev: `<html lang="pt-BR" class="--font-inter">` + provider tree visível em React DevTools como Analytics > SmoothScroll > MotionConfig > children > Toaster
- DevTools inspect: `<script id="meta-pixel">` e `<script id="ms-clarity">` no DOM (apenas se env vars set)
</verification>

<success_criteria>
- 6 requirements cobertos: FOUND-07 (provider order test), FOUND-08 (syncTouch false + skip reduced), FOUND-09 (5+ atoms incluindo WhatsAppCta), FOUND-10 (metadata completa), FOUND-12 (/dev guard), TRACK-03 (afterInteractive scripts)
- Atoms shadcn customizados com feel Linear/Stripe (D-02, D-03, D-04)
- /dev showcase renderiza atoms — Lenny pode aprovar feel premium em runtime
- BRAND.md fecha 3ª camada do brand-lock (orchestrator directive #2)
- Build de prod passa
- ~52 tests verdes ao final da Phase 1 inteira
- Phase 1 está PRONTA pra Phase 2 começar (motion primitives)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-04-SUMMARY.md` documenting:
- Total de tests passing por plan (esperado: 3+8+22+19 = 52)
- Lista de atoms criados (8 shadcn + 4 próprios = 12)
- Confirmação manual: /dev em dev mostra showcase, /dev em prod returns 404
- Confirmação manual: provider tree visível em React DevTools
- Confirmação manual: scripts ms-clarity e meta-pixel aparecem no DOM quando env vars set
- Lista de pendências Phase 1 → Phase 2 (motion primitives precisarão usar useDeviceTier; ScrollScene será criado em Phase 2)
- Update STATE.md: marcar Phase 1 complete; mover para Phase 2 planning
</output>
