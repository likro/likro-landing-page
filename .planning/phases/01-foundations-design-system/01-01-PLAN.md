---
phase: 01-foundations-design-system
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - next.config.ts
  - eslint.config.mjs
  - .prettierrc
  - .gitignore
  - vitest.config.ts
  - tests/setup.ts
  - tests/lib/utils.test.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/lib/utils.ts
  - components.json
  - .env.local.example
  - public/logos/likro-logo.svg
  - public/logos/likro-logo.png
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-04, FOUND-05, FOUND-11]

must_haves:
  truths:
    - "npx next dev inicia sem erro de compilação na rota / em http://localhost:3000"
    - "npm run build (next build) completa sem erros e gera .next/ com bundle de produção"
    - "npx tsc --noEmit roda sem erros em modo strict"
    - "npx vitest run executa o test de cn() em tests/lib/utils.test.ts e passa"
    - "next/font carrega Inter com weights 400/500/700 e expõe CSS var --font-inter usável em @theme"
    - "robots() retorna disallow:/ quando VERCEL_ENV !== production e allow:/ quando production"
  artifacts:
    - path: "package.json"
      provides: "Next 15.5.x, React 19, Tailwind 4.3.x, motion 12.x, lenis 1.3.23, RHF 7.75+, Zod 3.23+, lucide-react ^0.460, clsx, tailwind-merge, @next/third-parties, vitest, @testing-library, jsdom"
      contains: '"next":'
    - path: "src/app/globals.css"
      provides: "Tailwind v4 @theme block com tokens completos do brand book Likro (accent.primary/hover/glow apenas em roxo, surface dark/light, text, border, neutrals stone-calibrado, font-sans, radii 10/12px)"
      contains: "@theme"
    - path: "src/app/layout.tsx"
      provides: "Root layout RSC com next/font Inter (3 weights, swap, adjustFontFallback), metadata stub, html lang=pt-BR + className inter.variable"
      contains: "lang=\"pt-BR\""
    - path: "src/lib/utils.ts"
      provides: "cn() helper combining clsx + tailwind-merge"
      exports: ["cn"]
    - path: "src/app/robots.ts"
      provides: "robots dinâmico baseado em VERCEL_ENV"
      exports: ["default"]
    - path: "src/app/sitemap.ts"
      provides: "Sitemap estático com URL https://likro.com.br"
      exports: ["default"]
    - path: "vitest.config.ts"
      provides: "Vitest jsdom + alias @/* + setup file + plugin react"
    - path: "tsconfig.json"
      provides: "TypeScript strict + paths { @/*: ./src/* }"
      contains: '"strict": true'
    - path: "components.json"
      provides: "shadcn config com Tailwind v4 (tailwind.config blank)"
    - path: ".env.local.example"
      provides: "Template de NEXT_PUBLIC_WA_NUMBER, NEXT_PUBLIC_GA4_ID, NEXT_PUBLIC_META_PIXEL_ID, NEXT_PUBLIC_CLARITY_ID, LEAD_WEBHOOK_URL"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import './globals.css' + className={inter.variable} faz Inter ficar disponível no @theme via var(--font-inter)"
      pattern: "import\\s+\"\\./globals\\.css\""
    - from: "src/app/globals.css"
      to: "next/font Inter"
      via: "--font-sans: var(--font-inter), system-ui, sans-serif no @theme block"
      pattern: "--font-sans:\\s*var\\(--font-inter\\)"
    - from: "vitest.config.ts"
      to: "src/*"
      via: "alias @/* → ./src/* permite tests importarem src files"
      pattern: "@.*src"
---

<objective>
Bootstrap do repositório Next.js 15.5 + brand tokens Tailwind v4 + Vitest. Esta plan estabelece a fundação física e de teste sobre a qual todas as outras plans (e fases) constroem. No fim desta plan o `next dev` roda, o `next build` passa, o brand book está mecanicamente codificado em `@theme`, Vitest roda, e o setup do shadcn está pronto (mas componentes ainda não foram adicionados — isso fica pra Plan 04).

Purpose: Sem este Wave 0, nada mais funciona. Vitest precisa estar instalado antes das outras plans escreverem testes. Os tokens precisam existir antes dos atoms importarem classes. Inter precisa estar carregado antes do layout ser renderizado.

Output: Repositório Next 15.5 funcional + Tailwind v4 com tokens Likro + Vitest configurado + Inter via next/font + sitemap/robots + .env.local.example documentado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Esta plan CRIA as fundações; consumidores em downstream plans são: -->
<!-- - lib/utils.ts: export function cn(...inputs: ClassValue[]): string -->
<!-- - lib/env.ts (criado em Plan 03 mas referenciado por whatsapp.ts em Plan 02): -->
<!--   export const env = { NEXT_PUBLIC_WA_NUMBER, NEXT_PUBLIC_GA4_ID, NEXT_PUBLIC_META_PIXEL_ID, NEXT_PUBLIC_CLARITY_ID } -->
<!-- - globals.css fornece classes: bg-accent-primary, bg-accent-hover, bg-surface-dark, bg-surface-light, text-text-primary, text-text-on-dark-primary, border-border-subtle, font-sans, rounded-md, rounded-lg -->
<!-- - layout.tsx será MODIFICADO em Plan 04 para inserir provider tree -->

Tokens declarados em @theme (consumir via classes Tailwind):
- bg-accent-primary, bg-accent-hover, bg-accent-glow (ÚNICOS roxo)
- bg-surface-dark, bg-surface-darker, bg-surface-light, bg-surface-lighter
- text-text-primary, text-text-secondary, text-text-muted
- text-text-on-dark-primary, text-text-on-dark-secondary, text-text-on-dark-muted
- border-border-subtle, border-border-default, border-border-on-dark-subtle, border-border-on-dark-default
- bg-neutral-50..neutral-900 (stone calibrado)
- font-sans (Inter)
- rounded-md (10px), rounded-lg (12px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scaffold Next.js 15.5 + instalar todas as deps + Vitest config</name>
  <files>package.json, package-lock.json, tsconfig.json, next.config.ts, eslint.config.mjs, .prettierrc, .gitignore, vitest.config.ts, tests/setup.ts, tests/lib/utils.test.ts, src/lib/utils.ts, components.json, .env.local.example</files>
  <read_first>
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (Standard Stack table + Bootstrap & Stack section + Validation Architecture)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (D-01, D-02, D-18, D-19, D-21)
    - CLAUDE.md (constraints + tech stack pinning)
  </read_first>
  <behavior>
    - cn(["foo", null, "bar"], "baz") returns "foo bar baz"
    - cn("p-4", "p-2") returns "p-2" (tailwind-merge dedupes conflicts)
    - cn() returns ""
  </behavior>
  <action>
    Executar a sequência de bootstrap exatamente nesta ordem (PowerShell ok no Windows):

    1. Scaffold (no working dir atual, que é vazio exceto .planning/ e CLAUDE.md):
       ```
       npx --yes create-next-app@15.5 . --ts --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm --no-turbopack=false
       ```
       Se o CLI reclamar de diretório não vazio, usar `--force` ou criar manualmente conforme estrutura Plan 01-RESEARCH §"Recommended Project Structure". Aceitar respostas default para Turbopack (yes).

    2. Pinar versões exatas no package.json (editar após install — substituir os valores que create-next-app gerou):
       - "next": "15.5.x" (pin major.minor 15.5)
       - "react": "^19.0.0"
       - "react-dom": "^19.0.0"
       - "tailwindcss": "^4.3.0"
       - "@tailwindcss/postcss": "^4.3.0"
       - "typescript": "^5.6"

    3. Instalar deps de runtime:
       ```
       npm install motion@^12.38.0 lenis@^1.3.23 react-hook-form@^7.75.0 zod@^3.23.0 @hookform/resolvers@^3.9.0 clsx@^2.1.0 tailwind-merge@^2.5.0 lucide-react@^0.460.0 @next/third-parties@^15.5.0
       ```
       NÃO bumpar lucide-react para 1.x — pin ^0.460 conforme orchestrator directive #3 e STACK.md.

    4. Instalar dev deps (Vitest stack):
       ```
       npm install -D vitest@^1.6.0 @testing-library/react@^16.0.0 @testing-library/jest-dom@^6.5.0 @testing-library/user-event@^14.5.0 jsdom@^25.0.0 @vitejs/plugin-react@^4.3.0 prettier@^3.3.0 prettier-plugin-tailwindcss@^0.6.0 eslint-plugin-tailwindcss@^4.0.0
       ```

    5. Verificar tsconfig.json (create-next-app gera; ajustar se necessário) — DEVE conter:
       ```json
       {
         "compilerOptions": {
           "strict": true,
           "noUncheckedIndexedAccess": true,
           "paths": { "@/*": ["./src/*"] }
         }
       }
       ```

    6. Criar `.prettierrc`:
       ```json
       { "plugins": ["prettier-plugin-tailwindcss"], "semi": true, "singleQuote": false, "trailingComma": "all", "printWidth": 100 }
       ```

    7. Criar `vitest.config.ts` (snippet exato do RESEARCH §"Test fixture — vitest.config.ts"):
       ```ts
       import { defineConfig } from "vitest/config";
       import react from "@vitejs/plugin-react";
       import path from "path";

       export default defineConfig({
         plugins: [react()],
         test: {
           environment: "jsdom",
           setupFiles: ["./tests/setup.ts"],
           globals: true,
         },
         resolve: {
           alias: { "@": path.resolve(__dirname, "./src") },
         },
       });
       ```

    8. Criar `tests/setup.ts`:
       ```ts
       import "@testing-library/jest-dom";
       ```

    9. Criar `src/lib/utils.ts` (cn helper, FOUND-05):
       ```ts
       import { clsx, type ClassValue } from "clsx";
       import { twMerge } from "tailwind-merge";

       export function cn(...inputs: ClassValue[]): string {
         return twMerge(clsx(inputs));
       }
       ```

    10. Criar `tests/lib/utils.test.ts` PRIMEIRO (TDD red):
        ```ts
        import { describe, expect, it } from "vitest";
        import { cn } from "@/lib/utils";

        describe("cn()", () => {
          it("merges and dedupes classes", () => {
            expect(cn(["foo", null, "bar"], "baz")).toBe("foo bar baz");
          });
          it("dedupes Tailwind conflicts via tailwind-merge", () => {
            expect(cn("p-4", "p-2")).toBe("p-2");
          });
          it("returns empty string for no input", () => {
            expect(cn()).toBe("");
          });
        });
        ```
        Rodar `npx vitest run tests/lib/utils.test.ts` — deve passar (verde).

    11. Criar `components.json` (shadcn config para Tailwind v4 — tailwind.config FICA EM BRANCO):
        ```json
        {
          "$schema": "https://ui.shadcn.com/schema.json",
          "style": "new-york",
          "rsc": true,
          "tsx": true,
          "tailwind": {
            "config": "",
            "css": "src/app/globals.css",
            "baseColor": "neutral",
            "cssVariables": true,
            "prefix": ""
          },
          "aliases": {
            "components": "@/components",
            "utils": "@/lib/utils",
            "ui": "@/components/ui",
            "lib": "@/lib",
            "hooks": "@/hooks"
          },
          "iconLibrary": "lucide"
        }
        ```
        NOTA: `tailwind.config: ""` é a flag oficial v4 (RESEARCH §Bootstrap step 8).

    12. Criar `.env.local.example` (exatamente conforme RESEARCH §Pattern 11):
        ```
        # WhatsApp — número OFICIAL Likro (formato: 5511XXXXXXXXX, sem + nem espaços)
        NEXT_PUBLIC_WA_NUMBER=5511000000000

        # Analytics (Phase 6 valida nos dashboards reais; Phase 1 só monta scripts)
        NEXT_PUBLIC_GA4_ID=
        NEXT_PUBLIC_META_PIXEL_ID=
        NEXT_PUBLIC_CLARITY_ID=

        # Phase 5 — webhook de form
        LEAD_WEBHOOK_URL=
        ```

    13. Adicionar scripts ao package.json:
        ```json
        "scripts": {
          "dev": "next dev",
          "build": "next build",
          "start": "next start",
          "lint": "next lint",
          "test": "vitest run",
          "test:watch": "vitest",
          "typecheck": "tsc --noEmit"
        }
        ```

    14. Atualizar `.gitignore` (adicionar se faltar): `.env.local`, `coverage/`, `.vercel/`, `next-env.d.ts`.
  </action>
  <verify>
    <automated>npm install &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run tests/lib/utils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `package.json` exists and contains `"next":` with version starting `15.5`
    - `package.json` contains `"motion":`, `"lenis":`, `"lucide-react": "^0.460` (NOT 1.x), `"clsx":`, `"tailwind-merge":`, `"vitest":`, `"@testing-library/react":`
    - `tsconfig.json` contains `"strict": true` and `"@/*": ["./src/*"]`
    - `vitest.config.ts` exists, contains `environment: "jsdom"` and `alias` mapping `@` to `./src`
    - `src/lib/utils.ts` exists, exports `cn` function combining clsx + twMerge
    - `tests/lib/utils.test.ts` exists with 3 test cases for cn()
    - `npx vitest run tests/lib/utils.test.ts` exits 0 with 3 passed tests
    - `npx tsc --noEmit` exits 0 with no errors
    - `components.json` exists with `"config": ""` (blank tailwind config — Tailwind v4 mode)
    - `.env.local.example` exists, contains literal strings `NEXT_PUBLIC_WA_NUMBER=5511000000000`, `NEXT_PUBLIC_GA4_ID=`, `LEAD_WEBHOOK_URL=`
    - package.json `"scripts"` contains `"test": "vitest run"` and `"typecheck": "tsc --noEmit"`
  </acceptance_criteria>
  <done>Repo scaffolded; deps pinned; Vitest runs; cn() tested green; tsc strict passes; shadcn ready to add components but not yet added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Brand tokens em globals.css (Tailwind v4 @theme) + Inter via next/font + layout root mínimo</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/globals.css (estado atual gerado por create-next-app — apagar conteúdo default)
    - src/app/layout.tsx (estado atual gerado — substituir)
    - src/app/page.tsx (estado atual — substituir por placeholder mínimo)
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Pattern 1 — Tailwind v4 Brand Lock Pattern + §Pattern 5 — Provider Tree, mas SEM os providers ainda — só metadata + Inter)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (D-13, D-14, D-16, D-17 + 4 extremos cromáticos)
  </read_first>
  <behavior>
    - Ao rodar `next build`, o CSS gerado em `.next/static/css/*.css` contém regras para `.bg-accent-primary` (cor #7c3aed) e `.bg-surface-dark` (#0a0a0b).
    - Ao tentar usar `bg-accent-50` no JSX, NENHUMA regra CSS é gerada para essa classe (silent no-op — defesa do brand-lock está nos tokens não existirem; o grep test que falha CI fica em Plan 03).
    - A classe `font-sans` resolve para Inter via var(--font-inter) injetada no <html>.
  </behavior>
  <action>
    1. **Substituir COMPLETAMENTE** `src/app/globals.css` pelo conteúdo abaixo (snippet completo de RESEARCH §Pattern 1, com cabeçalho de comentário documentando os 4 extremos cromáticos e regra do roxo per D-17):

    ```css
    @import "tailwindcss";

    /* ──────────────────────────────────────────────────────────────────
       Likro Brand Tokens (Phase 1 — não modificar sem aprovação)

       REGRA: roxo é destaque, nunca protagonista. Permitido em:
         CTAs, badges, ícones ativos, focus rings, dots/indicators,
         texto de destaque pontual.
       PROIBIDO em: fundos de seção, cards inteiros, gradients de viewport.

       4 EXTREMOS A EVITAR (cromático):
         branco demais (sem personalidade) / preto demais (pesado)
         glow demais (AI SaaS) / minimalismo frio (clínico)

       Tokens de roxo são EXCLUSIVAMENTE accent-primary/hover/glow.
       Tentar usar bg-accent-50/100/.../900 vira no-op silencioso
       (Tailwind v4 não emite CSS para tokens não declarados).
       Defesa adicional: tests/brand-lock.test.ts (Plan 03) faz grep
       e falha CI se encontrar bg-accent-NN no src/.
       ────────────────────────────────────────────────────────────────── */

    @theme {
      /* ── Accent (roxo Likro) — ÚNICOS tokens roxo do sistema ── */
      --color-accent-primary: #7c3aed;
      --color-accent-hover: #6d28d9;
      --color-accent-glow: oklch(0.62 0.21 295 / 0.18);

      /* ── Surface (alternation escuro→claro→escuro por section) ── */
      --color-surface-dark: #0a0a0b;
      --color-surface-darker: #050506;
      --color-surface-light: #fafaf9;
      --color-surface-lighter: #ffffff;

      /* ── Text (variantes pra contexto dark e light) ── */
      --color-text-primary: #0a0a0b;
      --color-text-secondary: #3f3f46;
      --color-text-muted: #71717a;
      --color-text-on-dark-primary: #fafafa;
      --color-text-on-dark-secondary: #d4d4d8;
      --color-text-on-dark-muted: #a1a1aa;

      /* ── Border ── */
      --color-border-subtle: oklch(0.92 0 0);
      --color-border-default: oklch(0.86 0 0);
      --color-border-on-dark-subtle: oklch(0.22 0 0);
      --color-border-on-dark-default: oklch(0.30 0 0);

      /* ── Neutrals (Stone calibrado, NÃO Zinc — evita "frio") ── */
      --color-neutral-50: #fafaf9;
      --color-neutral-100: #f5f5f4;
      --color-neutral-200: #e7e5e4;
      --color-neutral-300: #d6d3d1;
      --color-neutral-400: #a8a29e;
      --color-neutral-500: #78716c;
      --color-neutral-600: #57534e;
      --color-neutral-700: #44403c;
      --color-neutral-800: #292524;
      --color-neutral-900: #1c1917;

      /* ── Typography (Inter via next/font CSS variable) ── */
      --font-sans: var(--font-inter), system-ui, sans-serif;

      /* ── Radii (brand book: 10px / 12px) ── */
      --radius-md: 0.625rem;
      --radius-lg: 0.75rem;

      /* ── Easing premium (Pitfall #11) ── */
      --ease-premium-out: cubic-bezier(0.16, 1, 0.3, 1);
      --ease-premium-in: cubic-bezier(0.7, 0, 0.84, 0);
    }

    /* Body defaults — superfície clara como baseline; sections override */
    body {
      font-family: var(--font-sans);
      background: var(--color-surface-light);
      color: var(--color-text-primary);
      -webkit-font-smoothing: antialiased;
      text-rendering: optimizeLegibility;
    }

    /* prefers-reduced-motion — desliga transitions globais (D-04) */
    @media (prefers-reduced-motion: reduce) {
      *, *::before, *::after {
        animation-duration: 0.01ms !important;
        animation-iteration-count: 1 !important;
        transition-duration: 0.01ms !important;
        scroll-behavior: auto !important;
      }
    }
    ```

    2. **Substituir COMPLETAMENTE** `src/app/layout.tsx` pelo seguinte (NOTA: este é o layout MÍNIMO sem providers — Plan 04 adiciona AnalyticsProvider/SmoothScrollProvider/MotionConfigProvider/Toaster):

    ```tsx
    import type { Metadata, Viewport } from "next";
    import { Inter } from "next/font/google";
    import "./globals.css";

    // FOUND-04: Inter, 3 pesos, swap, variable CSS — NUNCA mais que 3 pesos (brand book)
    const inter = Inter({
      subsets: ["latin"],
      weight: ["400", "500", "700"],
      display: "swap",
      variable: "--font-inter",
      adjustFontFallback: true,
    });

    // Metadata stub — Plan 04 expande para FOUND-10 completo (OG, twitter, manifest, themeColor=#0a0a0b)
    export const metadata: Metadata = {
      title: "Likro",
      description: "Operação comercial moderna para clínicas e estéticas.",
    };

    export const viewport: Viewport = {
      themeColor: "#0a0a0b",
      colorScheme: "light dark",
      width: "device-width",
      initialScale: 1,
    };

    export default function RootLayout({ children }: { children: React.ReactNode }) {
      return (
        <html lang="pt-BR" className={inter.variable}>
          <body>{children}</body>
        </html>
      );
    }
    ```

    3. **Substituir** `src/app/page.tsx` por placeholder mínimo (Phase 3 implementa o Hero):

    ```tsx
    export default function HomePage() {
      return (
        <main className="min-h-dvh bg-surface-light p-8">
          <h1 className="font-sans text-4xl font-bold text-text-primary">Likro</h1>
          <p className="mt-2 text-text-muted">Foundations OK — landing real chega na Phase 3.</p>
        </main>
      );
    }
    ```

    4. Não criar PostCSS config separado — Tailwind v4 usa `@tailwindcss/postcss` que `create-next-app` já configura via postcss.config.mjs. Verificar que `postcss.config.mjs` (gerado pelo CLI) referencia `@tailwindcss/postcss`.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `@import "tailwindcss";` and `@theme {` block
    - `src/app/globals.css` contains `--color-accent-primary: #7c3aed;` and `--color-accent-hover: #6d28d9;`
    - `src/app/globals.css` does NOT contain any of: `--color-accent-50`, `--color-accent-100`, `--color-accent-200`, `--color-accent-300`, `--color-accent-400`, `--color-accent-500`, `--color-accent-600`, `--color-accent-700`, `--color-accent-800`, `--color-accent-900`
    - `src/app/globals.css` contains `--font-sans: var(--font-inter)`
    - `src/app/globals.css` contains `--radius-md: 0.625rem` and `--radius-lg: 0.75rem`
    - `src/app/globals.css` contains `@media (prefers-reduced-motion: reduce)` block
    - `src/app/layout.tsx` contains `lang="pt-BR"` and `className={inter.variable}`
    - `src/app/layout.tsx` contains `weight: ["400", "500", "700"]` and `display: "swap"` and `variable: "--font-inter"`
    - `src/app/layout.tsx` does NOT contain `"use client"` (root layout MUST be Server Component — anti-pattern §"use client" no app/layout.tsx)
    - `npm run build` exits 0
    - `.next/` directory exists after build
  </acceptance_criteria>
  <done>Brand tokens declarados; Inter carregada via next/font como CSS var; layout root é Server Component; build de prod passa; placeholder page.tsx renderiza sem erro.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Sitemap + Robots dinâmico por VERCEL_ENV (FOUND-11)</name>
  <files>src/app/sitemap.ts, src/app/robots.ts, public/logos/likro-logo.svg, public/logos/likro-logo.png</files>
  <read_first>
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Pattern 10 — robots.ts &amp; sitemap.ts)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (§Brand assets disponíveis — logos em ../logos_likro/)
  </read_first>
  <behavior>
    - Quando `VERCEL_ENV === "production"`, `robots()` retorna `{ rules: [{ userAgent: "*", allow: "/" }], sitemap: "https://likro.com.br/sitemap.xml" }`.
    - Quando `VERCEL_ENV` é `preview` ou `development` ou undefined, `robots()` retorna `{ rules: [{ userAgent: "*", disallow: "/" }] }` SEM sitemap.
    - `sitemap()` retorna lista com 1 entry para `https://likro.com.br`.
  </behavior>
  <action>
    1. Criar `src/app/robots.ts`:
       ```ts
       import type { MetadataRoute } from "next";

       export default function robots(): MetadataRoute.Robots {
         const isProd = process.env.VERCEL_ENV === "production";
         return {
           rules: isProd
             ? [{ userAgent: "*", allow: "/" }]
             : [{ userAgent: "*", disallow: "/" }],
           sitemap: isProd ? "https://likro.com.br/sitemap.xml" : undefined,
         };
       }
       ```

    2. Criar `src/app/sitemap.ts`:
       ```ts
       import type { MetadataRoute } from "next";

       export default function sitemap(): MetadataRoute.Sitemap {
         return [
           {
             url: "https://likro.com.br",
             lastModified: new Date(),
             changeFrequency: "weekly",
             priority: 1,
           },
         ];
       }
       ```

    3. Copiar logos da pasta pai para `public/logos/`:
       - `../logos_likro/LIKRO LOGO.svg` → `public/logos/likro-logo.svg`
       - `../logos_likro/logolikro_2000x2000.png` → `public/logos/likro-logo.png`

       Comando (PowerShell):
       ```powershell
       New-Item -ItemType Directory -Force -Path public/logos
       Copy-Item "../logos_likro/LIKRO LOGO.svg" public/logos/likro-logo.svg
       Copy-Item "../logos_likro/logolikro_2000x2000.png" public/logos/likro-logo.png
       ```

       Se os arquivos não existirem (caminho diferente), criar diretório vazio `public/logos/` e deixar TODO no SUMMARY apontando os caminhos esperados pra Lenny copiar manualmente.

    4. NÃO criar testes para robots/sitemap nesta task — Plan 03 cobre os testes via `tests/app/robots.test.ts` (mocking `process.env.VERCEL_ENV`).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/robots.ts` exists, exports default function returning MetadataRoute.Robots
    - `src/app/robots.ts` contains `process.env.VERCEL_ENV === "production"`
    - `src/app/sitemap.ts` exists, exports default function returning array with 1 entry where `url: "https://likro.com.br"`
    - `npm run build` exits 0 (Next.js validates these files at build time)
    - `.next/server/app/robots.txt.body` exists OR `npm run build` log mentions building robots and sitemap routes
    - `public/logos/likro-logo.svg` exists OR a TODO note in plan summary explains the copy step required by user
  </acceptance_criteria>
  <done>robots e sitemap implementados; build de prod passa; logos copiados (ou TODO documentado).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → runtime | Tailwind v4 compila tokens; CSS gerado é static. process.env.VERCEL_ENV é injetado em build. |
| filesystem → public/ | Logos copiados de fora do repo (../logos_likro/) — paths assumidos da estrutura de Lenny. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information Disclosure) | src/app/robots.ts | mitigate | VERCEL_ENV gating impede indexação de previews; tested in Plan 03 (tests/app/robots.test.ts) com mock de env. |
| T-01-02 | T (Tampering) | package.json deps | mitigate | Versões pinadas conforme STACK.md + orchestrator directive #3 (lucide ^0.460). package-lock.json commitado garante reprodutibilidade. |
| T-01-03 | I | tsconfig strict | mitigate | `strict: true` + `noUncheckedIndexedAccess` impede runtime errors silenciosos. |
| T-01-04 | D (Denial of Service) | next/font Inter | accept | Self-host build-time elimina CDN dependency; weight reduzido a 3 evita bloat. |
</threat_model>

<verification>
- `npx tsc --noEmit` — 0 errors em modo strict
- `npm run lint` — 0 errors
- `npm run build` — completa sem erros, gera .next/
- `npx vitest run` — todos os tests (apenas cn() nesta plan) passam
- `npm run dev` — abre localhost:3000 com placeholder visível
- Inspecionar DOM em dev: `<html lang="pt-BR" class="--font-inter ...">` presente
- Inspecionar `.next/static/css/*.css` em build: contém `.bg-accent-primary{background-color:#7c3aed}` e NÃO contém `.bg-accent-50`
</verification>

<success_criteria>
- 5 requirements cobertos: FOUND-01 (Next 15.5 + TS strict + ESLint/Prettier), FOUND-02 (Tailwind v4 @theme), FOUND-04 (Inter 3 weights via next/font), FOUND-05 (cn helper testado), FOUND-11 (robots/sitemap dinâmico)
- 0 tokens roxo extras declarados (apenas accent.primary/hover/glow)
- 0 erros TypeScript em strict mode
- next build passa
- vitest roda
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-01-SUMMARY.md` documenting:
- Versions actually pinned (verificar `npm list next react tailwindcss motion lenis lucide-react`)
- Whether logos were copied successfully or TODO created
- Any deviation from RESEARCH.md snippets and why
- Confirmação que postcss.config.mjs gerado pelo CLI usa @tailwindcss/postcss
</output>
