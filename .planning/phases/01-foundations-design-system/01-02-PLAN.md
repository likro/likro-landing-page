---
phase: 01-foundations-design-system
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/lib/whatsapp.ts
  - src/content/whatsapp.ts
  - tests/lib/whatsapp.test.ts
autonomous: true
requirements: [CTA-01, CTA-02]

must_haves:
  truths:
    - "buildWhatsAppUrl é o ÚNICO ponto de criação de URL WhatsApp na codebase (verificável por grep — nenhum outro arquivo constrói URL wa.me)"
    - "buildWhatsAppUrl('oi', 'hero') com NEXT_PUBLIC_WA_NUMBER='5511999999999' retorna 'https://wa.me/5511999999999?text=oi'"
    - "buildWhatsAppUrl com mensagem contendo 'web.whatsapp.com' lança Error contendo 'Forbidden host'"
    - "buildWhatsAppUrl com mensagem contendo 'api.whatsapp.com' lança Error contendo 'Forbidden host'"
    - "buildWhatsAppUrl com NEXT_PUBLIC_WA_NUMBER='+55 11 99999-9999' (formato humano) lança Error contendo 'Invalid phone format'"
    - "buildWhatsAppUrl em dev sem NEXT_PUBLIC_WA_NUMBER retorna URL com placeholder 0000000000 e emite console.warn"
    - "Encoding correto: encodeURIComponent é usado no text param (espaços viram %20, ! vira %21)"
  artifacts:
    - path: "src/lib/whatsapp.ts"
      provides: "WhatsAppLocation type union (hero|pain|product|how|proof|footer|floating); buildWhatsAppUrl(message, location) helper com guards para FORBIDDEN_HOSTS e regex de phone /^\\d{12,13}$/"
      exports: ["buildWhatsAppUrl", "WhatsAppLocation"]
      min_lines: 35
    - path: "src/content/whatsapp.ts"
      provides: "WHATSAPP_MESSAGES Record<WhatsAppLocation, string> com 7 mensagens placeholder em pt-BR (rascunhadas pelo Claude, Lenny aprova no PR)"
      exports: ["WHATSAPP_MESSAGES"]
      contains: "Record<WhatsAppLocation, string>"
    - path: "tests/lib/whatsapp.test.ts"
      provides: "Vitest unit tests cobrindo CTA-01 (canonical wa.me format) e CTA-02 (forbidden hosts guard + invalid phone guard)"
      contains: "describe(\"buildWhatsAppUrl\""
  key_links:
    - from: "src/lib/whatsapp.ts"
      to: "process.env.NEXT_PUBLIC_WA_NUMBER"
      via: "Lê env var direta (lib/env.ts ainda não existe nesta plan; Plan 03 cria — esta plan acessa process.env.NEXT_PUBLIC_WA_NUMBER diretamente)"
      pattern: "process\\.env\\.NEXT_PUBLIC_WA_NUMBER"
    - from: "src/content/whatsapp.ts"
      to: "src/lib/whatsapp.ts"
      via: "import type { WhatsAppLocation }"
      pattern: "import type.*WhatsAppLocation.*@/lib/whatsapp"
---

<objective>
Implementar o helper `buildWhatsAppUrl` (CTA-01) com guards defensivos contra hosts proibidos (CTA-02), além do mapa de mensagens placeholder por location (D-12). Esta plan é o coração do RISCO CRÍTICO #5 (WhatsApp abrindo o app, não o browser): blindar via API única + tests garante que nenhum CTA da Phase 3+ vai vazar `web.whatsapp.com` ou formato inválido.

Purpose: Sem este helper validado, qualquer dev (incluindo Claude executando Phase 3) pode escrever um link WhatsApp direto e quebrar o deeplink em mobile. Tests TDD garantem que o guard funciona ANTES de qualquer atom consumir.

Output: `src/lib/whatsapp.ts` (~50 linhas), `src/content/whatsapp.ts` (mapa de 7 mensagens), `tests/lib/whatsapp.test.ts` (5+ casos cobrindo todos os cenários do RESEARCH §Pattern 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundations-design-system/01-CONTEXT.md
@.planning/phases/01-foundations-design-system/01-RESEARCH.md
@.planning/phases/01-foundations-design-system/01-01-SUMMARY.md

<interfaces>
<!-- API exportada por esta plan (consumida pelo <WhatsAppCta> em Plan 04): -->

```ts
// src/lib/whatsapp.ts
export type WhatsAppLocation =
  | "hero"
  | "pain"
  | "product"
  | "how"
  | "proof"
  | "footer"
  | "floating";

export function buildWhatsAppUrl(
  message: string,
  location: WhatsAppLocation,
): string;
```

```ts
// src/content/whatsapp.ts
export const WHATSAPP_MESSAGES: Record<WhatsAppLocation, string>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tests primeiro (RED) — tests/lib/whatsapp.test.ts</name>
  <files>tests/lib/whatsapp.test.ts</files>
  <read_first>
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Test fixture — tests/lib/whatsapp.test.ts + §Pattern 6 — WhatsApp Helper)
    - vitest.config.ts (criado em Plan 01 — confirma alias @ funciona em tests)
  </read_first>
  <behavior>
    Cobertura mínima dos 6 casos:
    - it("builds canonical wa.me URL with encoded message")
    - it("throws on web.whatsapp.com in message (CTA-02 guard)")
    - it("throws on api.whatsapp.com in message")
    - it("throws on invalid phone format (+ ou espaços)")
    - it("uses placeholder phone when NEXT_PUBLIC_WA_NUMBER missing in dev (warns)")
    - it("URL-encodes special chars (espaços → %20, ! → %21)")
  </behavior>
  <action>
    Criar `tests/lib/whatsapp.test.ts` com o conteúdo abaixo (baseado em RESEARCH §Test fixture, expandido para cobrir todos os cenários):

    ```ts
    import { afterEach, beforeAll, beforeEach, describe, expect, it, vi } from "vitest";
    import { buildWhatsAppUrl } from "@/lib/whatsapp";

    describe("buildWhatsAppUrl", () => {
      const ORIGINAL_ENV = process.env.NEXT_PUBLIC_WA_NUMBER;

      beforeEach(() => {
        process.env.NEXT_PUBLIC_WA_NUMBER = "5511999999999";
      });

      afterEach(() => {
        process.env.NEXT_PUBLIC_WA_NUMBER = ORIGINAL_ENV;
      });

      it("builds canonical wa.me URL with encoded message", () => {
        const url = buildWhatsAppUrl("Oi! Vi a Likro", "hero");
        expect(url).toMatch(/^https:\/\/wa\.me\/5511999999999\?text=/);
        expect(url).toContain("Oi%21%20Vi%20a%20Likro");
      });

      it("URL-encodes spaces and special chars", () => {
        const url = buildWhatsAppUrl("a b & c", "footer");
        expect(url).toContain("a%20b%20%26%20c");
      });

      it("throws on web.whatsapp.com in message (CTA-02 guard)", () => {
        expect(() => buildWhatsAppUrl("vai pra web.whatsapp.com", "hero")).toThrow(
          /Forbidden host "web\.whatsapp\.com"/,
        );
      });

      it("throws on api.whatsapp.com in message (CTA-02 guard)", () => {
        expect(() => buildWhatsAppUrl("api.whatsapp.com/send", "footer")).toThrow(
          /Forbidden host "api\.whatsapp\.com"/,
        );
      });

      it("throws on invalid phone format (+ or spaces)", () => {
        process.env.NEXT_PUBLIC_WA_NUMBER = "+55 11 99999-9999";
        expect(() => buildWhatsAppUrl("oi", "hero")).toThrow(/Invalid phone format/);
      });

      it("throws on phone too short (< 12 digits)", () => {
        process.env.NEXT_PUBLIC_WA_NUMBER = "55119999";
        expect(() => buildWhatsAppUrl("oi", "hero")).toThrow(/Invalid phone format/);
      });

      it("accepts 12-digit phone (DDD without 9 prefix)", () => {
        process.env.NEXT_PUBLIC_WA_NUMBER = "551299999999";
        const url = buildWhatsAppUrl("oi", "hero");
        expect(url).toMatch(/^https:\/\/wa\.me\/551299999999/);
      });

      it("uses placeholder when NEXT_PUBLIC_WA_NUMBER missing in dev", () => {
        delete process.env.NEXT_PUBLIC_WA_NUMBER;
        const warnSpy = vi.spyOn(console, "warn").mockImplementation(() => {});
        const url = buildWhatsAppUrl("oi", "hero");
        expect(url).toMatch(/^https:\/\/wa\.me\/0000000000\?text=/);
        expect(warnSpy).toHaveBeenCalled();
        warnSpy.mockRestore();
      });
    });
    ```

    Rodar `npx vitest run tests/lib/whatsapp.test.ts` — deve falhar com "Cannot find module '@/lib/whatsapp'" (RED).
  </action>
  <verify>
    <automated>npx vitest run tests/lib/whatsapp.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/lib/whatsapp.test.ts` exists
    - File contains literal strings: `describe("buildWhatsAppUrl"`, `Forbidden host "web\\.whatsapp\\.com"`, `Forbidden host "api\\.whatsapp\\.com"`, `Invalid phone format`
    - File contains 8 `it(...)` test cases
    - `npx vitest run tests/lib/whatsapp.test.ts` exits NON-ZERO (RED — module not yet implemented). Capture output to verify failure mode is "module not found", NOT a test logic error.
  </acceptance_criteria>
  <done>Tests escritos cobrindo CTA-01 e CTA-02; vitest falha por arquivo ainda não existir (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementação (GREEN) — src/lib/whatsapp.ts + src/content/whatsapp.ts</name>
  <files>src/lib/whatsapp.ts, src/content/whatsapp.ts</files>
  <read_first>
    - tests/lib/whatsapp.test.ts (criado na Task 1 — implementação deve passar todos esses tests)
    - .planning/phases/01-foundations-design-system/01-RESEARCH.md (§Pattern 6 — WhatsApp Helper, snippet completo de buildWhatsAppUrl)
    - .planning/phases/01-foundations-design-system/01-CONTEXT.md (D-08, D-09, D-12)
  </read_first>
  <behavior>
    Comportamento já especificado pelos tests da Task 1. Implementação deve fazê-los passar.
  </behavior>
  <action>
    1. Criar `src/lib/whatsapp.ts` com EXATAMENTE este conteúdo (RESEARCH §Pattern 6, com import de env opcional removido — esta plan acessa process.env diretamente já que lib/env.ts é criado em Plan 03; refactor pra usar lib/env é safe-merge na Plan 04):

    ```ts
    /**
     * CTA-01: ÚNICO ponto de construção de URL WhatsApp na codebase.
     * CTA-02: Guards defensivos contra hosts desktop-only.
     *
     * Risco crítico #5 (PITFALLS.md): WhatsApp deeplink que abre browser
     * em vez do app. Solução: forçar formato wa.me/<phone>?text=... e
     * bloquear web.whatsapp.com / api.whatsapp.com em qualquer input.
     */

    const FORBIDDEN_HOSTS = ["web.whatsapp.com", "api.whatsapp.com"];

    /** CTA-04: location é a chave do mapa em src/content/whatsapp.ts */
    export type WhatsAppLocation =
      | "hero"
      | "pain"
      | "product"
      | "how"
      | "proof"
      | "footer"
      | "floating";

    /**
     * Constrói URL canônica wa.me/<phone>?text=<encoded>.
     *
     * @example
     *   buildWhatsAppUrl("Oi! Vi a Likro no Instagram", "hero")
     *   → "https://wa.me/5511999999999?text=Oi%21%20Vi%20a%20Likro%20no%20Instagram"
     *
     * @throws Error se message contém web.whatsapp.com ou api.whatsapp.com
     * @throws Error se NEXT_PUBLIC_WA_NUMBER inválido (precisa ser 12 ou 13 dígitos puros)
     */
    export function buildWhatsAppUrl(message: string, location: WhatsAppLocation): string {
      // CTA-02 guard: bloqueia hosts proibidos em qualquer parte do input
      for (const host of FORBIDDEN_HOSTS) {
        if (message.includes(host)) {
          throw new Error(
            `[buildWhatsAppUrl] Forbidden host "${host}" found in message. ` +
              `Use wa.me/<phone>?text=... format only. Caller location: ${location}`,
          );
        }
      }

      const phone = process.env.NEXT_PUBLIC_WA_NUMBER;

      if (!phone) {
        if (process.env.NODE_ENV !== "production") {
          // eslint-disable-next-line no-console
          console.warn(
            "[buildWhatsAppUrl] NEXT_PUBLIC_WA_NUMBER missing — using placeholder 0000000000",
          );
          return `https://wa.me/0000000000?text=${encodeURIComponent(message)}`;
        }
        throw new Error("[buildWhatsAppUrl] NEXT_PUBLIC_WA_NUMBER missing in production");
      }

      // Validação: phone deve ser 12-13 dígitos puros, sem +, espaços, ou hífens
      // DDDs 11-19, 21, 22, 24, 27, 28: 13 dígitos (5511 + 9XXXXXXXX)
      // Outros DDDs: 12 dígitos (5512 + XXXXXXXX)
      if (!/^\d{12,13}$/.test(phone)) {
        throw new Error(
          `[buildWhatsAppUrl] Invalid phone format: "${phone}". ` +
            `Expected 12-13 digits (55 + DDD + number, no spaces/+/dashes). ` +
            `Caller location: ${location}`,
        );
      }

      return `https://wa.me/${phone}?text=${encodeURIComponent(message)}`;
    }
    ```

    2. Criar `src/content/whatsapp.ts` com o mapa de mensagens placeholder (RESEARCH §Pattern 6 — exemplo de content/whatsapp.ts):

    ```ts
    import type { WhatsAppLocation } from "@/lib/whatsapp";

    /**
     * D-12: Mensagens pré-preenchidas por location.
     * Claude rascunha, Lenny aprova no PR antes da seção entrar em dev.
     *
     * Phase 1 entrega placeholders. Phase 3+ revisa e aprova section-by-section.
     */
    export const WHATSAPP_MESSAGES: Record<WhatsAppLocation, string> = {
      hero: "Oi! Vi a Likro no Instagram e quero entender como funciona pra minha clínica.",
      pain: "Oi! Tô com dificuldade pra organizar o atendimento da clínica — pode me explicar como a Likro ajuda?",
      product:
        "Oi! Vi os recursos da Likro e quero conversar sobre como aplicar na minha clínica.",
      how: "Oi! Quero entender como ficaria o fluxo de lead do Instagram até a marcação na minha clínica.",
      proof: "Oi! Quero conversar sobre a Likro pra minha clínica.",
      footer: "Oi! Quero saber mais sobre a Likro.",
      floating: "Oi! Quero conversar sobre a Likro.",
    };
    ```

    3. Rodar `npx vitest run tests/lib/whatsapp.test.ts` — todos os 8 tests devem passar (GREEN).

    4. Rodar `npx tsc --noEmit` — confirmar tipagem strict OK.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/whatsapp.test.ts &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/lib/whatsapp.ts` exists, exports `buildWhatsAppUrl` function and `WhatsAppLocation` type
    - File contains literal strings: `FORBIDDEN_HOSTS`, `"web.whatsapp.com"`, `"api.whatsapp.com"`, `/^\d{12,13}$/`, `encodeURIComponent(message)`, `https://wa.me/`
    - File `src/content/whatsapp.ts` exists, exports `WHATSAPP_MESSAGES`
    - `WHATSAPP_MESSAGES` contains exactly 7 keys: hero, pain, product, how, proof, footer, floating
    - Each message in WHATSAPP_MESSAGES is in pt-BR (contains "Oi" or "clínica" or "Likro")
    - `npx vitest run tests/lib/whatsapp.test.ts` exits 0 with all 8 tests passing
    - `npx tsc --noEmit` exits 0
    - Grep verification: `grep -rEn "wa\\.me/" src/ --include="*.ts" --include="*.tsx"` returns ONLY occurrences in `src/lib/whatsapp.ts` (no other file constructs wa.me URLs)
  </acceptance_criteria>
  <done>Helper implementado; 8 tests verdes; tsc strict passa; mapa de mensagens placeholder criado em pt-BR.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → URL | Qualquer string passada como `message` por componente. Mesmo que content/whatsapp.ts seja static (Phase 1), no futuro Phase 5 forms podem enriquecer mensagem com input — guard precisa estar pronto. |
| env → runtime | NEXT_PUBLIC_WA_NUMBER é injetado em build (NEXT_PUBLIC_ prefix). Validado em runtime para evitar formatos humanos (+55 11 9...). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T (Tampering) / I | buildWhatsAppUrl message param | mitigate | encodeURIComponent garante que `&`, `?`, espaços são escapados — impede attacker injetar query params adicionais (ex: `&text2=phishing`). Tested in Task 1 case "URL-encodes spaces and special chars". |
| T-01-06 | I | FORBIDDEN_HOSTS guard | mitigate | Throw em `web.whatsapp.com` impede que call vaze para web client (que não abre app). Defensive depth: mesmo se attacker injetar via content/whatsapp.ts, throw early. |
| T-01-07 | D | Phone validation regex | mitigate | `/^\d{12,13}$/` rejeita formatos malformados que quebrariam o deeplink em silêncio. Throw early > silent fail. |
| T-01-08 | I | NEXT_PUBLIC_ env exposure | accept | Phone WhatsApp é PUBLIC info por design (cliente liga). NEXT_PUBLIC_ prefix correto, não tem secret aqui. |
</threat_model>

<verification>
- 8 tests verdes em `npx vitest run tests/lib/whatsapp.test.ts`
- `npx tsc --noEmit` 0 errors
- Grep manual: `grep -rEn "wa\\.me" src/` mostra ocorrências APENAS em `src/lib/whatsapp.ts` (uniqueness do helper confirmada)
- Grep manual: `grep -rEn "web\\.whatsapp\\.com|api\\.whatsapp\\.com" src/` retorna apenas FORBIDDEN_HOSTS array em whatsapp.ts (não vazou em outro lugar)
</verification>

<success_criteria>
- 2 requirements cobertos: CTA-01 (helper único + canonical format), CTA-02 (forbidden hosts guard + unit test)
- Mapa de 7 mensagens placeholder em pt-BR criado
- 8 unit tests passam
- Helper é o único produtor de URLs wa.me/ no src/
- TS strict passa
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-design-system/01-02-SUMMARY.md` documenting:
- Tests passed/failed (esperado: 8/8 green)
- Lista das 7 mensagens placeholder (para Lenny aprovar/ajustar no PR)
- Confirmação que grep `wa\.me` mostra apenas src/lib/whatsapp.ts
- Note: lib/env.ts (Plan 03) vai ser opção de refactor opcional para usar `env.NEXT_PUBLIC_WA_NUMBER` em vez de process.env direto — não bloqueante.
</output>
