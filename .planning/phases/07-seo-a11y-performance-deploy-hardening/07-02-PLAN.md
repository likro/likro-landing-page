---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - package.json
  - package-lock.json
  - src/components/seo/json-ld.tsx
  - src/app/layout.tsx
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-08
  - SEO-10
requirements_addressed:
  - SEO-01
  - SEO-02
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-08
  - SEO-10

must_haves:
  truths:
    - "JSON-LD Organization e WebPage são injetados na página, tipados via schema-dts"
    - "Twitter Card declara uma imagem summary_large_image"
    - "title < 60 chars e description < 160 chars permanecem válidos e auditados"
  artifacts:
    - path: "src/components/seo/json-ld.tsx"
      provides: "Componentes OrganizationJsonLd + WebPageJsonLd tipados (schema-dts)"
      contains: "schema-dts"
      min_lines: 30
    - path: "src/app/layout.tsx"
      provides: "JSON-LD montado no body + twitter.images preenchido"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/seo/json-ld.tsx"
      via: "import + render dentro de <body>"
      pattern: "JsonLd"
    - from: "src/components/seo/json-ld.tsx"
      to: "schema-dts"
      via: "import type Organization, WebPage, WithContext"
      pattern: "schema-dts"
---

<objective>
Instalar `schema-dts` e criar a camada de SEO estruturado: JSON-LD Organization + WebPage tipados, injetados no layout. Auditar e completar a metadata (twitter.images, comprimentos title/description) já existente.

Purpose: Tornar a landing legível por mecanismos de busca e validável no Google Rich Results Test (SEO-05/06/07). Completar o Twitter Card (SEO-04).
Output: módulo `json-ld.tsx`, `schema-dts` instalado, metadata auditada e corrigida.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/layout.tsx

<interfaces>
<!-- Padrão JSON-LD recomendado (RESEARCH § Pattern 1, CITED nextjs.org/docs/app/guides/json-ld) -->

```tsx
import type { Organization, WebPage, WithContext } from "schema-dts";

const orgSchema: WithContext<Organization> = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "Likro",
  url: "https://likro.com.br",
  logo: "https://likro.com.br/logos/likro-logo.png",
  // sameAs OMITIDO — Lenny não confirmou perfis Instagram/LinkedIn.
  // Invariante de credibilidade do projeto: zero dados inventados.
};

export function OrganizationJsonLd() {
  return (
    <script
      type="application/ld+json"
      dangerouslySetInnerHTML={{ __html: JSON.stringify(orgSchema) }}
    />
  );
}
```

Estado atual de src/app/layout.tsx:
- metadata.title.default = "Likro · Operação comercial moderna para clínicas" (49 chars — OK, SEO-01)
- metadata.description = 87 chars, contém "clínicas" (OK, SEO-02)
- metadata.twitter = { card: "summary_large_image", title, description } — FALTA `images` (SEO-04)
- metadata.openGraph.images já aponta para "/opengraph-image" 1200×630
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar schema-dts e criar módulo json-ld.tsx</name>
  <read_first>
    - package.json (fonte — schema-dts ausente, confirmado por RESEARCH)
    - src/components/seo/json-ld.tsx (criar)
    - tests/seo/json-ld.test.ts (contrato — define exports esperados OrganizationJsonLd, WebPageJsonLd)
    - src/app/layout.tsx (valores canônicos: name, url, description)
  </read_first>
  <action>
    Rodar `npm install schema-dts@^2.0.0` (versão verificada pelo RESEARCH em 2026-05-21; é dependency de tipos — pode ir em dependencies).

    Criar `src/components/seo/json-ld.tsx` (Server Component, sem `"use client"`). Exportar dois componentes:

    `OrganizationJsonLd`:
    - objeto `WithContext<Organization>` com `@context: "https://schema.org"`, `@type: "Organization"`, `name: "Likro"`, `url: "https://likro.com.br"`, `logo: "https://likro.com.br/logos/likro-logo.png"`, `description` (mesma da metadata).
    - `sameAs`: NÃO incluir. Adicionar comentário explicando que Instagram/LinkedIn não foram confirmados pelo Lenny e que inventar URLs viola o invariante de credibilidade do projeto.
    - renderiza `<script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(orgSchema) }} />`.

    `WebPageJsonLd`:
    - objeto `WithContext<WebPage>` com `@type: "WebPage"`, `name: "Likro · Operação comercial moderna para clínicas"`, `url: "https://likro.com.br"`, `inLanguage: "pt-BR"`, `description`, e `isPartOf`/`about` referenciando a Organization por `@id` (opcional — manter simples: só os 5 campos obrigatórios do teste).
    - renderiza o mesmo padrão `<script>`.

    Os valores `name`/`description` devem ser importados de uma constante compartilhada ou copiados literalmente da metadata em layout.tsx para evitar drift — preferir uma const `SITE` exportada de `src/components/seo/json-ld.tsx` e reutilizada.
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/json-ld.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `schema-dts` em dependencies.
    - `src/components/seo/json-ld.tsx` existe, exporta `OrganizationJsonLd` e `WebPageJsonLd`, importa tipos de `schema-dts`.
    - Arquivo NÃO contém a string `sameAs` (campo omitido propositalmente).
    - `tests/seo/json-ld.test.ts` passa (GREEN).
    - `npm run typecheck` passa.
  </acceptance_criteria>
  <done>schema-dts instalado; json-ld.tsx criado com Organization + WebPage tipados; teste json-ld GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Montar JSON-LD no layout + completar Twitter Card + auditar metadata</name>
  <read_first>
    - src/app/layout.tsx (arquivo modificado)
    - src/components/seo/json-ld.tsx (criado na Task 1 — fonte dos componentes)
    - tests/seo/metadata.test.ts (contrato — exige twitter.images, title<60, description<160)
  </read_first>
  <action>
    Editar `src/app/layout.tsx`:

    1. Importar `{ OrganizationJsonLd, WebPageJsonLd }` de `@/components/seo/json-ld`.
    2. Renderizar ambos dentro de `<body>`, como primeiros filhos antes dos providers (são `<script>`, posição não afeta layout visual). Ex.: `<body><OrganizationJsonLd /><WebPageJsonLd />...`.
    3. Completar `metadata.twitter`: adicionar `images: ["/opengraph-image"]` (SEO-04 — reusa a OG image 1200×630). Manter `card: "summary_large_image"`.
    4. Auditar `metadata.title.default` (49 chars — OK) e `metadata.description` (87 chars — OK). NÃO alterar a copy a menos que estoure os limites; se mantiver, deixar um comentário `// SEO-01/02: title 49c, description 87c — auditado, dentro dos limites`.
    5. NÃO mexer em `metadataBase` neste plano (será tratado no 07-04).

    Confirmar que o layout permanece Server Component (sem `"use client"`).
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/metadata.test.ts tests/seo/json-ld.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` importa e renderiza `OrganizationJsonLd` e `WebPageJsonLd` dentro de `<body>`.
    - `metadata.twitter.images` está definido como `["/opengraph-image"]`.
    - `tests/seo/metadata.test.ts` passa inteiro (GREEN — incluindo a asserção twitter.images).
    - `tests/seo/json-ld.test.ts` continua GREEN.
    - `npm run typecheck` e `npm run lint` passam.
  </acceptance_criteria>
  <done>JSON-LD montado no layout; Twitter Card completo; metadata auditada; testes de metadata e json-ld GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → HTML servido | JSON-LD entra no HTML via dangerouslySetInnerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering / Injection | json-ld.tsx `dangerouslySetInnerHTML` | accept | Conteúdo é objeto estático controlado, zero input de usuário. `JSON.stringify` de literal — sem vetor de XSS. Padrão oficial Next.js (RESEARCH § Security Domain). |
| T-07-03 | Information disclosure | Organization schema | mitigate | `sameAs` omitido — não expor perfis não confirmados nem inventar URLs. Apenas dados públicos da empresa. |
</threat_model>

<verification>
- `npm test -- --run tests/seo/` — metadata e json-ld GREEN.
- `npm run typecheck` + `npm run lint` passam.
- Inspeção manual: `npm run build` gera HTML com dois `<script type="application/ld+json">`.
</verification>

<success_criteria>
- schema-dts instalado e usado para tipar Organization + WebPage.
- JSON-LD injetado no layout; pronto para o Rich Results Test (SEO-07 — verificação humana no 07-06).
- Twitter Card `summary_large_image` completo com imagem.
- title/description auditados dentro dos limites SEO-01/02.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-02-SUMMARY.md`.
</output>
