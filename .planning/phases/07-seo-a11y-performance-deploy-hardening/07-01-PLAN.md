---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/seo/metadata.test.ts
  - tests/seo/json-ld.test.ts
  - tests/seo/semantic-html.test.ts
  - tests/seo/robots.test.ts
  - tests/a11y/skip-link.test.tsx
  - tests/layout/speed-insights.test.tsx
  - tests/mobile/device-tier-usage.test.ts
autonomous: true
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-08
  - SEO-10
  - SEO-11
  - A11Y-05
  - A11Y-06
  - A11Y-07
  - MOBILE-01
  - DEPLOY-05
requirements_addressed:
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
  - SEO-06
  - SEO-08
  - SEO-10
  - SEO-11
  - A11Y-05
  - A11Y-06
  - A11Y-07
  - MOBILE-01
  - DEPLOY-05

must_haves:
  truths:
    - "A suíte de testes da Phase 7 existe e roda (RED) antes de qualquer correção"
    - "Cada teste falha por motivo real (feature ausente), não por erro de import"
  artifacts:
    - path: "tests/seo/metadata.test.ts"
      provides: "Asserções de comprimento de title/description e shape de OG/Twitter"
    - path: "tests/seo/json-ld.test.ts"
      provides: "Asserções de shape do JSON-LD Organization + WebPage"
    - path: "tests/seo/semantic-html.test.ts"
      provides: "Grep tests de h1 único, lang=pt-BR, zero div onClick, mockups aria-hidden"
    - path: "tests/seo/robots.test.ts"
      provides: "Asserção de discriminação VERCEL_ENV em robots()"
    - path: "tests/a11y/skip-link.test.tsx"
      provides: "Render test do skip-link"
    - path: "tests/layout/speed-insights.test.tsx"
      provides: "Render test de <SpeedInsights /> no layout"
    - path: "tests/mobile/device-tier-usage.test.ts"
      provides: "Grep test de uso de useDeviceTier nas seções com motion"
  key_links:
    - from: "tests/seo/*"
      to: "src/app/layout.tsx"
      via: "import de metadata"
      pattern: "import.*layout"
---

<objective>
Criar a suíte de testes da Phase 7 (Wave 0) — todos os arquivos de teste que verificam SEO, A11y e wiring de deploy, em estado RED (falhando porque as features ainda não existem).

Purpose: Estabelecer o contrato de validação ANTES de qualquer correção. Os plans 07-02..07-05 implementam contra estes testes; cada um vira GREEN ao completar.
Output: 7 arquivos de teste em `tests/`, todos rodando e falhando por motivo real.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-VALIDATION.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/layout.tsx
@src/app/robots.ts
@src/app/page.tsx

<interfaces>
<!-- Estado atual relevante (extraído do codebase) -->

src/app/layout.tsx exporta:
```typescript
export const metadata: Metadata // title.default, title.template, description, openGraph, twitter, robots, manifest
export const viewport: Viewport
export default function RootLayout({ children })
```

src/app/robots.ts exporta:
```typescript
export default function robots(): MetadataRoute.Robots
// retorna allow:/ quando VERCEL_ENV==="production", senão disallow:/
```

Test framework (de 07-VALIDATION.md): vitest@3.2.4 + @testing-library/react@16 + jsdom@25.
Config: vitest.config.ts (alias `@`, setup ./tests/setup.ts). Run: `npm test -- --run <pattern>`.
Testes existentes vivem em `tests/` (ex.: tests/content/hero.test.ts, tests/app/layout-providers.test.tsx) — seguir o mesmo estilo de import.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Testes de SEO metadata + JSON-LD + robots</name>
  <read_first>
    - tests/seo/metadata.test.ts (criar)
    - tests/seo/json-ld.test.ts (criar)
    - tests/seo/robots.test.ts (criar)
    - src/app/layout.tsx (fonte de verdade — metadata atual)
    - src/app/robots.ts (fonte de verdade — robots atual)
    - tests/content/hero.test.ts (referência de estilo de teste vitest do projeto)
  </read_first>
  <action>
    Criar 3 arquivos de teste vitest.

    `tests/seo/metadata.test.ts` — importar `{ metadata }` de `@/app/layout`. Asserções:
    - `metadata.title.default` é string com `.length < 60` (SEO-01).
    - `metadata.description` é string com `.length < 160` e contém a palavra "clínicas" (SEO-02).
    - `metadata.openGraph` tem `type`, `locale === "pt_BR"`, `url`, `siteName`, `title`, `description`, e `images` array com um item `{ width: 1200, height: 630 }` (SEO-03).
    - `metadata.twitter` tem `card === "summary_large_image"` e um campo `images` definido (SEO-04 — note que o twitter atual NÃO tem `images`; este teste deve FALHAR até o plan 07-02 adicionar).

    `tests/seo/json-ld.test.ts` — importar de `@/components/seo/json-ld` (módulo ainda NÃO existe — import vai falhar, é o esperado RED). Esperar exports `OrganizationJsonLd` e `WebPageJsonLd` (componentes React). Asserções: renderizar cada um com `@testing-library/react`, pegar o `<script type="application/ld+json">`, fazer `JSON.parse` do `innerHTML`, e assertar:
    - Organization: `@context === "https://schema.org"`, `@type === "Organization"`, `name === "Likro"`, `url` é string não-vazia, `logo` é string não-vazia (SEO-05).
    - WebPage: `@type === "WebPage"`, `name` string, `url` string, `inLanguage === "pt-BR"` (SEO-06).

    `tests/seo/robots.test.ts` — importar `robots` default de `@/app/robots`. Usar `vi.stubEnv`:
    - Com `VERCEL_ENV="production"`: `robots().rules` permite (`allow` presente, sem `disallow:"/"`); `sitemap` definido (SEO-11).
    - Com `VERCEL_ENV="preview"`: `robots().rules` tem `disallow: "/"` (SEO-11).
    Restaurar env com `vi.unstubAllEnvs()` em `afterEach`.

    Criar o diretório `tests/seo/` se não existir.
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/metadata.test.ts tests/seo/json-ld.test.ts tests/seo/robots.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 3 arquivos existem em `tests/seo/`.
    - `npm test -- --run tests/seo/` executa os testes (não erro de config do vitest).
    - `metadata.test.ts`: passa nas asserções de title/description/OG, FALHA na asserção `metadata.twitter.images` (campo ausente hoje).
    - `json-ld.test.ts`: FALHA com erro de módulo não encontrado `@/components/seo/json-ld` OU asserção (RED esperado).
    - `robots.test.ts`: passa (robots.ts já discrimina VERCEL_ENV) — este é teste de verificação.
  </acceptance_criteria>
  <done>Os 3 test files de SEO existem; rodam; estão em RED para as features ainda não implementadas (twitter.images, json-ld module).</done>
</task>

<task type="auto">
  <name>Task 2: Testes de HTML semântico + skip-link + speed-insights + device-tier</name>
  <read_first>
    - tests/seo/semantic-html.test.ts (criar)
    - tests/a11y/skip-link.test.tsx (criar)
    - tests/layout/speed-insights.test.tsx (criar)
    - tests/mobile/device-tier-usage.test.ts (criar)
    - src/app/page.tsx (fonte — main sem id hoje)
    - src/app/layout.tsx (fonte — sem skip-link nem SpeedInsights hoje)
    - tests/brand-lock.test.ts (referência de estilo de grep test do projeto — usa fs + glob)
  </read_first>
  <action>
    Criar 4 arquivos de teste.

    `tests/seo/semantic-html.test.ts` — grep tests usando `node:fs` + leitura recursiva de `src/` (seguir o padrão de `tests/brand-lock.test.ts`). Asserções:
    - Exatamente 1 ocorrência de `<h1` por arquivo `.tsx` em `src/` que tenha h1; total esperado de arquivos com `<h1` ≤ 2 (HeroCopy + página /privacy). Se um único arquivo tiver 2× `<h1`, falha (SEO-08).
    - `src/app/layout.tsx` contém literal `lang="pt-BR"` (SEO-10).
    - Zero matches da regex `<div[^>]*\sonClick` em qualquer `.tsx` de `src/` (A11Y-06).
    - Todo arquivo em `src/` cujo nome contenha `mockup`/`Mockup` (case-insensitive) contém a string `aria-hidden` (A11Y-07).

    `tests/a11y/skip-link.test.tsx` — renderizar `RootLayout` de `@/app/layout` com children dummy via `@testing-library/react` (envolver children num `<main id="main-content">` mock se preciso). Asserção: existe um `<a>` cujo texto bate `/pular para o conteúdo/i` e tem `href === "#main-content"` (A11Y-05). Import direto do layout — RED esperado até 07-03.

    `tests/layout/speed-insights.test.tsx` — renderizar `RootLayout` e assertar que a árvore contém o componente de Speed Insights. Como `<SpeedInsights />` da Vercel é difícil de detectar por testid, fazer assert grep-style alternativo: importar o source de `src/app/layout.tsx` via `fs.readFileSync` e assertar que contém `import { SpeedInsights }` e `<SpeedInsights` (DEPLOY-05). RED esperado até 07-05. Criar diretório `tests/layout/`.

    `tests/mobile/device-tier-usage.test.ts` — grep test. Para cada seção em `src/sections/` que contém `motion`/`Reveal`/`Parallax`/`whileInView`, assertar que o arquivo (ou um arquivo importado pela seção) referencia `useDeviceTier` OU usa uma primitiva de `@/components/motion` (que já encapsula tier). Implementação pragmática: listar arquivos `.tsx` em `src/sections/`; para cada um que contenha `whileInView` literal, exigir também `useDeviceTier` no mesmo arquivo. Se a arquitetura usa só primitivas (sem `whileInView` direto), o teste passa trivialmente — documentar isso em comentário (MOBILE-01). Criar diretório `tests/mobile/`.
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/semantic-html.test.ts tests/a11y/skip-link.test.tsx tests/layout/speed-insights.test.tsx tests/mobile/device-tier-usage.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Os 4 arquivos existem nos diretórios `tests/seo/`, `tests/a11y/`, `tests/layout/`, `tests/mobile/`.
    - `semantic-html.test.ts`: passa (pesquisa do RESEARCH indica higiene já boa) — teste de verificação.
    - `skip-link.test.tsx`: FALHA (skip-link não existe ainda) — RED esperado.
    - `speed-insights.test.tsx`: FALHA (`SpeedInsights` não está no layout) — RED esperado.
    - `device-tier-usage.test.ts`: roda; passa ou falha conforme estado real (documentar resultado no SUMMARY).
  </acceptance_criteria>
  <done>Os 4 test files existem e rodam; skip-link e speed-insights em RED; semantic-html e device-tier rodando como verificação.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Plano cria apenas arquivos de teste — sem superfície de runtime, sem input de usuário |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | tests/seo/* | accept | Testes não rodam em produção; sem dado sensível. Risco nulo. |
</threat_model>

<verification>
- `npm test -- --run tests/seo/ tests/a11y/ tests/layout/ tests/mobile/` executa sem erro de configuração.
- `npm run typecheck` passa (arquivos de teste tipados).
- Cada teste RED falha por feature ausente (assert ou módulo não encontrado), nunca por erro de sintaxe/import quebrado de dependência inexistente.
</verification>

<success_criteria>
- 7 arquivos de teste criados, organizados em `tests/{seo,a11y,layout,mobile}/`.
- Estado RED claro para: `metadata.twitter.images`, módulo `json-ld`, skip-link, SpeedInsights.
- Estado GREEN (verificação) para: robots, semantic-html — confirmando o que o RESEARCH apurou.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-01-SUMMARY.md` documentando: quais testes ficaram RED, quais ficaram GREEN como verificação, e o resultado do device-tier-usage test.
</output>
