---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 04
type: execute
wave: 4
depends_on: ["07-01", "07-03"]
files_modified:
  - src/app/opengraph-image.tsx
  - src/app/robots.ts
  - src/app/sitemap.ts
  - src/lib/site-url.ts
  - src/app/layout.tsx
  - next.config.ts
requirements:
  - SEO-03
  - SEO-09
  - SEO-11
requirements_addressed:
  - SEO-03
  - SEO-09
  - SEO-11

must_haves:
  truths:
    - "Previews .vercel.app retornam X-Robots-Tag: noindex via header HTTP, além do robots.txt"
    - "A OG image 1200×630 renderiza polida (fundo escuro premium, marca Likro, tagline)"
    - "URLs absolutas (OG, sitemap, robots) resolvem corretamente em .vercel.app durante a v1"
  artifacts:
    - path: "src/lib/site-url.ts"
      provides: "Helper de URL base dinâmica (VERCEL_URL) — único ponto de verdade"
      min_lines: 8
    - path: "next.config.ts"
      provides: "Header X-Robots-Tag noindex para deploys não-produção"
      contains: "X-Robots-Tag"
    - path: "src/app/opengraph-image.tsx"
      provides: "OG image 1200×630 polida"
  key_links:
    - from: "src/app/robots.ts"
      to: "src/lib/site-url.ts"
      via: "import getSiteUrl"
      pattern: "site-url"
    - from: "next.config.ts"
      to: "VERCEL_ENV"
      via: "headers() condicional"
      pattern: "VERCEL_ENV"
---

<objective>
Endurecer a camada de descoberta: header `X-Robots-Tag: noindex` em previews (defesa em profundidade sobre o robots.txt), URL base dinâmica para que OG/sitemap funcionem em `.vercel.app` antes do DNS, e polish da OG image.

Purpose: Previews não vazam para o índice do Google (SEO-11); os canais de aquisição (WhatsApp/LinkedIn) mostram preview correto já na v1 sem esperar DNS (SEO-03, RESEARCH § Pitfall 5).
Output: `next.config.ts` com headers, helper `site-url.ts`, OG image polida.

NOTA de waves: este plano edita `src/app/layout.tsx` (Task 2 — metadataBase) e `next.config.ts`. Para evitar colisão last-write-wins, depende de 07-03 (que edita `layout.tsx`) e roda na wave 4, antes do 07-05 (wave 5, que também edita ambos os arquivos).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/opengraph-image.tsx
@src/app/robots.ts
@src/app/sitemap.ts
@src/app/layout.tsx
@next.config.ts

<interfaces>
<!-- X-Robots-Tag (RESEARCH § Code Examples, CITED nextjs.org headers config) -->

```ts
// next.config.ts
async headers() {
  const isProd = process.env.VERCEL_ENV === "production";
  if (isProd) return [];
  return [{
    source: "/:path*",
    headers: [{ key: "X-Robots-Tag", value: "noindex, nofollow" }],
  }];
}
```

<!-- URL base dinâmica (RESEARCH § Pitfall 5) -->
`process.env.VERCEL_URL` = host do deploy atual sem protocolo (ex.: "likro-landing-page.vercel.app").
`process.env.VERCEL_ENV` = "production" | "preview" | "development".

Estado atual:
- next.config.ts está VAZIO (`{ /* config options here */ }`).
- robots.ts hardcoda `https://likro.com.br/sitemap.xml`.
- sitemap.ts hardcoda `https://likro.com.br`.
- layout.tsx tem `metadataBase: new URL("https://likro.com.br")` — domínio sem DNS ainda. O skip-link do 07-03 já está montado no layout — PRESERVAR.
- opengraph-image.tsx: fundo #0a0a0b, "Likro" 80px + tagline 32px. Funcional mas minimalista (a Phase 1 marcou "polish na Phase 7").
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helper site-url + X-Robots-Tag header + URLs dinâmicas em robots/sitemap</name>
  <read_first>
    - src/lib/site-url.ts (criar)
    - next.config.ts (arquivo modificado — vazio hoje)
    - src/app/robots.ts (arquivo modificado — hardcoda likro.com.br)
    - src/app/sitemap.ts (arquivo modificado — hardcoda likro.com.br)
    - tests/seo/robots.test.ts (contrato — robots deve discriminar VERCEL_ENV)
  </read_first>
  <action>
    1. Criar `src/lib/site-url.ts` exportando `getSiteUrl(): string`:
       - Se `process.env.VERCEL_ENV === "production"` → retornar `"https://likro.com.br"` (domínio final; quando o DNS resolver, fica correto automaticamente).
       - Senão, se `process.env.VERCEL_URL` existir → retornar `https://${process.env.VERCEL_URL}` (preview/branch atual).
       - Fallback (dev local) → `"http://localhost:3000"`.
       Comentar a lógica: é o ponto único de verdade para URL absoluta; resolve o Pitfall 5 (OG quebrada em `.vercel.app`).

    2. Editar `next.config.ts`: adicionar a função `headers()` async exatamente como o padrão da seção `<interfaces>` — `X-Robots-Tag: noindex, nofollow` em `source: "/:path*"` para todo deploy onde `VERCEL_ENV !== "production"`; produção retorna `[]` (sem header, indexação permitida). Manter o tipo `NextConfig`. NOTA: o 07-05 (wave 5) vai envolver este `nextConfig` com `withBundleAnalyzer` — deixar o `nextConfig` como uma const nomeada exportada de forma que o wrapper do 07-05 seja drop-in.

    3. Editar `src/app/robots.ts`: substituir o `https://likro.com.br/sitemap.xml` hardcoded por `` `${getSiteUrl()}/sitemap.xml` `` usando o helper. Manter a lógica `isProd` existente para `allow`/`disallow` (já correta, não regredir o fallback `VERCEL_ENV undefined + NODE_ENV production`).

    4. Editar `src/app/sitemap.ts`: substituir `https://likro.com.br` hardcoded por `getSiteUrl()`.

    NÃO alterar `metadataBase` aqui — é coberto na Task 2.
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/robots.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/site-url.ts` existe e exporta `getSiteUrl`.
    - `next.config.ts` contém a função `headers()` com `X-Robots-Tag` e `noindex, nofollow`, condicionada a `VERCEL_ENV !== "production"`.
    - `robots.ts` e `sitemap.ts` usam `getSiteUrl()` — zero `likro.com.br` hardcoded nesses 2 arquivos.
    - `tests/seo/robots.test.ts` GREEN.
    - `npm run build` completa sem erro (valida o `next.config.ts`).
  </acceptance_criteria>
  <done>Helper site-url criado; X-Robots-Tag em previews; robots/sitemap usando URL dinâmica; build OK.</done>
</task>

<task type="auto">
  <name>Task 2: metadataBase dinâmico + polish da OG image</name>
  <read_first>
    - src/app/layout.tsx (arquivo modificado — metadataBase hardcoded; skip-link do 07-03 já presente, PRESERVAR)
    - src/app/opengraph-image.tsx (arquivo modificado — OG minimalista)
    - src/lib/site-url.ts (criado na Task 1 — fonte de getSiteUrl)
    - tests/seo/metadata.test.ts (contrato — não pode regredir)
  </read_first>
  <action>
    1. Editar `src/app/layout.tsx`: trocar `metadataBase: new URL("https://likro.com.br")` por `metadataBase: new URL(getSiteUrl())` importando de `@/lib/site-url`. Isso garante que `images: [{ url: "/opengraph-image" }]` resolva para a URL `.vercel.app` ativa durante a v1 (Pitfall 5). NÃO mexer no resto da metadata (já auditada no 07-02) NEM no skip-link (montado no 07-03).

    2. Polish da OG image em `src/app/opengraph-image.tsx` — manter 1200×630, `runtime = "edge"`, mas elevar o acabamento visual (SEO-03):
       - Fundo escuro premium: manter `#0a0a0b` como base; adicionar um brilho radial sutil roxo `#7C3AED` em baixa opacidade num canto (destaque pontual — respeitar brand book, roxo nunca domina o fundo).
       - Marca "Likro" em peso 700, com a tagline "CRM e atendimento para clínicas e estéticas" abaixo (copy curta — confirmar tom premium, sem buzzword).
       - Alinhamento: composição editorial à esquerda (não centralizada genérica) ou centralizada com mais respiro — escolher o que ficar mais premium; documentar a escolha.
       - Garantir contraste do texto sobre o fundo escuro.
       O preview real em WhatsApp/LinkedIn + validador Meta é HUMAN-UAT (07-06). Aqui é a arte.

    SEO-09 (alt de imagens): o RESEARCH apurou que o site usa ZERO `next/image` — não há imagens informativas para receber alt. A OG image já tem `export const alt`. Apenas confirmar no SUMMARY que SEO-09 tem escopo nulo no código atual (sem manifesto de imagens — não inventar). Se o Lenny entregar prints reais depois, vira pendência registrada.
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/metadata.test.ts && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` usa `metadataBase: new URL(getSiteUrl())`; skip-link do 07-03 preservado.
    - `src/app/opengraph-image.tsx` mantém 1200×630 e `runtime = "edge"`; arte elevada (brilho roxo sutil, marca + tagline, contraste OK).
    - `tests/seo/metadata.test.ts` continua GREEN (sem regressão).
    - `npm run build` completa sem erro.
    - SUMMARY documenta: SEO-09 tem escopo nulo (site sem `next/image`); decisão de composição da OG image.
  </acceptance_criteria>
  <done>metadataBase dinâmico; OG image polida; SEO-09 documentado como escopo nulo; build OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crawler → site | robots.txt + X-Robots-Tag controlam indexação |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Information disclosure | Previews .vercel.app indexáveis | mitigate | X-Robots-Tag: noindex em todo deploy não-produção (next.config headers) + robots.txt disallow — defesa em profundidade. |
| T-07-06 | Spoofing | metadataBase resolvendo host errado | accept | `getSiteUrl()` deriva de `VERCEL_URL` (controlado pela Vercel) ou domínio fixo em produção — sem input externo. |
</threat_model>

<verification>
- `npm test -- --run tests/seo/robots.test.ts tests/seo/metadata.test.ts` GREEN.
- `npm run build` OK.
- Verificação manual (07-06): `curl -I` numa preview confirma `X-Robots-Tag: noindex`; OG preview real no WhatsApp/LinkedIn.
</verification>

<success_criteria>
- Previews retornam noindex por header HTTP além do robots.txt.
- OG image polida e URLs absolutas resolvendo em `.vercel.app`.
- SEO-09 corretamente escopado como nulo (sem inventar manifesto de imagens).
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-04-SUMMARY.md`.
</output>
