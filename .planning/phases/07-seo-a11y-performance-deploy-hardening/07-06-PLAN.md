---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 06
type: execute
wave: 6
depends_on: ["07-02", "07-03", "07-04", "07-05", "07-07"]
files_modified:
  - .planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md
autonomous: false
requirements:
  - SEO-07
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-06
  - PERF-08
  - A11Y-01
  - A11Y-02
  - A11Y-04
  - MOBILE-04
  - MOBILE-07
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03
requirements_addressed:
  - SEO-07
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-06
  - PERF-08
  - A11Y-01
  - A11Y-02
  - A11Y-04
  - MOBILE-04
  - MOBILE-07
  - DEPLOY-01
  - DEPLOY-02
  - DEPLOY-03

must_haves:
  truths:
    - "Existe um checklist HUMAN-UAT consolidado de tudo que só fecha com ferramenta externa ou device real"
    - "Os bloqueios conhecidos (GitHub push, analytics IDs, prints reais) estão registrados como itens de ação do Lenny"
    - "O Lenny percorre o checklist e marca pass/fail antes do gate da phase"
  artifacts:
    - path: ".planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md"
      provides: "Checklist de verificação manual da Phase 7"
      min_lines: 60
  key_links:
    - from: "07-HUMAN-UAT.md"
      to: "07-RESEARCH.md § Validation Architecture"
      via: "tabela Manual-Only Verifications"
      pattern: "HUMAN-UAT"
---

<objective>
Consolidar num único `07-HUMAN-UAT.md` toda a verificação que NÃO pode ser automatizada — medição de Lighthouse/CWV, Rich Results Test, preview de OG em WhatsApp/LinkedIn, device matrix real, Reduce Motion no OS — e os itens de setup bloqueados (GitHub push access, analytics IDs). Pausar para o Lenny executar o checklist.

Purpose: Os gates numéricos e a verificação em ferramentas externas/devices reais são o fechamento real da Phase 7. Sem eles, "passa nos testes" não significa "produção pronta".
Output: `07-HUMAN-UAT.md` e um checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-VALIDATION.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Escrever 07-HUMAN-UAT.md consolidado</name>
  <read_first>
    - .planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md (criar)
    - .planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md (§ Validation Architecture — tabela Manual-Only; § Pitfalls)
    - .planning/phases/07-seo-a11y-performance-deploy-hardening/07-VALIDATION.md (lista Manual-Only Verifications + External Blockers)
    - .planning/phases/05-conversion/05-HUMAN-UAT.md (referência de formato de UAT do projeto, se existir)
  </read_first>
  <action>
    Criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md` — um checklist em Markdown que o Lenny percorre. Estruturar em seções, cada item com: o que verificar, como verificar (URL/ferramenta/passos exatos), resultado esperado, e um checkbox `[ ]` + campo para o resultado real. Cada item de verificação recebe um identificador estável `B1`, `B2`, ... (sequencial) para que o teste automatizado da verificação consiga conferir presença.

    **Seção A — Performance (gates numéricos):**
    - PERF-01: Lighthouse Performance ≥90 desktop / ≥85 mobile. Como: rodar Lighthouse no Chrome DevTools (ou PageSpeed Insights web) contra a URL `.vercel.app` de produção. Anotar os dois scores.
    - PERF-02: LCP <2.5s mobile / <2.0s desktop. Fonte: o mesmo relatório Lighthouse.
    - PERF-03: CLS <0.1 em todos os breakpoints.
    - PERF-04: INP <200ms.
    - PERF-06: peso total da página ≤1.5MB mobile. Como: DevTools Network, throttling mobile, somar transfer size.
    - Incluir nota: a baseline de First Load JS (gate PERF-05 ≤150KB) já foi medida no plan 07-05 — copiar o número do 07-05-SUMMARY aqui para referência.

    **Seção B — SEO / OG (ferramentas externas):**
    - SEO-07: JSON-LD validado no Google Rich Results Test. Como: colar a URL de produção em search.google.com/test/rich-results. Esperado: Organization + WebPage detectados, zero erros.
    - SEO-03 (arte OG): preview real. Como: (1) validador da Meta (Sharing Debugger); (2) colar a URL de produção numa conversa de WhatsApp e ver o card; (3) idem no LinkedIn Post Inspector. Esperado: imagem 1200×630 polida, título e descrição corretos.

    **Seção C — Acessibilidade (ferramenta + olho humano):**
    - A11Y-01: contraste. Como: rodar axe DevTools na página, conferir os pares das seções DARK (Pain, Proof) listados no 07-03-SUMMARY. Esperado: zero violações de contraste.
    - A11Y-02: navegação por teclado. Como: percorrer o site inteiro só com Tab — skip-link → header → CTAs → form → floating. Esperado: foco visível em todos, ordem lógica, skip-link funciona.
    - A11Y-04: `prefers-reduced-motion`. Como: ativar Reduce Motion no macOS E Animations off no Windows; recarregar. Esperado: todas as animações simplificam/desligam, nada quebra.

    **Seção D — Mobile (device matrix real):**
    - MOBILE-07: testar em iPhone (iOS Safari), Android mid-tier (Chrome), iPad (Safari). Verificar: layout, CTAs tocáveis, scroll fluido, sem overflow horizontal.
    - MOBILE-04: decisão final do Lenis em touch. Como: no iPhone real, rolar a página; sentir se o scroll está natural. Esperado: sem atrito/lag. Se houver atrito → ação: pular init do Lenis em `pointer: coarse` (registrar como follow-up).

    **Seção E — Itens de setup / bloqueios (ação do Lenny):**
    - DEPLOY-01/02: GitHub→Vercel auto-deploy. BLOQUEADO — a conta `lennywajcberg` não tem push no repo `likro/likro-landing-page` (403). Ação do Lenny: virar colaborador da org `likro` ou ajustar o remote, depois conectar o repo à Vercel. Até lá, deploys são manuais via `vercel` CLI. Marcar como pendência externa, NÃO como gap da phase.
    - DEPLOY-03: configurar na Vercel (Production + Preview + Development) as 3 env vars de analytics: `NEXT_PUBLIC_GA4_ID`, `NEXT_PUBLIC_META_PIXEL_ID`, `NEXT_PUBLIC_CLARITY_ID`. Pendente da Phase 6 Parte B. As demais (`NEXT_PUBLIC_WA_NUMBER`, `RESEND_*`, `GOOGLE_*`) já estão.
    - PERF-08: prints reais de produto. O site usa só mockups CSS hoje. Ação: se o Lenny tiver prints reais, entregar para um swap futuro via `next/image`; senão, PERF-08 fica "otimizado o que existe" — sem manifesto de imagens inventado.

    **Seção F — Sign-off:**
    - Checklist final: suíte de testes verde, todas as seções A-D marcadas pass, pendências da Seção E registradas. Campo de aprovação do Lenny.

    Cada item deve ser auto-contido — o Lenny não deveria precisar abrir outro arquivo para entender o que fazer. O arquivo deve conter pelo menos 15 checkboxes `[ ]` e o identificador `B1` no primeiro item da Seção A.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md';if(!fs.existsSync(p)){console.error('arquivo ausente');process.exit(1)}const c=fs.readFileSync(p,'utf8');const boxes=(c.match(/\[ \]/g)||[]).length;const hasB1=c.includes('B1');if(boxes>=15&&hasB1){console.log('OK checkboxes='+boxes);process.exit(0)}else{console.error('checkboxes='+boxes+' hasB1='+hasB1);process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `07-HUMAN-UAT.md` existe com as 6 seções A-F.
    - Cada um dos requisitos manuais (SEO-07, PERF-01..04/06/08, A11Y-01/02/04, MOBILE-04/07, DEPLOY-01/02/03) aparece como item com checkbox.
    - Cada item tem instruções de COMO verificar (URL/ferramenta/passos), não só o quê.
    - DEPLOY-01/02 marcado explicitamente como bloqueio externo (não gap da phase).
    - O comando Node de verificação retorna exit 0: ≥15 checkboxes `[ ]` e o identificador `B1` presente.
  </acceptance_criteria>
  <done>07-HUMAN-UAT.md criado, consolidando toda a verificação manual e os bloqueios externos da Phase 7.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Checkpoint — Lenny executa o HUMAN-UAT</name>
  <what-built>
    A Phase 7 completou a parte automatizável: JSON-LD + metadata (07-02), skip-link + auditoria a11y (07-03), X-Robots-Tag + OG + URLs dinâmicas (07-04), Speed Insights + bundle-analyzer + auditoria de performance (07-05), auditoria mobile QA + deploy (07-07). O `07-HUMAN-UAT.md` consolida tudo que só fecha com ferramenta externa ou device real.
  </what-built>
  <how-to-verify>
    1. Abrir `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-HUMAN-UAT.md`.
    2. Garantir que há um deploy `.vercel.app` atualizado (rodar `vercel --prod` se necessário — deploy manual, já que o auto-deploy está bloqueado).
    3. Percorrer as Seções A-D do checklist marcando pass/fail e anotando os números reais (scores Lighthouse, LCP/CLS/INP, peso da página, resultado do Rich Results Test, previews de OG).
    4. Na Seção E, registrar o estado dos bloqueios (GitHub access, analytics IDs, prints reais).
    5. Se algum gate numérico falhar (ex.: Lighthouse <85 mobile), descrever o que falhou — vira gap para `/gsd-plan-phase --gaps`.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se todos os gates A-D passaram (pendências da Seção E são aceitáveis), ou descreva os gates que falharam.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum) | Plano cria documentação de verificação — sem código de runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09 | (n/a) | 07-HUMAN-UAT.md | accept | Documento de checklist — sem superfície de ataque. |
</threat_model>

<verification>
- `07-HUMAN-UAT.md` existe e cobre todos os requisitos manuais da Phase 7.
- Checkpoint humano: Lenny percorre o checklist e aprova ou reporta gaps.
</verification>

<success_criteria>
- Checklist HUMAN-UAT consolidado e auto-contido.
- Bloqueios externos (DEPLOY-01/02, analytics IDs, prints reais) registrados como pendências, não como falhas da phase.
- Lenny tem um caminho claro para fechar os gates numéricos da v1.
</success_criteria>

<output>
Após o checkpoint, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-06-SUMMARY.md` com o resultado do UAT (gates que passaram, gaps abertos, pendências externas).
</output>
