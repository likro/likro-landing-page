---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 05
type: execute
wave: 5
depends_on: ["07-01", "07-04"]
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - src/app/layout.tsx
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
  - PERF-09
  - DEPLOY-05
requirements_addressed:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
  - PERF-09
  - DEPLOY-05

must_haves:
  truths:
    - "Vercel Speed Insights está montado e coleta CWV em produção"
    - "@next/bundle-analyzer está instalado e o build mede o First Load JS"
    - "Bundle, CLS e lazy-load below-fold estão auditados e documentados"
    - "PERF-09 (navigator.connection) é progressive enhancement com fallback no-op"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "<SpeedInsights /> montado no body"
      contains: "SpeedInsights"
    - path: "next.config.ts"
      provides: "wrapper withBundleAnalyzer condicionado a ANALYZE"
      contains: "withBundleAnalyzer"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "import + render <SpeedInsights />"
      pattern: "SpeedInsights"
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "withBundleAnalyzer wrapper"
      pattern: "bundle-analyzer"
---

<objective>
Fechar o stream de Performance + infra: instalar e ligar Speed Insights (CWV contínuo) e bundle-analyzer (gate de 150KB), auditar bundle/CLS/lazy-load below-fold, e implementar PERF-09 como progressive enhancement com fallback no-op.

Purpose: Monitoramento de produção (DEPLOY-05), instrumento de medição de bundle (PERF-05), auditoria de lazy-load (PERF-07) e degradação de conexão (PERF-09). Toda medição numérica de gate (Lighthouse, LCP, CLS, INP, peso) é HUMAN-UAT no 07-06.
Output: 2 pacotes instalados e ligados, relatório de auditoria de performance no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/layout.tsx
@next.config.ts
@src/hooks/use-device-tier.ts

<interfaces>
<!-- Speed Insights (RESEARCH § Code Examples, CITED vercel.com/docs/speed-insights) -->
```tsx
import { SpeedInsights } from "@vercel/speed-insights/next";
// dentro de <body>, após children:
<SpeedInsights />
```

<!-- bundle-analyzer wiring (RESEARCH § Code Examples) -->
```ts
import withBundleAnalyzer from "@next/bundle-analyzer";
const analyze = withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" });
export default analyze(nextConfig);
// rodar: ANALYZE=true npm run build
```

Estado atual / pitfalls do RESEARCH:
- `@vercel/speed-insights@^2.0.0` e `@next/bundle-analyzer` NÃO estão em package.json (CONTEXT errou).
- `@next/bundle-analyzer@16` é major 16 — se quebrar com Next 15.5, fixar `@^15` (Pitfall 2).
- Seções são quase todas RSC — `next/dynamic` nelas é net-neutro; aplicar só a ilhas client pesadas (LeadForm). (Pitfall 1 — PERF-07)
- `navigator.connection` não existe no Safari (browser dominante do tráfego alvo). PERF-09 é progressive enhancement com fallback no-op. (Pitfall 6)
- `next.config.ts` já terá `headers()` do plan 07-04 — PRESERVAR ao adicionar o wrapper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar e ligar Speed Insights + bundle-analyzer</name>
  <read_first>
    - package.json (fonte — ambos pacotes ausentes)
    - next.config.ts (arquivo modificado — já tem headers() do 07-04, PRESERVAR)
    - src/app/layout.tsx (arquivo modificado — montar SpeedInsights)
    - tests/layout/speed-insights.test.tsx (contrato — exige import e <SpeedInsights no layout)
  </read_first>
  <action>
    1. Rodar `npm install @vercel/speed-insights@^2.0.0` e `npm install -D @next/bundle-analyzer`. Após instalar, se `@next/bundle-analyzer` puxar major 16 e o build quebrar, reinstalar com `npm install -D @next/bundle-analyzer@^15` (Pitfall 2 — documentar no SUMMARY qual versão ficou).

    2. Editar `src/app/layout.tsx`: importar `{ SpeedInsights }` de `@vercel/speed-insights/next` e renderizar `<SpeedInsights />` dentro de `<body>`, após `children` (perto do `<Toaster />`). Layout permanece Server Component.

    3. Editar `next.config.ts`: envolver o `nextConfig` (que já contém `headers()` do plan 07-04) com `withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" })` exatamente como o padrão da seção `<interfaces>`. NÃO remover nem alterar a função `headers()`.

    4. Rodar `npm run build` uma vez e capturar do output a tabela "First Load JS" por rota. Registrar o número da rota `/` no SUMMARY — é a baseline do gate PERF-05 (≤150KB gzipped). Se já estiver acima, listar suspeitos (motion, lenis) para o SUMMARY; a otimização fina, se necessária, é decisão pós-medição.
  </action>
  <verify>
    <automated>npm test -- --run tests/layout/speed-insights.test.tsx && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `@vercel/speed-insights` (deps) e `@next/bundle-analyzer` (devDeps).
    - `src/app/layout.tsx` importa e renderiza `<SpeedInsights />`.
    - `next.config.ts` exporta o config envolvido por `withBundleAnalyzer` E mantém a função `headers()` do 07-04.
    - `tests/layout/speed-insights.test.tsx` GREEN.
    - `npm run build` completa; SUMMARY registra o First Load JS da rota `/`.
  </acceptance_criteria>
  <done>Speed Insights e bundle-analyzer instalados e ligados; build OK; baseline de bundle registrada.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoria de performance — bundle, lazy-load below-fold, PERF-09</name>
  <read_first>
    - src/sections/*.tsx (uso de RSC vs client — PERF-07)
    - src/components/forms/LeadForm.tsx (ilha client pesada — candidata a dynamic)
    - src/hooks/use-device-tier.ts (PERF-09 — onde encaixar o sinal de conexão)
    - tests/layout/speed-insights.test.tsx (contrato — não regredir)
  </read_first>
  <action>
    Auditoria — corrigir apenas o que falhar; documentar tudo no SUMMARY.

    1. **Bundle / First Load JS (PERF-05):** Com base no output "First Load JS" da Task 1, listar no SUMMARY o número da rota `/` e os maiores contribuintes. Se acima de 150KB gzipped, identificar suspeitos (motion, lenis, lucide). Rodar `ANALYZE=true npm run build` para gerar o treemap e confirmar. A otimização fina, se necessária, é decisão pós-medição registrada como follow-up — não inflar este plano.

    2. **CLS (PERF-07/CLS):** Auditar que seções below-fold têm `min-h-*` declarado e que nenhum elemento desloca layout após o paint inicial. Documentar os pontos auditados.

    3. **Lazy-load below-fold (PERF-07):** Conforme Pitfall 1 — as seções são RSC, `next/dynamic` nelas é net-neutro. Avaliar com base no output "First Load JS" da Task 1 quais ilhas CLIENT pesam (provável: `LeadForm` com react-hook-form+zod). SE o LeadForm/Form for client e estiver below-fold, aplicar `next/dynamic` a ele com `ssr: true` (preserva SEO). Se a análise mostrar que não há ganho, registrar no SUMMARY: "PERF-07 auditado — arquitetura RSC já entrega o efeito; dynamic não aplicado / aplicado seletivamente a X". NUNCA usar `ssr: false` em seção de conteúdo (anti-pattern — quebra SEO).

    4. **PERF-09 (conexões lentas):** Implementar como progressive enhancement com fallback no-op (Pitfall 6 / Open Question 4 RESOLVED). Abordagem: dentro de `src/hooks/use-device-tier.ts` (ou um helper consumido por ele), ler `navigator.connection?.effectiveType` de forma defensiva — `navigator.connection` é `undefined` no Safari (browser dominante do tráfego alvo); o código NÃO deve lançar erro, apenas retornar o tier inalterado quando a API ausente. Se `effectiveType` for `"slow-2g"` ou `"2g"`, rebaixar o tier para `reduced`. Documentar no SUMMARY que PERF-09 só beneficia Android Chrome e que Safari/iOS continua coberto por `useDeviceTier` + `prefers-reduced-motion`.
  </action>
  <verify>
    <automated>npm test -- --run tests/layout/speed-insights.test.tsx && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - First Load JS da rota `/` registrado no SUMMARY com os maiores contribuintes.
    - CLS auditado: seções below-fold com `min-h-*` confirmado.
    - PERF-07 decidido e documentado (RSC net-neutro / dynamic seletivo no Form).
    - PERF-09 implementado como progressive enhancement: `navigator.connection` lido defensivamente, fallback no-op no Safari, sem erro.
    - `npm run typecheck` + `npm run lint` + `npm run build` passam.
    - SUMMARY contém: baseline de bundle, decisão PERF-07, descrição da implementação PERF-09.
  </acceptance_criteria>
  <done>Performance de código auditada; bundle medido; lazy-load decidido; PERF-09 implementado com fallback no-op; build OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → Vercel runtime | Speed Insights envia métricas anônimas de CWV à Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information disclosure | @vercel/speed-insights | accept | Coleta só métricas anônimas de performance (CWV), sem PII. Produto oficial Vercel, sem backend próprio. |
| T-07-10 | Tampering | navigator.connection | accept | API só leitura, progressive enhancement. Ausência (Safari) trata como no-op — sem caminho de erro. |
</threat_model>

<verification>
- `npm test -- --run tests/layout/speed-insights.test.tsx` GREEN.
- `npm run build` OK; First Load JS registrado.
- `ANALYZE=true npm run build` abre o treemap (verificação manual opcional).
- Medição de gates (Lighthouse, LCP, CLS, INP, peso) é HUMAN-UAT no 07-06.
</verification>

<success_criteria>
- Speed Insights coletando CWV; bundle-analyzer disponível.
- Bundle, CLS e lazy-load auditados; correções aplicadas onde necessário.
- PERF-07/09 honestamente escopados (sem scope creep; PERF-09 com fallback no-op no Safari).
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-05-SUMMARY.md` com baseline de bundle e a auditoria de performance.
</output>
</output>
