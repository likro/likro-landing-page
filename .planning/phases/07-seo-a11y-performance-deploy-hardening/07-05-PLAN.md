---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 05
type: execute
wave: 4
depends_on: ["07-01", "07-03", "07-04"]
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - src/app/layout.tsx
  - .env.example
requirements:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
  - PERF-09
  - MOBILE-01
  - MOBILE-03
  - MOBILE-04
  - MOBILE-05
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05
requirements_addressed:
  - PERF-01
  - PERF-02
  - PERF-03
  - PERF-04
  - PERF-05
  - PERF-06
  - PERF-07
  - PERF-08
  - PERF-09
  - MOBILE-01
  - MOBILE-03
  - MOBILE-04
  - MOBILE-05
  - DEPLOY-03
  - DEPLOY-04
  - DEPLOY-05

must_haves:
  truths:
    - "Vercel Speed Insights está montado e coleta CWV em produção"
    - "@next/bundle-analyzer está instalado e o build mede o First Load JS"
    - "Tap targets, useDeviceTier, hover→active e lazy-load estão auditados e documentados"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "<SpeedInsights /> montado no body"
      contains: "SpeedInsights"
    - path: "next.config.ts"
      provides: "wrapper withBundleAnalyzer condicionado a ANALYZE"
      contains: "withBundleAnalyzer"
    - path: ".env.example"
      provides: "documentação das env vars de deploy"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/speed-insights/next"
      via: "import + render <SpeedInsights />"
      pattern: "SpeedInsights"
    - from: "next.config.ts"
      to: "@next/bundle-analyzer"
      via: "withBundleAnalyzer wrapper"
      pattern: "bundle-analyzer"
---

<objective>
Fechar Performance e a parte automatizável de Deploy: instalar e ligar Speed Insights (CWV contínuo) e bundle-analyzer (gate de 150KB), e auditar tap targets, `useDeviceTier`, hover→`:active` fallback e lazy-load below-fold.

Purpose: Monitoramento de produção (DEPLOY-05), instrumento de medição de bundle (PERF-05), e auditoria de mobile QA de código (MOBILE-01/03/05, PERF-07). Toda medição numérica de gate (Lighthouse, LCP, CLS, INP, peso) é HUMAN-UAT no 07-06.
Output: 2 pacotes instalados e ligados, `.env.example` documentado, relatório de auditoria mobile/perf no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/layout.tsx
@next.config.ts
@src/components/ui/button.tsx
@src/hooks/use-device-tier.ts

<interfaces>
<!-- Speed Insights (RESEARCH § Code Examples, CITED vercel.com/docs/speed-insights) -->
```tsx
import { SpeedInsights } from "@vercel/speed-insights/next";
// dentro de <body>, após children:
<SpeedInsights />
```

<!-- bundle-analyzer wiring (RESEARCH § Code Examples) -->
```ts
import withBundleAnalyzer from "@next/bundle-analyzer";
const analyze = withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" });
export default analyze(nextConfig);
// rodar: ANALYZE=true npm run build
```

Estado atual / pitfalls do RESEARCH:
- `@vercel/speed-insights@^2.0.0` e `@next/bundle-analyzer` NÃO estão em package.json (CONTEXT errou).
- `@next/bundle-analyzer@16` é major 16 — se quebrar com Next 15.5, fixar `@^15` (Pitfall 2).
- Button: `default`=h-10 (40px ✗ <44px), `sm`=h-9 (36px ✗), `lg`=h-12 (48px ✓), `icon`=size-10 (40px ✗). FloatingWhatsApp usa `size-14` (56px ✓). (Pitfall 4 — MOBILE-03)
- Seções são quase todas RSC — `next/dynamic` nelas é net-neutro; aplicar só a ilhas client pesadas (LeadForm). (Pitfall 1 — PERF-07)
- `navigator.connection` não existe no Safari (browser dominante do tráfego alvo). PERF-09 é discricionário. (Pitfall 6)
- `next.config.ts` já terá `headers()` do plan 07-04 — PRESERVAR ao adicionar o wrapper.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instalar e ligar Speed Insights + bundle-analyzer</name>
  <read_first>
    - package.json (fonte — ambos pacotes ausentes)
    - next.config.ts (arquivo modificado — já tem headers() do 07-04, PRESERVAR)
    - src/app/layout.tsx (arquivo modificado — montar SpeedInsights)
    - tests/layout/speed-insights.test.tsx (contrato — exige import e <SpeedInsights no layout)
  </read_first>
  <action>
    1. Rodar `npm install @vercel/speed-insights@^2.0.0` e `npm install -D @next/bundle-analyzer`. Após instalar, se `@next/bundle-analyzer` puxar major 16 e o build quebrar, reinstalar com `npm install -D @next/bundle-analyzer@^15` (Pitfall 2 — documentar no SUMMARY qual versão ficou).

    2. Editar `src/app/layout.tsx`: importar `{ SpeedInsights }` de `@vercel/speed-insights/next` e renderizar `<SpeedInsights />` dentro de `<body>`, após `children` (perto do `<Toaster />`). Layout permanece Server Component.

    3. Editar `next.config.ts`: envolver o `nextConfig` (que já contém `headers()` do plan 07-04) com `withBundleAnalyzer({ enabled: process.env.ANALYZE === "true" })` exatamente como o padrão da seção `<interfaces>`. NÃO remover nem alterar a função `headers()`.

    4. Rodar `npm run build` uma vez e capturar do output a tabela "First Load JS" por rota. Registrar o número da rota `/` no SUMMARY — é a baseline do gate PERF-05 (≤150KB gzipped). Se já estiver acima, listar suspeitos (motion, lenis) para o SUMMARY; a otimização fina, se necessária, é decisão pós-medição.
  </action>
  <verify>
    <automated>npm test -- --run tests/layout/speed-insights.test.tsx && npm run typecheck && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` lista `@vercel/speed-insights` (deps) e `@next/bundle-analyzer` (devDeps).
    - `src/app/layout.tsx` importa e renderiza `<SpeedInsights />`.
    - `next.config.ts` exporta o config envolvido por `withBundleAnalyzer` E mantém a função `headers()` do 07-04.
    - `tests/layout/speed-insights.test.tsx` GREEN.
    - `npm run build` completa; SUMMARY registra o First Load JS da rota `/`.
  </acceptance_criteria>
  <done>Speed Insights e bundle-analyzer instalados e ligados; build OK; baseline de bundle registrada.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoria mobile/perf — tap targets, useDeviceTier, hover→active, lazy-load, PERF-09</name>
  <read_first>
    - src/components/ui/button.tsx (tap target sizes — Pitfall 4)
    - src/components/ui/whatsapp-cta.tsx (uso de Button — auditar size em mobile)
    - src/components/layout/Header.tsx, src/components/layout/Footer.tsx (links — tap targets + hover)
    - src/sections/Floating/FloatingWhatsApp.tsx (size-14 — OK)
    - src/sections/*.tsx (uso de useDeviceTier / primitivas de motion — MOBILE-01)
    - src/components/providers/smooth-scroll-provider.tsx (Lenis config — MOBILE-04)
    - .env.example (arquivo modificado — documentar vars de deploy)
    - tests/mobile/device-tier-usage.test.ts (contrato — MOBILE-01)
  </read_first>
  <action>
    Auditoria — corrigir apenas o que falhar; documentar tudo no SUMMARY.

    1. **Tap targets ≥44×44px (MOBILE-03):** Auditar cada CTA/link/input tocável em viewport mobile. Para todo `<WhatsAppCta>` e `<Button>` que renderiza em contexto mobile (hero, fim de Pain/Product, header, footer, form submit), garantir área tocável ≥44×44px. Como `Button size="default"` é h-10 (40px), os CTAs primários mobile devem usar `size="lg"` (h-12=48px) OU receber padding extra. Links de texto do Footer: se forem alvos tocáveis, garantir `min-h-[44px]` + área de toque adequada (padding) sem alterar o visual desktop. Aplicar correções; listar cada alvo auditado e o veredito no SUMMARY.

    2. **useDeviceTier (MOBILE-01):** Confirmar que toda decisão de intensidade de motion passa por `useDeviceTier()` ou por uma primitiva de `@/components/motion` (que já encapsula). Rodar/checar `tests/mobile/device-tier-usage.test.ts`. Se alguma seção tiver motion direto sem tier, ajustar para usar a primitiva ou o hook. Documentar.

    3. **hover→`:active` fallback (MOBILE-05):** Grep por `hover:` nos componentes interativos. Para qualquer efeito de hover que comunique estado/affordance (não puramente decorativo), garantir um `active:` equivalente para touch. Button já tem `active:translate-y-px` no variant default. Auditar Header/Footer/cards. Documentar.

    4. **Lazy-load below-fold (PERF-07):** Conforme Pitfall 1 — as seções são RSC, `next/dynamic` nelas é net-neutro. Avaliar com base no output "First Load JS" da Task 1 quais ilhas CLIENT pesam (provável: `LeadForm` com react-hook-form+zod). SE o LeadForm/Form for client e estiver below-fold, aplicar `next/dynamic` a ele com `ssr: true` (preserva SEO). Se a análise mostrar que não há ganho, registrar no SUMMARY: "PERF-07 auditado — arquitetura RSC já entrega o efeito; dynamic não aplicado / aplicado seletivamente a X". NUNCA usar `ssr: false` em seção de conteúdo (anti-pattern — quebra SEO).

    5. **MOBILE-04 (Lenis touch):** Confirmar o estado atual em `smooth-scroll-provider.tsx` (`smoothTouch`/`syncTouch: false` — Lenis só wheel, touch nativo). NÃO mudar código aqui — registrar que a decisão final (manter assim vs. pular init em `pointer: coarse`) é validada em device real no HUMAN-UAT 07-06.

    6. **PERF-09 (conexões lentas):** Decisão discricionária do RESEARCH (Pitfall 6) — `navigator.connection` não existe no Safari (browser dominante do tráfego). Adotar a opção (b): NÃO implementar `navigator.connection`; documentar no SUMMARY que PERF-09 é coberto parcialmente por `useDeviceTier()` + `prefers-reduced-motion` (já degradam motion), e que `navigator.connection` beneficiaria só Android Chrome. Deixar como pendência de decisão do Lenny no HUMAN-UAT.

    7. **DEPLOY-03/04 (env vars):** Auditar `.env.example`. Garantir que documenta: `NEXT_PUBLIC_WA_NUMBER`, `NEXT_PUBLIC_GA4_ID`, `NEXT_PUBLIC_META_PIXEL_ID`, `NEXT_PUBLIC_CLARITY_ID`, e as vars reais de lead (`RESEND_*`/`GOOGLE_*`). Adicionar um comentário explicando que `LEAD_WEBHOOK_URL` do REQUIREMENTS.md é nome morto — a v1 usa Resend + Google Sheets (RESEARCH § State of the Art). NÃO criar a var morta. A configuração das 3 IDs de analytics na Vercel é HUMAN-UAT (07-06) — DEPLOY-03 fica parcial até a Phase 6 Parte B. DEPLOY-04 (`.vercel.app`) já está feito (site no ar) — apenas confirmar no SUMMARY.
  </action>
  <verify>
    <automated>npm test -- --run tests/mobile/device-tier-usage.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Tap targets mobile auditados; CTAs primários mobile ≥44×44px (correções aplicadas onde necessário).
    - `tests/mobile/device-tier-usage.test.ts` GREEN.
    - hover→`:active` auditado; fallbacks adicionados onde a affordance dependia de hover.
    - PERF-07 decidido e documentado (RSC net-neutro / dynamic seletivo no Form).
    - `.env.example` documenta as 5+ vars de deploy e a nota sobre `LEAD_WEBHOOK_URL` morto; sem var morta criada.
    - `npm run typecheck` + `npm run lint` passam.
    - SUMMARY contém: tabela de tap targets, veredito useDeviceTier, hover→active, decisão PERF-07, nota PERF-09, status DEPLOY-03/04.
  </acceptance_criteria>
  <done>Mobile QA e performance de código auditados; correções aplicadas; PERF-09 e MOBILE-04 documentados como HUMAN-UAT/parcial; .env.example atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → Vercel runtime | Speed Insights envia métricas anônimas de CWV à Vercel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Information disclosure | @vercel/speed-insights | accept | Coleta só métricas anônimas de performance (CWV), sem PII. Produto oficial Vercel, sem backend próprio. |
| T-07-08 | Information disclosure | .env.example | mitigate | `.env.example` lista só NOMES de vars, nunca valores. Secrets (`RESEND_*`/`GOOGLE_*`) sem prefixo `NEXT_PUBLIC_` — server-only, já correto. |
</threat_model>

<verification>
- `npm test -- --run tests/layout/speed-insights.test.tsx tests/mobile/` GREEN.
- `npm run build` OK; First Load JS registrado.
- `ANALYZE=true npm run build` abre o treemap (verificação manual opcional).
- Medição de gates (Lighthouse, LCP, CLS, INP, peso) é HUMAN-UAT no 07-06.
</verification>

<success_criteria>
- Speed Insights coletando CWV; bundle-analyzer disponível.
- Tap targets, motion tier, hover fallback auditados e corrigidos onde necessário.
- PERF-07/09 e MOBILE-04 honestamente escopados (sem scope creep, sem `navigator.connection` que o Safari ignora).
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-05-SUMMARY.md` com baseline de bundle e a auditoria mobile/perf.
</output>
