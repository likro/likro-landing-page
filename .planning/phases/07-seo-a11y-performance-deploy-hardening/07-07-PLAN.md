---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 07
type: execute
wave: 5
depends_on: ["07-01", "07-03"]
files_modified:
  - src/components/ui/button.tsx
  - src/components/ui/whatsapp-cta.tsx
  - src/components/layout/Header.tsx
  - src/components/layout/Footer.tsx
  - .env.example
requirements:
  - MOBILE-01
  - MOBILE-03
  - MOBILE-04
  - MOBILE-05
  - DEPLOY-03
  - DEPLOY-04
requirements_addressed:
  - MOBILE-01
  - MOBILE-03
  - MOBILE-04
  - MOBILE-05
  - DEPLOY-03
  - DEPLOY-04

must_haves:
  truths:
    - "Todo alvo tocável em viewport mobile tem área ≥44×44px"
    - "Toda decisão de intensidade de motion passa por useDeviceTier ou primitiva que o encapsula"
    - "Efeitos de hover que comunicam affordance têm equivalente :active para touch"
    - ".env.example documenta todas as env vars de deploy, sem var morta"
  artifacts:
    - path: ".env.example"
      provides: "documentação das env vars de deploy"
    - path: "src/components/ui/button.tsx"
      provides: "tap targets mobile ≥44×44px nos CTAs primários"
  key_links:
    - from: "src/sections/*"
      to: "src/hooks/use-device-tier.ts"
      via: "useDeviceTier ou primitiva de @/components/motion"
      pattern: "useDeviceTier"
    - from: ".env.example"
      to: "Vercel env vars"
      via: "documentação de NEXT_PUBLIC_* + RESEND_*/GOOGLE_*"
      pattern: "NEXT_PUBLIC_"
---

<objective>
Fechar o stream de Mobile QA + a parte automatizável de Deploy: auditar tap targets ≥44×44px, uso de `useDeviceTier`, fallback hover→`:active`, comportamento do Lenis em touch, e documentar as env vars de deploy no `.env.example`.

Purpose: Auditoria de mobile QA de código (MOBILE-01/03/04/05) e documentação de deploy (DEPLOY-03/04). Toda validação em device real (device matrix, Lenis touch real) é HUMAN-UAT no 07-06.
Output: correções de tap target/hover aplicadas onde necessário, `.env.example` documentado, relatório de auditoria mobile no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/components/ui/button.tsx
@src/hooks/use-device-tier.ts

<interfaces>
<!-- Estado atual / pitfalls do RESEARCH -->

Estado atual:
- Button: `default`=h-10 (40px ✗ <44px), `sm`=h-9 (36px ✗), `lg`=h-12 (48px ✓), `icon`=size-10 (40px ✗). FloatingWhatsApp usa `size-14` (56px ✓). (Pitfall 4 — MOBILE-03)
- `useDeviceTier()` existe desde a Phase 1 — auditar uso real nas seções. As primitivas de `@/components/motion` já encapsulam o tier (MOBILE-01).
- `smooth-scroll-provider.tsx` usa `syncTouch: false` — Lenis só wheel, touch nativo. MOBILE-04: decisão "manter assim" é validada em device real no HUMAN-UAT 07-06; este plano NÃO altera código de Lenis. (Pitfall 3)
- `.env.example` lista `RESEND_*`/`GOOGLE_*` (Phase 5) + `NEXT_PUBLIC_WA_NUMBER`. `LEAD_WEBHOOK_URL` do REQUIREMENTS.md é nome morto — a v1 usa Resend + Google Sheets.
- DEPLOY-04 (`.vercel.app`) já está feito (site no ar) — apenas confirmar no SUMMARY.

NOTA de waves: este plano NÃO toca `src/app/layout.tsx` nem `next.config.ts` — roda em paralelo com 07-05 sem colisão de arquivos.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditoria de tap targets + hover→active fallback</name>
  <read_first>
    - src/components/ui/button.tsx (tap target sizes — Pitfall 4)
    - src/components/ui/whatsapp-cta.tsx (uso de Button — auditar size em mobile)
    - src/components/layout/Header.tsx (links + CTA — tap targets + hover)
    - src/components/layout/Footer.tsx (links de texto — tap targets + hover)
    - src/sections/Floating/FloatingWhatsApp.tsx (size-14 — OK, referência)
  </read_first>
  <action>
    Auditoria — corrigir apenas o que falhar; documentar tudo no SUMMARY.

    1. **Tap targets ≥44×44px (MOBILE-03):** Auditar cada CTA/link/input tocável em viewport mobile. Para todo `<WhatsAppCta>` e `<Button>` que renderiza em contexto mobile (hero, fim de Pain/Product, header, footer, form submit), garantir área tocável ≥44×44px. Como `Button size="default"` é h-10 (40px), os CTAs primários mobile devem usar `size="lg"` (h-12=48px) OU receber padding extra. Links de texto do Footer: se forem alvos tocáveis, garantir `min-h-[44px]` + área de toque adequada (padding) sem alterar o visual desktop. Aplicar correções; listar cada alvo auditado e o veredito no SUMMARY.

    2. **hover→`:active` fallback (MOBILE-05):** Grep por `hover:` nos componentes interativos. Para qualquer efeito de hover que comunique estado/affordance (não puramente decorativo), garantir um `active:` equivalente para touch. Button já tem `active:translate-y-px` no variant default. Auditar Header/Footer/cards. Documentar cada efeito de hover auditado e o veredito.

    Toda alteração deve preservar o visual desktop e o brand book.
  </action>
  <verify>
    <automated>npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - Tap targets mobile auditados; CTAs primários mobile ≥44×44px (correções aplicadas onde necessário).
    - hover→`:active` auditado; fallbacks adicionados onde a affordance dependia de hover.
    - `npm run typecheck` + `npm run lint` passam.
    - SUMMARY contém: tabela de tap targets (alvo → tamanho → pass/corrigido) e tabela de hover→active.
  </acceptance_criteria>
  <done>Tap targets e hover→active auditados; correções aplicadas só onde necessário; relatório no SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoria useDeviceTier + Lenis touch + .env.example</name>
  <read_first>
    - src/sections/*.tsx (uso de useDeviceTier / primitivas de motion — MOBILE-01)
    - src/hooks/use-device-tier.ts (hook — MOBILE-01)
    - src/components/providers/smooth-scroll-provider.tsx (Lenis config — MOBILE-04)
    - .env.example (arquivo modificado — documentar vars de deploy)
    - tests/mobile/device-tier-usage.test.ts (contrato — MOBILE-01)
  </read_first>
  <action>
    Auditoria — corrigir apenas o que falhar; documentar tudo no SUMMARY.

    1. **useDeviceTier (MOBILE-01):** Confirmar que toda decisão de intensidade de motion passa por `useDeviceTier()` ou por uma primitiva de `@/components/motion` (que já encapsula). Rodar/checar `tests/mobile/device-tier-usage.test.ts` — o invariante é "zero `whileInView` direto em `src/sections/`" (motion via primitiva ou hook). Se alguma seção tiver `whileInView`/motion direto sem tier, ajustar para usar a primitiva ou o hook. Documentar.

    2. **MOBILE-04 (Lenis touch):** Confirmar o estado atual em `smooth-scroll-provider.tsx` (`smoothTouch`/`syncTouch: false` — Lenis só wheel, touch nativo). NÃO mudar código aqui — registrar que a decisão final (manter assim vs. pular init em `pointer: coarse`) é validada em device real no HUMAN-UAT 07-06.

    3. **DEPLOY-03/04 (env vars):** Auditar `.env.example`. Garantir que documenta: `NEXT_PUBLIC_WA_NUMBER`, `NEXT_PUBLIC_GA4_ID`, `NEXT_PUBLIC_META_PIXEL_ID`, `NEXT_PUBLIC_CLARITY_ID`, e as vars reais de lead (`RESEND_*`/`GOOGLE_*`). Adicionar um comentário explicando que `LEAD_WEBHOOK_URL` do REQUIREMENTS.md é nome morto — a v1 usa Resend + Google Sheets (RESEARCH § State of the Art). NÃO criar a var morta. A configuração das 3 IDs de analytics na Vercel é HUMAN-UAT (07-06) — DEPLOY-03 fica parcial até a Phase 6 Parte B. DEPLOY-04 (`.vercel.app`) já está feito (site no ar) — apenas confirmar no SUMMARY.
  </action>
  <verify>
    <automated>npm test -- --run tests/mobile/device-tier-usage.test.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `tests/mobile/device-tier-usage.test.ts` GREEN (zero `whileInView` direto em `src/sections/`).
    - `.env.example` documenta as 5+ vars de deploy e a nota sobre `LEAD_WEBHOOK_URL` morto; sem var morta criada.
    - `npm run typecheck` passa.
    - SUMMARY contém: veredito useDeviceTier, nota MOBILE-04 (HUMAN-UAT), status DEPLOY-03/04.
  </acceptance_criteria>
  <done>useDeviceTier e Lenis touch auditados; MOBILE-04 documentado como HUMAN-UAT; .env.example atualizado.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Plano audita markup/estilos mobile e documenta env vars — sem nova superfície de runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information disclosure | .env.example | mitigate | `.env.example` lista só NOMES de vars, nunca valores. Secrets (`RESEND_*`/`GOOGLE_*`) sem prefixo `NEXT_PUBLIC_` — server-only, já correto. |
</threat_model>

<verification>
- `npm test -- --run tests/mobile/device-tier-usage.test.ts` GREEN.
- `npm run typecheck` + `npm run lint` passam.
- Validação de device matrix real e Lenis touch é HUMAN-UAT no 07-06.
</verification>

<success_criteria>
- Tap targets ≥44×44px nos CTAs mobile; hover→active onde a affordance depende de hover.
- Motion intensity 100% via useDeviceTier/primitiva.
- `.env.example` documenta as vars de deploy sem inventar a var morta `LEAD_WEBHOOK_URL`.
- MOBILE-04 honestamente escopado como HUMAN-UAT.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-07-SUMMARY.md` com a auditoria mobile e o status de deploy.
</output>
