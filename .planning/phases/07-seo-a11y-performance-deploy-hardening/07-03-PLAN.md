---
phase: 07-seo-a11y-performance-deploy-hardening
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/a11y/skip-link.tsx
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - A11Y-05
  - A11Y-06
  - A11Y-07
requirements_addressed:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - A11Y-04
  - A11Y-05
  - A11Y-06
  - A11Y-07

must_haves:
  truths:
    - "Um skip-link 'Pular para o conteúdo principal' aparece ao receber foco e salta para o <main>"
    - "O <main> tem id=main-content como alvo do skip-link"
    - "HTML semântico, form aria e prefers-reduced-motion estão auditados e documentados"
  artifacts:
    - path: "src/components/a11y/skip-link.tsx"
      provides: "Componente skip-link sr-only / focus:not-sr-only"
      min_lines: 10
    - path: "src/app/page.tsx"
      provides: "<main id=main-content> como alvo de salto"
  key_links:
    - from: "src/components/a11y/skip-link.tsx"
      to: "src/app/page.tsx#main-content"
      via: "href=#main-content"
      pattern: "main-content"
    - from: "src/app/layout.tsx"
      to: "src/components/a11y/skip-link.tsx"
      via: "render como primeiro elemento focável do body"
      pattern: "SkipLink"
---

<objective>
Fechar o stream de Acessibilidade: criar o skip-link funcional, garantir o alvo `<main id>`, e auditar (com correção onde necessário) contraste, navegação por teclado, aria do form, `prefers-reduced-motion` e semântica de elementos.

Purpose: WCAG AA limpo. O grosso do stream A11y (semântica, form aria, onClick) já está bom segundo o RESEARCH — este plano constrói o que falta (skip-link) e formaliza a auditoria do resto.
Output: componente `skip-link.tsx`, `<main id>` corrigido, relatório de auditoria a11y no SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-seo-a11y-performance-deploy-hardening/07-RESEARCH.md
@src/app/layout.tsx
@src/app/page.tsx
@src/components/ui/button.tsx

<interfaces>
<!-- Padrão skip-link (RESEARCH § Pattern 2) -->

```tsx
// src/components/a11y/skip-link.tsx — Server Component
export function SkipLink() {
  return (
    <a
      href="#main-content"
      className="sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-[100] focus:rounded-md focus:bg-accent-primary focus:px-4 focus:py-2 focus:text-white"
    >
      Pular para o conteúdo principal
    </a>
  );
}
```

Estado atual:
- src/app/page.tsx tem `<main>` SEM `id` — precisa `<main id="main-content">`.
- src/components/ui/button.tsx já tem `focus-visible:ring-2 focus-visible:ring-accent-primary focus-visible:ring-offset-2` (A11Y-02 — cobertura de Button OK).
- Tailwind v4 tem `sr-only`/`not-sr-only` nativos.
- RESEARCH apurou: A11Y-03 (form aria) feito na Phase 5; A11Y-06 (zero div onClick) limpo; A11Y-07 (mockups aria-hidden) limpo.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar skip-link e ligar ao main</name>
  <read_first>
    - src/components/a11y/skip-link.tsx (criar)
    - src/app/layout.tsx (arquivo modificado — onde o SkipLink é renderizado)
    - src/app/page.tsx (arquivo modificado — main precisa de id)
    - tests/a11y/skip-link.test.tsx (contrato — exige texto /pular para o conteúdo/i e href=#main-content)
  </read_first>
  <action>
    1. Criar `src/components/a11y/skip-link.tsx` (Server Component, sem `"use client"`). Exportar `SkipLink` exatamente como o padrão da seção `<interfaces>`: `<a href="#main-content">` com classes `sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-[100] focus:rounded-md focus:bg-accent-primary focus:px-4 focus:py-2 focus:text-white`, texto "Pular para o conteúdo principal".

    2. Editar `src/app/layout.tsx`: importar `{ SkipLink }` de `@/components/a11y/skip-link` e renderizá-lo como PRIMEIRO elemento dentro de `<body>` (antes dos `<script>` JSON-LD e dos providers) — o skip-link deve ser o primeiro elemento focável da árvore para o Tab inicial cair nele.

    3. Editar `src/app/page.tsx`: trocar `<main>` por `<main id="main-content">`.

    Verificar que o `focus:bg-accent-primary` com `focus:text-white` passa contraste (roxo #7C3AED com texto branco — RESEARCH confirma uso de accent só como destaque pontual, e é estado de foco transitório).
  </action>
  <verify>
    <automated>npm test -- --run tests/a11y/skip-link.test.tsx && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/a11y/skip-link.tsx` existe e exporta `SkipLink`.
    - `src/app/layout.tsx` renderiza `<SkipLink />` como primeiro filho de `<body>`.
    - `src/app/page.tsx` tem `<main id="main-content">`.
    - `tests/a11y/skip-link.test.tsx` passa (GREEN).
    - `npm run typecheck` passa.
  </acceptance_criteria>
  <done>Skip-link criado, montado como primeiro focável do body, apontando para main#main-content; teste skip-link GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Auditoria A11y — contraste, teclado, form aria, reduced-motion, semântica</name>
  <read_first>
    - src/app/globals.css (tokens de cor — fonte para auditar contraste)
    - src/sections/Pain.tsx, src/sections/Proof.tsx (seções DARK — pares de contraste suspeitos)
    - src/components/forms/LeadForm.tsx (verificar A11Y-03 — label/aria-describedby/aria-invalid)
    - src/components/layout/Header.tsx (verificar foco visível em links/CTAs)
    - tests/seo/semantic-html.test.ts (contrato de A11Y-06/07 já coberto por grep test)
  </read_first>
  <action>
    Auditoria — corrigir apenas o que falhar; NÃO refazer o que já está bom.

    1. **Contraste (A11Y-01):** Ler os tokens de cor em `globals.css`. Para cada par texto/fundo nas seções DARK (Pain, Proof) e LIGHT, calcular ou estimar a razão de contraste. Focar nos suspeitos: texto secundário/muted sobre fundo escuro (`text-text-secondary`, `text-neutral-400/500` sobre `surface-dark`). Se algum par ficar abaixo de 4.5:1 (texto normal) ou 3:1 (texto grande ≥24px ou ≥18.66px bold), AJUSTAR o token neutro (clarear texto / escurecer fundo) — NUNCA usar roxo `#7C3AED` para "corrigir" contraste (anti-pattern do RESEARCH; viola brand book). Documentar cada par verificado e cada ajuste no SUMMARY. Medição final precisa (axe DevTools) é HUMAN-UAT no 07-06.

    2. **Navegação por teclado + foco visível (A11Y-02):** Verificar que Header (links + CTA), `<WhatsAppCta>`, inputs do form e `FloatingWhatsApp` têm estado de foco visível. Button já tem `focus-visible:ring`. Se algum `<a>` do Header/Footer ou o FloatingWhatsApp não tiver `focus-visible` ring, adicionar `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-accent-primary focus-visible:ring-offset-2` (alinhado ao brand). Documentar cobertura.

    3. **Form aria (A11Y-03):** Verificar em `LeadForm.tsx` que cada input tem `<label>` associado (via `htmlFor`/`id`), mensagens de erro com `aria-describedby` apontando para o id da mensagem, e `aria-invalid` nos campos com erro. RESEARCH diz que foi feito na Phase 5 — se estiver OK, apenas confirmar no SUMMARY; se faltar algo, corrigir.

    4. **prefers-reduced-motion (A11Y-04):** Auditar que TODAS as fontes de animação simplificam: primitivas em `src/components/motion/`, `Header.tsx` (hide-on-scroll), `FloatingWhatsApp`, e keyframes CSS em `globals.css`. Verificar especificamente se há `@media (prefers-reduced-motion: reduce)` cobrindo os keyframes CSS — se algum keyframe rodar sem guard, adicionar o media query desligando-o. Validação no OS real é HUMAN-UAT (07-06); aqui é auditoria de código.

    5. **Semântica (A11Y-06/07):** Já coberto pelo grep test `tests/seo/semantic-html.test.ts` (do plan 07-01). Apenas confirmar que o teste está GREEN; se algum match aparecer, corrigir o `<div onClick>` → `<button>` ou adicionar `aria-hidden` ao mockup faltante.

    Toda alteração de token de contraste deve preservar o invariante de marca (roxo só destaque).
  </action>
  <verify>
    <automated>npm test -- --run tests/seo/semantic-html.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `tests/seo/semantic-html.test.ts` GREEN (zero `<div onClick>`, mockups `aria-hidden`, h1 único, lang pt-BR).
    - Qualquer par de contraste corrigido NÃO introduz roxo como fundo de texto (verificável: nenhum novo `bg-accent` em torno de texto longo).
    - `npm run typecheck` + `npm run lint` passam.
    - SUMMARY contém: tabela de pares de contraste auditados (par → razão estimada → pass/ajustado), confirmação de foco visível por componente, confirmação de form aria, e cobertura de reduced-motion.
  </acceptance_criteria>
  <done>Contraste, teclado, form aria, reduced-motion e semântica auditados; correções aplicadas só onde necessário; relatório no SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (nenhum novo) | Plano só altera markup/estilos de a11y — sem nova superfície de runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Denial of Service (UX) | skip-link com z-index alto | accept | `z-[100]` só ativo em `:focus`; não obstrui conteúdo no estado normal. Risco estético nulo. |
</threat_model>

<verification>
- `npm test -- --run tests/a11y/ tests/seo/semantic-html.test.ts` — skip-link e semantic-html GREEN.
- `npm run typecheck` + `npm run lint` passam.
- Verificação manual (HUMAN-UAT 07-06): Tab pelo site, axe DevTools nos pares DARK, Reduce Motion no OS.
</verification>

<success_criteria>
- Skip-link funcional e como primeiro focável.
- `<main id="main-content">` presente.
- Auditoria documentada de contraste, teclado, form aria, reduced-motion.
- Nenhum par de contraste corrigido violando o brand book.
</success_criteria>

<output>
Após completar, criar `.planning/phases/07-seo-a11y-performance-deploy-hardening/07-03-SUMMARY.md` com a tabela de auditoria de contraste e o checklist a11y.
</output>
