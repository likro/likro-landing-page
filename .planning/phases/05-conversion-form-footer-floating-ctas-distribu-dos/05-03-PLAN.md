---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/lib/lead-resend.ts
  - src/lib/lead-sheets.ts
  - src/app/api/lead/route.ts
autonomous: true
requirements: [CTA-10, CTA-11, CTA-12]
requirements_addressed: [CTA-10, CTA-11, CTA-12]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "POST /api/lead aceita JSON, valida com Zod, retorna 200/400/422/502 conforme contrato"
    - "Resend e Sheets são chamados em paralelo via Promise.allSettled; success se pelo menos UM pegou"
    - "Honeypot field 'website' preenchido → fake 200 sem chamar Resend/Sheets (RESEARCH Pitfall 8)"
    - "Dedup por número WhatsApp em janela de 60s, segundo submit retorna { ok:true, deduped:true }"
    - "Sheets via JWT manual com jose — 100% edge runtime"
  artifacts:
    - path: "src/lib/lead-resend.ts"
      provides: "sendLeadEmail(lead) — usa Resend SDK + escapeHtml"
      contains: 'import "server-only"'
    - path: "src/lib/lead-sheets.ts"
      provides: "appendLeadRow(lead) — JWT RS256 via jose + REST direto na Sheets API"
      contains: "SignJWT"
    - path: "src/app/api/lead/route.ts"
      provides: "POST handler edge runtime + dual-write + honeypot (website) + dedup"
      contains: 'export const runtime = "edge"'
  key_links:
    - from: "src/app/api/lead/route.ts"
      to: "src/lib/lead-resend.ts + src/lib/lead-sheets.ts"
      via: "Promise.allSettled([sendLeadEmail(lead), appendLeadRow(lead)])"
      pattern: "Promise\\.allSettled"
    - from: "src/lib/lead-sheets.ts"
      to: "https://oauth2.googleapis.com/token + https://sheets.googleapis.com/v4/..."
      via: "fetch nativo + Bearer access token via jose JWT"
      pattern: "sheets\\.googleapis\\.com"
---

<objective>
Implementar o backend completo do form: edge route handler + libs de Resend e Sheets. Faz os 5 testes RED da Plan 02 que dependem do route (`tests/api/lead-route.test.ts`) virarem GREEN.

Purpose: Sem este plan, o form do Plan 04 submete pra um 404. Plan separado porque é trabalho server-side puro (zero UI), e tocar tanto auth Google quanto Resend de uma vez tem complexidade independente — isolar facilita debug.
Output: 3 arquivos server-only, edge route funcional, integrações dual-write idempotentes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@src/lib/server-env.ts
@src/lib/lead-schema.ts
@src/lib/lead-dedup.ts
@tests/api/lead-route.test.ts

<interfaces>
<!-- Contracts to honor (from Plan 02 tests): -->

POST /api/lead response shape:
```typescript
// 200 success: { ok: true } OR { ok: true, deduped: true }
// 400 invalid JSON: { ok: false, error: "invalid_json" }
// 422 validation: { ok: false, error: "validation_failed", issues: ZodFlatten }
// 502 both integrations failed: { ok: false, error: "delivery_failed" }
// 200 honeypot (body.website preenchido): { ok: true }  (FAKE SUCCESS — bot bait)
```

sendLeadEmail signature (mocked in tests/api/lead-route.test.ts):
```typescript
export async function sendLeadEmail(lead: Lead): Promise<void>;  // throws on failure
```

appendLeadRow signature (mocked in tests/api/lead-route.test.ts):
```typescript
export async function appendLeadRow(lead: Lead): Promise<void>;  // throws on failure
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar src/lib/lead-resend.ts (Resend SDK, edge-compatible)</name>
  <files>src/lib/lead-resend.ts</files>
  <read_first>
    - src/lib/server-env.ts (forma do `serverEnv`)
    - src/lib/lead-schema.ts (`type Lead`)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 3 + Pitfall 2 — domínio verificado)
  </read_first>
  <action>
    Criar `src/lib/lead-resend.ts`:

    ```typescript
    /**
     * sendLeadEmail — envio de email transacional ao Lenny via Resend.
     *
     * Edge-compatible (Resend SDK v6.x é fetch-based — verified em RESEARCH.md).
     * Domínio sender PRECISA estar verificado em resend.com/domains antes de prod
     * (Pitfall 2 do RESEARCH). Em preview/dev sem domínio, usar from sandbox.
     */
    import "server-only";
    import { Resend } from "resend";
    import { serverEnv } from "@/lib/server-env";
    import type { Lead } from "@/lib/lead-schema";

    const resend = new Resend(serverEnv.RESEND_API_KEY);

    // TODO Lenny verifica likro.com.br em resend.com/domains; até lá, sandbox.
    const FROM_VERIFIED = "Likro Leads <leads@likro.com.br>";
    const FROM_SANDBOX = "Likro Leads <onboarding@resend.dev>";

    function fromAddress(): string {
      // Heurística simples: production usa verified; preview/dev usa sandbox.
      // VERCEL_ENV é injetada pela Vercel; em local dev fica undefined.
      const isProd = process.env.VERCEL_ENV === "production";
      return isProd ? FROM_VERIFIED : FROM_SANDBOX;
    }

    export async function sendLeadEmail(lead: Lead): Promise<void> {
      const { error } = await resend.emails.send({
        from: fromAddress(),
        to: serverEnv.LEAD_TO_EMAIL,
        subject: `Novo lead — ${lead.name}`,
        html: buildLeadHtml(lead),
      });
      if (error) {
        throw new Error(`[lead-resend] ${error.message}`);
      }
    }

    function buildLeadHtml(lead: Lead): string {
      return `<div style="font-family: system-ui, -apple-system, sans-serif; max-width: 480px;">
      <h2 style="font-size: 18px; margin: 0 0 12px;">Novo lead da landing</h2>
      <p style="margin: 4px 0;"><strong>Nome:</strong> ${escapeHtml(lead.name)}</p>
      <p style="margin: 4px 0;"><strong>WhatsApp:</strong> ${escapeHtml(lead.whatsapp)}</p>
      ${lead.message ? `<p style="margin: 12px 0 4px;"><strong>Mensagem:</strong></p><p style="margin: 0; white-space: pre-wrap;">${escapeHtml(lead.message)}</p>` : ""}
      ${lead.utm ? `<p style="color:#666;font-size:12px;margin-top:16px;">UTM: ${escapeHtml(lead.utm)}</p>` : ""}
    </div>`;
    }

    function escapeHtml(s: string): string {
      const map: Record<string, string> = {
        "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
      };
      return s.replace(/[&<>"']/g, (c) => map[c] ?? c);
    }
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lead-resend.ts` existe
    - File contém `import "server-only"` na primeira linha de código
    - File contém `import { Resend } from "resend"`
    - File contém `export async function sendLeadEmail(lead: Lead): Promise<void>`
    - File contém `escapeHtml` helper (mitigação threat T-05-09 XSS no email)
    - File contém `VERCEL_ENV === "production"` (gate sandbox vs verified)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>sendLeadEmail pronto, escapeHtml previne XSS no email, gate sandbox/verified preserva fluxo dev até Lenny verificar domínio.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar src/lib/lead-sheets.ts (JWT manual via jose, edge-only)</name>
  <files>src/lib/lead-sheets.ts</files>
  <read_first>
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 2 EXATO + Pitfall 1 — newline escaping da private key)
    - src/lib/server-env.ts
    - src/lib/lead-schema.ts (`type Lead`)
  </read_first>
  <action>
    Criar `src/lib/lead-sheets.ts` COPIANDO o Pattern 2 do RESEARCH.md (com pequenos ajustes para o type Lead atual):

    ```typescript
    /**
     * appendLeadRow — escreve uma linha na planilha de leads via Google Sheets API v4.
     *
     * Estratégia: JWT RS256 minted via jose (edge-compatible) → trocado por
     * access token na endpoint OAuth2 do Google → POST direto no values:append
     * via fetch nativo. Zero deps Node-only.
     *
     * PITFALL 1 (RESEARCH.md): GOOGLE_SA_PRIVATE_KEY armazena \n literal em env var;
     * unescape para \n real antes de importPKCS8.
     */
    import "server-only";
    import { SignJWT, importPKCS8 } from "jose";
    import { serverEnv } from "@/lib/server-env";
    import type { Lead } from "@/lib/lead-schema";

    const SCOPES = "https://www.googleapis.com/auth/spreadsheets";
    const TOKEN_URL = "https://oauth2.googleapis.com/token";
    const RANGE = "Leads!A:E";

    async function getAccessToken(): Promise<string> {
      // Pitfall 1: unescape literal \n
      const privateKeyPem = serverEnv.GOOGLE_SA_PRIVATE_KEY.replace(/\\n/g, "\n");
      const privateKey = await importPKCS8(privateKeyPem, "RS256");

      const now = Math.floor(Date.now() / 1000);
      const jwt = await new SignJWT({ scope: SCOPES })
        .setProtectedHeader({ alg: "RS256", typ: "JWT" })
        .setIssuer(serverEnv.GOOGLE_SA_CLIENT_EMAIL)
        .setAudience(TOKEN_URL)
        .setIssuedAt(now)
        .setExpirationTime(now + 3600)
        .sign(privateKey);

      const res = await fetch(TOKEN_URL, {
        method: "POST",
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
        body: new URLSearchParams({
          grant_type: "urn:ietf:params:oauth:grant-type:jwt-bearer",
          assertion: jwt,
        }),
      });
      if (!res.ok) {
        const text = await res.text().catch(() => "");
        throw new Error(`[lead-sheets] token exchange failed: ${res.status} ${text}`);
      }
      const json = (await res.json()) as { access_token?: string };
      if (!json.access_token) {
        throw new Error("[lead-sheets] token response missing access_token");
      }
      return json.access_token;
    }

    export async function appendLeadRow(lead: Lead): Promise<void> {
      const token = await getAccessToken();
      const url =
        `https://sheets.googleapis.com/v4/spreadsheets/${serverEnv.GOOGLE_SHEET_ID}` +
        `/values/${encodeURIComponent(RANGE)}:append?valueInputOption=RAW`;

      const row: string[] = [
        new Date().toISOString(),
        lead.name,
        lead.whatsapp,
        lead.message ?? "",
        lead.utm ?? "",
      ];

      const res = await fetch(url, {
        method: "POST",
        headers: {
          Authorization: `Bearer ${token}`,
          "Content-Type": "application/json",
        },
        body: JSON.stringify({ values: [row] }),
      });
      if (!res.ok) {
        const text = await res.text().catch(() => "");
        throw new Error(`[lead-sheets] append failed: ${res.status} ${text}`);
      }
    }
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lead-sheets.ts` existe
    - File contém `import "server-only"`
    - File contém `import { SignJWT, importPKCS8 } from "jose"`
    - File contém `replace(/\\n/g, "\n")` (Pitfall 1)
    - File contém `https://oauth2.googleapis.com/token`
    - File contém `https://sheets.googleapis.com/v4/spreadsheets/`
    - File contém `Leads!A:E` (range)
    - File contém `export async function appendLeadRow(lead: Lead): Promise<void>`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>appendLeadRow edge-compatível via jose + fetch direto, sem googleapis SDK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar src/app/api/lead/route.ts (edge route handler)</name>
  <files>src/app/api/lead/route.ts</files>
  <read_first>
    - tests/api/lead-route.test.ts (criado no Plan 02 — contract das respostas e mocks; honeypot field é `website`)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 1 — edge route + Pitfall 8 — honeypot name)
    - src/lib/lead-schema.ts (leadSchema + normalizeWhatsapp)
    - src/lib/lead-dedup.ts (checkAndRegisterDedup)
  </read_first>
  <behavior>
    - GET → 405 (não suportado)
    - POST JSON inválido → 400 { ok: false, error: "invalid_json" }
    - POST body.website truthy → 200 { ok: true } sem chamar Resend/Sheets
    - POST safeParse fail → 422 { ok: false, error: "validation_failed", issues: parsed.error.flatten() }
    - POST dedup hit → 200 { ok: true, deduped: true } sem chamar integrações
    - POST dual-write ambos OK → 200 { ok: true }
    - POST dual-write apenas 1 OK → 200 { ok: true }
    - POST dual-write ambos falham → 502 { ok: false, error: "delivery_failed" }
  </behavior>
  <action>
    Criar `src/app/api/lead/route.ts`:

    ```typescript
    /**
     * POST /api/lead — edge route handler do form de leads (Phase 5).
     *
     * Flow:
     *   1. Parse JSON; 400 se inválido.
     *   2. Honeypot check (body.website); 200 fake-success se preenchido.
     *      Field name `website` evita autofill 1Password/LastPass (RESEARCH Pitfall 8).
     *   3. Zod safeParse; 422 se falhar.
     *   4. Dedup window 60s por número; 200 deduped se hit.
     *   5. Promise.allSettled([Resend, Sheets]); 200 se pelo menos um pegou; 502 se ambos falharam.
     */
    import "server-only";
    import { leadSchema } from "@/lib/lead-schema";
    import { sendLeadEmail } from "@/lib/lead-resend";
    import { appendLeadRow } from "@/lib/lead-sheets";
    import { checkAndRegisterDedup } from "@/lib/lead-dedup";

    export const runtime = "edge";
    export const dynamic = "force-dynamic";

    export async function POST(req: Request): Promise<Response> {
      let body: unknown;
      try {
        body = await req.json();
      } catch {
        return Response.json({ ok: false, error: "invalid_json" }, { status: 400 });
      }

      // Honeypot — bot preencheu o campo escondido `website` → silently 200.
      // (Field name `website` em vez de `company` evita falso-positivo de password
      // managers que autofilam `company` em forms B2B — Pitfall 8 RESEARCH.)
      if (
        typeof body === "object" &&
        body !== null &&
        typeof (body as Record<string, unknown>).website === "string" &&
        ((body as Record<string, unknown>).website as string).length > 0
      ) {
        return Response.json({ ok: true });
      }

      const parsed = leadSchema.safeParse(body);
      if (!parsed.success) {
        return Response.json(
          { ok: false, error: "validation_failed", issues: parsed.error.flatten() },
          { status: 422 },
        );
      }
      const lead = parsed.data;

      // Dedup (lead.whatsapp já está normalizado pelo schema transform)
      if (checkAndRegisterDedup(lead.whatsapp)) {
        return Response.json({ ok: true, deduped: true });
      }

      const [emailResult, sheetResult] = await Promise.allSettled([
        sendLeadEmail(lead),
        appendLeadRow(lead),
      ]);

      const emailOK = emailResult.status === "fulfilled";
      const sheetOK = sheetResult.status === "fulfilled";

      if (!emailOK) {
        // eslint-disable-next-line no-console
        console.error("[lead] resend failed:", emailResult.reason);
      }
      if (!sheetOK) {
        // eslint-disable-next-line no-console
        console.error("[lead] sheets failed:", sheetResult.reason);
      }

      if (emailOK || sheetOK) {
        return Response.json({ ok: true });
      }
      return Response.json({ ok: false, error: "delivery_failed" }, { status: 502 });
    }

    export function GET(): Response {
      return new Response("Method Not Allowed", { status: 405 });
    }
    ```

    NOTA importante para o executor: o teste de Plan 02 mocka `@/lib/server-env`, `@/lib/lead-resend`, e `@/lib/lead-sheets`. Os imports em route.ts são via path `@/lib/...` para casar com os mocks. NÃO usar relative imports.
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/api/lead-route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/api/lead/route.ts` existe
    - File contém literal `export const runtime = "edge"`
    - File contém `export async function POST(req: Request)`
    - File contém `Promise.allSettled([sendLeadEmail(lead), appendLeadRow(lead)])`
    - File contém honeypot check com `(body as Record<string, unknown>).website` (NÃO `.company`)
    - File NÃO contém literal `.company` em honeypot check (renomeado)
    - File contém `checkAndRegisterDedup(lead.whatsapp)`
    - File contém `parsed.error.flatten()`
    - File contém `export function GET()` retornando 405
    - `npm test -- --run tests/api/lead-route.test.ts` exits 0 (TODOS os 7 testes RED do Plan 02 viram GREEN)
  </acceptance_criteria>
  <done>Edge route funcional com honeypot=`website`, 7 testes RED → GREEN, dual-write + honeypot + dedup todos no contrato.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (browser) → edge route | Untrusted JSON; toda validação server-side via Zod |
| edge route → Resend HTTPS API | TLS; key em env var server-only |
| edge route → Google OAuth2 + Sheets | TLS; service account JWT RS256 short-lived (1h) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering (XSS in email body) | `lead-resend.buildLeadHtml` | mitigate | `escapeHtml()` em todo dynamic data (name, whatsapp, message, utm) — testes do email não cobrem mas grep prova presença |
| T-05-10 | Info Disclosure | Stack trace ao client em erro | mitigate | Response body em erro contém apenas `{ ok:false, error: "<code>" }`; reasons logados via `console.error` server-side only |
| T-05-11 | Spoofing (sender forgery) | Resend `from:` | mitigate | `from:` hard-coded server-side (`FROM_VERIFIED`/`FROM_SANDBOX`); client jamais controla. Em prod requer DNS DKIM/SPF verificado em resend.com (Lenny faz parte do setup) |
| T-05-12 | DoS (mass submission) | `/api/lead` | mitigate (partial) | Honeypot + dedup 60s; rate limit por IP fica como Phase 7 TODO (RESEARCH.md A3) |
| T-05-13 | Replay (dedup bypass) | normalizeWhatsapp inconsistente | mitigate | Schema transform aplica normalizeWhatsapp ANTES de dedup — única fonte. Plan 04 não duplica essa lógica no client |
| T-05-14 | Tampering (JWT forgery) | Sheets auth | accept (relies on jose) | RS256 com private key 2048-bit em env server-only; jose biblioteca audited; 1h TTL minimiza janela |
| T-05-15 | Info Disclosure (PII em logs) | console.error em failure | accept | Logs Vercel contêm `lead.whatsapp` apenas indiretamente (via Resend error message); aceitável v1 — Phase 6 revisa logging |
</threat_model>

<verification>
- `npm test -- --run tests/api/lead-route.test.ts` exits 0 (7/7 green)
- `npm run typecheck` exits 0
- `grep -r "from \"@/lib/server-env\"" src/` retorna 3 matches: `lead-resend.ts`, `lead-sheets.ts`, e nada mais (route.ts NÃO importa server-env diretamente — usa via libs)
- `grep -r "googleapis" src/` retorna 0 matches (Node-only dep banida — confirmed RESEARCH)
- `grep -r "jsonwebtoken" src/` retorna 0 matches (substituído por jose)
- `grep -r "\.company" src/app/api/lead/` retorna 0 matches (honeypot field foi renomeado para `website`)
</verification>

<success_criteria>
- 3 arquivos server-only criados em `src/lib/lead-resend.ts`, `src/lib/lead-sheets.ts`, `src/app/api/lead/route.ts`
- Todos os 7 testes de `tests/api/lead-route.test.ts` passam
- Build do Next compila sem warnings de "module not found"
- Bundle client NÃO contém `RESEND_API_KEY` (verificável após Plan 07 com `@next/bundle-analyzer` — opcional aqui)
- Honeypot field name é `website` (consistente com Plan 01 schema + Plan 02 tests)
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-03-SUMMARY.md` listando: endpoint, contract de resposta, honeypot field name (`website`), ação requerida do Lenny (env vars + domain verification + GCP service account + sheet sharing).
</output>
