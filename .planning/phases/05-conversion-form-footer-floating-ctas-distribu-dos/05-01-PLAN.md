---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .env.example
  - src/lib/server-env.ts
  - src/lib/lead-schema.ts
  - src/lib/lead-dedup.ts
  - src/content/form.ts
  - src/content/footer.ts
autonomous: true
requirements: [CTA-09, CTA-10, CTA-12]
requirements_addressed: [CTA-09, CTA-10, CTA-12]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "Server secrets só acessíveis em código server-side (build falha se importar do client)"
    - "Zod schema valida nome, WhatsApp e mensagem identicamente client e server"
    - "Dedup in-memory expira após 60s sem leak de memória"
    - "Microcopy do form e footer existem como TS puro, zero hard-code em JSX"
    - "Honeypot field name é 'website' (não 'company') — evita autofill de 1Password/LastPass em contexto B2B (RESEARCH Pitfall 8)"
  artifacts:
    - path: "src/lib/server-env.ts"
      provides: "schema validado de RESEND_API_KEY, LEAD_TO_EMAIL, GOOGLE_SA_*, GOOGLE_SHEET_ID"
      contains: 'import "server-only"'
    - path: "src/lib/lead-schema.ts"
      provides: "Zod schema + normalizeWhatsapp + type Lead — compartilhado client ↔ server"
      contains: "export const leadSchema"
    - path: "src/lib/lead-dedup.ts"
      provides: "checkAndRegisterDedup(phone) com TTL 60s + cleanup oportunista"
      contains: "WINDOW_MS = 60_000"
    - path: "src/content/form.ts"
      provides: "Microcopy do form (header, sub, placeholders, CTA submit, success, error) em 1 variante (3 propostas serão geradas no Plan 04)"
    - path: "src/content/footer.ts"
      provides: "Microcopy do footer (copyright, links)"
    - path: ".env.example"
      provides: "Documentação das env vars novas — server e public"
  key_links:
    - from: "src/lib/server-env.ts"
      to: "process.env"
      via: 'z.object().parse(process.env) em module scope (throws no build se var faltar)'
      pattern: 'schema\.parse\(process\.env\)'
    - from: "src/lib/lead-schema.ts"
      to: "normalizeWhatsapp (interna)"
      via: "transform do Zod aplicado dentro do schema"
      pattern: 'whatsapp:.*transform.*normalizeWhatsapp'
---

<objective>
Fundação da Phase 5: instalar deps novas (`resend`, `jose`, `server-only`), criar todos os módulos de lib que o edge route e o form vão consumir, e populá-los com content TS. Nenhuma UI ainda — só as peças que tudo o resto importa.

Purpose: Plans 03 (edge route) e 04 (form UI) dependem do schema, dedup, server-env e content existirem com types corretos. Centralizando aqui evita ripple-change.
Output: 3 libs (`server-env`, `lead-schema`, `lead-dedup`), 2 content files (`form.ts`, `footer.ts`), 1 `.env.example` atualizado, deps instaladas em `package.json`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@src/lib/whatsapp.ts
@src/lib/env.ts
@CLAUDE.md

<interfaces>
<!-- Tipos/contratos que o resto da phase consome. Replicar EXATAMENTE -->

From src/lib/whatsapp.ts:
```typescript
export type WhatsAppLocation =
  | "hero" | "header" | "pain" | "product" | "how"
  | "proof" | "footer" | "floating";
```

From src/lib/env.ts (NÃO mexer — server secrets vão para server-env.ts):
```typescript
export const env = {
  NEXT_PUBLIC_WA_NUMBER: process.env.NEXT_PUBLIC_WA_NUMBER,
  NEXT_PUBLIC_GA4_ID: process.env.NEXT_PUBLIC_GA4_ID,
  NEXT_PUBLIC_META_PIXEL_ID: process.env.NEXT_PUBLIC_META_PIXEL_ID,
  NEXT_PUBLIC_CLARITY_ID: process.env.NEXT_PUBLIC_CLARITY_ID,
} as const;
```

Contract for lead-schema.ts (a ser criado neste plan):
```typescript
export function normalizeWhatsapp(raw: string): string; // só dígitos
export const leadSchema: z.ZodSchema<Lead>;
export type Lead = {
  name: string;       // >= 2 chars, trimmed
  whatsapp: string;   // normalized (10-13 digits)
  message?: string;   // <= 1000 chars
  utm?: string;       // opaque string, optional
  website?: string;   // honeypot — schema NÃO rejeita; route handler trata
};
```

Contract for lead-dedup.ts:
```typescript
export function checkAndRegisterDedup(normalizedPhone: string): boolean;
// returns true if duplicate (was registered recently), false if new (and registers)
```

Contract for server-env.ts:
```typescript
export const serverEnv: {
  RESEND_API_KEY: string;
  LEAD_TO_EMAIL: string;            // ex: lenny@likro.com.br
  GOOGLE_SA_CLIENT_EMAIL: string;
  GOOGLE_SA_PRIVATE_KEY: string;    // PEM string with literal \n — caller faz unescape
  GOOGLE_SHEET_ID: string;
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Instalar deps server-side e atualizar .env.example</name>
  <files>package.json, .env.example</files>
  <read_first>
    - package.json (estado atual das deps — confirmar que resend, jose, server-only NÃO estão presentes)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (seção Standard Stack — versões verificadas)
  </read_first>
  <action>
    Rodar `npm install resend@^6.12.3 jose@^6.2.3 server-only@^0.0.1` (CWD = C:/Users/lenny/Desktop/Likro/landing_page). Confirmar via `cat package.json` que as 3 deps aparecem em `"dependencies"`.

    Atualizar `.env.example` (criar se não existir) adicionando as seguintes linhas — manter as existentes (NEXT_PUBLIC_WA_NUMBER, etc.) e ANEXAR estas no fim sob um comentário "# Phase 5 — Lead form server secrets (NÃO prefixar com NEXT_PUBLIC_ — server-only)":

    ```
    # Phase 5 — Lead form server secrets (NÃO prefixar com NEXT_PUBLIC_ — server-only)
    RESEND_API_KEY=re_xxxxxxxxxxxx
    LEAD_TO_EMAIL=lenny@likro.com.br
    GOOGLE_SA_CLIENT_EMAIL=likro-leads@your-project.iam.gserviceaccount.com
    GOOGLE_SA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nMIIE...\n-----END PRIVATE KEY-----\n"
    GOOGLE_SHEET_ID=1AbCdEfGhIjKlMnOpQrStUvWxYz0123456789AbCdE
    ```

    Se `.env.example` ainda não existir no repo, criar o arquivo com TODAS as env vars do projeto (consultar `src/lib/env.ts` para as NEXT_PUBLIC_*) seguidas pelas server-only acima.

    NÃO criar nem editar `.env.local` (Lenny preenche manualmente fora do plan).
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path package.json -Pattern '\"resend\"' -Quiet; Select-String -Path package.json -Pattern '\"jose\"' -Quiet; Select-String -Path package.json -Pattern '\"server-only\"' -Quiet; Select-String -Path .env.example -Pattern 'RESEND_API_KEY' -Quiet; Select-String -Path .env.example -Pattern 'GOOGLE_SHEET_ID' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contém `"resend":` (qualquer versão `^6.x`) em `"dependencies"`
    - `package.json` contém `"jose":` (qualquer versão `^6.x`) em `"dependencies"`
    - `package.json` contém `"server-only":` em `"dependencies"`
    - `npm ls resend jose server-only` exits 0 (sem extraneous/missing)
    - `.env.example` contém literal `RESEND_API_KEY=`, `LEAD_TO_EMAIL=`, `GOOGLE_SA_CLIENT_EMAIL=`, `GOOGLE_SA_PRIVATE_KEY=`, `GOOGLE_SHEET_ID=`
    - `.env.example` NÃO contém nenhuma das 5 keys server-only prefixadas com `NEXT_PUBLIC_` (`grep "NEXT_PUBLIC_RESEND" .env.example` retorna 0 matches)
  </acceptance_criteria>
  <done>3 deps instaladas, `.env.example` documenta 5 server secrets novos sem prefixo NEXT_PUBLIC_, build do projeto ainda passa (`npm run typecheck` exits 0).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar src/lib/server-env.ts (server-only, validado por Zod)</name>
  <files>src/lib/server-env.ts</files>
  <read_first>
    - src/lib/env.ts (padrão de export — copiar estilo, mas NÃO incluir as 5 keys novas aqui)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pitfall 3 — validação Zod no module load)
  </read_first>
  <action>
    Criar `src/lib/server-env.ts` com este conteúdo EXATO (ajustar imports se necessário):

    ```typescript
    /**
     * Server-only env vars. Phase 5 (Lead form).
     *
     * Importar este módulo de qualquer arquivo client (mesmo via cadeia transitiva)
     * causa erro de build — "server-only" garante o gate.
     *
     * Validação no module load: se qualquer var faltar, o servidor crasha no boot
     * com mensagem clara via Zod, em vez de falhar silenciosamente no primeiro POST.
     */
    import "server-only";
    import { z } from "zod";

    const schema = z.object({
      RESEND_API_KEY: z.string().min(1, "RESEND_API_KEY ausente"),
      LEAD_TO_EMAIL: z.string().email("LEAD_TO_EMAIL deve ser email válido"),
      GOOGLE_SA_CLIENT_EMAIL: z.string().email("GOOGLE_SA_CLIENT_EMAIL deve ser email válido"),
      GOOGLE_SA_PRIVATE_KEY: z.string().min(1, "GOOGLE_SA_PRIVATE_KEY ausente"),
      GOOGLE_SHEET_ID: z.string().min(1, "GOOGLE_SHEET_ID ausente"),
    });

    const parsed = schema.safeParse({
      RESEND_API_KEY: process.env.RESEND_API_KEY,
      LEAD_TO_EMAIL: process.env.LEAD_TO_EMAIL,
      GOOGLE_SA_CLIENT_EMAIL: process.env.GOOGLE_SA_CLIENT_EMAIL,
      GOOGLE_SA_PRIVATE_KEY: process.env.GOOGLE_SA_PRIVATE_KEY,
      GOOGLE_SHEET_ID: process.env.GOOGLE_SHEET_ID,
    });

    if (!parsed.success) {
      // Em dev: log + throw com lista de issues — Lenny vê no terminal do `next dev`
      const issues = parsed.error.issues.map((i) => `  - ${i.path.join(".")}: ${i.message}`).join("\n");
      throw new Error(
        `[server-env] env vars inválidas/ausentes:\n${issues}\n\nVeja .env.example para o formato esperado.`,
      );
    }

    export const serverEnv = parsed.data;
    ```

    IMPORTANTE: NÃO importar nada de `@/lib/env.ts` aqui — server-env é independente. NÃO exportar `process.env` diretamente.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/server-env.ts` existe
    - Arquivo contém literal `import "server-only";` (primeira linha de código)
    - Arquivo contém literal `z.object({` e as 5 keys: `RESEND_API_KEY`, `LEAD_TO_EMAIL`, `GOOGLE_SA_CLIENT_EMAIL`, `GOOGLE_SA_PRIVATE_KEY`, `GOOGLE_SHEET_ID`
    - Arquivo contém literal `export const serverEnv`
    - Arquivo NÃO contém literal `NEXT_PUBLIC_` (zero menções)
    - `npm run typecheck` exits 0
    - `grep -r "from \"@/lib/server-env\"" src/` retorna 0 matches (nenhum consumidor ainda — Plan 03 adiciona)
  </acceptance_criteria>
  <done>server-env.ts criado, tipo `serverEnv` inferido pelo Zod, typecheck passa, nenhum client import existe.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar src/lib/lead-schema.ts (Zod schema + normalizeWhatsapp)</name>
  <files>src/lib/lead-schema.ts</files>
  <read_first>
    - src/lib/whatsapp.ts (referência de validação de telefone — replicar lógica de "só dígitos" mas mais permissiva: aqui aceita 10-13)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md (seção "Form — campos e UX" para regras de validação — honeypot é `website`, não `company`)
  </read_first>
  <action>
    Criar `src/lib/lead-schema.ts` com este conteúdo:

    ```typescript
    /**
     * Lead form schema — compartilhado client ↔ server.
     *
     * Client: usado via @hookform/resolvers/zod no <LeadForm>.
     * Server: usado em /api/lead/route.ts via leadSchema.safeParse(body).
     *
     * normalizeWhatsapp DEVE ser idêntico nos dois lados — dedup depende disso.
     *
     * Honeypot: campo `website` (NÃO `company` — RESEARCH Pitfall 8: 1Password/LastPass
     * autofila `company` em contexto B2B, gerando falso-positivo).
     */
    import { z } from "zod";

    /** Remove tudo que não é dígito. "+55 (11) 99999-9999" → "5511999999999". */
    export function normalizeWhatsapp(raw: string): string {
      return raw.replace(/\D/g, "");
    }

    export const leadSchema = z.object({
      name: z
        .string()
        .trim()
        .min(2, "Nome muito curto")
        .max(120, "Nome muito longo"),
      whatsapp: z
        .string()
        .trim()
        .min(8, "WhatsApp inválido")
        .max(40, "WhatsApp muito longo")
        .transform(normalizeWhatsapp)
        .refine((v) => /^\d{10,13}$/.test(v), {
          message: "WhatsApp deve ter 10 a 13 dígitos (DDD + número)",
        }),
      message: z
        .string()
        .trim()
        .max(1000, "Mensagem muito longa (máximo 1000 caracteres)")
        .optional()
        .or(z.literal("").transform(() => undefined)),
      utm: z.string().max(500).optional(),
      // Honeypot — schema aceita mas route handler trata.
      // Nome `website` evita autofill 1Password/LastPass (Pitfall 8 RESEARCH).
      website: z.string().optional(),
    });

    export type Lead = z.infer<typeof leadSchema>;
    ```

    Garantir que `normalizeWhatsapp` é named-exported (não default) — testes em Plan 02 importam por nome.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lead-schema.ts` existe
    - Arquivo contém `export function normalizeWhatsapp(raw: string): string`
    - Arquivo contém `export const leadSchema = z.object({`
    - Arquivo contém `export type Lead = z.infer<typeof leadSchema>`
    - Schema tem as 5 keys: `name`, `whatsapp`, `message`, `utm`, `website`
    - Arquivo NÃO contém literal `company:` no schema (honeypot renomeado)
    - `npm run typecheck` exits 0
    - `npm test -- --run tests/lib/lead-schema.test.ts` exits non-zero (test ainda não existe — RED state esperado, criado no Plan 02)
  </acceptance_criteria>
  <done>Schema compartilhado disponível para client e server, types exportados, normalizeWhatsapp determinística, honeypot field name `website`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Criar src/lib/lead-dedup.ts (Map + TTL 60s)</name>
  <files>src/lib/lead-dedup.ts</files>
  <read_first>
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pitfall 4 — TTL + cleanup oportunista)
    - src/lib/lead-schema.ts (referência para `normalizeWhatsapp` — caller já normaliza antes de chamar checkAndRegisterDedup)
  </read_first>
  <action>
    Criar `src/lib/lead-dedup.ts` com este conteúdo:

    ```typescript
    /**
     * Dedup window in-memory para /api/lead.
     *
     * Limitação aceita v1: Map vive por instance da edge function.
     * Spans multi-instance NÃO são dedupadas. Pior caso v1: Lenny recebe
     * 2 emails do mesmo lead. Promover para Upstash KV se virar problema real.
     *
     * Caller responsabilidade: normalizar o número ANTES de chamar.
     */

    const WINDOW_MS = 60_000;
    const dedup = new Map<string, number>();

    /**
     * Retorna `true` se o número já foi registrado dentro da janela de 60s
     * (caller deve tratar como "duplicate, return fake success").
     * Retorna `false` se novo (e registra agora).
     *
     * Faz cleanup oportunista de entradas expiradas a cada chamada — evita
     * memory leak em instance long-lived sem precisar de timer/cron.
     */
    export function checkAndRegisterDedup(normalizedPhone: string): boolean {
      const now = Date.now();

      // Cleanup oportunista — O(n) no tamanho atual do Map, irrelevante v1
      for (const [k, ts] of dedup) {
        if (now - ts > WINDOW_MS) dedup.delete(k);
      }

      const existing = dedup.get(normalizedPhone);
      if (existing !== undefined && now - existing < WINDOW_MS) {
        return true; // duplicate
      }
      dedup.set(normalizedPhone, now);
      return false;
    }

    /** Test-only: reset interno entre testes. NÃO chamar em produção. */
    export function __resetDedupForTests(): void {
      dedup.clear();
    }

    /** Test-only: inspecionar estado interno. */
    export function __getDedupSizeForTests(): number {
      return dedup.size;
    }
    ```

    Não usar Vercel KV / Upstash nesta versão — escopo travado pelo CONTEXT (decisão A4 do RESEARCH).
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/lead-dedup.ts` existe
    - Arquivo contém `export function checkAndRegisterDedup(normalizedPhone: string): boolean`
    - Arquivo contém `WINDOW_MS = 60_000` (ou `60000` — ambos aceitáveis, grep `WINDOW_MS = 60` deve pegar)
    - Arquivo contém `export function __resetDedupForTests`
    - Arquivo contém literal `for (const [k, ts] of dedup)` (cleanup oportunista)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Dedup function pronta com TTL + cleanup, helpers de teste exportados, sem deps externas.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Criar content/form.ts e content/footer.ts (microcopy v1)</name>
  <files>src/content/form.ts, src/content/footer.ts</files>
  <read_first>
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md (seção <specifics> — rascunho de microcopy aprovado por Lenny via PR depois; honeypot é `website`)
    - src/content/whatsapp.ts (padrão de estrutura `as const` para content TS)
  </read_first>
  <action>
    Criar `src/content/form.ts` com este conteúdo EXATO (variante 1 — Plan 04 vai propor 3 variantes em PR para Lenny, esta é o ponto de partida do CONTEXT.md):

    ```typescript
    /**
     * Microcopy do lead form (Phase 5).
     *
     * v1 = variante "premium silencioso" aprovada por Lenny no PR.
     * Tom: convite consultivo, anti-infoproduto, sem caps lock, sem urgência fabricada.
     * Plan 04 propõe 3 variantes em PR; após aprovação, esta constante reflete a final.
     *
     * Honeypot: name="website" (NÃO "company" — RESEARCH Pitfall 8).
     */
    export const FORM_COPY = {
      sectionId: "lead-form-section",
      eyebrow: "Conversa, não cadastro",
      heading: "Prefere que a gente te procure?",
      subheading:
        "Manda seu nome e WhatsApp. Sem cadência de email marketing, sem corrente.",
      fields: {
        name: { label: "Nome", placeholder: "Seu nome" },
        whatsapp: { label: "WhatsApp", placeholder: "WhatsApp com DDD" },
        message: {
          label: "Mensagem (opcional)",
          placeholder: "Algo específico que você quer entender?",
        },
        honeypot: { label: "Seu site (não preencher)", name: "website" as const },
      },
      submit: {
        idle: "Quero conversar",
        submitting: "Enviando…",
      },
      success: {
        heading: "Recebido.",
        body: "Vou te chamar no WhatsApp pessoalmente em até 1 dia útil.",
        secondaryCtaLabel: "Ou fale agora no WhatsApp",
      },
      error: {
        heading: "Não consegui enviar.",
        body: "Tenta de novo ou fala agora no WhatsApp.",
        retryLabel: "Tentar de novo",
      },
      privacyNote:
        "Seus dados ficam só com a Likro. Sem terceiros, sem listas, sem spam.",
    } as const;
    ```

    Criar `src/content/footer.ts` com este conteúdo EXATO:

    ```typescript
    /**
     * Microcopy do footer institucional enxuto (Phase 5).
     *
     * 1 linha desktop / 2 linhas mobile. Fundo DARK editorial (continuidade da Proof).
     */
    export const FOOTER_COPY = {
      brand: "Likro",
      copyright: "© 2026 Likro. Operação para clínicas e estéticas.",
      links: [
        { label: "Política de privacidade", href: "/privacy", rel: "nofollow" },
        { label: "Conversar no WhatsApp", href: "#whatsapp" }, // href real vem do helper via <WhatsAppCta>
      ],
      whatsappCtaLabel: "Conversar no WhatsApp",
    } as const;
    ```

    NÃO embutir copy em JSX em nenhum momento — referenciar `FORM_COPY.x` e `FOOTER_COPY.y` nos Plans 04/06.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/form.ts` existe e contém `export const FORM_COPY` com keys `sectionId`, `heading`, `fields`, `submit`, `success`, `error`
    - `FORM_COPY.sectionId === "lead-form-section"` (verificar via grep `sectionId: "lead-form-section"`)
    - `FORM_COPY.fields.honeypot.name === "website"` (grep `name: "website"`)
    - `src/content/form.ts` NÃO contém literal `name: "company"` (renomeado)
    - `src/content/footer.ts` existe e contém `export const FOOTER_COPY` com keys `brand`, `copyright`, `links`
    - `FOOTER_COPY.links` array contém entry com `label: "Política de privacidade"` e `href: "/privacy"`
    - `npm run typecheck` exits 0
    - `grep -r "Quero conversar" src/sections/ src/components/` retorna 0 matches (copy NÃO foi vazada em JSX — ainda; será via import nos Plans 04/06)
  </acceptance_criteria>
  <done>Microcopy do form e footer disponível como TS puro, sectionId="lead-form-section" estabelecido (consumido pelo IntersectionObserver no Plan 06), honeypot field name "website" fixado (anti-1Password).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build → runtime (server) | Server-only modules NÃO podem vazar para bundle client |
| process.env → serverEnv | env vars não-validadas viram serverEnv validado tipado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Info Disclosure | `src/lib/server-env.ts` | mitigate | `import "server-only"` na primeira linha — Next.js bloqueia build se módulo for importado de client component (mesmo via cadeia transitiva) |
| T-05-02 | DoS (silent failure) | env var missing em prod | mitigate | Zod `safeParse` + `throw` no module load — falha alta e visível no boot/log, em vez de 500 no primeiro POST |
| T-05-03 | Tampering | `lead-dedup` memory growth | mitigate | Cleanup oportunista a cada chamada com TTL 60s; Map size monitorável via `__getDedupSizeForTests` |
| T-05-04 | Tampering | normalizeWhatsapp client/server drift | mitigate | Função única em `lead-schema.ts` importada por ambos os lados — impossível divergir |
| T-05-05 | Info Disclosure | Honeypot field acessível por screen readers / autofill | mitigate | Field name `website` evita autofill 1Password/LastPass (Pitfall 8); Plan 04 adiciona off-screen CSS + aria-hidden + tabIndex=-1 |
</threat_model>

<verification>
- `npm run typecheck` exits 0
- `npm test -- --run tests/lib/` exits non-zero (testes ainda não existem — esperado RED até Plan 02)
- `grep -r "import.*server-env" src/` retorna apenas import em `src/lib/server-env.ts` (self) — nenhum consumidor ainda
- `grep -r "RESEND_API_KEY" src/ --include="*.ts" --include="*.tsx"` aparece SOMENTE em `src/lib/server-env.ts`
- `grep -r "name: \"company\"" src/` retorna 0 matches (honeypot renomeado para `website`)
</verification>

<success_criteria>
- 3 deps novas instaladas (`resend`, `jose`, `server-only`)
- `.env.example` documenta 5 server secrets sem prefixo NEXT_PUBLIC_
- `src/lib/server-env.ts` + `src/lib/lead-schema.ts` + `src/lib/lead-dedup.ts` criados
- `src/content/form.ts` + `src/content/footer.ts` criados
- Honeypot field name unificado como `website` em schema + content
- `npm run typecheck` passa
- Zero hard-code de microcopy em JSX
- Sem imports de server-env em arquivos client
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-01-SUMMARY.md` listing: deps instaladas, arquivos criados, types exportados (`Lead`, `WhatsAppLocation` já existente), contract de `checkAndRegisterDedup`, honeypot field name (`website`).
</output>
