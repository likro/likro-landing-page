---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 02
type: tdd
wave: 1
depends_on: ["01"]
files_modified:
  - tests/lib/lead-schema.test.ts
  - tests/lib/lead-dedup.test.ts
  - tests/api/lead-route.test.ts
  - tests/sections/lead-form.test.tsx
  - tests/sections/floating-whatsapp.test.tsx
  - tests/components/header.test.tsx
  - tests/landing/cta-distribution.test.tsx
  - tests/content/whatsapp-messages.test.ts
  - src/lib/analytics.ts
autonomous: true
requirements: [CTA-05, CTA-06, CTA-08, CTA-09, CTA-10, CTA-11, CTA-12, MOBILE-02, MOBILE-06]
requirements_addressed: [CTA-05, CTA-06, CTA-08, CTA-09, CTA-10, CTA-11, CTA-12, MOBILE-02, MOBILE-06]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "8 test files existem em estado RED (failing) ANTES de qualquer feature task rodar"
    - "Cada teste referencia ao menos 1 requirement ID via comentário"
    - "tipo AnalyticsEvent inclui novos eventos da Phase 5 (form_focus, form_submit_*) — verificado em analytics.ts (ALREADY PRESENT — adicionar APENAS se faltar)"
    - "Vitest setup já cobre testing-library; nenhum framework adicional precisa ser instalado"
    - "Plan 02 depende do Plan 01 (lead-schema, lead-dedup, FORM_COPY existem) para testes de schema e dedup serem GREEN imediatos"
  artifacts:
    - path: "tests/lib/lead-schema.test.ts"
      provides: "Cobertura de normalizeWhatsapp + leadSchema validation"
      contains: 'normalizeWhatsapp'
    - path: "tests/lib/lead-dedup.test.ts"
      provides: "TTL behavior + cleanup oportunista"
      contains: 'checkAndRegisterDedup'
    - path: "tests/api/lead-route.test.ts"
      provides: "Edge route happy path + honeypot (website) + dedup + dual-write semantics"
      contains: 'Promise.allSettled'
    - path: "tests/sections/lead-form.test.tsx"
      provides: "Render, focus, submit, disabled, success, error states; honeypot name=website"
      contains: 'LeadForm'
    - path: "tests/sections/floating-whatsapp.test.tsx"
      provides: "Visibility >50vh, hide when form in view (com section id stub), safe-area class"
      contains: 'FloatingWhatsApp'
    - path: "tests/components/header.test.tsx"
      provides: "Hide-on-scroll-down, show-on-scroll-up, reduced-motion path"
      contains: 'useScroll'
    - path: "tests/landing/cta-distribution.test.tsx"
      provides: "Página tem ≥ 4 (target 6) CTAs WhatsApp com data-location distintos"
      contains: 'data-location'
    - path: "tests/content/whatsapp-messages.test.ts"
      provides: "Toda WhatsAppLocation tem entry em WHATSAPP_MESSAGES"
      contains: 'WHATSAPP_MESSAGES'
  key_links:
    - from: "tests/api/lead-route.test.ts"
      to: "src/app/api/lead/route.ts (Plan 03)"
      via: "vi.fn() mock de global fetch + import dinâmico do route handler"
      pattern: "POST.*\\(.*Request"
    - from: "tests/sections/lead-form.test.tsx"
      to: "src/sections/Form/LeadForm.tsx (Plan 04)"
      via: "RTL render + userEvent type/click"
      pattern: "render.*<LeadForm"
---

<objective>
Wave 0 do Nyquist: criar TODOS os 8 test files em estado RED, antes de qualquer feature task começar. Cada teste é a especificação executável da feature correspondente — feature tasks dos Plans 03/04/05/06/07 ficam GREEN quando esses testes passarem.

Purpose: Sampling rate da VALIDATION.md exige que cada feature task tenha `<automated>` apontando para um teste existente. Sem Wave 0, plans posteriores precisariam stubbar testes na hora — degrada o sinal.
Output: 8 test files failing (esperado — features ainda não codadas) + 1 ajuste em `src/lib/analytics.ts` (adicionar eventos que ainda não estão no tipo).

NOTA dependency: `depends_on: ["01"]` — Tasks 1-2 importam `@/lib/lead-schema` e `@/lib/lead-dedup` que existem APÓS Plan 01. Mesma wave (1) para preservar agrupamento de planning, ordem dentro da wave via depends_on edge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md
@src/lib/analytics.ts
@src/lib/whatsapp.ts
@src/content/whatsapp.ts
@vitest.config.ts
@tests/setup.ts

<interfaces>
<!-- AnalyticsEvent atual (de src/lib/analytics.ts) — Phase 5 já cobre todos os eventos necessários: -->
```typescript
export type AnalyticsEvent =
  | "whatsapp_click" | "whatsapp_cta_error" | "cta_click"
  | "form_focus" | "form_submit_attempt" | "form_submit_success" | "form_submit_error"
  | "section_view" | "scroll_depth";
```
(VERIFICADO no arquivo lido — todos os eventos da Phase 5 já existem. Task 9 abaixo é DEFENSIVA — verifica e adiciona apenas se faltar algum.)

<!-- Padrão de motion/react mock COMPLETO (usado por Tasks 6 e 7 — render de Header refatorado client) -->
```typescript
vi.mock("motion/react", async () => {
  const actual = await vi.importActual<typeof import("motion/react")>("motion/react");
  return {
    ...actual,
    useReducedMotion: () => true,
    useScroll: () => ({
      scrollY: { get: () => 0, getPrevious: () => 0, on: () => () => {} },
    }),
    useMotionValueEvent: () => {},
  };
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: tests/lib/lead-schema.test.ts (covers CTA-09, CTA-10)</name>
  <files>tests/lib/lead-schema.test.ts</files>
  <read_first>
    - src/lib/lead-schema.ts (criado no Plan 01 — confirmar signature de leadSchema e normalizeWhatsapp; honeypot field é `website`)
    - tests/setup.ts (padrão de setup do projeto)
  </read_first>
  <behavior>
    - normalizeWhatsapp remove TODOS os non-digits (espaços, +, (, ), -)
    - normalizeWhatsapp é idempotente (input já normalizado = output igual)
    - leadSchema.safeParse com nome de 1 char falha
    - leadSchema.safeParse com nome >=2 chars + WhatsApp válido (11 digits) sucede
    - leadSchema normaliza WhatsApp no parse: input "+55 (11) 99999-9999" → output "5511999999999"
    - leadSchema rejeita WhatsApp com menos de 10 dígitos puros
    - leadSchema rejeita WhatsApp com mais de 13 dígitos puros
    - leadSchema rejeita mensagem com 1001 chars
    - leadSchema aceita mensagem vazia (converte para undefined)
    - leadSchema aceita honeypot field "website" preenchido (route handler rejeita, não schema)
  </behavior>
  <action>
    Criar `tests/lib/lead-schema.test.ts` com este conteúdo:

    ```typescript
    /**
     * Cobertura: CTA-09 (form validation), CTA-10 (server-side Zod validation).
     * Honeypot field name = `website` (NÃO `company` — RESEARCH Pitfall 8).
     */
    import { describe, expect, it } from "vitest";
    import { leadSchema, normalizeWhatsapp } from "@/lib/lead-schema";

    describe("normalizeWhatsapp", () => {
      it("remove todos os non-digits", () => {
        expect(normalizeWhatsapp("+55 (11) 99999-9999")).toBe("5511999999999");
        expect(normalizeWhatsapp("11 9 9999-9999")).toBe("11999999999");
      });

      it("é idempotente", () => {
        const once = normalizeWhatsapp("5511999999999");
        const twice = normalizeWhatsapp(once);
        expect(once).toBe(twice);
      });
    });

    describe("leadSchema", () => {
      const validBase = {
        name: "Lenny Wajcberg",
        whatsapp: "11999999999",
      };

      it("aceita lead mínimo válido", () => {
        const r = leadSchema.safeParse(validBase);
        expect(r.success).toBe(true);
      });

      it("rejeita nome com 1 char", () => {
        const r = leadSchema.safeParse({ ...validBase, name: "L" });
        expect(r.success).toBe(false);
      });

      it("normaliza WhatsApp no parse", () => {
        const r = leadSchema.safeParse({ ...validBase, whatsapp: "+55 (11) 99999-9999" });
        expect(r.success).toBe(true);
        if (r.success) expect(r.data.whatsapp).toBe("5511999999999");
      });

      it("rejeita WhatsApp com 9 dígitos", () => {
        const r = leadSchema.safeParse({ ...validBase, whatsapp: "999999999" });
        expect(r.success).toBe(false);
      });

      it("rejeita WhatsApp com 14 dígitos", () => {
        const r = leadSchema.safeParse({ ...validBase, whatsapp: "12345678901234" });
        expect(r.success).toBe(false);
      });

      it("rejeita mensagem com 1001 chars", () => {
        const r = leadSchema.safeParse({ ...validBase, message: "x".repeat(1001) });
        expect(r.success).toBe(false);
      });

      it("converte mensagem vazia para undefined", () => {
        const r = leadSchema.safeParse({ ...validBase, message: "" });
        expect(r.success).toBe(true);
        if (r.success) expect(r.data.message).toBeUndefined();
      });

      it("aceita honeypot field 'website' preenchido (route handler valida, não schema)", () => {
        const r = leadSchema.safeParse({ ...validBase, website: "http://spam.example" });
        expect(r.success).toBe(true);
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/lib/lead-schema.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/lib/lead-schema.test.ts` existe
    - File contém literais `normalizeWhatsapp`, `leadSchema`, `safeParse`
    - File contém comentário com `CTA-09` e `CTA-10`
    - File testa honeypot field name `website` (não `company`)
    - `npm test -- --run tests/lib/lead-schema.test.ts` exits 0 (porque Plan 01 já criou o schema, este teste passa direto — green)
  </acceptance_criteria>
  <done>Schema testado, todas as edge cases cobertas, teste green imediatamente (Plan 01 fornece o código).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: tests/lib/lead-dedup.test.ts (covers CTA-12)</name>
  <files>tests/lib/lead-dedup.test.ts</files>
  <read_first>
    - src/lib/lead-dedup.ts (criado no Plan 01)
  </read_first>
  <behavior>
    - Primeira chamada com um número retorna false (não duplicado) e registra
    - Segunda chamada com mesmo número dentro de 60s retorna true
    - Chamada com número diferente sempre retorna false
    - Após avançar tempo > 60s (via vi.useFakeTimers), número é considerado novo de novo
    - Cleanup oportunista: após avançar > 60s e fazer call com OUTRO número, entradas expiradas são removidas (__getDedupSizeForTests volta a 1)
  </behavior>
  <action>
    Criar `tests/lib/lead-dedup.test.ts`:

    ```typescript
    /**
     * Cobertura: CTA-12 (dedup por número WhatsApp em janela de 60s).
     */
    import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
    import {
      checkAndRegisterDedup,
      __resetDedupForTests,
      __getDedupSizeForTests,
    } from "@/lib/lead-dedup";

    describe("checkAndRegisterDedup", () => {
      beforeEach(() => {
        __resetDedupForTests();
        vi.useFakeTimers();
      });
      afterEach(() => {
        vi.useRealTimers();
      });

      it("primeira chamada retorna false e registra", () => {
        expect(checkAndRegisterDedup("5511999999999")).toBe(false);
        expect(__getDedupSizeForTests()).toBe(1);
      });

      it("segunda chamada dentro de 60s retorna true", () => {
        checkAndRegisterDedup("5511999999999");
        vi.advanceTimersByTime(30_000);
        expect(checkAndRegisterDedup("5511999999999")).toBe(true);
      });

      it("número diferente sempre retorna false", () => {
        checkAndRegisterDedup("5511999999999");
        expect(checkAndRegisterDedup("5511888888888")).toBe(false);
      });

      it("após 60s, número é considerado novo de novo", () => {
        checkAndRegisterDedup("5511999999999");
        vi.advanceTimersByTime(61_000);
        expect(checkAndRegisterDedup("5511999999999")).toBe(false);
      });

      it("cleanup oportunista remove entradas expiradas", () => {
        checkAndRegisterDedup("5511111111111");
        checkAndRegisterDedup("5511222222222");
        expect(__getDedupSizeForTests()).toBe(2);
        vi.advanceTimersByTime(61_000);
        checkAndRegisterDedup("5511333333333"); // dispara cleanup
        expect(__getDedupSizeForTests()).toBe(1);
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/lib/lead-dedup.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém `checkAndRegisterDedup`, `__resetDedupForTests`, `__getDedupSizeForTests`
    - File contém comentário com `CTA-12`
    - File usa `vi.useFakeTimers()` + `vi.advanceTimersByTime(61_000)`
    - `npm test -- --run tests/lib/lead-dedup.test.ts` exits 0 (Plan 01 já forneceu lib — green imediato)
  </acceptance_criteria>
  <done>Dedup TTL + cleanup testados, green imediato.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/api/lead-route.test.ts (covers CTA-10, CTA-11, CTA-12 + threat T-05-06 honeypot)</name>
  <files>tests/api/lead-route.test.ts</files>
  <read_first>
    - src/lib/lead-schema.ts (Lead type — honeypot é `website`)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 1 — edge route handler)
  </read_first>
  <behavior>
    - POST com JSON inválido → 400 { ok: false, error: "invalid_json" }
    - POST com honeypot "website" preenchido → 200 { ok: true } SEM chamar fetch (Resend ou Sheets)
    - POST com body inválido pelo Zod → 422 { ok: false, error: "validation_failed" }
    - POST com lead válido + ambas as integrações OK → 200 { ok: true }, fetch chamado 2x (Resend + Sheets)
    - POST com lead válido + Resend falha, Sheets OK → 200 { ok: true }
    - POST com lead válido + ambas falham → 502 { ok: false, error: "delivery_failed" }
    - POST com mesmo WhatsApp 2x em < 60s → segundo retorna 200 { ok: true, deduped: true } sem nova chamada de fetch
  </behavior>
  <action>
    Criar `tests/api/lead-route.test.ts`. Como o handler ainda não existe (Plan 03), use dynamic import dentro do teste após mockar env e dependências. Pattern:

    ```typescript
    /**
     * Cobertura: CTA-10 (edge route + dual-write), CTA-11 (success path),
     * CTA-12 (dedup), threat T-05-06 (honeypot fake-success).
     * Honeypot field name = `website` (RESEARCH Pitfall 8).
     */
    import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

    // Mock server-env BEFORE importing the route (Plan 03 createz it).
    vi.mock("@/lib/server-env", () => ({
      serverEnv: {
        RESEND_API_KEY: "re_test",
        LEAD_TO_EMAIL: "test@likro.com.br",
        GOOGLE_SA_CLIENT_EMAIL: "sa@project.iam.gserviceaccount.com",
        GOOGLE_SA_PRIVATE_KEY:
          "-----BEGIN PRIVATE KEY-----\\ntest\\n-----END PRIVATE KEY-----\\n",
        GOOGLE_SHEET_ID: "test-sheet-id",
      },
    }));

    // Mock lead-resend & lead-sheets (Plan 03 creates them) to control results
    const sendLeadEmail = vi.fn();
    const appendLeadRow = vi.fn();
    vi.mock("@/lib/lead-resend", () => ({ sendLeadEmail: (...a: unknown[]) => sendLeadEmail(...a) }));
    vi.mock("@/lib/lead-sheets", () => ({ appendLeadRow: (...a: unknown[]) => appendLeadRow(...a) }));

    function mkRequest(body: unknown): Request {
      return new Request("http://localhost/api/lead", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: typeof body === "string" ? body : JSON.stringify(body),
      });
    }

    async function callPost(req: Request) {
      const { __resetDedupForTests } = await import("@/lib/lead-dedup");
      __resetDedupForTests();
      const mod = await import("@/app/api/lead/route");
      return mod.POST(req as never);
    }

    beforeEach(() => {
      sendLeadEmail.mockReset();
      appendLeadRow.mockReset();
    });
    afterEach(() => vi.clearAllMocks());

    describe("POST /api/lead", () => {
      const validBody = { name: "Lenny", whatsapp: "11999999999" };

      it("400 on invalid JSON", async () => {
        const res = await callPost(mkRequest("not-json"));
        expect(res.status).toBe(400);
      });

      it("honeypot triggered (website filled) → 200 + fake success, no fetch", async () => {
        const res = await callPost(mkRequest({ ...validBody, website: "http://bot.example" }));
        expect(res.status).toBe(200);
        expect(sendLeadEmail).not.toHaveBeenCalled();
        expect(appendLeadRow).not.toHaveBeenCalled();
      });

      it("422 on schema validation fail (name too short)", async () => {
        const res = await callPost(mkRequest({ name: "L", whatsapp: "11999999999" }));
        expect(res.status).toBe(422);
      });

      it("200 when both integrations succeed", async () => {
        sendLeadEmail.mockResolvedValue(undefined);
        appendLeadRow.mockResolvedValue(undefined);
        const res = await callPost(mkRequest(validBody));
        expect(res.status).toBe(200);
        expect(sendLeadEmail).toHaveBeenCalledOnce();
        expect(appendLeadRow).toHaveBeenCalledOnce();
      });

      it("200 when only Sheets succeeds (Resend fails)", async () => {
        sendLeadEmail.mockRejectedValue(new Error("resend down"));
        appendLeadRow.mockResolvedValue(undefined);
        const res = await callPost(mkRequest(validBody));
        expect(res.status).toBe(200);
      });

      it("502 when both fail", async () => {
        sendLeadEmail.mockRejectedValue(new Error("resend down"));
        appendLeadRow.mockRejectedValue(new Error("sheets down"));
        const res = await callPost(mkRequest(validBody));
        expect(res.status).toBe(502);
      });

      it("dedup: 2nd submit in window returns 200 deduped without re-calling integrations", async () => {
        sendLeadEmail.mockResolvedValue(undefined);
        appendLeadRow.mockResolvedValue(undefined);
        await callPost(mkRequest(validBody));
        const callsAfter1 = sendLeadEmail.mock.calls.length + appendLeadRow.mock.calls.length;

        // Same phone, immediate — dedup hits
        const res = await callPost(mkRequest(validBody));
        expect(res.status).toBe(200);
        const json = (await res.json()) as { ok: boolean; deduped?: boolean };
        expect(json.deduped).toBe(true);
        const callsAfter2 = sendLeadEmail.mock.calls.length + appendLeadRow.mock.calls.length;
        expect(callsAfter2).toBe(callsAfter1);
      });
    });
    ```

    NOTA: `callPost` re-importa o route handler em cada chamada — Vitest cache de modules + mocks são preservados via `vi.mock` no top. Se o handler usar `__resetDedupForTests` interno, ótimo; caso contrário, o cleanup acima cobre.
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/api/lead-route.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém literais: `vi.mock("@/lib/server-env"`, `vi.mock("@/lib/lead-resend"`, `vi.mock("@/lib/lead-sheets"`
    - File contém testes: `400 on invalid JSON`, `honeypot triggered (website filled)`, `422 on schema validation`, `200 when both`, `502 when both fail`, `dedup`
    - File contém literal `website: "http://bot.example"` (honeypot field name renomeado)
    - File NÃO contém literal `company: "Spam` ou `company: "Bot` (honeypot antigo eliminado)
    - File contém comentário com `CTA-10`, `CTA-11`, `CTA-12`
    - `npm test -- --run tests/api/lead-route.test.ts` exits non-zero — RED (route handler do Plan 03 ainda não existe). Falha esperada: `Cannot find module '@/app/api/lead/route'`.
  </acceptance_criteria>
  <done>Spec executável do edge route pronto com honeypot=`website`. RED esperado até Plan 03 implementar.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: tests/sections/lead-form.test.tsx (covers CTA-09, CTA-11)</name>
  <files>tests/sections/lead-form.test.tsx</files>
  <read_first>
    - tests/setup.ts (jsdom + testing-library config)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 6 — honeypot off-screen + Pitfall 8 — field name `website`)
    - src/content/form.ts (microcopy keys que o teste vai grep; FORM_COPY.fields.honeypot.name = "website")
  </read_first>
  <behavior>
    - Render mostra 3 campos visíveis (nome, WhatsApp, mensagem) + honeypot off-screen com name="website"
    - Honeypot tem aria-hidden="true" + tabIndex={-1} + autoComplete="off" + estilo position:absolute;left:-9999px
    - Submit com campos vazios mostra erros inline (aria-invalid)
    - Submit com dados válidos chama fetch("/api/lead", POST)
    - Durante submit, botão fica disabled
    - Após sucesso (mock fetch 200 ok:true), form é substituído por bloco success (texto de FORM_COPY.success.heading)
    - Após erro (mock fetch 502), aparece bloco error + botão retry
    - data-clarity-mask="true" no root wrapper do LeadForm (em isolation — Task 3 do Plan 04 garante isso no `<form>` element)
  </behavior>
  <action>
    Criar `tests/sections/lead-form.test.tsx`:

    ```typescript
    /**
     * Cobertura: CTA-09 (3 campos + honeypot `website`), CTA-11 (disabled + success inline).
     * PII masking (data-clarity-mask) — preparação Phase 6.
     */
    import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
    import { render, screen, waitFor } from "@testing-library/react";
    import userEvent from "@testing-library/user-event";
    import { LeadForm } from "@/sections/Form/LeadForm";
    import { FORM_COPY } from "@/content/form";

    beforeEach(() => {
      vi.stubGlobal("fetch", vi.fn());
    });
    afterEach(() => {
      vi.unstubAllGlobals();
      vi.clearAllMocks();
    });

    describe("<LeadForm>", () => {
      it("renderiza 3 campos visíveis + honeypot off-screen (name=website)", () => {
        render(<LeadForm />);
        expect(screen.getByLabelText(FORM_COPY.fields.name.label)).toBeInTheDocument();
        expect(screen.getByLabelText(FORM_COPY.fields.whatsapp.label)).toBeInTheDocument();
        expect(screen.getByLabelText(FORM_COPY.fields.message.label, { exact: false })).toBeInTheDocument();
        const honeypot = document.querySelector('input[name="website"]') as HTMLInputElement;
        expect(honeypot).not.toBeNull();
        expect(honeypot.getAttribute("tabindex")).toBe("-1");
        expect(honeypot.getAttribute("autocomplete")).toBe("off");
      });

      it("honeypot wrapper tem aria-hidden e está off-screen", () => {
        render(<LeadForm />);
        const honeypot = document.querySelector('input[name="website"]') as HTMLInputElement;
        const wrapper = honeypot.closest('[aria-hidden="true"]') as HTMLElement | null;
        expect(wrapper).not.toBeNull();
        // Off-screen via position:absolute;left:-9999px (Pattern 6 RESEARCH)
        if (wrapper) {
          expect(wrapper.style.position).toBe("absolute");
          expect(wrapper.style.left).toBe("-9999px");
        }
      });

      it("root wrapper tem data-clarity-mask=true", () => {
        const { container } = render(<LeadForm />);
        // Em isolation, o root element renderizado pelo LeadForm DEVE ter o atributo
        // (Plan 04 Task 3 garante via <form data-clarity-mask="true" ...> OU wrapper div).
        expect(container.querySelector('[data-clarity-mask="true"]')).not.toBeNull();
      });

      it("submete dados válidos via fetch POST /api/lead", async () => {
        (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValue(
          new Response(JSON.stringify({ ok: true }), { status: 200 }),
        );
        const user = userEvent.setup();
        render(<LeadForm />);
        await user.type(screen.getByLabelText(FORM_COPY.fields.name.label), "Lenny");
        await user.type(screen.getByLabelText(FORM_COPY.fields.whatsapp.label), "11999999999");
        await user.click(screen.getByRole("button", { name: new RegExp(FORM_COPY.submit.idle, "i") }));
        await waitFor(() => {
          expect(global.fetch).toHaveBeenCalledWith(
            "/api/lead",
            expect.objectContaining({ method: "POST" }),
          );
        });
      });

      it("botão fica disabled durante submit", async () => {
        let resolve!: (v: Response) => void;
        (global.fetch as ReturnType<typeof vi.fn>).mockImplementation(
          () => new Promise<Response>((r) => (resolve = r)),
        );
        const user = userEvent.setup();
        render(<LeadForm />);
        await user.type(screen.getByLabelText(FORM_COPY.fields.name.label), "Lenny");
        await user.type(screen.getByLabelText(FORM_COPY.fields.whatsapp.label), "11999999999");
        const btn = screen.getByRole("button", { name: new RegExp(FORM_COPY.submit.idle, "i") });
        await user.click(btn);
        await waitFor(() => expect(btn).toBeDisabled());
        resolve(new Response(JSON.stringify({ ok: true }), { status: 200 }));
      });

      it("após sucesso, mostra bloco com FORM_COPY.success.heading", async () => {
        (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValue(
          new Response(JSON.stringify({ ok: true }), { status: 200 }),
        );
        const user = userEvent.setup();
        render(<LeadForm />);
        await user.type(screen.getByLabelText(FORM_COPY.fields.name.label), "Lenny");
        await user.type(screen.getByLabelText(FORM_COPY.fields.whatsapp.label), "11999999999");
        await user.click(screen.getByRole("button", { name: new RegExp(FORM_COPY.submit.idle, "i") }));
        await waitFor(() => {
          expect(screen.getByText(FORM_COPY.success.heading)).toBeInTheDocument();
        });
      });

      it("após 502, mostra bloco error com retry", async () => {
        (global.fetch as ReturnType<typeof vi.fn>).mockResolvedValue(
          new Response(JSON.stringify({ ok: false, error: "delivery_failed" }), { status: 502 }),
        );
        const user = userEvent.setup();
        render(<LeadForm />);
        await user.type(screen.getByLabelText(FORM_COPY.fields.name.label), "Lenny");
        await user.type(screen.getByLabelText(FORM_COPY.fields.whatsapp.label), "11999999999");
        await user.click(screen.getByRole("button", { name: new RegExp(FORM_COPY.submit.idle, "i") }));
        await waitFor(() => {
          expect(screen.getByText(FORM_COPY.error.heading)).toBeInTheDocument();
          expect(screen.getByRole("button", { name: new RegExp(FORM_COPY.error.retryLabel, "i") })).toBeInTheDocument();
        });
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/sections/lead-form.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém literais: `LeadForm`, `userEvent`, `data-clarity-mask`, `name="website"`, `tabindex`, `FORM_COPY.success.heading`
    - File NÃO contém literal `name="company"` em queries (renomeado)
    - File contém comentário com `CTA-09` e `CTA-11`
    - File tem 6 `it(` blocks
    - File verifica `wrapper.style.position === "absolute"` e `wrapper.style.left === "-9999px"`
    - `npm test -- --run tests/sections/lead-form.test.tsx` exits non-zero — RED (Plan 04 ainda não criou LeadForm). Falha esperada: `Cannot find module '@/sections/Form/LeadForm'`.
  </acceptance_criteria>
  <done>LeadForm spec pronto com honeypot=`website` + off-screen position:absolute. RED esperado até Plan 04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: tests/sections/floating-whatsapp.test.tsx (covers CTA-06, MOBILE-02)</name>
  <files>tests/sections/floating-whatsapp.test.tsx</files>
  <read_first>
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 5 — IntersectionObserver via hook)
    - src/components/ui/whatsapp-cta.tsx (variant "floating" já tem padding-bottom env safe-area)
    - src/content/form.ts (FORM_COPY.sectionId === "lead-form-section")
  </read_first>
  <behavior>
    - Antes de scroll, FloatingWhatsApp NÃO está visível (opacity 0 / pointer-events none)
    - Após scroll > 50vh, FloatingWhatsApp fica visível
    - Quando form está em viewport (mock IntersectionObserver), FloatingWhatsApp esconde
    - Container do componente tem `md:hidden` class (mobile only)
    - Botão renderizado tem `data-location="floating"`
    - Botão renderizado tem className contendo `safe-area-inset-bottom` ou `env(safe-area-inset-bottom)` (via WhatsAppCta variant=floating)

    SETUP CRÍTICO: `useFormInView` (Plan 04) só registra IntersectionObserver se
    `document.getElementById("lead-form-section")` retornar elemento. Em render isolado
    do FloatingWhatsApp, esse elemento NÃO existe naturalmente. Sem ele, o callback do
    observer nunca é registrado → `formInView` permanece false → teste "quando form
    em viewport, esconde" falha mesmo invocando `observerCallback` manualmente.

    FIX: cada `it()` que depende do observer cria `<section id="lead-form-section">`
    em `document.body` antes do render, e remove em `afterEach`.
  </behavior>
  <action>
    Criar `tests/sections/floating-whatsapp.test.tsx`:

    ```typescript
    /**
     * Cobertura: CTA-06 (floating mobile + scroll threshold + hide when form), MOBILE-02 (safe-area).
     *
     * SETUP: cria <section id="lead-form-section"> no document.body antes de cada render,
     * porque useFormInView só registra observer se o target id existir no DOM.
     */
    import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
    import { act, render } from "@testing-library/react";
    import { FloatingWhatsApp } from "@/sections/Floating/FloatingWhatsApp";

    // Mock IntersectionObserver — controllable via callback handle
    let observerCallback: ((entries: Array<{ isIntersecting: boolean }>) => void) | null = null;
    let formSectionEl: HTMLElement | null = null;

    beforeEach(() => {
      observerCallback = null;
      class MockIO {
        constructor(cb: (entries: Array<{ isIntersecting: boolean }>) => void) {
          observerCallback = cb;
        }
        observe() {}
        disconnect() {}
        unobserve() {}
        takeRecords() { return []; }
      }
      vi.stubGlobal("IntersectionObserver", MockIO as never);
      Object.defineProperty(window, "innerHeight", { writable: true, value: 800 });
      Object.defineProperty(window, "scrollY", { writable: true, value: 0 });

      // CRÍTICO: useFormInView (Plan 04) busca document.getElementById("lead-form-section").
      // Sem este elemento, o observer nunca é registrado e formInView fica preso em false.
      formSectionEl = document.createElement("section");
      formSectionEl.id = "lead-form-section";
      document.body.appendChild(formSectionEl);
    });

    afterEach(() => {
      vi.unstubAllGlobals();
      if (formSectionEl && formSectionEl.parentNode) {
        formSectionEl.parentNode.removeChild(formSectionEl);
      }
      formSectionEl = null;
    });

    function fireScroll(y: number) {
      Object.defineProperty(window, "scrollY", { writable: true, value: y });
      window.dispatchEvent(new Event("scroll"));
    }

    describe("<FloatingWhatsApp>", () => {
      it("container tem md:hidden (mobile only)", () => {
        const { container } = render(<FloatingWhatsApp />);
        const root = container.firstElementChild as HTMLElement;
        expect(root.className).toMatch(/md:hidden/);
      });

      it("renderiza WhatsAppCta com location=floating", () => {
        render(<FloatingWhatsApp />);
        const cta = document.querySelector('[data-location="floating"]');
        expect(cta).not.toBeNull();
      });

      it("antes de scroll, NÃO visível (opacity 0 ou pointer-events none)", () => {
        const { container } = render(<FloatingWhatsApp />);
        const root = container.firstElementChild as HTMLElement;
        // Aceita qualquer um dos sinais de "escondido"
        const styleOpacity = root.style.opacity;
        const hidden =
          styleOpacity === "0" ||
          root.style.pointerEvents === "none" ||
          root.getAttribute("aria-hidden") === "true";
        expect(hidden).toBe(true);
      });

      it("após scroll > 50vh, fica visível", async () => {
        const { container } = render(<FloatingWhatsApp />);
        await act(async () => { fireScroll(500); });
        const root = container.firstElementChild as HTMLElement;
        // pointerEvents=auto OU opacity=1 OU aria-hidden=false
        const visible =
          root.style.opacity === "1" ||
          root.style.pointerEvents === "auto" ||
          root.getAttribute("aria-hidden") !== "true";
        expect(visible).toBe(true);
      });

      it("quando form em viewport, esconde de novo", async () => {
        // O lead-form-section foi criado em beforeEach, então useFormInView
        // consegue registrar o observer e capturar nosso callback mockado.
        const { container } = render(<FloatingWhatsApp />);
        await act(async () => { fireScroll(500); });
        await act(async () => {
          if (observerCallback) observerCallback([{ isIntersecting: true }]);
        });
        const root = container.firstElementChild as HTMLElement;
        const hidden =
          root.style.opacity === "0" ||
          root.style.pointerEvents === "none";
        expect(hidden).toBe(true);
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/sections/floating-whatsapp.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém literais: `FloatingWhatsApp`, `IntersectionObserver`, `md:hidden`, `data-location="floating"`
    - File contém comentário com `CTA-06` e `MOBILE-02`
    - File tem 5 `it(` blocks
    - `beforeEach` cria `document.createElement("section")` com `id = "lead-form-section"` em `document.body`
    - `afterEach` remove o elemento criado
    - `npm test -- --run tests/sections/floating-whatsapp.test.tsx` exits non-zero — RED (Plan 06 ainda não criou). Falha esperada: `Cannot find module '@/sections/Floating/FloatingWhatsApp'`.
  </acceptance_criteria>
  <done>FloatingWhatsApp spec pronto com section stub no DOM para useFormInView registrar observer. RED esperado até Plan 06.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: tests/components/header.test.tsx (covers MOBILE-06)</name>
  <files>tests/components/header.test.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx (estado atual — server component, vai virar client no Plan 05)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 4 — Motion useScroll + useMotionValueEvent)
  </read_first>
  <behavior>
    - Render inicial: header tem y=0 (visible)
    - Após scroll down passando primeira viewport (>800px) com delta >80px, header recebe `aria-hidden` ou y="-100%" no style/data attribute
    - Após scroll up com delta < -8px, header volta para y=0
    - Quando useReducedMotion=true, header NÃO esconde mesmo com scroll down (fica visível)
  </behavior>
  <action>
    Criar `tests/components/header.test.tsx`:

    ```typescript
    /**
     * Cobertura: MOBILE-06 (hide-on-scroll-down, show-on-scroll-up) + reduced-motion path.
     *
     * Estratégia: mockamos motion/react useScroll/useMotionValueEvent/useReducedMotion
     * de forma controlável para forçar o estado interno do Header sem precisar
     * simular um scroll real em jsdom.
     */
    import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";
    import { render } from "@testing-library/react";

    // Capture o callback registrado por useMotionValueEvent para invocar manualmente.
    let scrollChangeCallback: ((value: number) => void) | null = null;
    let getPrevious: () => number = () => 0;
    let reduced = false;

    vi.mock("motion/react", async () => {
      const actual = await vi.importActual<typeof import("motion/react")>("motion/react");
      return {
        ...actual,
        useReducedMotion: () => reduced,
        useScroll: () => ({
          scrollY: {
            get: () => 0,
            getPrevious: () => getPrevious(),
            on: () => () => {},
          },
        }),
        useMotionValueEvent: (_value: unknown, _event: string, cb: (v: number) => void) => {
          scrollChangeCallback = cb;
        },
      };
    });

    // Import APÓS o mock estar configurado.
    let Header: typeof import("@/components/layout/Header").Header;

    beforeEach(async () => {
      reduced = false;
      getPrevious = () => 0;
      scrollChangeCallback = null;
      Object.defineProperty(window, "innerHeight", { writable: true, value: 800 });
      const mod = await import("@/components/layout/Header");
      Header = mod.Header;
    });

    afterEach(() => {
      vi.resetModules();
    });

    describe("<Header> hide-on-scroll", () => {
      it("estado inicial: visível", () => {
        const { container } = render(<Header />);
        const header = container.querySelector("header") as HTMLElement;
        expect(header).not.toBeNull();
        const hidden = header.getAttribute("data-hidden") === "true";
        expect(hidden).toBe(false);
      });

      it("scroll down > 800px + delta > 80px esconde", () => {
        const { container } = render(<Header />);
        // Simular: previous=900, current=1000 → delta=100 (>80), current > innerHeight=800
        getPrevious = () => 900;
        scrollChangeCallback?.(1000);
        const header = container.querySelector("header") as HTMLElement;
        expect(header.getAttribute("data-hidden")).toBe("true");
      });

      it("scroll up < -8px reaparece", () => {
        const { container } = render(<Header />);
        // primeiro esconde
        getPrevious = () => 900;
        scrollChangeCallback?.(1000);
        // depois scroll up
        getPrevious = () => 1000;
        scrollChangeCallback?.(950); // delta=-50
        const header = container.querySelector("header") as HTMLElement;
        expect(header.getAttribute("data-hidden")).toBe("false");
      });

      it("reduced-motion: NUNCA esconde mesmo com scroll down forte", () => {
        reduced = true;
        const { container } = render(<Header />);
        getPrevious = () => 900;
        scrollChangeCallback?.(1000);
        const header = container.querySelector("header") as HTMLElement;
        expect(header.getAttribute("data-hidden")).toBe("false");
      });
    });
    ```

    Convenção de implementação para o Header (Plan 05): expor o estado via `data-hidden="true|false"` na `<header>` tag — mais robusto pra testar que ler `style.transform` que Motion escreve assincronamente.
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/components/header.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém: `vi.mock("motion/react"`, `useReducedMotion`, `useMotionValueEvent`, `data-hidden`
    - File contém comentário com `MOBILE-06`
    - File tem 4 `it(` blocks (incluindo o reduced-motion)
    - `npm test -- --run tests/components/header.test.tsx` exits non-zero — RED (Plan 05 ainda não refatorou Header com `data-hidden`)
  </acceptance_criteria>
  <done>Header spec inclui contract `data-hidden` attribute. RED esperado até Plan 05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: tests/landing/cta-distribution.test.tsx (covers CTA-05)</name>
  <files>tests/landing/cta-distribution.test.tsx</files>
  <read_first>
    - src/app/page.tsx (será editado no Plan 07)
    - src/components/ui/whatsapp-cta.tsx (já tem `data-testid="whatsapp-cta"` + `data-location={location}`)
    - src/components/layout/Header.tsx (Plan 05 vai refatorar para client + usar useScroll/useMotionValueEvent — render de HomePage atinge essa cadeia)
  </read_first>
  <behavior>
    - Render `<HomePage>` renderiza pelo menos 4 elementos com `data-testid="whatsapp-cta"`
    - O set de `data-location` cobre no mínimo: hero, header, pain, product, proof, footer, floating (target: 6 únicos)
    - Nenhum `data-location` duplicado em diferentes seções AO MESMO TEMPO no resultado do query (cada location aparece exatamente uma vez por seção esperada)
  </behavior>
  <action>
    Criar `tests/landing/cta-distribution.test.tsx`:

    ```typescript
    /**
     * Cobertura: CTA-05 (CTAs persistentes em ≥ 4 pontos da página).
     *
     * Mock motion/react COMPLETO: HomePage inclui o Header (refatorado pelo Plan 05
     * para client component que usa useScroll/useMotionValueEvent). Sem esses mocks,
     * jsdom estoura "useScroll is not a function" ou retorna undefined ao acessar
     * scrollY. Match com o padrão usado em tests/components/header.test.tsx.
     */
    import { describe, expect, it, vi } from "vitest";
    import { render } from "@testing-library/react";
    import HomePage from "@/app/page";

    // Mock motion/react para HomePage inteira (inclui Header client + animações)
    vi.mock("motion/react", async () => {
      const actual = await vi.importActual<typeof import("motion/react")>("motion/react");
      return {
        ...actual,
        useReducedMotion: () => true, // simplifica reveals em test env
        useScroll: () => ({
          scrollY: {
            get: () => 0,
            getPrevious: () => 0,
            on: () => () => {},
          },
        }),
        useMotionValueEvent: () => {},
      };
    });

    describe("CTA distribution on HomePage", () => {
      const REQUIRED_LOCATIONS = ["hero", "header", "pain", "product", "proof", "footer", "floating"] as const;

      it("renderiza pelo menos 4 CTAs WhatsApp", () => {
        const { container } = render(<HomePage />);
        const ctas = container.querySelectorAll('[data-testid="whatsapp-cta"]');
        expect(ctas.length).toBeGreaterThanOrEqual(4);
      });

      it("cobre todas as 7 locations esperadas (hero, header, pain, product, proof, footer, floating)", () => {
        const { container } = render(<HomePage />);
        const ctas = Array.from(container.querySelectorAll('[data-testid="whatsapp-cta"]'));
        const locations = new Set(ctas.map((el) => el.getAttribute("data-location")));
        for (const loc of REQUIRED_LOCATIONS) {
          expect(locations.has(loc)).toBe(true);
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/landing/cta-distribution.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém: `REQUIRED_LOCATIONS`, `data-testid="whatsapp-cta"`, `data-location`
    - File contém comentário com `CTA-05`
    - File contém literal `["hero", "header", "pain", "product", "proof", "footer", "floating"]` (todas as 7)
    - Mock `motion/react` inclui `useScroll`, `useMotionValueEvent`, E `useReducedMotion` (não apenas o último)
    - `npm test -- --run tests/landing/cta-distribution.test.tsx` exits non-zero — RED (page.tsx ainda só tem hero+header CTAs; Plans 06+07 adicionam o resto)
  </acceptance_criteria>
  <done>Distribution test pronto com motion mocks completos. RED esperado até Plan 07 finalizar wire-up.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 8: tests/content/whatsapp-messages.test.ts (covers CTA-08)</name>
  <files>tests/content/whatsapp-messages.test.ts</files>
  <read_first>
    - src/content/whatsapp.ts (WHATSAPP_MESSAGES atual — todas as 8 keys já existem)
    - src/lib/whatsapp.ts (type WhatsAppLocation)
  </read_first>
  <behavior>
    - WHATSAPP_MESSAGES tem entry não-vazia para CADA WhatsAppLocation
    - Nenhuma entry contém frases banidas anti-IA: "desbloqueie", "potencialize", "transforme sua", "próximo nível", "solução inovadora", "do início ao fim"
    - Nenhuma entry contém literal "wa.me" (CTA-01: zero hard-code de URL em copy)
    - Nenhuma entry contém literal "web.whatsapp.com" (CTA-02 guard)
  </behavior>
  <action>
    Criar `tests/content/whatsapp-messages.test.ts`:

    ```typescript
    /**
     * Cobertura: CTA-08 (mensagem pré-preenchida específica por location).
     */
    import { describe, expect, it } from "vitest";
    import { WHATSAPP_MESSAGES } from "@/content/whatsapp";
    import type { WhatsAppLocation } from "@/lib/whatsapp";

    const ALL_LOCATIONS: WhatsAppLocation[] = [
      "hero", "header", "pain", "product", "how", "proof", "footer", "floating",
    ];

    const BANNED_PHRASES = [
      "desbloqueie",
      "potencialize",
      "transforme sua",
      "próximo nível",
      "solução inovadora",
      "do início ao fim",
    ];

    describe("WHATSAPP_MESSAGES", () => {
      it.each(ALL_LOCATIONS)("tem entry não-vazia para location=%s", (loc) => {
        expect(WHATSAPP_MESSAGES[loc]).toBeTruthy();
        expect(WHATSAPP_MESSAGES[loc].length).toBeGreaterThan(5);
      });

      it("não contém frases banidas anti-IA", () => {
        for (const loc of ALL_LOCATIONS) {
          const lower = WHATSAPP_MESSAGES[loc].toLowerCase();
          for (const banned of BANNED_PHRASES) {
            expect(lower, `location=${loc} contains banned phrase "${banned}"`).not.toContain(banned);
          }
        }
      });

      it("não hard-codeia wa.me ou web.whatsapp.com", () => {
        for (const loc of ALL_LOCATIONS) {
          expect(WHATSAPP_MESSAGES[loc]).not.toMatch(/wa\.me/i);
          expect(WHATSAPP_MESSAGES[loc]).not.toMatch(/web\.whatsapp\.com/i);
        }
      });
    });
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/content/whatsapp-messages.test.ts"</automated>
  </verify>
  <acceptance_criteria>
    - File existe e contém: `WHATSAPP_MESSAGES`, `BANNED_PHRASES`, `it.each(ALL_LOCATIONS)`
    - File contém comentário com `CTA-08`
    - `npm test -- --run tests/content/whatsapp-messages.test.ts` exits 0 (greens imediato — content já existe e é compliant)
  </acceptance_criteria>
  <done>Content test green imediato — atua como guard contra regressões futuras.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 9: Defensive — confirmar AnalyticsEvent type cobre eventos Phase 5</name>
  <files>src/lib/analytics.ts</files>
  <read_first>
    - src/lib/analytics.ts (estado atual — type AnalyticsEvent)
  </read_first>
  <action>
    Ler `src/lib/analytics.ts` e confirmar que o tipo `AnalyticsEvent` inclui as 4 chaves usadas na Phase 5: `"form_focus"`, `"form_submit_attempt"`, `"form_submit_success"`, `"form_submit_error"`.

    Se TODAS as 4 já existirem (caso atual, conforme arquivo lido no contexto deste plan), NÃO editar o arquivo — só rodar typecheck para confirmar.

    Se alguma estiver faltando, ANEXAR ao union type sem remover nada existente. Exemplo:

    ```typescript
    export type AnalyticsEvent =
      | "whatsapp_click"
      | "whatsapp_cta_error"
      | "cta_click"
      | "form_focus"               // adicionar se faltar
      | "form_submit_attempt"      // adicionar se faltar
      | "form_submit_success"      // adicionar se faltar
      | "form_submit_error"        // adicionar se faltar
      | "section_view"
      | "scroll_depth";
    ```

    NÃO alterar a função `track()` em si — só o union type, e somente se necessário.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck; Select-String -Path src/lib/analytics.ts -Pattern 'form_focus|form_submit_attempt|form_submit_success|form_submit_error' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/analytics.ts` contém os literais `form_focus`, `form_submit_attempt`, `form_submit_success`, `form_submit_error`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Type AnalyticsEvent cobre todos os eventos da Phase 5 (já cobria — task é guard).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production code | Testes importam `@/...` mas NUNCA são bundled em prod (vitest.config separa) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | Tampering | Honeypot bypass | mitigate | Test #2 do `lead-route` força que fetch NÃO seja chamado quando `website` está presente — garante o fake-success path |
| T-05-07 | Info Disclosure | PII em fixtures de teste | accept | Fixtures usam "Lenny" + 11999999999 (nada real); testes mockam fetch (nada vai para Resend/Sheets reais) |
| T-05-08 | Tampering | Test bypass via removal | accept | Plan 07 inclui task de UAT que re-roda full suite; CI futura (Phase 7) adiciona gate |
</threat_model>

<verification>
- 8 test files existem em `tests/`
- 3 deles GREEN imediato (lead-schema, lead-dedup, whatsapp-messages — Plan 01 + content existente cobrem)
- 5 deles RED até Plans 03/04/05/06/07 implementarem (lead-route, lead-form, floating-whatsapp, header, cta-distribution)
- `npm test` reporta exatamente 8 test files + brand-lock.test.ts e outros existentes
- `npm run typecheck` passa
</verification>

<success_criteria>
- 8 test files criados em `tests/` cobrindo todos os requirements Phase 5
- 3 GREEN imediatos (sanity check do Plan 01 + content existente)
- 5 RED esperados (executor anota como expected-failures até wave seguinte)
- AnalyticsEvent type cobre eventos Phase 5
- Nenhum teste flaky (todos determinísticos via vi.useFakeTimers/vi.mock)
- VALIDATION.md `wave_0_complete: true` pode ser flipado depois deste plan
- Honeypot field name = `website` em todos os testes (lead-route + lead-form)
- floating-whatsapp test cria section stub no body para useFormInView funcionar
- cta-distribution test mocka motion/react completo (useScroll + useMotionValueEvent + useReducedMotion)
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-02-SUMMARY.md` listando: 8 test files (status GREEN/RED), eventos analytics verificados, contracts implícitos (e.g. `data-hidden` no Header, `data-clarity-mask` no form wrapper, `sectionId="lead-form-section"`, honeypot name=`website`).
</output>
