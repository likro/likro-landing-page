---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 06
type: execute
wave: 4
depends_on: [01, 02, 04, 05]
files_modified:
  - src/sections/Floating/FloatingWhatsApp.tsx
  - src/components/layout/Footer.tsx
  - src/app/privacy/page.tsx
  - src/sections/Pain/index.tsx
  - src/sections/Product/index.tsx
  - src/sections/Proof/index.tsx
autonomous: true
requirements: [CTA-05, CTA-06, CTA-08, MOBILE-02]
requirements_addressed: [CTA-05, CTA-06, CTA-08, MOBILE-02]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "FloatingWhatsApp aparece após scroll > 50vh em mobile (md:hidden), some quando form visível"
    - "Footer renderizado como dark editorial com logo + copyright + 2 links sutis + 1 CTA WhatsApp"
    - "/privacy retorna 200 com placeholder mínimo (Phase 7 escreve conteúdo)"
    - "Seções Pain, Product, Proof receberam <WhatsAppCta> no fim com locations corretos"
    - "Safe-area-inset-bottom respeitado no floating via variant existente do WhatsAppCta"
  artifacts:
    - path: "src/sections/Floating/FloatingWhatsApp.tsx"
      provides: "Client component mobile-only com scroll threshold + useFormInView gate"
      contains: '"use client"'
    - path: "src/components/layout/Footer.tsx"
      provides: "Server component dark editorial + WhatsAppCta location=footer"
    - path: "src/app/privacy/page.tsx"
      provides: "Placeholder mínimo de página de política (200 OK)"
    - path: "src/sections/Pain/index.tsx"
      provides: "Section existente + <WhatsAppCta location='pain'> no fim"
    - path: "src/sections/Product/index.tsx"
      provides: "Section existente + <WhatsAppCta location='product'> no fim"
    - path: "src/sections/Proof/index.tsx"
      provides: "Section existente + <WhatsAppCta location='proof'> dominante na transição pro form"
  key_links:
    - from: "FloatingWhatsApp.tsx"
      to: "useFormInView (Plan 04)"
      via: "import + boolean gate"
      pattern: "useFormInView"
    - from: "FloatingWhatsApp.tsx"
      to: "WhatsAppCta variant='floating'"
      via: "child render"
      pattern: 'variant="floating"'
---

<objective>
Completar os componentes restantes da UI da phase: FloatingWhatsApp, Footer, /privacy placeholder, e adicionar CTAs WhatsApp distribuídos nas seções Pain/Product/Proof. Faz `tests/sections/floating-whatsapp.test.tsx` virar GREEN e fornece todos os pré-requisitos para o wire-up final do Plan 07.

Purpose: Estes 6 arquivos são editados/criados em paralelo (zero file overlap entre si após Plan 05 ter feito Header). Plan separado do 04 porque o LeadForm já estava grande sozinho.
Output: 6 arquivos, 4 novos CTAs WhatsApp distribuídos, 1 floating mobile, 1 footer, 1 privacy placeholder.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@src/components/ui/whatsapp-cta.tsx
@src/components/ui/container.tsx
@src/content/footer.ts
@src/sections/Pain/index.tsx
@src/sections/Product/index.tsx
@src/sections/Proof/index.tsx
@src/hooks/use-form-in-view.ts
@tests/sections/floating-whatsapp.test.tsx

<interfaces>
<!-- WhatsAppCta atual variants (de src/components/ui/whatsapp-cta.tsx): -->
```typescript
type Variant = "primary" | "secondary" | "floating" | "inline";
// floating já inclui:
//   fixed bottom-4 right-4 z-50 size-14 rounded-full shadow-lg
//   [padding-bottom:env(safe-area-inset-bottom)]  ← MOBILE-02 mitigation
```

<!-- IMPORTANTE: o "floating" variant do WhatsAppCta JÁ tem position: fixed.
     Por isso o FloatingWhatsApp wrapper aqui NÃO precisa de fixed adicional —
     ele só controla VISIBILITY (opacity + pointer-events) e mobile-only (md:hidden).
     O posicionamento (bottom-right + safe-area) vem do WhatsAppCta intrinsic. -->

<!-- useFormInView (Plan 04) — boolean derivado de IntersectionObserver no #lead-form-section -->
```typescript
export function useFormInView(opts?: { targetId?: string; threshold?: number }): boolean;
```

<!-- FOOTER_COPY (Plan 01) — brand, copyright, links[]; whatsappCtaLabel -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar src/sections/Floating/FloatingWhatsApp.tsx (mobile-only, scroll gate, form-in-view gate)</name>
  <files>src/sections/Floating/FloatingWhatsApp.tsx</files>
  <read_first>
    - tests/sections/floating-whatsapp.test.tsx (CONTRACT — replicar attributes/queries exatos)
    - src/components/ui/whatsapp-cta.tsx (variant="floating" já tem fixed + safe-area)
    - src/hooks/use-form-in-view.ts (hook do Plan 04)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 5)
  </read_first>
  <behavior>
    - Container raiz tem className `md:hidden` (mobile only)
    - Inicialmente: opacity 0 + pointer-events none (não visible)
    - Após `window.scrollY > window.innerHeight * 0.5`, opacity 1 + pointer-events auto
    - Quando useFormInView() === true (form visible), volta a opacity 0 / pointer-events none
    - WhatsAppCta interno tem variant="floating", location="floating"
    - Pitfall 9 (RESEARCH): também esconde quando algum input do form está focado — listener focusin/focusout no document
  </behavior>
  <action>
    Criar `src/sections/Floating/FloatingWhatsApp.tsx`:

    ```tsx
    "use client";

    /**
     * FloatingWhatsApp — botão flutuante mobile-only (Phase 5).
     *
     * Comportamento (CTA-06, MOBILE-02):
     *  - Mobile only (md:hidden no container).
     *  - Aparece após scroll > 50vh.
     *  - Esconde quando o form está em viewport (useFormInView do Plan 04).
     *  - Pitfall 9: esconde também quando QUALQUER input do form está focado
     *    (teclado virtual sobe e cobriria o botão).
     *  - Discreto: fade simples 200ms, sem pulse/bounce/badge.
     *
     * Posicionamento + safe-area: vem do variant="floating" do <WhatsAppCta>
     * (fixed bottom-4 right-4 + [padding-bottom:env(safe-area-inset-bottom)]).
     */
    import { useEffect, useState } from "react";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    import { useFormInView } from "@/hooks/use-form-in-view";

    const SCROLL_THRESHOLD_RATIO = 0.5; // 50vh

    export function FloatingWhatsApp() {
      const [pastThreshold, setPastThreshold] = useState(false);
      const [inputFocused, setInputFocused] = useState(false);
      const formInView = useFormInView();

      useEffect(() => {
        if (typeof window === "undefined") return;

        const onScroll = () => {
          const threshold = window.innerHeight * SCROLL_THRESHOLD_RATIO;
          setPastThreshold(window.scrollY > threshold);
        };
        onScroll();
        window.addEventListener("scroll", onScroll, { passive: true });
        return () => window.removeEventListener("scroll", onScroll);
      }, []);

      useEffect(() => {
        if (typeof document === "undefined") return;
        const onFocusIn = (e: FocusEvent) => {
          const target = e.target as HTMLElement | null;
          if (!target) return;
          // Esconde se foco entrou em qualquer input/textarea do form section
          if (target.closest('[data-clarity-mask="true"]')) {
            setInputFocused(true);
          }
        };
        const onFocusOut = (e: FocusEvent) => {
          const target = e.target as HTMLElement | null;
          if (!target) return;
          if (target.closest('[data-clarity-mask="true"]')) {
            setInputFocused(false);
          }
        };
        document.addEventListener("focusin", onFocusIn);
        document.addEventListener("focusout", onFocusOut);
        return () => {
          document.removeEventListener("focusin", onFocusIn);
          document.removeEventListener("focusout", onFocusOut);
        };
      }, []);

      const visible = pastThreshold && !formInView && !inputFocused;

      return (
        <div
          aria-hidden={visible ? "false" : "true"}
          className="md:hidden"
          style={{
            opacity: visible ? 1 : 0,
            pointerEvents: visible ? "auto" : "none",
            transition: "opacity 200ms ease",
          }}
        >
          <WhatsAppCta variant="floating" location="floating" />
        </div>
      );
    }
    ```

    Decisões importantes:
    - Não usei Motion `useScroll` aqui — `window.addEventListener` simples + `{ passive: true }` é mais barato para um único threshold check sem interpolation. Motion ganha quando há `useTransform`, não aqui.
    - O wrapper raiz é apenas o controlador de visibilidade. O `WhatsAppCta variant="floating"` interno traz `position: fixed` + `bottom-4 right-4` + safe-area. Isso casa exatamente com o que o teste do Plan 02 espera (verifica md:hidden + data-location + opacity/pointer-events).
    - Pitfall 9 implementado via listener `focusin/focusout` no document, com seletor `[data-clarity-mask="true"]` para limitar ao form wrapper.
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/sections/floating-whatsapp.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Floating/FloatingWhatsApp.tsx` existe e começa com `"use client"`
    - File contém `md:hidden`
    - File contém `WhatsAppCta variant="floating" location="floating"` (ordem dos props irrelevante)
    - File contém `useFormInView()` consumido em variável
    - File contém `window.scrollY > threshold` e `SCROLL_THRESHOLD_RATIO = 0.5`
    - File contém listener `focusin` e `focusout` (Pitfall 9)
    - File contém `aria-hidden={visible ? "false" : "true"}` (ou logic equivalente que produz o atributo)
    - `npm test -- --run tests/sections/floating-whatsapp.test.tsx` exits 0 (5/5 testes do Plan 02 GREEN)
  </acceptance_criteria>
  <done>FloatingWhatsApp funcional com 3 gates (scroll, form-in-view, input-focused) e cobertura de testes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar src/components/layout/Footer.tsx (dark editorial + CTA WhatsApp + links)</name>
  <files>src/components/layout/Footer.tsx</files>
  <read_first>
    - src/content/footer.ts (FOOTER_COPY)
    - src/sections/Proof/index.tsx (para usar a mesma família de cor escura na transição contínua)
    - src/components/ui/container.tsx
    - src/components/ui/whatsapp-cta.tsx
  </read_first>
  <action>
    Criar `src/components/layout/Footer.tsx`:

    ```tsx
    /**
     * Footer — institucional enxuto (Phase 5).
     *
     * Dark editorial (continuidade da Proof).
     * 1 linha desktop / 2 linhas mobile.
     * Logo + copyright + 2 links sutis + 1 CTA WhatsApp.
     *
     * Política de privacidade aponta para /privacy (placeholder até Phase 7 escrever).
     */
    import Link from "next/link";
    import { Container } from "@/components/ui/container";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    import { FOOTER_COPY } from "@/content/footer";

    export function Footer() {
      return (
        <footer className="bg-bg-dark py-10 text-text-on-dark-secondary md:py-12">
          <Container>
            <div className="flex flex-col items-start justify-between gap-6 md:flex-row md:items-center md:gap-8">
              <div className="flex items-center gap-3">
                <span
                  aria-hidden="true"
                  className="grid size-7 place-items-center rounded-[7px] bg-gradient-to-br from-violet-500 to-violet-700 text-[13px] font-bold text-white shadow-[0_0_0_1px_rgba(124,58,237,0.4),0_4px_12px_-2px_rgba(124,58,237,0.5)]"
                >
                  L
                </span>
                <span className="text-sm font-semibold tracking-[-0.01em] text-text-on-dark-primary">
                  {FOOTER_COPY.brand}
                </span>
                <span className="hidden h-4 w-px bg-white/10 md:inline-block" aria-hidden="true" />
                <span className="text-xs text-text-on-dark-secondary md:text-sm">
                  {FOOTER_COPY.copyright}
                </span>
              </div>

              <div className="flex flex-wrap items-center gap-x-6 gap-y-3">
                {FOOTER_COPY.links
                  .filter((l) => !l.href.startsWith("#"))
                  .map((link) => (
                    <Link
                      key={link.href}
                      href={link.href}
                      rel={link.rel ?? undefined}
                      className="text-sm text-text-on-dark-secondary transition-colors hover:text-text-on-dark-primary"
                    >
                      {link.label}
                    </Link>
                  ))}
                <WhatsAppCta variant="inline" location="footer">
                  {FOOTER_COPY.whatsappCtaLabel}
                </WhatsAppCta>
              </div>
            </div>
          </Container>
        </footer>
      );
    }
    ```

    Tokens (`bg-bg-dark`, `text-text-on-dark-primary`, `text-text-on-dark-secondary`) — se algum não existir, usar equivalentes: `bg-zinc-950`, `text-zinc-50`, `text-zinc-400`. NÃO usar `bg-accent-50/100/200`.

    NOTA: o footer ITERA sobre `FOOTER_COPY.links` mas filtra entries com `href.startsWith("#")` porque o "#whatsapp" no FOOTER_COPY era apenas marker semântico — o CTA WhatsApp real vem via `<WhatsAppCta location="footer">`.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Footer.tsx` existe e exporta `Footer`
    - File contém `import { FOOTER_COPY } from "@/content/footer"` (zero hard-code de copy)
    - File contém `<WhatsAppCta variant="inline" location="footer">`
    - File contém Link para `/privacy` (via iteração sobre FOOTER_COPY.links)
    - File NÃO contém literal `"© 2026 Likro"` em JSX (deve vir via FOOTER_COPY.copyright)
    - File NÃO contém `bg-accent-50/100/200` (FOUND-03)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Footer pronto para wire em page.tsx (Plan 07).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar src/app/privacy/page.tsx (placeholder mínimo)</name>
  <files>src/app/privacy/page.tsx</files>
  <read_first>
    - src/app/page.tsx (estilo de page.tsx do projeto)
    - src/components/ui/container.tsx
  </read_first>
  <action>
    Criar `src/app/privacy/page.tsx`:

    ```tsx
    /**
     * Placeholder de política de privacidade.
     *
     * Phase 5 só linka este arquivo via footer; conteúdo real será escrito na Phase 7
     * (com revisão jurídica antes do go-live oficial — está no PROJECT.md).
     */
    import type { Metadata } from "next";
    import Link from "next/link";
    import { Container } from "@/components/ui/container";

    export const metadata: Metadata = {
      title: "Política de privacidade — Likro",
      description: "Em construção. A versão completa será publicada antes do lançamento oficial.",
      robots: { index: false, follow: false },
    };

    export default function PrivacyPage() {
      return (
        <main className="bg-bg-base py-20 md:py-28">
          <Container className="max-w-2xl">
            <h1 className="text-3xl font-semibold tracking-tight text-text-primary md:text-4xl">
              Política de privacidade
            </h1>
            <p className="mt-4 text-base text-text-secondary md:text-lg">
              Em construção. A versão completa será publicada antes do lançamento oficial.
              Enquanto isso, os dados enviados pelo formulário ficam apenas com a Likro —
              sem terceiros, sem listas, sem spam.
            </p>
            <p className="mt-6 text-sm text-text-secondary">
              <Link href="/" className="underline underline-offset-2 hover:text-text-primary">
                Voltar para a página inicial
              </Link>
            </p>
          </Container>
        </main>
      );
    }
    ```

    Token fallbacks: `bg-zinc-50`, `text-zinc-900`, `text-zinc-600` se os tokens semânticos não existirem.

    `robots: { index: false, follow: false }` — placeholder NÃO deve ser indexado (vai redirecionar buscas para `/`).
  </action>
  <verify>
    <automated>powershell -Command "npm run build" 2>&1 | Select-String -Pattern "privacy" -Quiet</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/privacy/page.tsx` existe e exporta `PrivacyPage` como default
    - File contém literal `robots: { index: false, follow: false }` em metadata
    - File contém Link para `/`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>/privacy retorna 200 com placeholder e noindex.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Adicionar CTAs WhatsApp nas seções Pain, Product, Proof</name>
  <files>src/sections/Pain/index.tsx, src/sections/Product/index.tsx, src/sections/Proof/index.tsx</files>
  <read_first>
    - src/sections/Pain/index.tsx (estado atual — entender estrutura)
    - src/sections/Product/index.tsx
    - src/sections/Proof/index.tsx
    - src/components/ui/whatsapp-cta.tsx (variants existentes)
    - src/content/whatsapp.ts (WHATSAPP_MESSAGES tem entries para pain/product/proof)
  </read_first>
  <action>
    Para CADA uma das 3 seções (Pain, Product, Proof), ADICIONAR um CTA WhatsApp no fim do conteúdo da section, ANTES do `</section>` (ou estrutura equivalente).

    REGRAS:
    1. NÃO remover ou alterar nada existente — apenas APPEND no final do conteúdo da section.
    2. Importar `WhatsAppCta` se não estiver importado.
    3. Usar `location` correspondente (`'pain'`, `'product'`, `'proof'`).
    4. Variant:
       - Pain (DARK section): `variant="secondary"` (botão ghost roxo sobre fundo escuro).
       - Product (LIGHT off-white): `variant="primary"` (botão accent destaque suave).
       - Proof (DARK editorial, transição pré-form): `variant="primary"` (CTA DOMINANTE — é a entrega antes do form). Pode ser maior visualmente — adicionar classes de spacing/centering inline (`className="mx-auto"` ou wrapper `<div className="mt-12 flex justify-center md:mt-16">...</div>`).
    5. Wrapper de spacing: cada CTA fica num wrapper `<div className="mt-10 flex justify-center md:mt-12">` (ajustar valor conforme já existir consistência na section) — NÃO encostado no último parágrafo da copy.
    6. Copy do CTA: usar o label default do componente (vai pegar "Falar no WhatsApp") OU passar `children` específico:
       - Pain: `children="Quero entender como organizar"` (continuação natural da copy de dor)
       - Product: `children="Ver tudo isso na minha clínica"` (continuação dos pilares)
       - Proof: `children="Falar com a Likro agora"` (CTA dominante — sem perífrase)

    Exemplo concreto para Pain (estrutura pode variar):

    ```tsx
    // Antes:
    import { PainStatement } from "./PainStatement";
    // ...
    export function Pain() {
      return (
        <section ...>
          <PainHeader />
          <PainConstellation />
          <PainStatement />
        </section>
      );
    }

    // Depois:
    import { PainStatement } from "./PainStatement";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    // ...
    export function Pain() {
      return (
        <section ...>
          <PainHeader />
          <PainConstellation />
          <PainStatement />
          <div className="mt-10 flex justify-center md:mt-12">
            <WhatsAppCta variant="secondary" location="pain">
              Quero entender como organizar
            </WhatsAppCta>
          </div>
        </section>
      );
    }
    ```

    Adaptar a mesma lógica para Product (location="product", variant="primary", children="Ver tudo isso na minha clínica") e Proof (location="proof", variant="primary", children="Falar com a Likro agora", wrapper com mais margem `mt-12 md:mt-16`).

    Sobre o `<Container>` wrapping: se a section atual já wrappa o conteúdo num `<Container>`, o CTA deve ficar DENTRO do mesmo Container (alinhamento com o resto). Se a section usa wrappers diferentes por bloco, criar um Container próprio para o CTA respeitando o padrão.

    IMPORTANTE: não inventar children do CTA que viole o anti-IA filter — frases acima são curtas e específicas, evitando "desbloqueie/potencialize/jornada/etc". Lenny aprovará no PR; estas são variante 1.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck; Select-String -Path src/sections/Pain/index.tsx,src/sections/Product/index.tsx,src/sections/Proof/index.tsx -Pattern 'WhatsAppCta' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Pain/index.tsx` contém `<WhatsAppCta` e `location="pain"`
    - `src/sections/Product/index.tsx` contém `<WhatsAppCta` e `location="product"`
    - `src/sections/Proof/index.tsx` contém `<WhatsAppCta` e `location="proof"`
    - `grep -r "wa.me/" src/sections/Pain src/sections/Product src/sections/Proof` retorna 0 matches (zero hard-code de URL)
    - `grep -r "buildWhatsAppUrl" src/sections/Pain src/sections/Product src/sections/Proof` retorna 0 matches (NÃO consomem o helper direto — só via WhatsAppCta)
    - `npm run typecheck` exits 0
    - Existing structure de cada section permanece intacta (PainHeader/PainConstellation/PainStatement em Pain, etc.)
  </acceptance_criteria>
  <done>3 CTAs WhatsApp adicionados nas seções narrativas, locations corretos, copy inicial pronta para PR de Lenny.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| document focus events → FloatingWhatsApp | Listener é UX gate; não há decisão de segurança baseada em foco |
| /privacy page → outros sites | Link `Voltar` aponta apenas para `/` interno |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | Tampering (CTA hard-code regression) | Sections Pain/Product/Proof | mitigate | `grep -r "wa.me/" src/sections/` retorna 0 matches; CTAs só via `<WhatsAppCta>` que usa `buildWhatsAppUrl()` |
| T-05-23 | Info Disclosure (privacy placeholder com PII falso) | /privacy | accept | Conteúdo é placeholder sem PII; `noindex` no metadata previne descoberta via Google |
| T-05-24 | DoS (scroll listener jank) | FloatingWhatsApp | mitigate | `{ passive: true }` no addEventListener; single threshold check sem interpolation |
| T-05-25 | Tampering (focusin selector bypass) | FloatingWhatsApp Pitfall 9 | accept | Bypass do gate só causa floating cobrir teclado virtual — UX issue, não security |
</threat_model>

<verification>
- `npm test` exits 0 (full suite: 8 test files do Plan 02 todos GREEN exceto cta-distribution que precisa do Plan 07)
- `npm run typecheck` exits 0
- `grep -r "wa.me/" src/` retorna apenas matches em `src/lib/whatsapp.ts` (helper canonical)
- `grep -rE "<WhatsAppCta" src/sections/` retorna ao menos 4 matches (Pain, Product, Proof + qualquer existing — não conta Hero/Header que estão em layout/section diferente)
- `grep -c "import.*FOOTER_COPY" src/components/layout/Footer.tsx` retorna 1
</verification>

<success_criteria>
- FloatingWhatsApp com 3 gates (scroll, form-in-view, input-focused) + cobertura de testes (5/5 GREEN)
- Footer renderizado com copy de FOOTER_COPY (zero hard-code), CTA WhatsApp incluído
- /privacy retorna 200 + noindex
- Pain, Product e Proof receberam CTAs WhatsApp com locations corretos
- Zero hard-code de wa.me em qualquer arquivo de seção
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-06-SUMMARY.md` com: lista dos 6 arquivos, contagem de CTAs WhatsApp atual no projeto (esperado >= 7 após Plan 07 fechar — hero, header, pain, product, proof, footer, floating), Pitfall 9 implementado.
</output>
