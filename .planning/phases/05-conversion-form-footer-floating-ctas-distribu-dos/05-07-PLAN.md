---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 07
type: execute
wave: 5
depends_on: [01, 02, 03, 04, 05, 06]
files_modified:
  - src/app/page.tsx
  - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md
  - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md
autonomous: false
requirements: [CTA-03, CTA-05, CTA-07, CTA-08, MOBILE-02, MOBILE-06]
requirements_addressed: [CTA-03, CTA-05, CTA-07, CTA-08, MOBILE-02, MOBILE-06]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "HomePage renderiza Hero + Pain + Bridge + Product + HowItWorks + Proof + Form + Footer + FloatingWhatsApp"
    - "cta-distribution.test.tsx GREEN: 7 locations únicas (hero, header, pain, product, proof, footer, floating)"
    - "Full suite green: npm test exits 0"
    - "PR de copy review (microcopy form + CTAs distribuídos) aberto para Lenny aprovar"
    - "UAT manual matrix documentado em 05-HUMAN-UAT.md com checklist real-device (iPhone + Android + iPad + desktop)"
    - "VALIDATION.md atualizado: wave_0_complete=true, nyquist_compliant=true, per-task verification map preenchida"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Server component wirando Form, Footer, FloatingWhatsApp ao DOM tree"
    - path: ".planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md"
      provides: "Checklist UAT no estilo Phase 4 — Lenny preenche em iOS + Android reais"
    - path: ".planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md"
      provides: "Frontmatter flip (status=ready, wave_0_complete=true, nyquist_compliant=true) + Per-Task Verification Map preenchida"
  key_links:
    - from: "src/app/page.tsx"
      to: "Form, Footer, FloatingWhatsApp"
      via: "JSX children no <main> e fora do <main>"
      pattern: "<Form />.*<Footer />.*<FloatingWhatsApp"
---

<objective>
Wire-up final + gates de qualidade. Plan 07 fecha a Phase 5 conectando todos os componentes ao `src/app/page.tsx`, rodando full suite, atualizando o VALIDATION.md para o estado de phase concluída, e criando o UAT manual que o Lenny vai preencher em dispositivos reais (CTA-07 + MOBILE-02 + MOBILE-06 + lead chega ao Lenny).

Purpose: page.tsx só pode ser editado por 1 plan (single ownership). Esse plan também consolida o estado de validação para o gsd-verify-work.
Output: page.tsx editado, UAT criado, VALIDATION.md atualizada, checkpoint para Lenny.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md
@src/app/page.tsx
@src/sections/Form/index.tsx
@src/sections/Floating/FloatingWhatsApp.tsx
@src/components/layout/Footer.tsx
@tests/landing/cta-distribution.test.tsx

<interfaces>
<!-- Componentes a wirar (criados em Plans anteriores): -->
import { Form } from "@/sections/Form";
import { Footer } from "@/components/layout/Footer";
import { FloatingWhatsApp } from "@/sections/Floating/FloatingWhatsApp";

<!-- ORDEM esperada no DOM:
  <Header />            ← já no page.tsx (Phase 3) — sticky no topo
  <main>
    <Hero />            ← já
    <Pain />            ← já + CTA novo no fim (Plan 06)
    <Bridge />          ← já
    <Product />         ← já + CTA novo no fim (Plan 06)
    <HowItWorks />      ← já
    <Proof />           ← já + CTA novo no fim (Plan 06)
    <Form />            ← NOVO (Plan 04)
  </main>
  <Footer />            ← NOVO (Plan 06) — fora do <main> para semântica institucional
  <FloatingWhatsApp />  ← NOVO (Plan 06) — fixed, fora do flow
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wirar Form + Footer + FloatingWhatsApp no src/app/page.tsx</name>
  <files>src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (estado atual — Hero..Proof já wirados)
    - src/sections/Form/index.tsx (Plan 04)
    - src/components/layout/Footer.tsx (Plan 06)
    - src/sections/Floating/FloatingWhatsApp.tsx (Plan 06)
    - tests/landing/cta-distribution.test.tsx (test que vira GREEN com este wire)
  </read_first>
  <action>
    Editar `src/app/page.tsx` mantendo TODO o conteúdo existente e adicionando os 3 componentes novos nos lugares corretos.

    Estado final esperado:

    ```tsx
    import { Header } from "@/components/layout/Header";
    import { Footer } from "@/components/layout/Footer";
    import { Hero } from "@/sections/Hero";
    import { Pain } from "@/sections/Pain";
    import { Bridge } from "@/sections/Bridge";
    import { Product } from "@/sections/Product";
    import { HowItWorks } from "@/sections/HowItWorks";
    import { Proof } from "@/sections/Proof";
    import { Form } from "@/sections/Form";
    import { FloatingWhatsApp } from "@/sections/Floating/FloatingWhatsApp";

    export default function HomePage() {
      return (
        <>
          <Header />
          <main>
            <Hero />
            <Pain />
            <Bridge />
            <Product />
            <HowItWorks />
            <Proof />
            <Form />
          </main>
          <Footer />
          <FloatingWhatsApp />
        </>
      );
    }
    ```

    Regras:
    - NÃO remover ou reordenar sections existentes (Hero..Proof).
    - `<Form />` é a ÚLTIMA section dentro de `<main>` — encerra a narrativa.
    - `<Footer />` fica FORA de `<main>` (semântica HTML5 — footer não é parte do main content).
    - `<FloatingWhatsApp />` fica FORA de `<main>`, NO FIM, para ficar acima de tudo no z-stack (variant=floating tem z-50 intrinsic).
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/landing/cta-distribution.test.tsx; npm test; npm run typecheck; npm run lint"</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/page.tsx` contém imports de `Form`, `Footer`, `FloatingWhatsApp`
    - File contém JSX `<Form />`, `<Footer />`, `<FloatingWhatsApp />`
    - `<Form />` aparece DENTRO de `<main>` (após `<Proof />`)
    - `<Footer />` e `<FloatingWhatsApp />` aparecem FORA de `<main>`
    - Hero, Pain, Bridge, Product, HowItWorks, Proof continuam presentes na ordem
    - `npm test -- --run tests/landing/cta-distribution.test.tsx` exits 0 (2/2 GREEN — agora cobre todas as 7 locations)
    - `npm test` (FULL SUITE) exits 0
    - `npm run typecheck` exits 0
    - `npm run lint` exits 0
  </acceptance_criteria>
  <done>page.tsx wirado, full suite green, cta-distribution test GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Atualizar VALIDATION.md (frontmatter + per-task verification map + body stale "6" → "7")</name>
  <files>.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md (estado atual)
    - Cada PLAN.md da phase (01-06) — para listar quais tests cobrem quais requirements
  </read_first>
  <action>
    Editar `05-VALIDATION.md`:

    1. Frontmatter: trocar `status: draft` → `status: ready`, `nyquist_compliant: false` → `nyquist_compliant: true`, `wave_0_complete: false` → `wave_0_complete: true`.

    2. Preencher a tabela "Per-Task Verification Map" com 7-10 linhas reais, uma por feature task que tem `<automated>` apontando para um teste. Estrutura sugerida:

    ```
    | Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
    |---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
    | 01.T2 (server-env)   | 01 | 1 | CTA-10 | T-05-01 | server-only guard | unit | npm run typecheck | ✅ | ✅ green |
    | 01.T3 (lead-schema)  | 01 | 1 | CTA-09, CTA-10 | T-05-04 | Zod shared client/server | unit | npm test -- --run tests/lib/lead-schema.test.ts | ✅ | ✅ green |
    | 01.T4 (lead-dedup)   | 01 | 1 | CTA-12 | T-05-03 | TTL window | unit | npm test -- --run tests/lib/lead-dedup.test.ts | ✅ | ✅ green |
    | 03.T3 (lead-route)   | 03 | 2 | CTA-10, CTA-11, CTA-12 | T-05-06, T-05-09, T-05-12 | dual-write + honeypot + dedup | integration | npm test -- --run tests/api/lead-route.test.ts | ✅ | ✅ green |
    | 04.T3 (lead-form)    | 04 | 3 | CTA-09, CTA-11 | T-05-16, T-05-19 | data-clarity-mask + state machine | unit | npm test -- --run tests/sections/lead-form.test.tsx | ✅ | ✅ green |
    | 05.T1 (header)       | 05 | 3 | MOBILE-06 | T-05-20, T-05-21 | hide-on-scroll + reduced-motion | unit | npm test -- --run tests/components/header.test.tsx | ✅ | ✅ green |
    | 06.T1 (floating)     | 06 | 4 | CTA-06, MOBILE-02 | T-05-24 | scroll threshold + form-in-view gate | unit | npm test -- --run tests/sections/floating-whatsapp.test.tsx | ✅ | ✅ green |
    | 07.T1 (page wire)    | 07 | 5 | CTA-05 | T-05-22 | 7 distinct data-location | integration | npm test -- --run tests/landing/cta-distribution.test.tsx | ✅ | ✅ green |
    | content guard        | 02 | 1 | CTA-08 | — | banned phrases + zero hard-coded URLs | unit | npm test -- --run tests/content/whatsapp-messages.test.ts | ✅ | ✅ green |
    ```

    3. Garantir que tabela "Manual-Only Verifications" continua intacta (CTA-07 deeplink real device, MOBILE-02 visual real iPhone, MOBILE-06 smooth scroll real, CTA-10 email + sheet, A11y reduced-motion OS).

    4. Atualizar `**Approval:** pending` → `**Approval:** awaiting manual UAT + Lenny copy review`.

    5. **Corrigir referências stale "6" → "7" no body** (as plans settled on 7 CTA locations: hero, header, pain, product, proof, footer, floating):
       - Em "Wave 0 Requirements", a bullet do `tests/landing/cta-distribution.test.tsx` atualmente diz: `render <HomePage>, assert [data-location] covers all 6 expected locations`. Trocar para: `render <HomePage>, assert [data-location] covers all 7 expected locations (hero, header, pain, product, proof, footer, floating)`.
       - Em "Manual-Only Verifications", a row de CTA-07 (WhatsApp deeplink) atualmente diz: `Lenny abre cada um dos 6 CTAs em iPhone real e Android real`. Trocar para: `Lenny abre cada um dos 7 CTAs em iPhone real e Android real`.
       - Validar que NÃO sobrou nenhuma ocorrência de "6 expected locations" nem "6 CTAs" no arquivo após a edição.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Path .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md -Pattern 'nyquist_compliant: true' -Quiet; Select-String -Path .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md -Pattern 'wave_0_complete: true' -Quiet; Select-String -Path .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md -Pattern '7 expected locations' -Quiet; Select-String -Path .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md -Pattern '7 CTAs' -Quiet"</automated>
  </verify>
  <acceptance_criteria>
    - Frontmatter contém `status: ready`, `nyquist_compliant: true`, `wave_0_complete: true`
    - Per-Task Verification Map tem >= 7 linhas com Task ID, Plan, Wave, Requirement, Test command, Status
    - Cada linha tem `Status` em ✅ green / ⚠️ flaky / ❌ red
    - Manual-Only Verifications section preservada (5 linhas)
    - Approval field atualizado
    - `grep -c "7 CTAs" .planning/phases/05-*/05-VALIDATION.md` returns ≥ 1
    - `grep -c "7 expected locations" .planning/phases/05-*/05-VALIDATION.md` returns ≥ 1
    - `grep -c "6 expected locations\|6 CTAs" .planning/phases/05-*/05-VALIDATION.md` returns 0
  </acceptance_criteria>
  <done>VALIDATION.md reflete estado executável final, Nyquist compliance signal flipado, body sem referências stale a "6 locations / 6 CTAs".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Criar 05-HUMAN-UAT.md (checklist real-device + email + planilha)</name>
  <files>.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/04-narrative-sections/04-HUMAN-UAT.md (formato do anterior — replicar estilo)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Real-Device QA Matrix)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-VALIDATION.md (manual-only verifications)
  </read_first>
  <action>
    Criar `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md` com este conteúdo (Lenny preenche `[ ]` → `[x]` durante teste real):

    ```markdown
    # Phase 5 — Human UAT

    **Status:** awaiting Lenny
    **Pré-requisitos:** Vercel preview deployada (`vercel --yes` ou push para PR); 5 env vars de produção configuradas (RESEND_API_KEY, LEAD_TO_EMAIL, GOOGLE_SA_CLIENT_EMAIL, GOOGLE_SA_PRIVATE_KEY, GOOGLE_SHEET_ID); domínio Resend verificado OU sandbox aceitável em preview.

    ## A. WhatsApp deeplink — real device (CTA-07)

    Para CADA um dos 7 CTAs WhatsApp, clicar e verificar que abre o app WhatsApp instalado (não browser, não web.whatsapp.com). Mensagem pré-preenchida deve casar com WHATSAPP_MESSAGES da location.

    ### iPhone (iOS Safari)
    - [ ] CTA hero (location='hero')
    - [ ] CTA header (location='header')
    - [ ] CTA fim Pain (location='pain')
    - [ ] CTA fim Product (location='product')
    - [ ] CTA fim Proof (location='proof')
    - [ ] CTA footer (location='footer')
    - [ ] CTA floating mobile (location='floating')

    ### Android Chrome (mid-tier)
    - [ ] CTA hero
    - [ ] CTA header
    - [ ] CTA fim Pain
    - [ ] CTA fim Product
    - [ ] CTA fim Proof
    - [ ] CTA footer
    - [ ] CTA floating mobile

    ## B. Floating WhatsApp — comportamento mobile (CTA-06 + MOBILE-02)

    Mobile (iPhone + Android):
    - [ ] Floating NÃO aparece na primeira viewport (antes de 50vh)
    - [ ] Após scroll > 50vh, floating aparece com fade suave (200ms)
    - [ ] Quando scrolla até o form em viewport, floating desaparece
    - [ ] Ao focar input do form, floating fica escondido (Pitfall 9)
    - [ ] Floating NÃO cobre o home indicator do iPhone (safe-area-inset-bottom respeitado)
    - [ ] Floating é discreto: sem pulse, sem bounce, sem badge, sem crescimento

    iPad Safari:
    - [ ] Floating NÃO aparece (≤ md breakpoint cutoff)

    Desktop:
    - [ ] Floating NÃO aparece em nenhuma largura desktop

    ## C. Header hide-on-scroll (MOBILE-06)

    iPhone + Android:
    - [ ] Primeira viewport: header sempre visível
    - [ ] Após primeira viewport, scroll down significativo (>80px delta) esconde o header
    - [ ] Scroll up reaparece o header
    - [ ] Transição translateY é suave, sem jitter visível
    - [ ] Z-index OK: header (z-30) NÃO fica abaixo do conteúdo

    Desktop:
    - [ ] Mesmo comportamento (Chrome / Safari / Firefox)

    Reduced motion:
    - [ ] macOS System Settings → Accessibility → Reduce Motion ATIVADO: header fica SEMPRE visível, NÃO esconde
    - [ ] Windows Settings → Accessibility → Visual effects → Animation effects OFF: header fica SEMPRE visível

    ## D. Form de leads — submissão real (CTA-10 + CTA-11)

    iPhone Safari ou Android Chrome:
    - [ ] Form renderiza 3 campos visíveis (Nome, WhatsApp, Mensagem opcional)
    - [ ] Submit com nome vazio mostra erro inline + aria-invalid
    - [ ] Submit com WhatsApp inválido (ex: "12345") mostra erro inline
    - [ ] Submit com dados válidos: botão fica disabled, spinner aparece
    - [ ] Após sucesso, bloco success ("Recebido. Vou te chamar no WhatsApp...") substitui o form sem redirect
    - [ ] Em até 60 segundos, Lenny recebe email em LEAD_TO_EMAIL (assunto "Novo lead — <nome>")
    - [ ] Em até 60 segundos, nova linha aparece em Google Sheet "Leads!A:E" com timestamp, nome, WhatsApp, mensagem, utm
    - [ ] Resubmeter com MESMO WhatsApp em <60s retorna sucesso silencioso (sem novo email/linha duplicada)

    ## E. Tracking events (preparação Phase 6)

    Chrome DevTools → Network tab + Console (filtro "track"):
    - [ ] Focar primeiro campo do form: `form_focus` event dispara
    - [ ] Submeter form com dados válidos: `form_submit_attempt` + `form_submit_success` events
    - [ ] Clicar qualquer CTA WhatsApp: `whatsapp_click` event com `location` correto
    - [ ] Cada event tem `event_id` UUID v4 no payload (preparação Meta CAPI)

    ## F. Honeypot anti-spam (CTA-09 segurança)

    Manual via DevTools Console:
    ```js
    await fetch("/api/lead", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({
        name: "Test",
        whatsapp: "11999999999",
        website: "http://spam.example", // honeypot triggered (field = website)
      }),
    }).then(r => r.json());
    ```
    - [ ] Resposta = `{ ok: true }` (fake success)
    - [ ] Lenny NÃO recebe email
    - [ ] Sheet NÃO recebe linha

    ## G. Acessibilidade básica (preparação Phase 7)

    - [ ] Form navegável por teclado (Tab atravessa nome → WhatsApp → mensagem → botão)
    - [ ] Honeypot NÃO recebe foco via Tab (tabIndex=-1)
    - [ ] aria-invalid em campos com erro
    - [ ] Focus visível em todos os campos

    ## H. Copy review (CTA-08, COPY-04)

    Lenny revisa no PR:
    - [ ] Microcopy do form (eyebrow, heading, sub, placeholders, submit, success, error) sem cara de IA, sem urgência fabricada, alinhado tom premium silencioso
    - [ ] Children dos CTAs distribuídos (Pain, Product, Proof) sem buzzwords, específicos pra clínica
    - [ ] Mensagens pré-preenchidas em WHATSAPP_MESSAGES.{location} aprovadas

    ## Sign-off

    - [ ] Todas as 7 categorias acima 100% checadas
    - [ ] Vercel preview URL: ___
    - [ ] Aprovado por Lenny em (data): ___
    ```
  </action>
  <verify>
    <automated>powershell -Command "Test-Path .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `05-HUMAN-UAT.md` existe
    - File contém 8 seções (A-H)
    - File contém grep `CTA-07`, `CTA-06`, `MOBILE-02`, `MOBILE-06`, `CTA-10`, `CTA-11`, `CTA-09`, `CTA-08`
    - File contém ao menos 30 checkboxes `[ ]`
    - File menciona `LEAD_TO_EMAIL`, `Google Sheet`, `event_id`, `honeypot`, `reduced-motion`
    - Seção F (Honeypot) contém o campo `website` (não `company`) no exemplo DevTools console
  </acceptance_criteria>
  <done>UAT manual pronto para Lenny preencher após o deploy do PR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Checkpoint - Vercel preview + UAT real-device + copy approval</name>
  <files>.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-HUMAN-UAT.md</files>
  <what-built>
    Phase 5 completa: edge route /api/lead com dual-write Resend+Sheets, form RHF+Zod, hide-on-scroll header, floating mobile, footer institucional, /privacy placeholder, CTAs WhatsApp em 7 pontos. Todos os 8 test files automatizados GREEN.
  </what-built>
  <action>
    Pausar execucao automatizada. Executor deve, ANTES de pausar:
      a. Rodar `npm test && npm run typecheck && npm run lint` - todos exit 0.
      b. Rodar `npm run build` - sem warnings de "module not found" nem env vars faltando.
      c. Confirmar com Lenny: abrir PR no GitHub (Vercel cria preview automaticamente) OU push direto para main.

    Apos pausa, esperar Lenny:
      1. Configurar 5 env vars NOVAS em Vercel (Production + Preview): RESEND_API_KEY, LEAD_TO_EMAIL, GOOGLE_SA_CLIENT_EMAIL, GOOGLE_SA_PRIVATE_KEY, GOOGLE_SHEET_ID.
      2. Criar service account no GCP + habilitar Sheets API + baixar JSON + compartilhar planilha "Likro - Leads" (headers timestamp/name/whatsapp/message/utm) com client_email da SA.
      3. Preencher 05-HUMAN-UAT.md em iPhone real + Android real + iPad + Desktop marcando [x] em cada item das 8 secoes (A-H).
      4. Revisar microcopy do form e dos 4 CTAs novos no PR. Se quiser variantes, abrir issue de copy review (padrao Phase 4 - 3 variantes).
      5. Confirmar email recebido + linha em planilha apos submit real.

    Quando Lenny responder "aprovado" + URL do preview + "UAT marcado", executor continua para o Plan SUMMARY final (05-07-SUMMARY.md). Se algum item do UAT falhar, executor agrega os gaps em estrutura para `/gsd-plan-phase 5 --gaps`.
  </action>
  <how-to-verify>
    1. Pre-check automatizado: `npm test && npm run typecheck && npm run lint && npm run build` todos exit 0 antes de pausar.
    2. Lenny faz UAT real-device completo (instrucoes detalhadas em 05-HUMAN-UAT.md).
    3. Lenny confirma email + linha em planilha apos submit real (caminho A do form).
  </how-to-verify>
  <verify>Manual gate - aprovacao do Lenny via resume-signal abaixo. Pre-check automatizado: `npm test && npm run typecheck && npm run lint && npm run build` todos exit 0.</verify>
  <done>Lenny respondeu "aprovado" + colou URL Vercel preview + confirmou 30+ items do 05-HUMAN-UAT.md marcados como [x] + microcopy do form e CTAs aprovada no PR.</done>
  <resume-signal>Type "aprovado" + cole URL do Vercel preview + diga "UAT marcado" quando 100% dos itens estiverem [x]. Se algum item falhar, descreva o que vai pra `/gsd-plan-phase 5 --gaps`.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → Vercel preview | Build local NÃO injeta secrets reais; Vercel preview tem secrets verdadeiros |
| Vercel preview → Resend + Google APIs | TLS; env vars únicas por environment (preview vs production) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-26 | Info Disclosure | Lenny commit acidental de .env.local | mitigate | `.gitignore` já inclui `.env*` (verificar com `git check-ignore .env.local`); executor confirma antes do checkpoint |
| T-05-27 | Spoofing | Vercel preview vs production env confusion | mitigate | `VERCEL_ENV` gate em lead-resend.ts diferencia `from:` sandbox vs verified; Lenny configura ambas environments separadamente |
| T-05-28 | Tampering (real lead spam during preview) | Vercel preview pública | accept | Honeypot + dedup + Lenny apaga manual se preview público recebe spam; produção tem rate limit como Phase 7 TODO |
</threat_model>

<verification>
- `npm test` exits 0 (todos os 8 test files do Plan 02 GREEN + brand-lock e qualquer outro existente)
- `npm run typecheck` exits 0
- `npm run lint` exits 0
- `npm run build` exits 0 (com env vars stubadas)
- `grep -c "<Form />" src/app/page.tsx` retorna 1
- `grep -c "<Footer />" src/app/page.tsx` retorna 1
- `grep -c "<FloatingWhatsApp />" src/app/page.tsx` retorna 1
- `05-HUMAN-UAT.md` existe com 8 seções
- `05-VALIDATION.md` frontmatter contém `nyquist_compliant: true` e `wave_0_complete: true`
- `05-VALIDATION.md` body contém `7 expected locations` e `7 CTAs` (não mais `6 expected locations` / `6 CTAs`)
</verification>

<success_criteria>
- HomePage renderiza Hero, Pain, Bridge, Product, HowItWorks, Proof, Form (em main) + Footer + FloatingWhatsApp (fora)
- 7 locations únicas de CTA WhatsApp na página (hero, header, pain, product, proof, footer, floating)
- Full vitest suite GREEN (8 test files novos + tudo existente)
- VALIDATION.md atualizada para Nyquist compliance
- UAT manual criado para Lenny preencher
- Checkpoint humano fecha a phase após Vercel preview + 30+ items de UAT [x]
</success_criteria>

<output>
After completion AND human approval, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-07-SUMMARY.md` com:
  - Estado final da page.tsx (lista de imports + ordem JSX)
  - Métricas: tests/total, GREEN/RED, files touched, requirements addressed (11 IDs cobertos)
  - Action items resolvidos: webhook target (Resend+Sheets), 5 env vars Vercel, GCP SA criado, Sheet compartilhada
  - Pendências v1 → v2: rate limit por IP (Upstash), Meta CAPI retrofit, /privacy conteúdo (Phase 7), Lighthouse gates (Phase 7)
  - Approval signature: Lenny + data
</output>
</output>
