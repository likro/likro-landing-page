---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_modified:
  - src/hooks/use-form-in-view.ts
  - src/sections/Form/LeadForm.tsx
  - src/sections/Form/FormSuccess.tsx
  - src/sections/Form/FormError.tsx
  - src/sections/Form/index.tsx
autonomous: true
requirements: [CTA-09, CTA-11]
requirements_addressed: [CTA-09, CTA-11]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "Form renderiza 3 campos visíveis + honeypot off-screen com name='website' (NÃO 'company' — Pitfall 8)"
    - "Submissão via fetch POST /api/lead com body JSON-encoded passando pelo schema do client (RHF + Zod resolver)"
    - "Botão fica disabled durante submit; success substitui form inline; error mostra retry"
    - "LeadForm root element (<form>) tem data-clarity-mask='true' GARANTIDO em isolation (não depende do <Form> wrapper)"
    - "Form/index.tsx section wrapper ALSO tem data-clarity-mask='true' (defense in depth)"
    - "Honeypot wrapper tem aria-hidden=true + position:absolute + left:-9999px"
    - "UTM extraído de window.location.search no mount e enviado no payload"
    - "Hook useFormInView observa o section id 'lead-form-section' e expõe boolean compartilhado via context"
  artifacts:
    - path: "src/sections/Form/LeadForm.tsx"
      provides: "Client component RHF + Zod, fetch submit, state idle|submitting|success|error; root <form> tem data-clarity-mask"
      contains: '"use client"'
    - path: "src/sections/Form/FormSuccess.tsx"
      provides: "Bloco inline de confirmação substitui form ao success — usa FORM_COPY.success + WhatsAppCta location='proof'"
    - path: "src/sections/Form/FormError.tsx"
      provides: "Bloco inline de erro com retry button + CTA fallback WhatsApp"
    - path: "src/sections/Form/index.tsx"
      provides: "Server component wrapper — section id='lead-form-section', data-clarity-mask, eyebrow + heading + subheading + <LeadForm>"
    - path: "src/hooks/use-form-in-view.ts"
      provides: "IntersectionObserver hook — Plan 06 FloatingWhatsApp consome"
      contains: "IntersectionObserver"
  key_links:
    - from: "LeadForm.tsx"
      to: "/api/lead"
      via: "fetch in handleSubmit"
      pattern: 'fetch\\("/api/lead"'
    - from: "LeadForm.tsx → leadSchema (client)"
      to: "leadSchema (server in route.ts)"
      via: "shared import from @/lib/lead-schema"
      pattern: 'from "@/lib/lead-schema"'
    - from: "Form/index.tsx"
      to: "useFormInView (Plan 06 consumer)"
      via: "id='lead-form-section' attribute"
      pattern: 'id="lead-form-section"'
---

<objective>
Construir a UI do lead form: section wrapper + RHF/Zod client + success/error blocks + hook compartilhado de "form-em-viewport" que o Floating do Plan 06 consome. Faz `tests/sections/lead-form.test.tsx` virar GREEN.

Purpose: Coração da phase de conversão. Plan separado do edge route (Plan 03) e do floating/CTAs (Plan 06) porque RHF + estados de UI consomem contexto independente do backend.
Output: 5 arquivos (1 hook, 1 section index, 3 form components). Form 100% funcional contra endpoint do Plan 03.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@src/lib/lead-schema.ts
@src/lib/analytics.ts
@src/lib/utils.ts
@src/content/form.ts
@src/components/ui/whatsapp-cta.tsx
@src/components/ui/container.tsx
@src/components/ui/button.tsx
@tests/sections/lead-form.test.tsx

<interfaces>
<!-- LeadForm props (interno — sem props externas) -->
<!-- FormState union: -->
```typescript
type FormState =
  | { status: "idle" }
  | { status: "submitting" }
  | { status: "success" }
  | { status: "error"; canRetry: boolean };
```

<!-- Container/Button atoms (lib existente da Phase 1): -->
```typescript
// src/components/ui/container.tsx — wrapper de largura padrão
export function Container(props: { className?: string; children: ReactNode }): JSX.Element;

// src/components/ui/button.tsx — Radix Slot + variants (default, secondary, link, ghost)
export function Button(props: ButtonProps): JSX.Element;
```

<!-- WhatsAppCta (lib existente da Phase 1 — variants primary/secondary/floating/inline) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar src/hooks/use-form-in-view.ts (IntersectionObserver compartilhado)</name>
  <files>src/hooks/use-form-in-view.ts</files>
  <read_first>
    - src/hooks/use-in-view.ts (padrão de hook do projeto — replicar estilo)
    - src/content/form.ts (FORM_COPY.sectionId === "lead-form-section")
  </read_first>
  <action>
    Criar `src/hooks/use-form-in-view.ts`:

    ```typescript
    "use client";

    /**
     * useFormInView — observa o elemento DOM com id "lead-form-section"
     * (FORM_COPY.sectionId) via IntersectionObserver e retorna boolean.
     *
     * Phase 5 consumer: FloatingWhatsApp (Plan 06) usa para esconder quando o form está visível.
     *
     * Por que NÃO context: na v1 só há 1 consumer (FloatingWhatsApp) e 1 target (a section do form).
     * Cada consumer cria seu próprio observer — over-engineering criar provider compartilhado v1.
     * Se v2 adicionar mais consumers, refatorar para context.
     */

    import { useEffect, useState } from "react";
    import { FORM_COPY } from "@/content/form";

    interface Options {
      /** Override do target id. Default: FORM_COPY.sectionId */
      targetId?: string;
      /** Threshold do IntersectionObserver. Default: 0.1 (10% visible). */
      threshold?: number;
    }

    export function useFormInView({
      targetId = FORM_COPY.sectionId,
      threshold = 0.1,
    }: Options = {}): boolean {
      const [inView, setInView] = useState(false);

      useEffect(() => {
        if (typeof window === "undefined") return;
        const el = document.getElementById(targetId);
        if (!el) return;

        const observer = new IntersectionObserver(
          (entries) => {
            const entry = entries[0];
            if (entry) setInView(entry.isIntersecting);
          },
          { threshold },
        );
        observer.observe(el);
        return () => observer.disconnect();
      }, [targetId, threshold]);

      return inView;
    }
    ```
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/hooks/use-form-in-view.ts` existe
    - File começa com `"use client";`
    - File contém `IntersectionObserver`
    - File contém `FORM_COPY.sectionId`
    - File exporta `useFormInView`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Hook pronto, Plan 06 consome diretamente.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar FormSuccess.tsx e FormError.tsx (blocos inline)</name>
  <files>src/sections/Form/FormSuccess.tsx, src/sections/Form/FormError.tsx</files>
  <read_first>
    - src/content/form.ts (FORM_COPY.success + FORM_COPY.error)
    - src/components/ui/whatsapp-cta.tsx (variant=secondary para fallback)
    - src/components/ui/button.tsx (Button para retry)
  </read_first>
  <action>
    Criar `src/sections/Form/FormSuccess.tsx`:

    ```tsx
    /**
     * Sucesso inline — substitui o form no DOM ao 200 ok:true.
     * Sem redirect (CTA-11). Inclui fallback WhatsApp como CTA secundário.
     */
    import { FORM_COPY } from "@/content/form";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";

    export function FormSuccess() {
      return (
        <div
          role="status"
          aria-live="polite"
          className="rounded-2xl border border-border-subtle bg-bg-card p-8 text-center md:p-10"
        >
          <h3 className="text-2xl font-semibold tracking-tight text-text-primary md:text-3xl">
            {FORM_COPY.success.heading}
          </h3>
          <p className="mx-auto mt-3 max-w-md text-balance text-base text-text-secondary">
            {FORM_COPY.success.body}
          </p>
          <div className="mt-6 flex justify-center">
            <WhatsAppCta variant="secondary" location="proof">
              {FORM_COPY.success.secondaryCtaLabel}
            </WhatsAppCta>
          </div>
        </div>
      );
    }
    ```

    Criar `src/sections/Form/FormError.tsx`:

    ```tsx
    /**
     * Erro inline — mostra heading + retry button + fallback WhatsApp.
     */
    import { FORM_COPY } from "@/content/form";
    import { Button } from "@/components/ui/button";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";

    interface Props {
      onRetry: () => void;
    }

    export function FormError({ onRetry }: Props) {
      return (
        <div
          role="alert"
          className="rounded-2xl border border-border-subtle bg-bg-card p-6 text-center md:p-8"
        >
          <h3 className="text-xl font-semibold tracking-tight text-text-primary">
            {FORM_COPY.error.heading}
          </h3>
          <p className="mx-auto mt-2 max-w-md text-balance text-sm text-text-secondary">
            {FORM_COPY.error.body}
          </p>
          <div className="mt-5 flex flex-col items-center justify-center gap-3 sm:flex-row">
            <Button variant="default" onClick={onRetry}>
              {FORM_COPY.error.retryLabel}
            </Button>
            <WhatsAppCta variant="inline" location="proof">
              Falar agora no WhatsApp
            </WhatsAppCta>
          </div>
        </div>
      );
    }
    ```

    Verificar quais tokens Tailwind realmente existem no projeto (`border-border-subtle`, `bg-bg-card`, `text-text-primary`, `text-text-secondary`). Se algum não existir, usar equivalentes: `border-zinc-200`, `bg-white`, `text-zinc-900`, `text-zinc-600`. NÃO usar `bg-accent-50/100/200` (proibido por FOUND-03).
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Form/FormSuccess.tsx` existe e exporta `FormSuccess`
    - `src/sections/Form/FormError.tsx` existe e exporta `FormError`
    - FormSuccess contém `FORM_COPY.success.heading` (referência, não hard-code)
    - FormError contém `FORM_COPY.error.heading`, `FORM_COPY.error.retryLabel`, e prop `onRetry: () => void`
    - Nenhum dos dois arquivos contém literal `bg-accent-50`, `bg-accent-100`, `bg-accent-200` (FOUND-03 guard)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Blocos inline prontos para o LeadForm chavear estado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Criar LeadForm.tsx (RHF + Zod resolver + fetch submit + state machine + data-clarity-mask no root)</name>
  <files>src/sections/Form/LeadForm.tsx</files>
  <read_first>
    - tests/sections/lead-form.test.tsx (CONTRACT — replicar nomes de labels e queries exatamente; verifica `name="website"` e `data-clarity-mask` no root via `container.querySelector`)
    - src/content/form.ts (FORM_COPY — honeypot.name === "website")
    - src/lib/lead-schema.ts (leadSchema — usar como resolver; field `website` é o honeypot)
    - src/lib/analytics.ts (track function para eventos)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 6 — honeypot off-screen + Pitfall 8 — name `website`)
  </read_first>
  <behavior>
    - Render 3 campos visíveis + honeypot off-screen com name="website"
    - Honeypot input tem name="website", tabIndex={-1}, autoComplete="off"
    - Honeypot wrapper div tem aria-hidden="true" + style { position:"absolute", left:"-9999px" }
    - Root `<form>` element TEM data-clarity-mask="true" — esse atributo PRECISA estar no DOM renderizado pelo LeadForm em isolation (teste do Plan 02 Task 4 verifica via `container.querySelector('[data-clarity-mask="true"]')` direto no render de `<LeadForm />`)
    - Submit chama fetch("/api/lead", { method: "POST", headers: {...}, body: JSON.stringify(values) })
    - Durante submit, botão fica disabled
    - On 200 ok:true → mostra <FormSuccess /> em vez do form (continua renderizado em wrapper com data-clarity-mask)
    - On 4xx/5xx ou network error → mostra <FormError onRetry={...} />
    - track() chamadas: form_focus (no primeiro focus), form_submit_attempt, form_submit_success ou form_submit_error
    - UTM extraído de window.location.search no mount e incluído no payload
  </behavior>
  <action>
    Criar `src/sections/Form/LeadForm.tsx`:

    ```tsx
    "use client";

    import { useEffect, useRef, useState } from "react";
    import { useForm } from "react-hook-form";
    import { zodResolver } from "@hookform/resolvers/zod";
    import { Loader2 } from "lucide-react";
    import { Button } from "@/components/ui/button";
    import { leadSchema, type Lead } from "@/lib/lead-schema";
    import { track } from "@/lib/analytics";
    import { FORM_COPY } from "@/content/form";
    import { cn } from "@/lib/utils";
    import { FormSuccess } from "./FormSuccess";
    import { FormError } from "./FormError";

    type FormState =
      | { status: "idle" }
      | { status: "submitting" }
      | { status: "success" }
      | { status: "error" };

    function readUtm(): string | undefined {
      if (typeof window === "undefined") return undefined;
      const params = new URLSearchParams(window.location.search);
      const keys = ["utm_source", "utm_medium", "utm_campaign", "utm_term", "utm_content"];
      const found = keys
        .map((k) => {
          const v = params.get(k);
          return v ? `${k}=${v}` : null;
        })
        .filter((v): v is string => v !== null);
      return found.length > 0 ? found.join("&") : undefined;
    }

    export function LeadForm() {
      const [state, setState] = useState<FormState>({ status: "idle" });
      const focusFiredRef = useRef(false);
      const utmRef = useRef<string | undefined>(undefined);

      useEffect(() => {
        utmRef.current = readUtm();
      }, []);

      const {
        register,
        handleSubmit,
        formState: { errors },
        reset,
      } = useForm<Lead>({
        resolver: zodResolver(leadSchema),
        defaultValues: { name: "", whatsapp: "", message: "", website: "" },
      });

      const handleFirstFocus = () => {
        if (focusFiredRef.current) return;
        focusFiredRef.current = true;
        track("form_focus", {});
      };

      const onSubmit = handleSubmit(async (values) => {
        setState({ status: "submitting" });
        track("form_submit_attempt", { has_message: Boolean(values.message) });

        const payload = { ...values, utm: utmRef.current };

        try {
          const res = await fetch("/api/lead", {
            method: "POST",
            headers: { "Content-Type": "application/json" },
            body: JSON.stringify(payload),
          });
          const json = (await res.json().catch(() => ({}))) as { ok?: boolean };
          if (res.ok && json.ok) {
            setState({ status: "success" });
            track("form_submit_success", {});
          } else {
            setState({ status: "error" });
            track("form_submit_error", { status: res.status });
          }
        } catch (err) {
          setState({ status: "error" });
          track("form_submit_error", { reason: String(err) });
        }
      });

      const handleRetry = () => {
        reset(undefined, { keepValues: true });
        setState({ status: "idle" });
      };

      // IMPORTANTE: success/error states devem manter data-clarity-mask no root
      // para preservar PII masking em qualquer estado da máquina.
      if (state.status === "success") {
        return (
          <div data-clarity-mask="true">
            <FormSuccess />
          </div>
        );
      }
      if (state.status === "error") {
        return (
          <div data-clarity-mask="true">
            <FormError onRetry={handleRetry} />
          </div>
        );
      }

      const submitting = state.status === "submitting";

      return (
        <form
          data-clarity-mask="true"
          onSubmit={onSubmit}
          onFocus={handleFirstFocus}
          noValidate
          className="space-y-4"
          aria-label="Form de contato Likro"
        >
          {/* Honeypot — off-screen, NÃO display:none (Pattern 6 RESEARCH).
              Field name `website` evita autofill 1Password/LastPass em B2B (Pitfall 8). */}
          <div
            aria-hidden="true"
            style={{
              position: "absolute",
              left: "-9999px",
              top: 0,
              width: "1px",
              height: "1px",
              overflow: "hidden",
            }}
          >
            <label htmlFor="website">{FORM_COPY.fields.honeypot.label}</label>
            <input
              id="website"
              type="text"
              tabIndex={-1}
              autoComplete="off"
              {...register("website")}
            />
          </div>

          <div>
            <label
              htmlFor="lead-name"
              className="block text-sm font-medium text-text-primary"
            >
              {FORM_COPY.fields.name.label}
            </label>
            <input
              id="lead-name"
              type="text"
              autoComplete="name"
              placeholder={FORM_COPY.fields.name.placeholder}
              aria-invalid={errors.name ? "true" : "false"}
              aria-describedby={errors.name ? "lead-name-error" : undefined}
              className={cn(
                "mt-1 block w-full rounded-lg border bg-bg-base px-3 py-2 text-base text-text-primary",
                "focus:outline-none focus:ring-2 focus:ring-accent-primary/50",
                errors.name ? "border-red-500" : "border-border-subtle",
              )}
              {...register("name")}
            />
            {errors.name && (
              <p id="lead-name-error" className="mt-1 text-sm text-red-600">
                {errors.name.message}
              </p>
            )}
          </div>

          <div>
            <label
              htmlFor="lead-whatsapp"
              className="block text-sm font-medium text-text-primary"
            >
              {FORM_COPY.fields.whatsapp.label}
            </label>
            <input
              id="lead-whatsapp"
              type="tel"
              inputMode="tel"
              autoComplete="tel"
              placeholder={FORM_COPY.fields.whatsapp.placeholder}
              aria-invalid={errors.whatsapp ? "true" : "false"}
              aria-describedby={errors.whatsapp ? "lead-whatsapp-error" : undefined}
              className={cn(
                "mt-1 block w-full rounded-lg border bg-bg-base px-3 py-2 text-base text-text-primary",
                "focus:outline-none focus:ring-2 focus:ring-accent-primary/50",
                errors.whatsapp ? "border-red-500" : "border-border-subtle",
              )}
              {...register("whatsapp")}
            />
            {errors.whatsapp && (
              <p id="lead-whatsapp-error" className="mt-1 text-sm text-red-600">
                {errors.whatsapp.message}
              </p>
            )}
          </div>

          <div>
            <label
              htmlFor="lead-message"
              className="block text-sm font-medium text-text-primary"
            >
              {FORM_COPY.fields.message.label}
            </label>
            <textarea
              id="lead-message"
              rows={3}
              placeholder={FORM_COPY.fields.message.placeholder}
              aria-invalid={errors.message ? "true" : "false"}
              aria-describedby={errors.message ? "lead-message-error" : undefined}
              className={cn(
                "mt-1 block w-full resize-y rounded-lg border bg-bg-base px-3 py-2 text-base text-text-primary",
                "focus:outline-none focus:ring-2 focus:ring-accent-primary/50",
                errors.message ? "border-red-500" : "border-border-subtle",
              )}
              {...register("message")}
            />
            {errors.message && (
              <p id="lead-message-error" className="mt-1 text-sm text-red-600">
                {errors.message.message}
              </p>
            )}
          </div>

          <div className="pt-2">
            <Button type="submit" disabled={submitting} className="w-full sm:w-auto">
              {submitting ? (
                <>
                  <Loader2 className="mr-2 size-4 animate-spin" />
                  {FORM_COPY.submit.submitting}
                </>
              ) : (
                FORM_COPY.submit.idle
              )}
            </Button>
          </div>

          <p className="text-xs text-text-secondary">{FORM_COPY.privacyNote}</p>
        </form>
      );
    }
    ```

    Verificar tokens Tailwind reais — se `bg-bg-base`, `text-text-primary`, etc. não existirem, substituir pelos disponíveis no design system (consultar `src/app/globals.css` ou `tailwind.config`). NÃO usar `bg-accent-50/100/200` (FOUND-03).
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/sections/lead-form.test.tsx"</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Form/LeadForm.tsx` existe e começa com `"use client"`
    - File contém `useForm`, `zodResolver(leadSchema)`, `fetch("/api/lead"`
    - File contém honeypot off-screen com `position: "absolute"`, `left: "-9999px"`, `aria-hidden="true"`, `tabIndex={-1}`, `autoComplete="off"`
    - File contém `register("website")` (NÃO `register("company")`) — honeypot renomeado
    - File contém literal `name="website"` ou `id="website"` no input honeypot
    - File contém literal `data-clarity-mask="true"` DIRETAMENTE no `<form>` element (E nos wrappers de success/error states)
    - File contém named labels matching `FORM_COPY.fields.name.label`, `FORM_COPY.fields.whatsapp.label`, `FORM_COPY.fields.message.label` (não hard-code)
    - File chama `track("form_focus", {})`, `track("form_submit_attempt", ...)`, `track("form_submit_success", ...)`, `track("form_submit_error", ...)`
    - File extrai UTM via `URLSearchParams(window.location.search)`
    - File renderiza `<FormSuccess />` quando state.status==="success" (envolvido em wrapper com data-clarity-mask); `<FormError onRetry={...} />` quando "error"
    - `npm test -- --run tests/sections/lead-form.test.tsx` exits 0 (todos os 6 testes do Plan 02 viram GREEN — sem precisar do `<Form>` wrapper externo)
  </acceptance_criteria>
  <done>LeadForm 100% funcional com honeypot=`website`, data-clarity-mask no root garantido em isolation, 6 tests RED → GREEN.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Criar Form/index.tsx (server component wrapper com section id + data-clarity-mask na seção)</name>
  <files>src/sections/Form/index.tsx</files>
  <read_first>
    - src/content/form.ts (FORM_COPY — eyebrow/heading/subheading)
    - src/components/ui/container.tsx (wrapper de largura)
    - tests/sections/lead-form.test.tsx (verifica data-clarity-mask no LeadForm em isolation — Task 3 já garante)
  </read_first>
  <action>
    Criar `src/sections/Form/index.tsx`:

    ```tsx
    /**
     * Section wrapper do lead form (Phase 5).
     *
     * Server component — renderiza heading/subheading SSR + delega o LeadForm
     * (client) por baixo. id="lead-form-section" para o IntersectionObserver
     * do FloatingWhatsApp (Plan 06) detectar.
     *
     * data-clarity-mask="true" no <section> wrapper — Microsoft Clarity NÃO grava
     * inputs/labels dentro deste container. PII protegida (TRACK-05).
     * Defense in depth: LeadForm (Task 3) já tem data-clarity-mask no root <form>;
     * o atributo aqui na <section> cobre eyebrow/heading/subheading e qualquer
     * markup futuro adjacente ao form.
     */
    import { Container } from "@/components/ui/container";
    import { FORM_COPY } from "@/content/form";
    import { LeadForm } from "./LeadForm";

    export function Form() {
      return (
        <section
          id={FORM_COPY.sectionId}
          data-clarity-mask="true"
          className="bg-bg-base py-20 md:py-28"
          aria-labelledby="lead-form-heading"
        >
          <Container className="max-w-2xl">
            <div className="mb-8 md:mb-10">
              <p className="text-sm font-medium uppercase tracking-[0.18em] text-accent-primary">
                {FORM_COPY.eyebrow}
              </p>
              <h2
                id="lead-form-heading"
                className="mt-3 text-balance text-3xl font-semibold tracking-tight text-text-primary md:text-4xl"
              >
                {FORM_COPY.heading}
              </h2>
              <p className="mt-3 text-balance text-base text-text-secondary md:text-lg">
                {FORM_COPY.subheading}
              </p>
            </div>
            <LeadForm />
          </Container>
        </section>
      );
    }
    ```

    Tokens (`bg-bg-base`, `text-accent-primary`, `text-text-primary`, `text-text-secondary`) — se algum não existir, usar equivalentes do design system real do projeto (Phase 1 estabeleceu). NÃO criar tokens novos. NÃO usar `bg-accent-50/100/200`.

    NOTA importante: o teste do Plan 02 (tests/sections/lead-form.test.tsx) renderiza `<LeadForm />` direto, NÃO `<Form />`. O atributo `data-clarity-mask` no LeadForm já foi adicionado na Task 3 (root `<form>` element + wrappers de success/error). Esta task NÃO precisa editar LeadForm retroativamente — só adicionar o atributo aqui no `<section>` wrapper para defense in depth.

    DO NOT retroactively edit LeadForm.tsx in this task — Task 3 já é a fonte canônica do data-clarity-mask em isolation.
  </action>
  <verify>
    <automated>powershell -Command "npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Form/index.tsx` existe e exporta `Form`
    - File contém `id={FORM_COPY.sectionId}` (ou `id="lead-form-section"`)
    - File contém literal `data-clarity-mask="true"` no `<section>` element
    - File contém `<LeadForm />` (consumido como child)
    - File contém `aria-labelledby="lead-form-heading"` + `<h2 id="lead-form-heading">`
    - LeadForm.tsx NÃO foi editado retroativamente nesta task (Task 3 já é fonte canônica)
    - `npm run typecheck` exits 0
    - `npm test -- --run tests/sections/lead-form.test.tsx` continua exit 0 (sem regressão; data-clarity-mask em isolation veio da Task 3)
  </acceptance_criteria>
  <done>Section wrapper pronto, sectionId estabelecido para FloatingWhatsApp consumir, PII masking applied (defense in depth: <form> via Task 3 + <section> via Task 4).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → RHF state | RHF mantém input controlado; Zod resolver bloqueia submit em invalid |
| client form → /api/lead | Validação client é UX-only; server-side Zod (Plan 03) é fonte de verdade |
| LeadForm DOM → Microsoft Clarity recorder | `data-clarity-mask="true"` no `<form>` root + `<section>` wrapper bloqueia gravação de inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-16 | Info Disclosure (PII recording) | Microsoft Clarity gravando WhatsApp/nome | mitigate | `data-clarity-mask="true"` no `<form>` root (Task 3) E no `<section>` wrapper (Task 4) — defense in depth. Validação real na Phase 6 (Clarity recording review). |
| T-05-17 | Tampering (client-side schema bypass) | Submit sem JS hidratado / fetch direto | accept (delegated) | Plan 03 server route faz safeParse — client validation é UX-only. Documentado em ASVS V5. |
| T-05-18 | Spoofing (UTM tampering) | window.location.search controlado por user | accept | UTM é tag interna para análise; risco é "lead com UTM inventada" — impacto baixo, sem mitigação dedicada |
| T-05-19 | Tampering (state machine race) | Multiple submits via Enter key | mitigate | `state.status === "submitting"` desabilita botão; RHF `handleSubmit` previne re-entry; backend Plan 03 também dedupa por número |
| T-05-20 | Tampering (password manager honeypot autofill) | 1Password/LastPass autofillando `company` em B2B forms | mitigate | Honeypot field renomeado para `website` (RESEARCH Pitfall 8) — evita falso-positivo de spam |
</threat_model>

<verification>
- `npm test -- --run tests/sections/lead-form.test.tsx` exits 0 (6/6 green)
- `npm run typecheck` exits 0
- `grep -r "data-clarity-mask" src/sections/Form/` retorna ao menos 2 matches (LeadForm.tsx no <form> + Form/index.tsx no <section>)
- `grep "use client" src/sections/Form/LeadForm.tsx` exits 0
- `grep "use client" src/sections/Form/index.tsx` exits non-zero (server component)
- `grep -r "fetch(\"/api/lead\"" src/sections/Form/` exits 0
- `grep -r "register(\"website\")" src/sections/Form/LeadForm.tsx` exits 0 (honeypot renomeado)
- `grep -r "register(\"company\")" src/sections/Form/` retorna 0 matches
</verification>

<success_criteria>
- LeadForm submete via fetch para /api/lead com Zod resolver e state machine completo
- FormSuccess substitui form ao 200, FormError mostra retry ao 4xx/5xx
- Hook useFormInView observa o section id "lead-form-section"
- Honeypot field name = `website` em LeadForm (consistente com Plan 01 schema e Plan 03 route)
- data-clarity-mask presente no `<form>` root (Task 3, garante em isolation) E no `<section>` wrapper (Task 4, defense in depth)
- LeadForm.tsx NÃO foi editado retroativamente em Task 4
- 6 testes de tests/sections/lead-form.test.tsx GREEN
- Nenhum hard-code de microcopy em JSX (tudo via FORM_COPY)
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-04-SUMMARY.md` com: estrutura final de `src/sections/Form/`, signature de useFormInView, eventos analytics disparados, honeypot field name (`website`), localização do data-clarity-mask (root `<form>` + `<section>` wrapper), prep para PR de copy review (microcopy variants).
</output>
