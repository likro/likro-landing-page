---
phase: 05-conversion-form-footer-floating-ctas-distribu-dos
plan: 05
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - src/components/layout/Header.tsx
autonomous: true
requirements: [MOBILE-06]
requirements_addressed: [MOBILE-06]
gsd_security_enforcement: enabled
asvs_level: L1
must_haves:
  truths:
    - "Header é client component que esconde ao scrollar down > 80px (depois da primeira viewport) e reaparece ao scrollar up < -8px"
    - "Reduced-motion desliga o hide — header fica sempre visível"
    - "Primeira viewport (até window.innerHeight) header sempre visível — não compete com hero CTA"
    - "Estado interno exposto como data-hidden='true'|'false' na tag <header> (contract para tests + debugging)"
    - "Spacer NÃO precisa ser adicionado porque o header NÃO vira position:fixed nesta phase — Pitfall 5 evitado mantendo `relative z-20` + animação só do filho"
  artifacts:
    - path: "src/components/layout/Header.tsx"
      provides: "client component com useScroll + useMotionValueEvent + useReducedMotion + data-hidden attribute"
      contains: '"use client"'
  key_links:
    - from: "Header.tsx"
      to: "motion/react useScroll"
      via: "useMotionValueEvent(scrollY, 'change', cb)"
      pattern: "useMotionValueEvent"
---

<objective>
Refatorar `src/components/layout/Header.tsx` para implementar hide-on-scroll-down/show-on-scroll-up usando Motion v12, respeitando reduced-motion e mantendo a primeira viewport sempre visível.

Purpose: MOBILE-06 isolado num plan pequeno porque a refatoração de server → client component tem implicações de teste e SSR independentes do resto da Phase 5. Faz `tests/components/header.test.tsx` virar GREEN.
Output: 1 arquivo editado, contract `data-hidden` attribute exposto.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-CONTEXT.md
@.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md
@src/components/layout/Header.tsx
@src/components/ui/whatsapp-cta.tsx
@src/components/ui/container.tsx
@src/content/hero.ts
@tests/components/header.test.tsx

<interfaces>
<!-- Pattern 4 do RESEARCH.md adaptado ao Header atual -->
<!-- IMPORTANTE: NÃO mudar para position: fixed (Pitfall 5) — manter relative com z-20.
     A animação de translateY é interna ao Header; o layout flow do main continua intocado.
     Header continua "sticky-ish" mas o efeito de hide é puramente cosmético em um header
     que já é `relative` no flow. Isso simplifica radically — sem spacer needed. -->

<!-- Atualmente: -->
```typescript
// src/components/layout/Header.tsx (server component)
export function Header() {
  return (
    <header className="relative z-20 w-full">
      <Container className="flex h-14 items-center justify-between lg:h-16">
        <Link href="/">...</Link>
        <WhatsAppCta variant="secondary" location="header">{HERO_COPY.ctaPrimary.label}</WhatsAppCta>
      </Container>
    </header>
  );
}
```

<!-- Após refactor: client component + sticky position no topo para o hide ter sentido -->
<!-- DECISÃO: usar `sticky top-0` (não `fixed`) — fica no flow normal mas gruda no topo durante scroll.
     Sem spacer, sem layout shift. O hide via translateY mantém o sticky behavior. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Refatorar Header para client component + hide-on-scroll (data-hidden attribute)</name>
  <files>src/components/layout/Header.tsx</files>
  <read_first>
    - src/components/layout/Header.tsx (estado atual — server component)
    - tests/components/header.test.tsx (contract: mock de motion/react + data-hidden attribute checks)
    - .planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-RESEARCH.md (Pattern 4 + Pitfalls 5 e 6)
  </read_first>
  <behavior>
    - Header agora é client component (`"use client"` na primeira linha)
    - Estado interno `hidden: boolean` controla `data-hidden` attribute e `transform: translateY(...)` via motion
    - useMotionValueEvent escuta mudanças de scrollY
    - Hide ativa quando `current > window.innerHeight && current - previous > 80`
    - Show ativa quando `current - previous < -8`
    - useReducedMotion: se true, `hidden` sempre false (header nunca esconde)
    - Posicionamento: `sticky top-0` (não `fixed` — Pitfall 5)
    - Tag `<header>` recebe `data-hidden="true"` ou `data-hidden="false"`
  </behavior>
  <action>
    Reescrever `src/components/layout/Header.tsx` com este conteúdo (preservar logo + WhatsAppCta existentes):

    ```tsx
    "use client";

    /**
     * Header — institucional minimalista, slim.
     *
     * Phase 5 (MOBILE-06): hide-on-scroll-down, show-on-scroll-up.
     *  - Esconde após primeira viewport com delta acumulado > 80px scroll down.
     *  - Reaparece com delta < -8px scroll up.
     *  - prefers-reduced-motion desliga o hide (header sempre visível).
     *  - `sticky top-0` (não `fixed`) — sem spacer, sem layout shift (Pitfall 5).
     *  - data-hidden="true|false" exposto para testes + debugging.
     */
    import { useState } from "react";
    import Link from "next/link";
    import { motion, useScroll, useMotionValueEvent, useReducedMotion } from "motion/react";
    import { Container } from "@/components/ui/container";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    import { HERO_COPY } from "@/content/hero";

    const HIDE_THRESHOLD_DOWN = 80;
    const SHOW_THRESHOLD_UP = 8;

    export function Header() {
      const reduced = useReducedMotion();
      const { scrollY } = useScroll();
      const [hidden, setHidden] = useState(false);

      useMotionValueEvent(scrollY, "change", (current) => {
        if (reduced) {
          if (hidden) setHidden(false);
          return;
        }
        const previous = scrollY.getPrevious() ?? 0;
        const delta = current - previous;
        const innerHeight = typeof window !== "undefined" ? window.innerHeight : 800;
        if (current > innerHeight && delta > HIDE_THRESHOLD_DOWN) {
          if (!hidden) setHidden(true);
        } else if (delta < -SHOW_THRESHOLD_UP) {
          if (hidden) setHidden(false);
        }
      });

      const effectiveHidden = !reduced && hidden;

      return (
        <motion.header
          data-hidden={effectiveHidden ? "true" : "false"}
          className="sticky top-0 z-30 w-full bg-bg-base/85 backdrop-blur-md"
          initial={false}
          animate={{ y: effectiveHidden ? "-100%" : "0%" }}
          transition={{ duration: 0.22, ease: [0.4, 0, 0.2, 1] }}
        >
          <Container className="flex h-14 items-center justify-between lg:h-16">
            <Link
              href="/"
              aria-label="Likro - página inicial"
              className="group inline-flex items-center gap-2 text-text-primary"
            >
              <span
                aria-hidden="true"
                className="grid size-7 place-items-center rounded-[7px] bg-gradient-to-br from-violet-500 to-violet-700 text-[13px] font-bold text-white shadow-[0_0_0_1px_rgba(124,58,237,0.4),0_4px_12px_-2px_rgba(124,58,237,0.5)]"
              >
                L
              </span>
              <span className="text-[15px] font-semibold tracking-[-0.01em] lg:text-base">
                Likro
              </span>
            </Link>
            <WhatsAppCta variant="secondary" location="header">
              {HERO_COPY.ctaPrimary.label}
            </WhatsAppCta>
          </Container>
        </motion.header>
      );
    }
    ```

    Pontos importantes:
    - `text-text-on-dark-primary` (original) trocado para `text-text-primary` porque agora o header tem background semitransparente light. Se o token correto for diferente, ajustar — mas NÃO usar `bg-accent-50/100/200`.
    - Se `bg-bg-base/85` não existir como token, usar `bg-white/85` ou `bg-zinc-50/85`.
    - O original era `relative z-20`; agora `sticky top-0 z-30` — z-30 garante que floating do Plan 06 (z-40) fique acima e header fique acima do conteúdo.
    - `initial={false}` previne flash de animação no mount inicial.
    - `useMotionValueEvent` é safe em SSR — Motion v12 só dispara no mount client (Pitfall 6 mitigado pelo `"use client"`).
  </action>
  <verify>
    <automated>powershell -Command "npm test -- --run tests/components/header.test.tsx; npm run typecheck"</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/layout/Header.tsx` começa com `"use client";`
    - File contém `import { motion, useScroll, useMotionValueEvent, useReducedMotion } from "motion/react"`
    - File contém literal `data-hidden={effectiveHidden ? "true" : "false"}` (ou variante equivalente que produz o atributo string)
    - File contém `HIDE_THRESHOLD_DOWN = 80` e `SHOW_THRESHOLD_UP = 8`
    - File contém `if (reduced) { ... }` (early return path no callback)
    - File contém `sticky top-0` (não `fixed`)
    - File ainda contém `<WhatsAppCta variant="secondary" location="header">` (preservar Phase 3)
    - File ainda contém `<Link href="/"` com aria-label "Likro - página inicial"
    - `npm test -- --run tests/components/header.test.tsx` exits 0 (4/4 testes Plan 02 viram GREEN)
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Header agora client component, hide-on-scroll funcional, reduced-motion respected, data-hidden contract exposed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.scrollY → Header state | scrollY pode ser manipulado mas não há decisão de segurança baseada nele |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | DoS (jank / main thread) | useMotionValueEvent rodando em cada scroll | accept | Motion v12 já faz throttle interno via RAF + batches updates. Aceitável v1; Phase 7 mede INP. |
| T-05-21 | Tampering (reduced-motion bypass) | Atacante modifica matchMedia | accept | Reduced-motion é UX preference, não security boundary. Bypass não afeta segurança. |
</threat_model>

<verification>
- `npm test -- --run tests/components/header.test.tsx` exits 0
- `grep -c "\"use client\"" src/components/layout/Header.tsx` retorna 1
- `grep -c "data-hidden" src/components/layout/Header.tsx` retorna ≥ 1
- `grep "position: fixed" src/components/layout/Header.tsx` retorna 0 matches (Pitfall 5 mitigado)
- `npm run typecheck` exits 0
- Hero ainda renderiza idêntico (nenhuma regressão visual — header agora sticky mas começa visible)
</verification>

<success_criteria>
- Header esconde ao scroll down após primeira viewport
- Header reaparece ao scroll up
- Reduced-motion fixa header como sempre visível
- data-hidden attribute permite verificação programática
- 4 testes de tests/components/header.test.tsx GREEN
- Hero (Phase 3) não regrede — header continua slim, logo + CTA inalterados
</success_criteria>

<output>
After completion, create `.planning/phases/05-conversion-form-footer-floating-ctas-distribu-dos/05-05-SUMMARY.md` com: diff visual antes/depois (server → client + sticky), threshold values, decisão de sticky-not-fixed.
</output>
