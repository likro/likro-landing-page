---
phase: 03-hero-benchmarked-isolado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.local
  - .env.local.example
  - src/lib/whatsapp.ts
  - src/content/whatsapp.ts
  - tests/lib/whatsapp.test.ts
  - tests/sections/hero-invariants.test.ts
  - public/mockups/atendimentos.png
  - .planning/STATE.md
autonomous: true
requirements:
  - CTA-04
threats:
  - T-3-02
  - T-3-03
  - T-3-05
user_setup:
  - service: vercel
    why: "NEXT_PUBLIC_WA_NUMBER precisa ser configurado em 3 ambientes Vercel (Production + Preview + Development) — UI-only, sem CLI scriptable em todos os casos"
    env_vars:
      - name: NEXT_PUBLIC_WA_NUMBER
        source: "Valor fornecido pelo Lenny no D-16 = 5511922324329"
    dashboard_config:
      - task: "Adicionar NEXT_PUBLIC_WA_NUMBER=5511922324329 em Production, Preview e Development"
        location: "Vercel Dashboard → Project Settings → Environment Variables"
must_haves:
  truths:
    - "Build de produção carrega NEXT_PUBLIC_WA_NUMBER=5511922324329 (sem placeholder 0000000000)"
    - "WhatsAppLocation aceita 'header' em tempo de compilação (typecheck verde)"
    - "WHATSAPP_MESSAGES.header existe e tem mensagem PT-BR não-vazia"
    - "Wave 0: grep tests Node-puro existem e falham quando Hero violar invariantes LCP/copy"
    - "public/mockups/atendimentos.png existe e tem ≤ 1MB"
  artifacts:
    - path: "src/lib/whatsapp.ts"
      provides: "WhatsAppLocation union com 'header' incluído"
      contains: '"header"'
    - path: "src/content/whatsapp.ts"
      provides: "WHATSAPP_MESSAGES.header string PT-BR"
      contains: "header:"
    - path: "tests/lib/whatsapp.test.ts"
      provides: "Caso de teste para location='header'"
      contains: "header"
    - path: "tests/sections/hero-invariants.test.ts"
      provides: "Grep tests Node-puro: zero motion.* em src/sections/Hero/, zero vh puro, exatamente 1 priority em src/, zero strings PT-BR hard-coded em Hero/Header JSX"
      min_lines: 80
    - path: "public/mockups/atendimentos.png"
      provides: "Screenshot otimizado da caixa de entrada multicanal Atendimentos"
    - path: ".env.local.example"
      provides: "Atualizado in-place — não criar .env.example duplicado (warning-5 fix). Comentário mantido + NEXT_PUBLIC_WA_NUMBER placeholder 5511000000000 já presente"
      contains: "NEXT_PUBLIC_WA_NUMBER"
  key_links:
    - from: "src/lib/whatsapp.ts"
      to: "src/content/whatsapp.ts"
      via: "Record<WhatsAppLocation, string> exhaustiveness"
      pattern: "Record<WhatsAppLocation"
    - from: "tests/lib/whatsapp.test.ts"
      to: "src/lib/whatsapp.ts"
      via: "import buildWhatsAppUrl + mock env"
      pattern: 'from "@/lib/whatsapp"'
---

<objective>
Configurar ambiente e estender helpers existentes pra suportar o hero. Esta plan NÃO escreve copy nem cria componentes — só prepara o terreno: env var Vercel, união `WhatsAppLocation` ganha membro `"header"`, mapa `WHATSAPP_MESSAGES` ganha entrada `header`, asset do mockup vai pro `public/`, e os grep tests Wave 0 que validam invariantes do Hero são criados ANTES de qualquer componente Hero existir (TDD-light: tests falham por arquivo inexistente até Plan 02 entregar).

Purpose: Materializa CTA-04 (env var) + a fundação de tipos/dados que o Plan 02 consome. Garante que invariantes LCP/copy do Hero (HERO-02, HERO-04, HERO-05, COPY-01) sejam grep-verifiáveis em CI antes do executor do Plan 02 sequer começar — pegando regressão na primeira commit, não no Lighthouse mobile.

Output: Helpers estendidos, env vars configuradas, asset do mockup pronto, grep tests Wave 0 prontos pra falhar se Plan 02 violar invariantes. Plan 02 destrava com tudo isso no lugar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md
@.planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md
@.planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md

<interfaces>
<!-- Tipos / contratos existentes que esta plan consome. Não explorar codebase. -->

From src/lib/whatsapp.ts (atual — vai ser editado):
```typescript
export type WhatsAppLocation =
  | "hero"
  | "pain"
  | "product"
  | "how"
  | "proof"
  | "footer"
  | "floating";
// FALTANDO: "header"

export function buildWhatsAppUrl(message: string, location: WhatsAppLocation): string;
// Valida phone 12-13 dígitos, bloqueia web.whatsapp.com / api.whatsapp.com,
// usa @/lib/env (mockável). Não muda nesta plan — apenas a union acima.
```

From src/content/whatsapp.ts (atual — vai ser editado):
```typescript
export const WHATSAPP_MESSAGES: Record<WhatsAppLocation, string> = {
  hero: "Oi! Vi a Likro no Instagram e quero entender como funciona pra minha clínica.",
  pain: "...",
  product: "...",
  how: "...",
  proof: "...",
  footer: "...",
  floating: "...",
  // FALTANDO: header — TS força exhaustiveness; build falha sem isso após estender union
};
```

From src/lib/env.ts (atual — não modificar):
```typescript
export const env = {
  NEXT_PUBLIC_WA_NUMBER: process.env.NEXT_PUBLIC_WA_NUMBER,
  NEXT_PUBLIC_GA4_ID: process.env.NEXT_PUBLIC_GA4_ID,
  NEXT_PUBLIC_META_PIXEL_ID: process.env.NEXT_PUBLIC_META_PIXEL_ID,
  NEXT_PUBLIC_CLARITY_ID: process.env.NEXT_PUBLIC_CLARITY_ID,
} as const;
```

From tests/brand-lock.test.ts (existing — replicar padrão Node-puro):
```typescript
import fs from "node:fs";
import path from "node:path";
const SRC_DIR = path.resolve(__dirname, "../src");
const EXTS = new Set([".tsx", ".ts", ".jsx", ".js", ".css"]);
function walk(dir: string): string[] { /* recursivo readdirSync */ }
// Pattern: ler arquivo, regex.match(), montar lista de violações, expect(matches).toEqual([])
```

From tests/lib/whatsapp.test.ts (existing — extender):
```typescript
const mockEnv = { NEXT_PUBLIC_WA_NUMBER: "5511999999999" as string | undefined };
vi.mock("@/lib/env", () => ({ get env() { return mockEnv; } }));
// Padrão de teste: mockEnv mutável + buildWhatsAppUrl(msg, location) + asserts em URL string
```

From parent directory `../prints_funcionalidades/`:
50 screenshots `.png` (Screenshot 2026-03-04 *.png). Plan 02 / executor escolhe o frame
exato; esta plan apenas copia 1 frame escolhido pra `public/mockups/atendimentos.png`.
Critério (D-03 + UI-SPEC §Image Contract): mostra caixa de entrada multicanal com
WhatsApp + Instagram visíveis, leads centralizados, atendentes; legível em viewport mobile.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Estender WhatsAppLocation e WHATSAPP_MESSAGES com "header"</name>
  <files>src/lib/whatsapp.ts, src/content/whatsapp.ts, tests/lib/whatsapp.test.ts</files>
  <read_first>
    - src/lib/whatsapp.ts (estado atual da união)
    - src/content/whatsapp.ts (estado atual do mapa — todas as 7 entradas existentes)
    - tests/lib/whatsapp.test.ts (padrão dos testes existentes, especialmente mockEnv mutável)
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-12, §Integration Points (especifica que header é nova location)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Common Operation 4: Adicionar 'header' ao tipo WhatsAppLocation" (diff exato esperado)
  </read_first>
  <action>
    Edição 1 — `src/lib/whatsapp.ts`: adicionar `"header"` à união `WhatsAppLocation` logo após `"hero"`. Diff exato:
    ```typescript
    export type WhatsAppLocation =
      | "hero"
      | "header"
      | "pain"
      | "product"
      | "how"
      | "proof"
      | "footer"
      | "floating";
    ```
    NÃO renomear, NÃO reordenar outras entradas. Apenas inserir `"header"` na posição 2.

    Edição 2 — `src/content/whatsapp.ts`: adicionar entrada `header` ao map `WHATSAPP_MESSAGES` (TS vai forçar exhaustiveness check via `Record<WhatsAppLocation, string>`; build falha sem essa entrada após Edição 1). Conteúdo provisional (Lenny aprova/edita junto com o resto da copy no Plan 03 PR):
    ```typescript
    header: "Oi! Vim do site da Likro e quero entender como funciona pra clínica.",
    ```
    Posição: logo após `hero:` no objeto literal (mesma ordem da union).

    Edição 3 — `tests/lib/whatsapp.test.ts`: adicionar caso de teste cobrindo `location='header'`. Append ao describe block existente, antes do fechamento (mantém os 8 testes atuais intactos):
    ```typescript
    it("builds url with location='header' (CTA-04 — Phase 3 D-12)", () => {
      const url = buildWhatsAppUrl("Oi do header", "header");
      expect(url).toMatch(/^https:\/\/wa\.me\/5511999999999\?text=/);
      expect(url).toContain("Oi%20do%20header");
    });
    ```
    Não precisa importar nada novo — `WhatsAppLocation` é inferido do segundo parâmetro.

    Por que essa ordem (T-3-02 mitigation): estender a union ANTES do map força build break até o map estar coerente. TS exhaustiveness é a guarda de runtime contra injection de location desconhecida.
  </action>
  <verify>
    <automated>npx vitest run tests/lib/whatsapp.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/whatsapp.ts` contém literal `| "header"` na união WhatsAppLocation
    - `src/content/whatsapp.ts` contém entrada `header:` com string PT-BR não-vazia (regex: `header:\s*"[^"]{10,}"`)
    - `tests/lib/whatsapp.test.ts` contém literal `location='header'` OU `"header"` em pelo menos um `it(...)` novo
    - `npx vitest run tests/lib/whatsapp.test.ts` exits 0 com ≥ 9 testes passando (8 existentes + 1 novo)
    - `npm run typecheck` exits 0 (sem erros de exhaustiveness em `Record<WhatsAppLocation, string>`)
    - `grep -c '"header"' src/lib/whatsapp.ts` retorna ≥ 1
  </acceptance_criteria>
  <done>
    WhatsAppLocation aceita "header" em compile-time. WHATSAPP_MESSAGES.header existe (mensagem provisional, Lenny aprova no Plan 03 PR). Teste de unidade cobre o novo location. Typecheck verde. Plan 02 pode importar `<WhatsAppCta location="header">` sem erro TS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Criar grep tests Wave 0 para invariantes do Hero (LCP + copy + priority)</name>
  <files>tests/sections/hero-invariants.test.ts</files>
  <read_first>
    - tests/brand-lock.test.ts (padrão Node-puro fs.readdirSync + regex match; copiar a função walk e a estrutura describe/it)
    - tests/setup.ts (verificar se há setup que afeta o teste — se sim, herdar)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Validation Architecture" + §"Risks the plan must mitigate" (lista exata dos invariantes a testar)
    - .planning/phases/03-hero-benchmarked-isolado/03-VALIDATION.md §"Wave 0 Requirements" (especifica os arquivos de teste a criar)
    - .planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md §"Interaction & Motion Contract" §"Image / Asset Contract" (define o que é "violação")
    - .planning/STATE.md §"State invariants" (zero motion.div em sections, único priority, etc.)
  </read_first>
  <action>
    Criar `tests/sections/hero-invariants.test.ts` seguindo o mesmo padrão Node-puro de `tests/brand-lock.test.ts` (sem dependência de grep no PATH; cross-platform Windows/macOS/Linux).

    Implementar 5 grupos de testes em um único `describe("hero invariants — Phase 3 LCP + copy gates", () => { ... })`:

    **Test 1: zero `motion.*` em src/sections/Hero/ e src/components/layout/Header.tsx**
    - Regex: `/\bmotion\.(div|h1|h2|h3|p|span|section|article|img|button|a|ul|li)\b/g`
    - Escopo: walk em `src/sections/Hero/` (se existir) + leitura de `src/components/layout/Header.tsx` (se existir). Skip silencioso se diretório/arquivo não existir AINDA (Plan 02 cria); usar `fs.existsSync` guard.
    - Mensagem de erro: cita STATE.md invariant "motion.div direto em arquivos de seção é proibido" + HERO-02/03.

    **Test 2: zero `vh` puro em src/sections/Hero/ e src/components/layout/**
    - Regex: `/\b(min-h|h|max-h)-\[?100vh\]?\b|\bh-screen\b|\bmin-h-screen\b/g`
    - Escopo: mesmo que Test 1
    - Mensagem: cita HERO-05 ("Hero usa dvh ou svh para altura, não vh").

    **Test 3: `priority` count em src/ — gate strict quando HeroMockup.tsx existir (BLOCKER 2 fix)**
    - Regex: `/\bpriority\b(?=\s*(?:\}|\/|>))/g` aplicado em arquivos `.tsx` apenas (filter `.tsx` exclui `src/app/sitemap.ts` cujo `priority: 1,` é literal de MetadataRoute.Sitemap — false-positive evitado)
    - Escopo: walk em `src/` retornando apenas `.tsx` files
    - **Lógica condicional (BLOCKER 2 — gate fica STRICT post-Plan-02):**
      ```typescript
      const heroMockupExists = fs.existsSync(path.resolve(SRC_DIR, "sections/Hero/HeroMockup.tsx"));
      if (heroMockupExists) {
        expect(count, `priority count MUST === 1 once HeroMockup.tsx exists (HERO-04 strict gate)`).toBe(1);
      } else {
        expect(count, `priority count MUST be <= 1 before HeroMockup.tsx exists (pre-Plan-02 phase)`).toBeLessThanOrEqual(1);
      }
      ```
    - Por que: garante que se uma plan futura DELETAR o `priority` no HeroMockup, o teste falha com `count === 0`. Sem condicional, count=0 passaria silenciosamente após o ship inicial.
    - Mensagem de erro inclui: `HERO-04 — Hero usa exatamente um <Image> com priority. Encontrados: ${count} em ${files.join(", ")}`.

    **Test 4: zero strings PT-BR longas hard-coded em JSX do Hero/Header (COPY-01) — BLOCKER 3 fix, regex genérico não keyword-narrow**
    - O regex anterior era keyword-narrow (clínica/estética/...) e missing ~60-70% de violações (e.g. "Lead que chegou pelo Instagram, conversa no WhatsApp, agendamento" passaria pq não contém aquelas palavras). Substituir por DOIS detectores genéricos PT-BR aplicados em conjunto:
      ```typescript
      // Detector A: texto entre JSX tags com 2+ vogais acentuadas portuguesas — captura PT-BR sentence-like
      const HARDCODED_PT_ACCENTED = />\s*[^<{}]*(?:[áàâãéêíóôõúç][^<{}]*){2,}<\/[a-z]/gi;
      // Detector B: qualquer string entre JSX tags com >=2 palavras e total >30 chars (texto longo provavelmente PT-BR)
      const HARDCODED_PT_LONG = />\s*[A-ZÀ-Úa-zà-ú][^<{}]*\s[a-zà-ú]+\s[a-zà-ú]+[^<{}]{15,}<\/[a-z]/gi;
      ```
    - **Allowlist explícito** (matches que devem ser ignorados — NÃO violações):
      - Conteúdo de atributos `aria-label`, `title`, `alt` (esses TÊM que ser strings em JSX; re-flag em Phase 7 A11Y audit se quisermos centralizar)
      - Entidades HTML (`&nbsp;`, `&rarr;`, etc.) e arrows unicode (`→`, `←`)
      - Strings em comentários JSX (`{/* ... */}`)
      - Strings que são apenas pontuação ou single-char (`>`, `/`, `·`)
      - Implementação: strip allowlist BEFORE regex; OR split por linha e ignorar linhas que match `(aria-(label|description)|title|alt)=["']`
    - **Canários (regex MUST CATCH estes strings se aparecerem em `src/sections/Hero/*.tsx` ou `src/components/layout/Header.tsx` — adicionar como inline-test comments dentro do arquivo de teste):**
      - `<p>Lead que chegou pelo Instagram, conversa no WhatsApp, agendamento — a sua clínica organizada.</p>` ← MUST flag (Detector B com 3+ palavras e >30 chars; e Detector A se tiver acentos)
      - `<span>Atendendo agora · 2 atendentes online</span>` ← MUST flag (Detector B)
      - `<h1>A operação da sua clínica, em um só lugar.</h1>` ← MUST flag (Detector A — "operação" + "clínica" + "só" = 3 vogais acentuadas)
    - Escopo: walk em `src/sections/Hero/` + `src/components/layout/Header.tsx` (skip se não existir)
    - Mensagem: cita COPY-01 + lista cada `{arquivo: linha: trecho}` + sugere mover pra `src/content/hero.ts`.

    **Test 5: zero frases banidas "cara de IA" em src/content/hero.ts (preventivo — Plan 02 cria o arquivo; teste falha enquanto não existir)**
    - Wait: se `src/content/hero.ts` não existir ainda, teste é skipped (`it.skipIf`). Quando existir, aplica regex case-insensitive:
      ```
      /(desbloqueie|desbloque|potencialize|potencia|transforme sua|próximo nível|proximo nivel|solução inovadora|solucao inovadora|jornada do cliente|do início ao fim|do inicio ao fim|feito para você|feito para voce)/i
      ```
    - Mensagem: cita COPY-02 + Pitfall A (anti-IA voice).

    **Estrutura base do arquivo** (boilerplate Node-puro, ajustar imports/paths):
    ```typescript
    import { describe, expect, it } from "vitest";
    import fs from "node:fs";
    import path from "node:path";

    const SRC_DIR = path.resolve(__dirname, "../../src");
    const HERO_DIR = path.resolve(SRC_DIR, "sections/Hero");
    const HEADER_FILE = path.resolve(SRC_DIR, "components/layout/Header.tsx");
    const HERO_COPY_FILE = path.resolve(SRC_DIR, "content/hero.ts");
    const TSX_EXTS = new Set([".tsx", ".ts"]);

    function walk(dir: string): string[] {
      if (!fs.existsSync(dir)) return [];
      const out: string[] = [];
      for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) out.push(...walk(full));
        else if (TSX_EXTS.has(path.extname(entry.name))) out.push(full);
      }
      return out;
    }

    function heroAndHeaderFiles(): string[] {
      return [...walk(HERO_DIR), ...(fs.existsSync(HEADER_FILE) ? [HEADER_FILE] : [])];
    }

    describe("hero invariants — Phase 3 LCP + copy gates", () => {
      // 5 it() blocks aqui — um por invariante listado acima
    });
    ```

    Notas críticas:
    - Cada `it()` deve produzir uma mensagem de erro CLARA com a regra violada (HERO-02 / HERO-04 / HERO-05 / COPY-01 / COPY-02) + lista de arquivos:matches.
    - Skip silencioso (não pular `it`, apenas retornar lista vazia) quando diretório `src/sections/Hero/` ainda não existe — isso permite que esta plan landeie ANTES do Plan 02. Teste passa nessa fase mas começa a falhar a partir do momento que Plan 02 violar.
    - Test 3 (priority count) é o único que precisa cobrir o src/ inteiro, não apenas Hero dir.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/hero-invariants.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `tests/sections/hero-invariants.test.ts` existe
    - `grep -c 'describe(' tests/sections/hero-invariants.test.ts` retorna ≥ 1
    - `grep -c 'it(' tests/sections/hero-invariants.test.ts` retorna ≥ 5 (um por invariante)
    - `grep -E "motion\\\\\\.|HERO-02|HERO-04|HERO-05|COPY-01|COPY-02|h-screen|priority" tests/sections/hero-invariants.test.ts` retorna ≥ 5 referências (regras citadas)
    - `npx vitest run tests/sections/hero-invariants.test.ts` exits 0 nesta fase (sections/Hero ainda não existe — tudo skipped/empty matches)
    - File não importa `motion` nem `react` (é Node-puro, fs + regex apenas)
    - Helper `walk(dir)` retorna `[]` quando dir não existe (sem throw)
  </acceptance_criteria>
  <done>
    Wave 0 dos invariantes do Hero está commit-pronto e verde. Quando Plan 02 acidentalmente importar `motion`, usar `h-screen`, adicionar 2º `priority`, ou inlinar string "clínica" em JSX, este teste falha em `npx vitest run` em < 5s. Os 5 invariantes do success criterion #1 e #2 da Phase 3 estão gated mecanicamente.
  </done>
</task>

<task type="auto">
  <name>Task 3: Preparar asset do mockup + atualizar .env.example + STATE.md</name>
  <files>public/mockups/atendimentos.png, .env.example, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-03, §D-16, §"Claude's Discretion" (especifica que executor escolhe o frame ideal)
    - .planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md §"Image / Asset Contract" (define que mockup vai pra public/mockups/atendimentos.png)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Environment Availability" (lista de prints disponíveis)
    - .planning/STATE.md §"Open Todos / Pendências Bloqueantes" (pendência "Número WhatsApp" precisa ser removida — atualizar lista)
    - Diretório `../prints_funcionalidades/` (50 prints disponíveis — escolher 1)
  </read_first>
  <action>
    **Sub-task 3a — Copiar mockup:**
    Inspecionar visualmente os 50 prints em `../prints_funcionalidades/Screenshot 2026-03-04 *.png` (caminho absoluto: `C:\Users\lenny\Desktop\Likro\prints_funcionalidades\`). Escolher 1 frame que mostre **a caixa de entrada multicanal (Atendimentos)** com:
    - WhatsApp + Instagram visíveis como canais
    - Lista de conversas/leads centralizados (densidade informacional alta mas legível)
    - Pelo menos 1 atendente visível ou avatares de conversas
    - Composição que funcione em viewport mobile (centro do frame não pode ter detalhe crítico cortável)

    Criar diretório `public/mockups/` (não existe). Copiar o frame escolhido para `public/mockups/atendimentos.png`.

    Se o arquivo > 1MB: rodar otimização. Opções (escolher uma):
    - Manual: abrir em editor e salvar como PNG-8 com compressão
    - `npx sharp-cli -i <input>.png -o public/mockups/atendimentos.png --resize 1440` (requires sharp; provavelmente já presente como peer)
    - Aceitar tamanho atual se ≤ 1MB (next/image converte pra AVIF/WebP em build)

    Anotar nome original do screenshot escolhido em comentário ou commit message para reprodutibilidade.

    **Sub-task 3b — Reconciliar .env.local.example (WARNING 5 fix):**
    Arquivo `.env.local.example` JÁ EXISTE (Phase 1 artifact) com conteúdo:
    ```
    # WhatsApp — número OFICIAL Likro (formato: 5511XXXXXXXXX, sem + nem espaços)
    NEXT_PUBLIC_WA_NUMBER=5511000000000

    # Analytics (Phase 6 valida nos dashboards reais; Phase 1 só monta scripts)
    NEXT_PUBLIC_GA4_ID=
    NEXT_PUBLIC_META_PIXEL_ID=
    NEXT_PUBLIC_CLARITY_ID=

    # Phase 5 — webhook de form
    LEAD_WEBHOOK_URL=
    ```

    **NÃO criar `.env.example` duplicado.** Editar `.env.local.example` in-place — manter o placeholder `5511000000000` (alinhado com o que JÁ está lá; não inventar formato novo `5511XXXXXXXXX`). Garantir que comentário explica como configurar Vercel — adicionar/atualizar o comentário acima da linha `NEXT_PUBLIC_WA_NUMBER`:
    ```
    # WhatsApp — número OFICIAL Likro (formato: 5511XXXXXXXXX, sem + nem espaços)
    # Configurar no Vercel Dashboard → Settings → Environment Variables (Production + Preview + Development) com valor REAL
    NEXT_PUBLIC_WA_NUMBER=5511000000000
    ```

    NÃO usar o número real `5511922324329` no `.env.local.example` — placeholder `5511000000000` permanece (não é secret mas é fingerprint do negócio; valor real só em `.env.local` local + Vercel dashboard).

    **Sub-task 3c — Configurar .env.local (CHECKPOINT humano se necessário):**
    Verificar se `.env.local` já existe e contém `NEXT_PUBLIC_WA_NUMBER`. Se não:
    - Criar (ou editar) `.env.local` adicionando `NEXT_PUBLIC_WA_NUMBER=5511922324329`
    - **NÃO commitar** `.env.local` (já está em `.gitignore` — verificar)
    - Se já existe e tem placeholder/outro valor, sobrescrever para `5511922324329`

    **Sub-task 3d — Atualizar STATE.md:**
    Editar `.planning/STATE.md`:
    1. Remover (ou marcar como resolvido) o bullet `Número oficial do WhatsApp da Likro = 5511922324329 (D-16 do 03-CONTEXT.md) — configurar em .env.local + Vercel env vars` da seção "Open Todos / Pendências Bloqueantes"
    2. Adicionar uma linha curta em "Session Continuity > Last session work" registrando: `Phase 3 Plan 01 mergeado: NEXT_PUBLIC_WA_NUMBER configurado em .env.local + Vercel (Production + Preview + Development); WhatsAppLocation estendida com "header"; mockup atendimentos.png commitado em public/mockups/; grep tests hero-invariants.test.ts verde (Wave 0).`
    3. NÃO mudar progresso/percent ainda — Phase 3 não está completa, só 1 plan.

    **Sub-task 3e — Logo asset existence gate (BLOCKER 4 fix):**
    Verificar que `public/logos/likro-logo.svg` existe ANTES de Plan 02 rodar. O arquivo é artefato da Phase 1 mas Plan 02 Header.tsx depende dele em runtime.

    Comandos verificadores (acceptance — fail-fast se faltar):
    ```bash
    test -f public/logos/likro-logo.svg || { echo "BLOCKER: logo SVG missing"; exit 1; }
    test -f public/logos/likro-logo.png || echo "WARNING: PNG fallback missing (não-blocking; SVG é primary)"
    ```

    **⚠️ UI-SPEC.md DIVERGÊNCIA (flag pro Lenny):** `03-UI-SPEC.md` linha 217 (§Image / Asset Contract) referencia `public/likro-logo.svg` (sem subfolder `logos/`). A realidade do filesystem é `public/logos/likro-logo.svg`. **Plan 02 está CORRETO** (`src="/logos/likro-logo.svg"`); UI-SPEC é que está stale. **NÃO silenciosamente alterar UI-SPEC**; sinalizar no PR de Plan 01 pra Lenny decidir:
    - (a) Atualizar UI-SPEC.md linha 217 `public/likro-logo.svg` → `public/logos/likro-logo.svg` (recomendado — apenas refletir realidade)
    - (b) Mover o asset pra raiz de `public/` e atualizar Plan 02 Header.tsx `src=`

    Decisão default (se Lenny não responder pré-merge do Plan 01): **caminho (a)** — atualizar UI-SPEC quando executar Sub-task 3e.

    Se Lenny escolher (b) ou silêncio = (a) com edit autorizado: aplicar a mudança correspondente antes do Plan 02 começar.

    **Configuração Vercel (manual — fora desta plan, mas anotar):**
    Executor (humano) precisa configurar `NEXT_PUBLIC_WA_NUMBER=5511922324329` no Vercel Dashboard em 3 ambientes (Production, Preview, Development). Esta plan NÃO automatiza Vercel — o `user_setup` no frontmatter já avisa. Executor confirma após plan rodada.

    Mitigations (T-3-02, T-3-05): número WhatsApp é validado em compile-time por `buildWhatsAppUrl` (12-13 dígitos puros). `wa.me` é hardcoded no helper — sem URL injection possível. Asset PNG é estático (sem user input) — sem XSS surface.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.statSync('public/mockups/atendimentos.png'); console.log('size:', s.size); process.exit(s.size > 0 &amp;&amp; s.size &lt; 2000000 ? 0 : 1)" &amp;&amp; node -e "if(!require('fs').readFileSync('.env.local.example','utf8').includes('NEXT_PUBLIC_WA_NUMBER')) process.exit(1)" &amp;&amp; node -e "if(!require('fs').existsSync('public/logos/likro-logo.svg')) { console.error('BLOCKER: public/logos/likro-logo.svg missing — Plan 02 Header.tsx will fail'); process.exit(1); } else { console.log('logo OK'); }"</automated>
  </verify>
  <acceptance_criteria>
    - `public/mockups/atendimentos.png` existe e tem tamanho entre 10KB e 2MB (≤ 1MB ideal)
    - `.env.local.example` (existing — atualizado in-place; NÃO criar `.env.example` duplicado) contém `NEXT_PUBLIC_WA_NUMBER=5511000000000` e comentário sobre Vercel Dashboard
    - `.env.local.example` NÃO contém o número real `5511922324329` (grep retorna 0 matches)
    - `.env.local` existe localmente e contém `NEXT_PUBLIC_WA_NUMBER=5511922324329` (verificável via `grep -c '5511922324329' .env.local` ≥ 1) — NÃO commitado (verificar `git status` mostra `.env.local` em untracked OU ignored)
    - **`public/logos/likro-logo.svg` existe (BLOCKER 4 gate — Plan 02 Header.tsx depende disso).** `node -e "require('fs').accessSync('public/logos/likro-logo.svg')"` exits 0.
    - UI-SPEC.md divergência sinalizada no PR (ou atualizada conforme decisão default caminho-a)
    - `.planning/STATE.md` não contém mais a linha "Número oficial do WhatsApp da Likro = 5511922324329... configurar em .env.local" na seção "Open Todos" (grep retorna 0 matches da pendência)
    - `.planning/STATE.md` "Session Continuity" menciona "Phase 3 Plan 01" (grep retorna ≥ 1 match)
  </acceptance_criteria>
  <done>
    Mockup pronto pro Plan 02 referenciar via `/mockups/atendimentos.png`. Env var local funciona (build de produção lê valor real, não placeholder). `.env.example` documentado pra novos contributors sem vazar número real. STATE.md reflete realidade. Vercel env config fica como handoff humano (avisado no user_setup frontmatter; Plan 03 reconfirma antes do Lighthouse).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-time → runtime | `NEXT_PUBLIC_WA_NUMBER` injetado no bundle JS; valor visível no client (público por design) |
| disk → bundle | `public/mockups/atendimentos.png` servido como asset estático; sem processamento de conteúdo |
| TS type system → runtime | `WhatsAppLocation` union é a única defesa contra location desconhecida no analytics payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Tampering | `buildWhatsAppUrl` | mitigate | União `WhatsAppLocation` agora inclui `"header"`; helper já bloqueia hosts proibidos (`web.whatsapp.com`/`api.whatsapp.com`) + valida phone 12-13 dígitos. Não regredir essas guards nesta plan — apenas estender union. |
| T-3-03 | Information Disclosure | `track('whatsapp_click', {location})` | accept | `location` é literal enum string (compile-time-bounded). Zero user input. Sem PII surface. Confirmado em `<WhatsAppCta>` que `location` flui direto pro `track()` payload sem mistura. |
| T-3-05 | Tampering | `wa.me` URL | mitigate | `wa.me` é hardcoded em `buildWhatsAppUrl`; sem user-supplied URL. Esta plan NÃO modifica essa parte do helper — apenas a union de tipo. Threat permanece mitigado por code que já existe (Phase 1). |
</threat_model>

<verification>
**Comandos a rodar localmente após mergear esta plan:**

1. `npm run typecheck` — verifica que TS exhaustiveness está coberto (WHATSAPP_MESSAGES tem todas as 8 entradas após estender union)
2. `npx vitest run tests/lib/whatsapp.test.ts` — 9 testes passam (8 existentes + 1 novo para `"header"`)
3. `npx vitest run tests/sections/hero-invariants.test.ts` — 5 testes passam (Hero dir ainda vazio, todos retornam matches=[])
4. `ls public/mockups/atendimentos.png` — arquivo existe
5. `cat .env.example | grep NEXT_PUBLIC_WA_NUMBER` — placeholder presente
6. `git status` — `.env.local` NÃO aparece em changes (gitignored)

**Verificação Vercel (humano, manual, OUT-OF-BAND):**

Executor precisa logar em vercel.com → Project Likro Landing Page → Settings → Environment Variables → Adicionar:
- Key: `NEXT_PUBLIC_WA_NUMBER`
- Value: `5511922324329`
- Environments: ✅ Production ✅ Preview ✅ Development

Trigger redeploy depois (push qualquer commit ou clique "Redeploy" na dashboard).

**Sem essa configuração, build na Vercel terá placeholder `0000000000` no link WhatsApp e CTA-04 falha em real-device test (Plan 03).**
</verification>

<success_criteria>
1. `WhatsAppLocation` union compreende `"hero"`, `"header"`, `"pain"`, `"product"`, `"how"`, `"proof"`, `"footer"`, `"floating"` — verificável via grep ≥ 1 e typecheck verde.
2. `WHATSAPP_MESSAGES.header` retorna string PT-BR não-vazia em runtime — verificável via shape do objeto literal.
3. `npm test` no diretório raiz passa com ≥ 9 testes no whatsapp.test.ts + 5 testes em hero-invariants.test.ts.
4. `public/mockups/atendimentos.png` carrega via URL `/mockups/atendimentos.png` em `next dev` sem 404.
5. `.env.local` (não-commitado) tem `NEXT_PUBLIC_WA_NUMBER=5511922324329` e dev server lê esse valor (verificável: `console.log(env.NEXT_PUBLIC_WA_NUMBER)` em qualquer RSC retorna o número real).
6. Vercel env vars (humano) configuradas em 3 ambientes — confirmação via screenshot ou Vercel CLI no Plan 03 antes do Lighthouse.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-benchmarked-isolado/03-01-SUMMARY.md` documentando:
- Arquivos modificados (`src/lib/whatsapp.ts`, `src/content/whatsapp.ts`, `tests/lib/whatsapp.test.ts`)
- Arquivos criados (`tests/sections/hero-invariants.test.ts`, `public/mockups/atendimentos.png`, `.env.example`)
- Nome do screenshot escolhido em `../prints_funcionalidades/` (pra reprodutibilidade)
- Confirmação visual/manual de Vercel env vars configuradas (ou nota de "pendente até Plan 03")
- Tamanho final do PNG após otimização (se rodada)
- Confirmação dos invariantes Wave 0 verdes (`npx vitest run tests/sections/hero-invariants.test.ts` output anexado)
</output>
