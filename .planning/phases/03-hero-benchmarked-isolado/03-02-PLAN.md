---
phase: 03-hero-benchmarked-isolado
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/content/hero.ts
  - src/sections/Hero/index.tsx
  - src/sections/Hero/HeroCopy.tsx
  - src/sections/Hero/HeroMockup.tsx
  - src/sections/Hero/HeroMicroCard.tsx
  - src/sections/Hero/HeroBackground.tsx
  - src/components/layout/Header.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - tests/content/hero.test.ts
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05
  - COPY-01
threats:
  - T-3-01
  - T-3-04
  - T-3-05
must_haves:
  truths:
    - "Página raiz / em next dev renderiza Header + Hero (placeholder substituído)"
    - "Headline H1 e mockup principal renderizam estado final imediato, zero animação de entrada"
    - "Existe exatamente 1 <Image priority> em todo src/ (mockup do hero)"
    - "Hero usa min-h-svh ou min-h-dvh, jamais 100vh/h-screen"
    - "CTA hero abre wa.me com mensagem PT-BR de WHATSAPP_MESSAGES.hero (location='hero')"
    - "Header CTA secundário usa location='header' e abre wa.me"
    - "Glow ambiente é a ÚNICA animação no hero; reduce-motion congela em estado estático"
    - "Toda copy textual vive em src/content/hero.ts ou src/content/whatsapp.ts (zero strings hard-coded em JSX do Hero/Header)"
    - "HERO_COPY_VARIANTS exporta 3 variantes contrastantes (v1, v2, v3); HERO_COPY = HERO_COPY_VARIANTS.v1 como ativo provisional"
  artifacts:
    - path: "src/content/hero.ts"
      provides: "HeroCopy type + HERO_COPY_VARIANTS (3 variantes) + HERO_COPY ativo + as const satisfies"
      contains: "satisfies"
      min_lines: 80
    - path: "src/sections/Hero/index.tsx"
      provides: "Server Component Hero orchestrator (section min-h-svh + Container grid + sub-componentes)"
      min_lines: 15
    - path: "src/sections/Hero/HeroCopy.tsx"
      provides: "RSC com Headline + sub + WhatsAppCta primary + trust signal — importa HERO_COPY"
      contains: "HERO_COPY"
    - path: "src/sections/Hero/HeroMockup.tsx"
      provides: "RSC com next/image priority + fetchPriority high + sizes responsivo + alt descritivo"
      contains: "priority"
    - path: "src/sections/Hero/HeroMicroCard.tsx"
      provides: "RSC com 1 micro-card sobreposto estático + lucide icon"
      contains: "HERO_COPY.microCard"
    - path: "src/sections/Hero/HeroBackground.tsx"
      provides: "RSC com gradient + grid sutil + glow pulsante (CSS @keyframes; aria-hidden)"
      contains: "aria-hidden"
    - path: "src/components/layout/Header.tsx"
      provides: "Header estático com logo SVG + WhatsAppCta secondary location='header'"
      contains: 'location="header"'
    - path: "src/app/page.tsx"
      provides: "Substituição do placeholder atual; renderiza <Header /> + <Hero />"
      contains: "<Hero"
    - path: "tests/content/hero.test.ts"
      provides: "Shape test: HERO_COPY_VARIANTS tem v1/v2/v3; cada variante satisfies HeroCopy; clínica aparece em h1 E sub"
      min_lines: 30
  key_links:
    - from: "src/sections/Hero/HeroCopy.tsx"
      to: "src/content/hero.ts"
      via: "import { HERO_COPY }"
      pattern: 'from "@/content/hero"'
    - from: "src/sections/Hero/HeroCopy.tsx"
      to: "src/components/ui/whatsapp-cta.tsx"
      via: "<WhatsAppCta variant=primary location=hero>"
      pattern: 'location="hero"'
    - from: "src/components/layout/Header.tsx"
      to: "src/components/ui/whatsapp-cta.tsx"
      via: "<WhatsAppCta variant=secondary location=header>"
      pattern: 'location="header"'
    - from: "src/sections/Hero/HeroMockup.tsx"
      to: "public/mockups/atendimentos.png"
      via: "next/image src"
      pattern: "/mockups/atendimentos"
    - from: "src/app/page.tsx"
      to: "src/sections/Hero/index.tsx"
      via: "import { Hero }"
      pattern: 'from "@/sections/Hero"'
---

<objective>
Implementar o Hero verticalizado para clínicas — Header + Hero como Server Components, copy module com 3 variantes, mockup priority único da página, glow ambiente como única animação. Substituir o placeholder atual em `src/app/page.tsx`. Goal: aparecer no navegador hoje, com LCP gate cumprido (zero animação no h1/mockup), copy em módulo tipado, pronto pro Lenny revisar as 3 variantes no PR.

Purpose: Materializa HERO-01..05 e COPY-01 — sucesso = página `/` em `next dev` renderiza o hero exatamente como o UI-SPEC.md trava, e os grep tests do Plan 01 (Wave 0) ficam verdes. CTA `<WhatsAppCta location="hero">` reaproveita 100% do que Phase 1 entregou (sem código duplicado).

Output: Hero ao vivo em dev. Pronto pro Plan 03 abrir o PR de copy review + benchmark Lighthouse + real-device test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md
@.planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md
@.planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md
@.planning/phases/03-hero-benchmarked-isolado/03-01-PLAN.md
@src/lib/whatsapp.ts
@src/components/ui/whatsapp-cta.tsx
@src/components/ui/headline.tsx
@src/components/ui/container.tsx
@src/app/layout.tsx
@src/app/globals.css

<interfaces>
<!-- Tipos / contratos consumidos por esta plan. Não explorar codebase. -->

From src/components/ui/whatsapp-cta.tsx:
```typescript
// Já é "use client"; já chama track() antes de window.open; já tem 250ms loading
type Variant = "primary" | "secondary" | "floating" | "inline";
interface Props {
  variant?: Variant;
  location: WhatsAppLocation;  // após Plan 01: "hero" | "header" | ...
  label?: string;
  className?: string;
  children?: React.ReactNode;
}
export function WhatsAppCta(props: Props): JSX.Element;

// USO no Hero:
//   <WhatsAppCta variant="primary" location="hero">{HERO_COPY.ctaLabel}</WhatsAppCta>
// USO no Header:
//   <WhatsAppCta variant="secondary" location="header">{HERO_COPY.ctaLabel}</WhatsAppCta>
```

From src/components/ui/headline.tsx:
```typescript
type HeadlineSize = "hero" | "section" | "sub";
type HeadlineAs = "h1" | "h2" | "h3";
interface HeadlineProps extends React.HTMLAttributes<HTMLHeadingElement> {
  as?: HeadlineAs;
  size?: HeadlineSize;
}
// size="hero" aplica: "text-4xl sm:text-5xl lg:text-7xl font-bold tracking-tight"
// USO: <Headline as="h1" size="hero" className="text-text-on-dark-primary leading-[1.05]">{HERO_COPY.h1}</Headline>
```

From src/components/ui/container.tsx:
```typescript
// max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 (verificar no source antes de usar)
// USO: <Container className="relative z-10 grid min-h-svh items-center gap-12 py-16 lg:grid-cols-[1fr_1.3fr] lg:py-24">
```

From src/lib/whatsapp.ts (após Plan 01):
```typescript
export type WhatsAppLocation = "hero" | "header" | "pain" | "product" | "how" | "proof" | "footer" | "floating";
// "header" agora válido — Plan 01 já estendeu
```

From src/app/globals.css @theme tokens (não adicionar tokens novos):
```css
--color-accent-primary: #7c3aed;        /* CTA fill */
--color-accent-hover: #6d28d9;           /* CTA hover */
--color-accent-glow: oklch(0.62 0.21 295 / 0.18);  /* Glow ambiente */
--color-surface-dark: #0a0a0b;
--color-surface-darker: #050506;
--color-surface-lighter: #ffffff;
--color-text-on-dark-primary: #fafafa;
--color-text-on-dark-secondary: #d4d4d8;
--color-text-on-dark-muted: #a1a1aa;
--color-border-on-dark-subtle: oklch(0.22 0 0);
--radius-lg: 0.75rem;
--radius-md: 0.625rem;
--ease-premium-out: cubic-bezier(0.16, 1, 0.3, 1);
```

Tailwind v4 utility names (sem prefix):
- `bg-surface-dark`, `bg-surface-darker`, `bg-surface-lighter`
- `text-text-on-dark-primary`, `text-text-on-dark-secondary`, `text-text-on-dark-muted`
- `bg-accent-primary`, `bg-accent-glow`
- `border-border-on-dark-subtle`
- `rounded-lg` (12px), `rounded-md` (10px)

prefers-reduced-motion já é tratado globalmente em globals.css:
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    ...
  }
}
```
→ glow pulse vira no-op automaticamente em reduce-motion. Não precisa de hook React.

From public/logos/likro-logo.svg (existe):
- SVG vetor; embutir inline ou usar <img src="/logos/likro-logo.svg" alt="Likro"> (NÃO next/image — preserva o slot priority único).

From public/mockups/atendimentos.png (criado pelo Plan 01):
- PNG do screenshot escolhido; next/image serve AVIF/WebP em build.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar copy module src/content/hero.ts + shape test</name>
  <files>src/content/hero.ts, tests/content/hero.test.ts</files>
  <read_first>
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-07, §D-08, §D-09, §D-10, §D-11, §D-13 (LOCKED: 3 variantes contrastantes, "clínica" em h1 E sub, CTA "Falar no WhatsApp", trust sussurrado sem Dolce Home)
    - .planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md §"Copywriting Contract" (especificação detalhada dos slots: h1, sub, ctaLabel, trust, microCard.text, microCard.iconName)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Common Operation 3: content/hero.ts module pattern" + §"`content/hero.ts` Content Module Pattern" (pattern as const satisfies + HERO_COPY ativo)
    - src/content/whatsapp.ts (padrão de organização de copy)
    - tests/brand-lock.test.ts (padrão Node-puro pra tests/content/hero.test.ts — mas o shape test usa import direto, não fs)
  </read_first>
  <behavior>
    - Test 1: `HERO_COPY_VARIANTS` exporta exatamente 3 keys: `v1`, `v2`, `v3`
    - Test 2: cada variante satisfaz HeroCopy (h1, sub, ctaLabel, trust, microCard com text + iconName)
    - Test 3 (D-09): palavra "clínica" (ou "clínicas") aparece em `h1` E em `sub` em TODAS as 3 variantes (case-insensitive, com ou sem acento)
    - Test 4 (D-11): `ctaLabel` é exatamente `"Falar no WhatsApp"` em todas as 3 variantes
    - Test 5 (D-05): `microCard.iconName` está no allowlist `["Instagram", "MessageSquare", "BellRing"]`
    - Test 6 (COPY-06): trust signal NÃO contém "Dolce Home" em nenhuma variante (autorização pendente, D-13)
    - Test 7 (COPY-02 anti-IA): nenhuma string das 3 variantes contém frases banidas (regex: `/(desbloqueie|desbloque|potencialize|potencia|transforme sua|próximo nível|proximo nivel|solução inovadora|solucao inovadora|jornada do cliente|do início ao fim|do inicio ao fim|feito para você|feito para voce)/i`)
    - Test 8: `HERO_COPY` é estritamente igual a uma das variantes (`HERO_COPY_VARIANTS.v1` por default — Lenny aprova mudança no PR)
    - **Test 9 (BLOCKER 1 fix — D-17 copy gate cobre WhatsApp messages):** `WHATSAPP_MESSAGES.hero` E `WHATSAPP_MESSAGES.header` (de `src/content/whatsapp.ts`) também passam pelo gate anti-IA:
      - Ambas as strings têm >= 10 chars e <= 200 chars (mensagem WhatsApp pré-preenchida não pode ser vazia nem ensaio)
      - NÃO contêm frases banidas (mesmo regex BANNED_PHRASES que cobre HERO_COPY_VARIANTS)
      - NÃO contêm "Dolce Home" (autorização pendente — mesma regra do trust signal)
      - Lenny aprova as duas strings no MESMO PR do Plan 02 (template `.github/PULL_REQUEST_TEMPLATE.md` já tem checkbox: "Aprovar mensagem pré-preenchida `WHATSAPP_MESSAGES.<location>`")
    - **Por que aqui (não em Plan 01):** Plan 01 entrega um stub provisional pra desbloquear typecheck; Plan 02 traz a copy final + os 3 strings (h1+sub variantes E as 2 mensagens WhatsApp) num único PR de copy review (D-17 unifica gate por seção, não por arquivo).
  </behavior>
  <action>
    **Arquivo 1 — `src/content/hero.ts`** (escrever PRIMEIRO os tests; depois implementar conforme contract abaixo):

    Conteúdo completo (Claude escreve as 3 variantes seguindo D-07 com direções contrastantes — não micro-ajustes):

    ```typescript
    /**
     * D-07: 3 variantes contrastantes de headline+sub.
     * Lenny aprova UMA via PR (mudando a linha export const HERO_COPY = ...).
     *
     * Convenção (D-17 estabelece pra todas as seções da Phase 4):
     *   - HERO_COPY_VARIANTS é o catálogo (não consumido em runtime do Hero diretamente)
     *   - HERO_COPY é o ativo — APENAS este é importado por src/sections/Hero/*
     *   - Mudar o ativo = mudar a linha `export const HERO_COPY = HERO_COPY_VARIANTS.X`
     *   - Lenny aprova via comentário no PR ("LGTM v2") OU empurra commit alterando essa linha
     *
     * D-09: palavra "clínica" aparece em h1 E sub — repetição deliberada, verticalização cristalina.
     * D-11: ctaLabel é exatamente "Falar no WhatsApp" — sem variação por device.
     * D-13: trust signal é uma linha sussurrada; SEM citar Dolce Home (autorização pendente).
     * D-05: microCard é estático (HeroMicroCard.tsx não anima entrada); iconName limitado.
     */

    export type HeroCopy = {
      h1: string;
      sub: string;
      ctaLabel: string;
      trust: string;
      microCard: {
        text: string;
        iconName: "Instagram" | "MessageSquare" | "BellRing";
      };
    };

    export const HERO_COPY_VARIANTS = {
      // VARIANTE A — afirmação de identidade vertical pura (D-08 principal)
      v1: {
        h1: "A operação da sua clínica, em um só lugar.",
        sub: "Lead que chegou pelo Instagram, conversa no WhatsApp, agendamento, retorno — a sua clínica organizada numa plataforma só.",
        ctaLabel: "Falar no WhatsApp",
        trust: "Operação ativa em clínicas reais hoje.",
        microCard: { text: "Novo lead pelo Instagram", iconName: "Instagram" },
      },
      // VARIANTE B — afirmação + influência leve de categoria-criação (D-07.b)
      v2: {
        h1: "A plataforma da clínica moderna brasileira.",
        sub: "Não é um CRM pesado, não é um chatbot solto. É a operação da sua clínica — atendimento, agendamento e equipe — em um lugar só.",
        ctaLabel: "Falar no WhatsApp",
        trust: "Em uso por clínicas brasileiras agora.",
        microCard: { text: "Atendendo agora · 2 atendentes online", iconName: "MessageSquare" },
      },
      // VARIANTE C — ângulo de especificidade operacional concreta (D-07.c)
      v3: {
        h1: "Do DM no Instagram ao agendamento da sua clínica, sem perder a conversa no caminho.",
        sub: "Atendimento centralizado, retorno automático, equipe da sua clínica vendo tudo do mesmo lugar.",
        ctaLabel: "Falar no WhatsApp",
        trust: "Resposta humana em minutos no horário comercial.",
        microCard: { text: "Lead respondido · 2 min", iconName: "BellRing" },
      },
    } as const satisfies Record<string, HeroCopy>;

    // Ativo provisional — Lenny aprova mudança no Plan 03 PR
    export const HERO_COPY: HeroCopy = HERO_COPY_VARIANTS.v1;
    ```

    Notas sobre as variantes (NÃO incluir no arquivo — é raciocínio):
    - Todas mantêm tom premium, voz humana, "clínica" em h1+sub, ctaLabel idêntico
    - Zero frases banidas (test 7 valida automaticamente)
    - Trust signal evita números fabricados e Dolce Home (D-13)
    - microCard varia entre as 3 direções permitidas em D-05 (notificação Instagram / chip status / chip de timing)

    **Arquivo 2 — `tests/content/hero.test.ts`** (8 testes listados em <behavior>):

    ```typescript
    import { describe, expect, it } from "vitest";
    import { HERO_COPY, HERO_COPY_VARIANTS, type HeroCopy } from "@/content/hero";

    const BANNED_PHRASES = /(desbloqueie|desbloque|potencialize|potencia|transforme sua|próximo nível|proximo nivel|solução inovadora|solucao inovadora|jornada do cliente|do início ao fim|do inicio ao fim|feito para você|feito para voce)/i;
    const CLINICA_REGEX = /cl[ií]nicas?/i;
    const ICON_ALLOWLIST = ["Instagram", "MessageSquare", "BellRing"] as const;

    describe("HERO_COPY_VARIANTS — Phase 3 D-07/D-09/D-11/D-13 contracts", () => {
      it("exporta exatamente 3 variantes: v1, v2, v3 (D-07)", () => {
        expect(Object.keys(HERO_COPY_VARIANTS).sort()).toEqual(["v1", "v2", "v3"]);
      });

      it("cada variante satisfaz HeroCopy shape", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          expect(variant.h1, `${key}.h1`).toBeTypeOf("string");
          expect(variant.sub, `${key}.sub`).toBeTypeOf("string");
          expect(variant.ctaLabel, `${key}.ctaLabel`).toBeTypeOf("string");
          expect(variant.trust, `${key}.trust`).toBeTypeOf("string");
          expect(variant.microCard.text, `${key}.microCard.text`).toBeTypeOf("string");
          expect(variant.microCard.iconName, `${key}.microCard.iconName`).toBeTypeOf("string");
        }
      });

      it("'clínica' aparece em h1 E sub em todas as variantes (D-09)", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          expect(CLINICA_REGEX.test(variant.h1), `${key}.h1 missing 'clínica'`).toBe(true);
          expect(CLINICA_REGEX.test(variant.sub), `${key}.sub missing 'clínica'`).toBe(true);
        }
      });

      it("ctaLabel é exatamente 'Falar no WhatsApp' (D-11 locked)", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          expect(variant.ctaLabel, `${key}.ctaLabel`).toBe("Falar no WhatsApp");
        }
      });

      it("microCard.iconName está no allowlist (D-05)", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          expect(ICON_ALLOWLIST).toContain(variant.microCard.iconName);
        }
      });

      it("trust signal NÃO menciona Dolce Home (D-13 — autorização pendente)", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          expect(/dolce home/i.test(variant.trust), `${key}.trust mentions Dolce Home`).toBe(false);
        }
      });

      it("nenhuma string contém frases banidas anti-IA (COPY-02)", () => {
        for (const [key, variant] of Object.entries(HERO_COPY_VARIANTS)) {
          const combined = `${variant.h1} ${variant.sub} ${variant.trust} ${variant.microCard.text}`;
          expect(BANNED_PHRASES.test(combined), `${key} contains banned phrase: ${combined.match(BANNED_PHRASES)?.[0]}`).toBe(false);
        }
      });

      it("HERO_COPY é igual a uma das variantes (ativo provisional)", () => {
        const variants = Object.values(HERO_COPY_VARIANTS);
        expect(variants).toContain(HERO_COPY);
      });

      // Test 9 (BLOCKER 1 fix) — WhatsApp pre-filled messages share the same anti-IA + Lenny gate
      it("WHATSAPP_MESSAGES.hero + .header pass length, anti-IA, no Dolce Home (D-17 copy gate)", async () => {
        const { WHATSAPP_MESSAGES } = await import("@/content/whatsapp");
        for (const loc of ["hero", "header"] as const) {
          const msg = WHATSAPP_MESSAGES[loc];
          expect(msg, loc + " msg present").toBeTypeOf("string");
          expect(msg.length, loc + " msg length > 10").toBeGreaterThan(10);
          expect(msg.length, loc + " msg length < 200").toBeLessThan(200);
          expect(BANNED_PHRASES.test(msg), loc + " contains banned phrase").toBe(false);
          expect(/dolce home/i.test(msg), loc + " mentions Dolce Home").toBe(false);
        }
      });
    });
    ```

    Mitigation T-3-01 (XSS): `src/content/hero.ts` é módulo TS estático compilado em build. Zero runtime injection. JSX escapa por default — sem `dangerouslySetInnerHTML` em lugar nenhum.
  </action>
  <verify>
    <automated>npx vitest run tests/content/hero.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/content/hero.ts` existe e exporta `HeroCopy` (type), `HERO_COPY_VARIANTS` (const), `HERO_COPY` (const)
    - `grep -E "as const satisfies Record<string, HeroCopy>" src/content/hero.ts` retorna ≥ 1
    - `grep -E "^\\s*v[123]:" src/content/hero.ts` retorna exatamente 3 matches
    - `grep -c "Falar no WhatsApp" src/content/hero.ts` retorna ≥ 3 (uma por variante)
    - `grep -ciE "cl[ií]nicas?" src/content/hero.ts` retorna ≥ 6 (h1 + sub × 3 variantes mínimo)
    - `tests/content/hero.test.ts` existe e tem 8 `it(...)` blocks
    - `npx vitest run tests/content/hero.test.ts` exits 0 com 8 testes passando
    - `npm run typecheck` exits 0
    - `grep -c "Dolce Home" src/content/hero.ts` retorna 0
  </acceptance_criteria>
  <done>
    Copy module pronto, 3 variantes contrastantes escritas seguindo D-07/D-08, todos os contracts (D-09, D-11, D-13, D-05) testados em CI. Lenny pode revisar via PR no Plan 03 e aprovar/editar uma variante mudando 1 linha. Plan 02 Task 2+ pode importar `HERO_COPY` direto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar Hero (5 componentes RSC) + Header + integrar em app/page.tsx</name>
  <files>src/sections/Hero/index.tsx, src/sections/Hero/HeroCopy.tsx, src/sections/Hero/HeroMockup.tsx, src/sections/Hero/HeroMicroCard.tsx, src/sections/Hero/HeroBackground.tsx, src/components/layout/Header.tsx, src/app/page.tsx, src/app/globals.css</files>
  <read_first>
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-01..D-15 (composição visual + mockup + header + glow)
    - .planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md §"Layout Contract" §"Color" §"Interaction & Motion Contract" §"Accessibility Pre-Audit" §"Image / Asset Contract"
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Common Operation 1: Hero composition" §"Common Operation 2: HeroCopy" §"Pattern 2: Single Image priority" §"Pattern 3: Glow pulse via CSS keyframe"
    - src/components/ui/whatsapp-cta.tsx (verificar API exata — já lido em <interfaces>)
    - src/components/ui/headline.tsx (verificar classes do size="hero")
    - src/components/ui/container.tsx (verificar max-width e padding default)
    - src/app/globals.css (tokens disponíveis + reduced-motion já tratado)
    - src/content/hero.ts (criado em Task 1; HERO_COPY exportado)
    - tests/sections/hero-invariants.test.ts (criado no Plan 01; vai falhar se Task 2 violar)
  </read_first>
  <behavior>
    - `src/app/page.tsx` renderiza `<Header />` seguido de `<Hero />` (sem mais o placeholder)
    - Em `next dev` (npm run dev), GET `/` retorna 200 e HTML inclui:
      - 1 `<h1>` com texto de `HERO_COPY.h1` (variante v1: "A operação da sua clínica, em um só lugar.")
      - 1 `<img>` derivado de next/image apontando pra `/mockups/atendimentos.*` (AVIF/WebP em produção)
      - 2 elementos com `data-testid="whatsapp-cta"` (header secondary + hero primary), com `data-location="header"` e `data-location="hero"` respectivamente
    - Hero usa `min-h-svh` ou `min-h-dvh` (NÃO `min-h-screen` nem `h-[100vh]`)
    - Mockup tem `priority` e `fetchPriority="high"` e `sizes="(max-width: 1024px) 100vw, 60vw"`
    - HeroBackground tem `aria-hidden="true"` e contém `@keyframes hero-glow-pulse` (em globals.css OU inline `<style>`)
    - Glow pulse anima APENAS `transform: scale()` + `opacity` (MOTION-08); ciclo 8-12s (D-15)
    - `tests/sections/hero-invariants.test.ts` (do Plan 01) continua passando: zero `motion.*` em sections/Hero/, zero `vh` puro, exatamente 1 `priority` em src/, zero strings PT-BR hard-coded em JSX
    - `npm run build` exits 0
    - `npm run typecheck` exits 0
  </behavior>
  <action>
    **Arquivo 1 — `src/sections/Hero/index.tsx`** (RSC orchestrator):

    ```tsx
    /**
     * D-01/D-02: split assimétrico cinema editorial.
     * D-15: HeroBackground é a ÚNICA fonte de animação no hero (glow pulse).
     * HERO-02/03: este componente e seus filhos NÃO importam motion/* — invariantes
     * grep-validados em tests/sections/hero-invariants.test.ts.
     * HERO-05: min-h-svh (NÃO 100vh / h-screen).
     *
     * WARNING 7 nota: <section> E <Container> ambos têm min-h-svh DELIBERADAMENTE.
     *   - <section> precisa min-h-svh pra sustentar o full-bleed background (gradient + glow)
     *     em toda a altura da viewport, mesmo quando o conteúdo for menor.
     *   - <Container> precisa min-h-svh pra que items-center (Grid) vertical-centre
     *     o conteúdo dentro da viewport (não dentro de caixa colapsada).
     *   Remover qualquer um quebra a composição cinematográfica. Não "consolidar".
     */
    import { Container } from "@/components/ui/container";
    import { HeroCopy } from "./HeroCopy";
    import { HeroMockup } from "./HeroMockup";
    import { HeroBackground } from "./HeroBackground";

    export function Hero() {
      return (
        <section
          className="relative isolate min-h-svh overflow-hidden bg-surface-darker"
          aria-labelledby="hero-headline"
        >
          <HeroBackground />
          {/* min-h-svh no Container é INTENCIONAL: section sustenta bg full-bleed; Container vertical-centra conteúdo via items-center. Ver doc block acima — não consolidar. */}
          <Container className="relative z-10 grid min-h-svh items-center gap-12 py-16 lg:grid-cols-[1fr_1.3fr] lg:gap-16 lg:py-24">
            <HeroCopy />
            <HeroMockup />
          </Container>
        </section>
      );
    }
    ```

    **Arquivo 2 — `src/sections/Hero/HeroCopy.tsx`** (RSC; renderiza `<WhatsAppCta>` client island):

    ```tsx
    /**
     * HERO-02: H1, sub, CTA e trust renderizam estado final imediato.
     * COPY-01: toda string vem de HERO_COPY (src/content/hero.ts).
     */
    import { Headline } from "@/components/ui/headline";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    import { HERO_COPY } from "@/content/hero";

    export function HeroCopy() {
      return (
        <div className="flex flex-col gap-6 lg:gap-8">
          <Headline
            as="h1"
            size="hero"
            id="hero-headline"
            className="text-text-on-dark-primary leading-[1.05]"
          >
            {HERO_COPY.h1}
          </Headline>
          <p className="max-w-xl text-xl leading-relaxed text-text-on-dark-secondary sm:text-2xl">
            {HERO_COPY.sub}
          </p>
          <div className="flex flex-col gap-3">
            <WhatsAppCta variant="primary" location="hero" className="w-fit">
              {HERO_COPY.ctaLabel}
            </WhatsAppCta>
            <p className="text-sm text-text-on-dark-muted">{HERO_COPY.trust}</p>
          </div>
        </div>
      );
    }
    ```

    **Arquivo 3 — `src/sections/Hero/HeroMockup.tsx`** (RSC; ÚNICO `<Image priority>` da página inteira):

    ```tsx
    /**
     * HERO-04: este é o ÚNICO <Image priority> da landing.
     *   Logo do Header é <img> direto (não next/image) — preserva esse slot.
     *   Micro-card icon é lucide-react SVG inline — sem image.
     * HERO-02: zero animação de entrada no mockup. Renderiza estado final.
     * PERF-03: aspect-[4/3] reserva espaço, evita CLS no swap da imagem.
     * Pitfall G: sizes responsivo — mobile baixa 100vw, desktop 60vw.
     */
    import Image from "next/image";
    import { HeroMicroCard } from "./HeroMicroCard";

    export function HeroMockup() {
      return (
        <div className="relative">
          <div className="relative aspect-[4/3] w-full max-w-[720px] overflow-hidden rounded-lg border border-border-on-dark-subtle shadow-2xl">
            <Image
              src="/mockups/atendimentos.png"
              alt="Caixa de entrada multicanal Likro: atendimentos centralizados de WhatsApp e Instagram para clínicas"
              fill
              priority
              fetchPriority="high"
              sizes="(max-width: 1024px) 100vw, 60vw"
              quality={85}
              className="object-cover"
            />
          </div>
          <HeroMicroCard className="absolute -bottom-4 -left-4 lg:-bottom-6 lg:-left-6" />
        </div>
      );
    }
    ```

    **Arquivo 4 — `src/sections/Hero/HeroMicroCard.tsx`** (RSC; estático — D-05):

    ```tsx
    /**
     * D-05: 1 único micro-card sobreposto, ESTÁTICO no carregamento.
     *   HERO-03 veta animação de entrada mesmo em elementos "secundários" do hero
     *   pra evitar competir visualmente com o protagonista.
     * Icon: lucide-react SVG inline (não <Image>) — preserva o slot priority do mockup.
     * Decorativo: aria-hidden="true" (não é status semântico se for "Novo lead pelo Instagram"
     *   sem feed real; mantém leve a a11y tree). Se Lenny escolher chip de status
     *   ("Atendendo · 2 online"), executor pode trocar pra role="status" — discutir no PR.
     */
    import { Instagram, MessageSquare, BellRing } from "lucide-react";
    import { cn } from "@/lib/utils";
    import { HERO_COPY } from "@/content/hero";

    const ICONS = {
      Instagram,
      MessageSquare,
      BellRing,
    } as const;

    interface Props {
      className?: string;
    }

    export function HeroMicroCard({ className }: Props) {
      const Icon = ICONS[HERO_COPY.microCard.iconName];
      return (
        <div
          aria-hidden="true"
          className={cn(
            "flex items-center gap-2 rounded-md border border-border-subtle bg-surface-lighter px-4 py-3 shadow-lg",
            className,
          )}
        >
          <Icon className="size-4 text-accent-primary" />
          <span className="text-sm font-medium text-text-primary">
            {HERO_COPY.microCard.text}
          </span>
        </div>
      );
    }
    ```

    **Arquivo 5 — `src/sections/Hero/HeroBackground.tsx`** (RSC; CSS-only — D-06, D-15):

    ```tsx
    /**
     * D-06: glow roxo extremamente sutil + grid abstrato leve + gradient editorial.
     * D-15: glow pulsa 8-12s ease-in-out, scale 1↔1.05 + opacity sutil — única animação no hero.
     * MOTION-07: prefers-reduced-motion já é tratado globalmente em globals.css:93-102
     *   (animation-duration: 0.01ms) → pulse vira no-op, glow estático permanece.
     * MOTION-08: anima APENAS transform + opacity (GPU-friendly).
     * @keyframes hero-glow-pulse é registrado em globals.css (Task 2 deste plan).
     */
    export function HeroBackground() {
      return (
        <div aria-hidden="true" className="pointer-events-none absolute inset-0 -z-10">
          {/* Base gradient editorial — vertical, neutro, quase imperceptível */}
          <div className="absolute inset-0 bg-gradient-to-b from-surface-darker via-surface-dark to-surface-darker" />
          {/* Grid sutil — textura editorial abstrata */}
          <div
            className="absolute inset-0 opacity-[0.04]"
            style={{
              backgroundImage:
                "linear-gradient(to right, rgba(255,255,255,0.5) 1px, transparent 1px), linear-gradient(to bottom, rgba(255,255,255,0.5) 1px, transparent 1px)",
              backgroundSize: "40px 40px",
            }}
          />
          {/* Glow pulsante atrás do mockup (right side, vertical center) */}
          <div className="absolute right-[10%] top-1/2 size-[600px] -translate-y-1/2 rounded-full bg-accent-glow blur-[120px] [animation:hero-glow-pulse_10s_cubic-bezier(0.4,0,0.6,1)_infinite]" />
        </div>
      );
    }
    ```

    **Arquivo 6 — `src/components/layout/Header.tsx`** (NOVO — RSC; estático D-12):

    ```tsx
    /**
     * D-12: header institucional minimalista — logo + 1 CTA secundário, MUITO respiro.
     *   SEM hide-on-scroll, SEM mobile menu (Phase 5 owns MOBILE-06).
     *   Não compete com o hero.
     * HERO-04: logo é <img> direto (não next/image) — preserva o slot priority único.
     */
    import Link from "next/link";
    import { Container } from "@/components/ui/container";
    import { WhatsAppCta } from "@/components/ui/whatsapp-cta";
    import { HERO_COPY } from "@/content/hero";

    export function Header() {
      return (
        <header className="relative z-20 w-full">
          <Container className="flex h-16 items-center justify-between lg:h-20">
            <Link href="/" aria-label="Likro - página inicial" className="inline-flex">
              {/* Logo inline <img> — preserva slot priority único do hero mockup */}
              {/* eslint-disable-next-line @next/next/no-img-element */}
              <img
                src="/logos/likro-logo.svg"
                alt="Likro"
                className="h-7 w-auto lg:h-8"
                width={120}
                height={32}
              />
            </Link>
            <WhatsAppCta variant="secondary" location="header">
              {HERO_COPY.ctaLabel}
            </WhatsAppCta>
          </Container>
        </header>
      );
    }
    ```

    Nota: usa `next/link` (não `<a>`) pro logo — quando Phase 4+ adicionar outras rotas/anchors, navigation client-side já está wired. Em Phase 3 isolada `/` aponta pra si mesmo, comportamento idêntico a `<a>`.

    **Arquivo 7 — `src/app/page.tsx`** (substituir placeholder):

    ```tsx
    import { Header } from "@/components/layout/Header";
    import { Hero } from "@/sections/Hero";

    export default function HomePage() {
      return (
        <>
          <Header />
          <main>
            <Hero />
          </main>
        </>
      );
    }
    ```

    **Arquivo 8 — `src/app/globals.css`** (adicionar `@keyframes hero-glow-pulse`):

    Adicionar ao final do arquivo (após o bloco `@media (prefers-reduced-motion: reduce)` existente):

    ```css
    /* ──────────────────────────────────────────────────────────────────
       Phase 3 D-15: glow ambiente do hero — única animação on-load.
       Anima APENAS transform + opacity (MOTION-08 GPU-friendly).
       Ciclo 8-12s (D-15). Reduced motion já trata globalmente acima.
       ────────────────────────────────────────────────────────────────── */
    @keyframes hero-glow-pulse {
      0%, 100% {
        transform: translateY(-50%) scale(1);
        opacity: 0.55;
      }
      50% {
        transform: translateY(-50%) scale(1.05);
        opacity: 0.85;
      }
    }
    ```

    **Observação T-3-04 (deep-link spoofing):** `WhatsAppCta` usa `buildWhatsAppUrl` que retorna URL canônica `https://wa.me/<phone>?text=<encoded>` — sem intermediário redirector. Esta plan NÃO toca esse fluxo. Mitigation permanece pelo helper existente.

    **Observação T-3-05 (open redirect):** O `<a href>` (na verdade `window.open` do `<WhatsAppCta>`) é sempre `https://wa.me/...` com phone hardcoded em env. Sem user-supplied URL em nenhum lugar do hero.

    **Notas críticas pra evitar regressão LCP:**
    - NÃO importar `motion`, `framer-motion`, `motion/react` em NENHUM dos 6 arquivos novos (grep test do Plan 01 falha)
    - NÃO usar `whileInView`, `initial`, `animate`, `transition` props
    - Reduced motion já é global — NÃO adicionar `useReducedMotion()` hook (custaria conversão pra "use client")
    - Logo do header é `<img>` cru com width/height pra reservar espaço (CLS guard) + ESLint disable do `no-img-element` justificado por HERO-04
    - Container component: confirmar via read que a API aceita `className` propagation. Se não aceitar, usar `<div className="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 ...">` direto.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npx vitest run tests/sections/hero-invariants.test.ts tests/content/hero.test.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/sections/Hero/index.tsx` existe e contém literais `min-h-svh`, `<HeroBackground`, `<HeroCopy`, `<HeroMockup`
    - `src/sections/Hero/HeroCopy.tsx` contém `import { HERO_COPY } from "@/content/hero"` e `<WhatsAppCta variant="primary" location="hero">`
    - `src/sections/Hero/HeroMockup.tsx` contém `priority` e `fetchPriority="high"` e `sizes=` e `src="/mockups/atendimentos.png"`
    - `src/sections/Hero/HeroMicroCard.tsx` contém `import` de pelo menos um icon de `lucide-react` e `aria-hidden="true"`
    - `src/sections/Hero/HeroBackground.tsx` contém `aria-hidden="true"` e `hero-glow-pulse` (referência ao keyframe)
    - `src/components/layout/Header.tsx` existe e contém `<WhatsAppCta variant="secondary" location="header">` e `src="/logos/likro-logo.svg"`
    - `src/app/page.tsx` NÃO contém mais `"Foundations OK"` (placeholder removido); contém `<Header />` e `<Hero />`
    - `src/app/globals.css` contém `@keyframes hero-glow-pulse` com `transform` e `opacity` (sem `width`/`height`/`top`/`left` na animação)
    - `grep -rE "from .motion" src/sections/Hero/ src/components/layout/Header.tsx` retorna 0 matches (nenhum import de motion)
    - `grep -rE "h-screen|min-h-screen|min-h-\\[100vh\\]" src/sections/Hero/ src/components/layout/` retorna 0 matches (HERO-05)
    - **WARNING 6 fix + BLOCKER 2 reinforcement:** Substituir bash grep por execução do invariant test (tem regex correto e exclui .ts files como sitemap.ts): `npx vitest run tests/sections/hero-invariants.test.ts -t "priority"` exits 0 e o teste reporta count === 1 (não <= 1) porque src/sections/Hero/HeroMockup.tsx AGORA EXISTE — o branch strict do Test 3 (Plan 01 Task 2 BLOCKER 2 fix) deve estar ativo. Validar: `node -e "const fs=require('fs'); console.log('mockup exists:', fs.existsSync('src/sections/Hero/HeroMockup.tsx'))"` retorna true, garantindo que regression futura (deletar priority) faria o teste falhar (count=0 não passa porque mockup existe).
    - (Fallback bash sem vitest): `grep -rcE "\\bpriority\\b" src/sections src/components/layout` (excluindo src/app/sitemap.ts cujo `priority: 1,` é literal de MetadataRoute.Sitemap e é .ts não .tsx) — total exatamente 1.
    - `npm run typecheck` exits 0
    - `npx vitest run tests/sections/hero-invariants.test.ts` exits 0 (todos os 5 grep invariantes verdes agora que Hero existe)
    - `npx vitest run tests/content/hero.test.ts` exits 0 (8 testes passando)
    - `npm run build` exits 0 (build de produção compila sem erro RSC/client boundary)
    - **WARNING 9 + INFO 13 fix (Playwright MCP smoke pre-Plan-03):** Após `npm run dev` subir, executor roda Playwright MCP autonomous smoke contra http://localhost:3000:
      - `mcp__plugin_playwright_playwright__browser_resize({ width: 375, height: 667 })` (iPhone SE)
      - `mcp__plugin_playwright_playwright__browser_navigate({ url: "http://localhost:3000" })`
      - `mcp__plugin_playwright_playwright__browser_snapshot()`

      Verificar no snapshot retornado:
      - Header logo visível no topo
      - H1 (ex: "A operação da sua clínica...") visível no viewport sem scroll
      - CTA primário "Falar no WhatsApp" visível no viewport sem scroll (HERO-01 — CTA acima da dobra mobile)
      - Mockup pode estar parcialmente cortado (stack vertical mobile, copy primeiro) — aceitável
      - Console sem erros JS críticos (`browser_console_messages` retorna apenas info/log)

      Se CTA estiver abaixo da dobra em 375x667 -> calibrar py-* ou min-h-svh ANTES de Plan 03 (rollback barato agora; caro depois do real-device test).
      Repetir para 1280x720 (desktop) confirmando split assimétrico copy/mockup lado-a-lado.
      Alinhado com CLAUDE.md global ("Playwright MCP após qualquer mudança em código de frontend").
  </acceptance_criteria>
  <done>
    `npm run dev` abre `/` e mostra: Header com logo Likro à esquerda + CTA "Falar no WhatsApp" secundário à direita; Hero com headline gigante de `HERO_COPY.h1` à esquerda, mockup atendimentos.png à direita com micro-card sobreposto, glow roxo pulsando devagar atrás do mockup; trust signal sussurrado abaixo do CTA primário. Sem flickering, sem fade-in. Reduced motion (macOS System Settings → Accessibility → Reduce motion) congela o glow estático. Plan 03 destrava: PR de copy review + Lighthouse + real-device.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client (`<WhatsAppCta>`) ↔ external WhatsApp app | `wa.me` deep-link com mensagem `?text=` pré-preenchida; conteúdo vem de `WHATSAPP_MESSAGES[location]` (static) |
| RSC (Hero/Header) ↔ client island (`<WhatsAppCta>`) | RSC props serializadas; `location` é literal string enum compile-time-bounded |
| build-time copy (`src/content/hero.ts`) ↔ rendered HTML | JSX escapa por default; sem `dangerouslySetInnerHTML` em lugar nenhum desta plan |
| disk (`public/mockups/*.png`, `public/logos/*.svg`) ↔ HTML | Assets estáticos servidos via path absoluto; sem user-supplied src |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Tampering (XSS) | `src/content/hero.ts` consumido em JSX | mitigate | Copy é módulo TS literal compilado em build (zero runtime source). JSX `{HERO_COPY.h1}` escapa automaticamente. Zero `dangerouslySetInnerHTML` em qualquer arquivo desta plan — verificável via `grep -r dangerouslySetInnerHTML src/sections/Hero/ src/components/layout/` retornando 0 matches. |
| T-3-04 | Spoofing | `wa.me` deep-link no `<WhatsAppCta>` | mitigate | URL construída por `buildWhatsAppUrl` (Phase 1) que: (a) hardcoda `https://wa.me/` (sem user-supplied host), (b) bloqueia `web.whatsapp.com`/`api.whatsapp.com`, (c) valida phone 12-13 dígitos puros. Esta plan NÃO modifica `<WhatsAppCta>` nem `buildWhatsAppUrl` — apenas consome. Real-device test no Plan 03 confirma que iOS/Android universal link abre o app correto (sem interstitial spoofable). |
| T-3-05 | Tampering / Open Redirect | CTA buttons (hero + header) | mitigate | `<WhatsAppCta>` usa `window.open(buildWhatsAppUrl(WHATSAPP_MESSAGES[location], location), "_blank", "noopener,noreferrer")`. `noopener,noreferrer` quebra `window.opener` reference (mitigando reverse tabnabbing). URL é deterministicamente construída a partir de env + content map — sem input do usuário. |
</threat_model>

<verification>
**Comandos a rodar localmente após a plan:**

1. `npm run typecheck` — TS sem erros (incluindo exhaustiveness de WhatsAppLocation e shape de HeroCopy)
2. `npm run lint` — sem warnings críticos
3. `npx vitest run tests/sections/hero-invariants.test.ts` — 5 invariantes (Plan 01 Wave 0) agora todos PASSANDO com Hero criado
4. `npx vitest run tests/content/hero.test.ts` — 8 testes copy passam
5. `npm test` — suite completa verde
6. `npm run build` — production build OK, sem erro RSC/client boundary
7. `npm run dev` → abrir `http://localhost:3000/` → verificar visualmente:
   - Header no topo: logo Likro esquerda, CTA "Falar no WhatsApp" secundário direita
   - Hero abaixo: headline gigante "A operação da sua clínica, em um só lugar." (v1)
   - Sub-headline em cinza claro
   - CTA primário "Falar no WhatsApp" abaixo (botão roxo)
   - Trust signal em cinza muted abaixo do CTA
   - Mockup à direita (desktop) ou abaixo (mobile) com radius + sombra + border sutil
   - Micro-card sobreposto no canto do mockup (Instagram icon + "Novo lead pelo Instagram")
   - Glow roxo pulsando devagar atrás do mockup
   - Background dark editorial com grid sutil
8. DevTools → Sources → Performance → Slow 4G + 4x CPU → record reload → confirmar LCP é o mockup OU o h1 (ambos sem animação) e timing < 2.5s LOCAL (Lighthouse Vercel é o gate real no Plan 03)
9. DevTools → Rendering → "Emulate CSS media feature: prefers-reduced-motion: reduce" → confirmar glow para de pulsar
</verification>

<success_criteria>
1. Página `/` em `next dev` renderiza Header + Hero exatamente conforme UI-SPEC.md (split assimétrico, CTA acima da dobra mobile, mockup à direita desktop).
2. Headline H1 e mockup renderizam no estado final imediatamente — sem fade-in, translateY, scale-up — verificado por grep tests Wave 0 verde + inspeção visual.
3. Exatamente 1 `<Image priority>` em todo `src/` (`HeroMockup.tsx`) — grep test verde.
4. Hero usa `min-h-svh` (não `vh`) — grep test verde.
5. CTA hero clica → abre `wa.me/5511922324329?text=...` com mensagem PT-BR de `WHATSAPP_MESSAGES.hero` (assumindo `.env.local` populado pelo Plan 01).
6. Header CTA clica → mesma URL pero com mensagem de `WHATSAPP_MESSAGES.header` e analytics `track('whatsapp_click', {location: "header"})`.
7. Glow ambiente pulsa devagar (ciclo 10s); reduced-motion (macOS Reduce Motion) congela em estado estático.
8. Zero strings PT-BR longas em JSX do Hero/Header (toda copy via `HERO_COPY` ou `<WhatsAppCta>` children) — grep test verde.
9. `npm run build` sucesso; bundle JS first-load <  ~150KB (informativo na Phase 3; gate formal é Phase 7).
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-benchmarked-isolado/03-02-SUMMARY.md` documentando:
- Arquivos criados (6 sections/layout + 1 content + 1 test) e arquivos editados (page.tsx, globals.css)
- Confirmação dos 5 grep invariantes verdes (output de `npx vitest run tests/sections/hero-invariants.test.ts`)
- Confirmação dos 8 testes copy verdes (output de `npx vitest run tests/content/hero.test.ts`)
- Screenshot ou descrição do hero em `npm run dev` (mobile e desktop)
- Resultado de `npm run build` (build size + warnings se houver)
- Variante de copy ativa default (`HERO_COPY = HERO_COPY_VARIANTS.v1`) — sinaliza pro Plan 03 PR que Lenny precisa escolher/aprovar
- Pendência clara: Vercel env vars (`NEXT_PUBLIC_WA_NUMBER`) — Plan 03 valida antes do Lighthouse
</output>
