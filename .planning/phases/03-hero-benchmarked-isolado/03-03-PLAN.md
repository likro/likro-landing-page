---
phase: 03-hero-benchmarked-isolado
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - docs/copy-review.md
  - docs/device-test-checklist.md
  - docs/five-second-test-plan.md
  - .github/PULL_REQUEST_TEMPLATE.md
  - .planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md
  - .planning/STATE.md
autonomous: false
requirements:
  - HERO-06
  - HERO-07
  - COPY-04
threats:
  - T-3-02
  - T-3-04
must_haves:
  truths:
    - "Cadência de copy review está documentada em docs/copy-review.md e referenciada pelo PR template — usável pelas 5 seções narrativas da Phase 4"
    - "Checklist de device test (iPhone + Android real) existe em docs/device-test-checklist.md com passos verificáveis"
    - "Plano do 5-second test existe com prompt exato + critérios de pass/fail (≥2 de 3 respostas acertam 3 itens)"
    - "Lenny aprovou (ou editou) uma das 3 variantes de copy via PR — HERO_COPY ativo reflete a escolha"
    - "Vercel preview URL roda Lighthouse mobile e o resultado (LCP, CLS, Performance score) está registrado em 03-VERIFICATION.md"
    - "WhatsApp deep link testado em iPhone real e Android real — abrem app WhatsApp (não browser), com mensagem PT-BR pré-preenchida"
    - "5-second test rodado com ≥3 pessoas — respostas literais registradas em 03-VERIFICATION.md; pass = ≥2 acertam Likro + clínica + WhatsApp"
    - "STATE.md atualizado: Phase 3 marcada como complete, percent ajustado, próximo passo é Phase 4"
  artifacts:
    - path: "docs/copy-review.md"
      provides: "Padrão da cadência async via PR seção-a-seção (D-17) — instruções pra Claude (como rascunhar 3 variantes) e pra Lenny (como aprovar). Template reutilizado em Phase 4."
      min_lines: 60
    - path: "docs/device-test-checklist.md"
      provides: "Checklist passo-a-passo iPhone Safari + Android Chrome + Instagram IAB pra validar WhatsApp deep link"
      min_lines: 40
    - path: "docs/five-second-test-plan.md"
      provides: "Prompt literal + protocolo + critérios de pass/fail do 5-second test (HERO-07)"
      min_lines: 30
    - path: ".github/PULL_REQUEST_TEMPLATE.md"
      provides: "Bloco 'Copy review' que vira gate em todo PR que toca src/content/*.ts"
      contains: "Copy review"
    - path: ".planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md"
      provides: "Registro literal de: Lighthouse mobile (score, LCP, CLS), PageSpeed Insights URL+score, iPhone+Android real-device pass, 5-second test 3 respostas literais, Lenny copy approval link"
      min_lines: 80
  key_links:
    - from: ".github/PULL_REQUEST_TEMPLATE.md"
      to: "docs/copy-review.md"
      via: "link markdown"
      pattern: "copy-review.md"
    - from: "docs/copy-review.md"
      to: "src/content/hero.ts"
      via: "exemplo do HERO_COPY_VARIANTS pattern"
      pattern: "HERO_COPY_VARIANTS"
    - from: ".planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md"
      to: "Vercel preview URL"
      via: "literal URL .vercel.app"
      pattern: ".vercel.app"
---

<objective>
Fechar a Phase 3 com 4 gates manuais que NÃO podem ser automatizados: (1) Lenny revisa as 3 variantes de copy do Plan 02 via PR e aprova/edita uma; (2) Lighthouse mobile rodado contra Vercel preview URL atinge LCP < 2.5s; (3) WhatsApp deep link testado em iPhone real + Android real abre o app; (4) 5-second test com 3 pessoas confirma a mensagem do hero. Documentar o padrão de copy review como convenção reutilizável pra Phase 4 (5 seções narrativas).

Purpose: HERO-06, HERO-07 e COPY-04 são por natureza manuais. Esta plan cria os artefatos (docs/checklists) que materializam os critérios + registra os resultados em `03-VERIFICATION.md`. Sem essa plan, "Phase 3 complete" seria afirmação sem prova; com ela, vira artefato auditável.

Output: 4 docs (PR template + 3 checklists), `03-VERIFICATION.md` populado, STATE.md marca Phase 3 complete e desbloqueia Phase 4.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md
@.planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md
@.planning/phases/03-hero-benchmarked-isolado/03-UI-SPEC.md
@.planning/phases/03-hero-benchmarked-isolado/03-VALIDATION.md
@.planning/phases/03-hero-benchmarked-isolado/03-01-PLAN.md
@.planning/phases/03-hero-benchmarked-isolado/03-02-PLAN.md
@src/content/hero.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar docs/copy-review.md + docs/device-test-checklist.md + docs/five-second-test-plan.md + PR template</name>
  <files>docs/copy-review.md, docs/device-test-checklist.md, docs/five-second-test-plan.md, .github/PULL_REQUEST_TEMPLATE.md</files>
  <read_first>
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-17 (cadência async via PR seção-a-seção — convenção exata)
    - .planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md §D-19 (checklist mínimo LCP gate + real-device + 5-second)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Copy Review Cadence" (PR description template recomendado)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"5-Second Test Plan" (prompt + logística + pass criteria)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Lighthouse Mobile Config" (settings exatos)
    - .planning/phases/03-hero-benchmarked-isolado/03-RESEARCH.md §"Pitfall E" (real-device test: iOS Safari + Instagram IAB; Android Chrome + Instagram IAB)
    - src/content/hero.ts (variantes existentes — usar como exemplo concreto no doc)
    - Verificar se `docs/` existe (provavelmente não — criar)
    - Verificar se `.github/PULL_REQUEST_TEMPLATE.md` existe (provavelmente não — criar)
  </read_first>
  <action>
    **Arquivo 1 — `docs/copy-review.md`** (convenção async PR — D-17, reutilizada em Phase 4):

    Estrutura obrigatória (mínimo 60 linhas):

    ```markdown
    # Copy review — cadência async via PR (D-17)

    > Estabelecida na Phase 3 (Hero). Padrão pras 5 seções narrativas da Phase 4 e qualquer copy futura em `src/content/*.ts`.

    ## Princípio

    Copy review é **gate, não polish de fim** (COPY-04). Uma seção só fecha quando a copy dela tem merge aprovado pelo Lenny via PR.

    ## Workflow

    1. **Claude rascunha 3 variantes contrastantes** em `src/content/<secao>.ts` seguindo o pattern Phase 3:
       - `export const <SEC>_COPY_VARIANTS = { v1, v2, v3 } as const satisfies Record<string, <Sec>Copy>`
       - `export const <SEC>_COPY: <Sec>Copy = <SEC>_COPY_VARIANTS.v1` (ativa provisional)
       - 3 direções DISTINTAS (não micro-ajustes de frase) — vide §"Como rascunhar"
    2. **Claude abre PR** com PR description usando o bloco "Copy review" (vide PR template em `.github/PULL_REQUEST_TEMPLATE.md`)
    3. **Lenny revisa no GitHub** — escolhe via 1 de 3 caminhos:
       - **(a) Aprovar como está:** comenta `LGTM v1` (ou `v2`/`v3`) — Claude muda 1 linha (`HERO_COPY = HERO_COPY_VARIANTS.v2`) e re-pushea
       - **(b) Editar inline:** Lenny aperta "Edit file" no GitHub na variante escolhida, ajusta texto, commita direto no branch
       - **(c) Pedir nova rodada:** comenta direcionamento ("v2 tá perto mas troca 'plataforma' por 'sistema'") — Claude itera
    4. **Merge** acontece somente após aprovação explícita

    ## Como rascunhar 3 variantes contrastantes (não Frankenstein de frase)

    - **Variante A — identidade vertical pura** (D-08 default): afirmação clara da categoria + vertical, sofisticada, tom premium
    - **Variante B — identidade + categoria-criação**: leve sinal de "não é só X" (sem usar literalmente "não é só"), posiciona como categoria própria
    - **Variante C — especificidade operacional**: ângulo concreto da operação ("DM → agendamento → retorno") em vez de afirmação abstrata

    Todas as 3 devem:
    - Passar no grep anti-IA: nada de "desbloqueie", "potencialize", "transforme sua X", "jornada do cliente", "próximo nível", "solução inovadora", "feito para você", "do início ao fim", "fricção" abstrato (COPY-02)
    - Falhar no teste de especificidade se substituirmos a palavra vertical: trocar "clínica" por "empresa" deve quebrar o significado (COPY-03)
    - Não conter números fabricados ou depoimentos placeholder (COPY-06)
    - **Phase 4 específico:** não citar Dolce Home sem autorização explícita do Lenny (STATE.md pendência)

    ## Exemplo (Phase 3 Hero — referência)

    Ver `src/content/hero.ts`: 3 variantes (v1 vertical pura, v2 categoria-criação, v3 especificidade operacional) seguem este pattern. PR original que estabeleceu a cadência: [link do PR Phase 3 Plan 02 — preencher depois do merge].

    ## Anti-padrões

    - **5+ variantes**: vira overload de revisão e risco de Frankenstein (D-07).
    - **Variantes que são micro-ajustes de frase**: gasta o tempo do Lenny sem dar escolha real.
    - **Pass de copy review único no fim**: viola D-17. Cada seção tem o próprio gate.

    ## Gate técnico

    Antes do PR estar pronto pra review do Lenny:
    - `npm run typecheck` verde
    - `npx vitest run tests/content/<secao>.test.ts` verde (shape test + clínica em h1+sub + anti-IA grep)
    - `tests/sections/hero-invariants.test.ts` (Phase 3 Wave 0) ainda verde se a seção é o Hero

    ## Histórico de decisões

    Git serve como histórico canônico — cada merge de PR de copy é a decisão registrada. Não duplicar em outro doc.
    ```

    **Arquivo 2 — `docs/device-test-checklist.md`** (real-device test D-19):

    ```markdown
    # Real-device test — WhatsApp deep link

    > Cumpre HERO-04 / CTA-04 / CTA-07. Manual; não automatizável (Playwright não simula iOS Universal Link / Android Intent).

    ## Pré-requisitos

    - Vercel preview URL com hero deployado (`.vercel.app`)
    - `NEXT_PUBLIC_WA_NUMBER=5511922324329` configurado em Vercel (Production + Preview + Development)
    - iPhone real (iOS 15+) com WhatsApp instalado e número logado
    - Android real (Android 10+) com WhatsApp instalado e número logado
    - Instagram instalado em ambos (pra testar IAB — In-App Browser — que é o tráfego principal vindo de Meta Ads)

    ## Checklist iPhone

    ### iOS Safari (direto)

    1. Abrir Safari → digitar Vercel preview URL
    2. Confirmar página renderiza (hero visível)
    3. Tap no CTA primário "Falar no WhatsApp" do hero
    4. Esperado: WhatsApp app foreground (não Safari, não wa.me página intermediária)
    5. Esperado: chat com número Likro (`+55 11 92232-4329`) aberto, mensagem pré-preenchida visível na text box
    6. Esperado: mensagem é `WHATSAPP_MESSAGES.hero` literal (verificar contra `src/content/whatsapp.ts`)
    7. Registrar: ✓ ou ✗ + descrição literal do comportamento

    ### iOS — Instagram In-App Browser

    1. Abrir Instagram app
    2. Postar (story, feed, ou DM) com a Vercel preview URL como link clicável (próprio usuário pode mandar pra si mesmo via DM)
    3. Tap no link no Instagram → abre IAB do Instagram (NÃO Safari)
    4. Confirmar página renderiza no IAB
    5. Tap no CTA primário "Falar no WhatsApp"
    6. Esperado: WhatsApp app foreground OU página intermediária `wa.me/<phone>` com botão "Continue to chat"
    7. Se intermediária: tap "Continue" → WhatsApp app deve foreground
    8. Registrar: ✓ ou ✗ + se houve intermediária (UX subóptima conhecida, não é falha do site)

    ### iOS — CTA secundário do Header

    9. Repetir passos Safari direto + IAB Instagram pro CTA do Header
    10. Esperado: mesma URL `wa.me/...` MAS mensagem é `WHATSAPP_MESSAGES.header` (texto diferente)
    11. Registrar

    ## Checklist Android

    ### Chrome (direto)

    1. Mesmo fluxo do Safari direto, mas com Chrome
    2. Esperado: Android Intent System resolve `wa.me` pro app WhatsApp
    3. Registrar

    ### Android — Instagram IAB

    4. Mesmo fluxo do iOS Instagram IAB
    5. Esperado: geralmente Universal Link funciona; pode haver "Open in WhatsApp?" prompt
    6. Registrar

    ## Critérios de pass

    - **Pass obrigatório:** Safari iOS direto + Chrome Android direto abrem WhatsApp app em ambos os CTAs
    - **Pass desejável:** Instagram IAB iOS + Instagram IAB Android abrem WhatsApp app diretamente
    - **Aceitável:** Instagram IAB iOS mostra intermediária `wa.me` page (UX do IAB, não falha do site) — desde que "Continue to chat" funcione

    ## Registro

    Resultados literais (incluindo screenshots se possível) vão em `.planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md` §"Real-device test".
    ```

    **Arquivo 3 — `docs/five-second-test-plan.md`** (HERO-07):

    ```markdown
    # 5-second test — Hero (HERO-07)

    > 3 pessoas sem contexto olham apenas a primeira viewport por 5 segundos e descrevem em 1 frase. Manual; não automatizável.

    ## Prompt literal (mandar via WhatsApp ou em pessoa)

    > "Oi, preciso de uma ajuda rápida. Abre esse link no celular: `<VERCEL_PREVIEW_URL>` — olha só o que aparece na primeira tela por 5 segundos (pode marcar no relógio), depois fecha e me escreve em UMA frase: que produto é, pra quem é, e qual a ação principal. Sem ver de novo depois, vale a primeira impressão."

    ## Logística

    1. Lenny escolhe 3 pessoas SEM contexto do projeto:
       - Não pode ser quem participou da discuss/research
       - Idealmente: 1 dono de clínica/estética real (público-alvo), 1 amigo/colega aleatório, 1 pessoa fora do setor SaaS
    2. Mandar prompt acima via WhatsApp ou conduzir presencialmente
    3. Aceitar a primeira frase enviada — sem follow-up que enviese
    4. Copiar literalmente as 3 respostas (sem edição) em `03-VERIFICATION.md`

    ## Critérios de pass

    Cada resposta é avaliada em 3 dimensões:

    | Item | Pass se a frase menciona... |
    |------|------------------------------|
    | **Categoria do produto** | "plataforma", "sistema", "CRM", "operação", "atendimento" — não precisa dizer "Likro" |
    | **Público (verticalização)** | "clínica", "estética", "consultório", "salão" — explicitamente vertical |
    | **CTA primário** | "WhatsApp" como ação |

    **Resultado:**
    - **Pass:** ≥2 das 3 respostas acertam os 3 itens (D-19 / HERO-07)
    - **Soft pass:** 2 respostas acertam 2 dos 3 itens — discutir com Lenny se itera ou aceita
    - **Fail:** ≥2 respostas não mencionam "clínica" OU "WhatsApp" — reavaliar copy/composição ANTES de Phase 4

    ## Viés social — mitigação

    Pessoas próximas tendem a "ajudar" e dar feedback inflado. Mitigação:
    - Prompt explícito ("primeira impressão", "sem follow-up")
    - ≥3 pessoas, idealmente 1 fora do círculo próximo
    - Se 3 respostas forem todas elogiosas mas erram itens, considerar fail (sinal de viés)

    ## Registro

    Em `.planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md` §"5-second test", incluir:
    - Data
    - Quem (relação com Lenny — "irmão dono de salão", "colega Insper", etc.)
    - Resposta literal (sem editar)
    - Avaliação dos 3 itens (✓/✗ cada)
    - Veredicto final: pass / soft pass / fail
    ```

    **Arquivo 4 — `.github/PULL_REQUEST_TEMPLATE.md`** (criar; se já existir, preservar conteúdo e adicionar a seção Copy review):

    ```markdown
    ## Summary

    <!-- 1-3 bullets descrevendo o que o PR muda -->

    ## Copy review (preencher SE o PR toca src/content/*.ts)

    <!-- Convenção D-17 / docs/copy-review.md -->

    **Seção:** <!-- ex: Hero, Pain, Bridge, Product, HowItWorks, Proof -->

    **3 variantes em `src/content/<secao>.ts`:**
    - `v1` (ativa por default): <!-- 1 linha descrevendo a direção -->
    - `v2`: <!-- direção contrastante -->
    - `v3`: <!-- 3a direção -->

    **Decisões pendentes do reviewer:**
    - [ ] Aprovar uma variante (comentário `LGTM vX`) — Claude muda a linha `<SEC>_COPY = <SEC>_COPY_VARIANTS.X`
    - [ ] Aprovar texto do micro-card (se a seção tiver)
    - [ ] Aprovar trust signal / sub-elementos textuais
    - [ ] Aprovar mensagem pré-preenchida `WHATSAPP_MESSAGES.<location>` (se a seção tiver CTA WhatsApp)

    **Gates técnicos verificados:**
    - [ ] `npm run typecheck` verde
    - [ ] `npx vitest run tests/content/<secao>.test.ts` verde
    - [ ] `tests/sections/hero-invariants.test.ts` ainda verde (se a seção é Hero)
    - [ ] Grep anti-IA em `src/content/<secao>.ts` retorna 0 matches

    Detalhes da cadência: [docs/copy-review.md](../docs/copy-review.md)

    ## Test plan

    <!-- Bullets dos testes manuais/automatizados rodados antes do merge -->

    ## Notes

    <!-- Qualquer outro contexto relevante -->
    ```

    Notas:
    - Se `.github/` ainda não existe no repo, criar.
    - Se já existe um `PULL_REQUEST_TEMPLATE.md` previamente, PRESERVAR o conteúdo e adicionar a seção "Copy review" no meio (não sobrescrever cego).
  </action>
  <verify>
    <automated>node -e "['docs/copy-review.md','docs/device-test-checklist.md','docs/five-second-test-plan.md','.github/PULL_REQUEST_TEMPLATE.md'].forEach(f=>{const c=require('fs').readFileSync(f,'utf8'); if(c.length<200)throw new Error(f+' too short: '+c.length)}); console.log('ok')"
</automated>
  </verify>
  <acceptance_criteria>
    - `docs/copy-review.md` existe, ≥ 60 linhas, contém literais "D-17", "3 variantes", "HERO_COPY_VARIANTS", "Lenny"
    - `docs/device-test-checklist.md` existe, ≥ 40 linhas, contém literais "iPhone", "Android", "Instagram", "Vercel preview", "wa.me"
    - `docs/five-second-test-plan.md` existe, ≥ 30 linhas, contém literais "5 segundos", "clínica", "WhatsApp", "≥2", "pass"
    - `.github/PULL_REQUEST_TEMPLATE.md` existe e contém a string literal "Copy review"
    - `.github/PULL_REQUEST_TEMPLATE.md` contém link relativo a `docs/copy-review.md`
    - `grep -c "src/content" docs/copy-review.md` retorna ≥ 1 (referência ao pattern)
    - `grep -c "5511922324329" docs/device-test-checklist.md` retorna ≥ 1 (número Likro presente para verificar contato exato no chat)
  </acceptance_criteria>
  <done>
    4 docs no lugar formando a convenção operacional da Phase 3: copy review async-via-PR documentado (reutilizável Phase 4), checklist real-device acionável, plano do 5-second test com critérios de pass/fail mensuráveis, PR template com gate de copy. Plan 02 PR pode usar este template já no próximo push.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Lenny revisa as 3 variantes de copy do Plan 02 PR e aprova/edita uma (COPY-04)</name>
  <files>src/content/hero.ts, src/content/whatsapp.ts</files>
  <action>
    HUMAN-IN-LOOP gate: NAO e trabalho autonomo de codigo. Resumo do entregue pelo Plan 02 (contexto pro reviewer):
    - src/content/hero.ts com 3 variantes contrastantes (v1 identidade vertical, v2 categoria-criacao, v3 especificidade operacional)
    - HERO_COPY = HERO_COPY_VARIANTS.v1 como ativa provisional
    - PR aberto usando .github/PULL_REQUEST_TEMPLATE.md (criado na Task 1 desta plan) com secao Copy review preenchida
    - Vercel auto-preview do PR renderiza hero v1 em URL .vercel.app

    Aguardar Lenny executar uma das 4 acoes em <how-to-verify>. Apos a decisao, ajustar src/content/hero.ts (e/ou src/content/whatsapp.ts se o reviewer pediu nova mensagem pre-preenchida) conforme escolha (tipicamente 1 linha: HERO_COPY = HERO_COPY_VARIANTS.vN) e re-pushar pro mesmo PR. Merge somente apos aprovacao explicita registrada em comentario ou commit no PR.
  </action>
  <verify>
    <automated>node -e "const fs=require(\"fs\"); const c=fs.readFileSync(\"src/content/hero.ts\",\"utf8\"); const m=c.match(/HERO_COPY:\s*HeroCopy\s*=\s*HERO_COPY_VARIANTS\.(v[123])/); if(!m) throw new Error(\"HERO_COPY ativo nao encontrado\"); console.log(\"ativa:\",m[1])"</automated>
  </verify>
  <done>
    Lenny escolheu (ou editou) uma das 3 variantes via PR. HERO_COPY = HERO_COPY_VARIANTS.vN reflete a escolha (verificavel via grep). Merge realizado. Branch da Phase 3 contem a copy final aprovada. Plan 03 Task 3 pode prosseguir com Vercel preview da copy aprovada.
  </done>
  <what-built>
    - Plan 02 entregou `src/content/hero.ts` com 3 variantes contrastantes (v1 identidade vertical, v2 categoria-criação, v3 especificidade operacional)
    - `HERO_COPY = HERO_COPY_VARIANTS.v1` como ativa provisional
    - PR aberto usando `.github/PULL_REQUEST_TEMPLATE.md` (criado em Task 1 desta plan) com seção "Copy review" preenchida
    - Vercel auto-preview do PR já está com o hero v1 renderizado em URL `.vercel.app`
  </what-built>
  <how-to-verify>
    1. Abrir o PR do Plan 02 (likro-landing-page) no GitHub
    2. Ler a seção "Copy review" do PR description — descrições curtas das 3 direções
    3. Abrir o preview Vercel do PR — ver v1 em ação
    4. Para revisar v2 e v3 sem deploy: ler diretamente `src/content/hero.ts` no arquivo do PR
    5. **Decidir uma das 3 ações:**
       - (a) **Aprovar v1 como está:** comentar `LGTM v1` — Claude não muda nada, merge
       - (b) **Trocar a ativa:** comentar `LGTM v2` (ou v3) — Claude muda 1 linha (`HERO_COPY = HERO_COPY_VARIANTS.v2`) e re-pusha; nova rodada de Vercel preview
       - (c) **Editar inline:** "Edit file" no GitHub na variante escolhida, ajustar texto, commitar direto no branch do PR (Vercel re-build automático)
       - (d) **Pedir nova rodada:** comentar direcionamento livre — Claude itera as variantes
    6. Verificar também:
       - Texto do micro-card (HERO_COPY.microCard.text) — combina com a direção escolhida?
       - Trust signal (HERO_COPY.trust) — está sussurrado? Sem Dolce Home?
       - `WHATSAPP_MESSAGES.hero` e `WHATSAPP_MESSAGES.header` (mensagens pré-preenchidas) — tom alinhado?
    7. Após aprovação final: merge do PR no main (ou na branch da Phase 3)
  </how-to-verify>
  <resume-signal>Comentar no PR com "LGTM v1/v2/v3" OU empurrar commit com a variante editada; após merge, sinalizar "copy aprovada, prosseguir Phase 3 Plan 03"</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Lighthouse mobile na Vercel preview + PageSpeed Insights + real-device + 5-second test → registrar em 03-VERIFICATION.md (HERO-06, HERO-07, CTA-04)</name>
  <files>.planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md, .planning/phases/03-hero-benchmarked-isolado/lighthouse-mobile.json</files>
  <action>
    HUMAN-IN-LOOP gate: 4 sub-checkpoints (Vercel env check, Lighthouse, real-device, 5-second test) documentados em <how-to-verify>. NAO automatizavel — Lighthouse precisa URL Vercel; real-device precisa iPhone+Android fisicos; 5-second test precisa 3 humanos.

    Apos cada sub-checkpoint, registrar resultado literal em .planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md (estrutura template fornecida em <how-to-verify>). Anexar lighthouse-mobile.json se possivel. Preencher TODOS os campos com valores reais (nao placeholders). Veredicto final no fim do arquivo: complete / soft pass / blocked-on-<item>.
  </action>
  <verify>
    <automated>node -e "const fs=require(\"fs\"); const c=fs.readFileSync(\".planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md\",\"utf8\"); const need=[\"LCP\",\"5-second\",\"Real-device\",\"Veredicto\"]; for(const k of need) if(!c.includes(k)) throw new Error(\"missing section: \"+k); if(c.includes(\"<URL>\")||c.includes(\"YYYY-MM-DD\")) throw new Error(\"placeholder literal <URL> ou YYYY-MM-DD\"); if(/^\\s*\\|\\s*_\\s*\\|/m.test(c)) throw new Error(\"WARNING 8 fix: placeholder _ em célula de tabela não preenchido\"); if(/:\\s*_\\s*$/m.test(c)) throw new Error(\"WARNING 8 fix: campo com placeholder _ no fim da linha\"); if(/\"_\"/.test(c)) throw new Error(\"WARNING 8 fix: placeholder _ em quoted string\"); console.log(\"ok\")"</automated>
  </verify>
  <done>
    03-VERIFICATION.md preenchido com Lighthouse mediana real (3 runs), 8 cenarios real-device documentados, 3 respostas do 5-second test literais, e veredicto final. Phase 3 atinge o gate D-19 com prova auditavel — pronto pra Task 4 fechar o STATE.md.
  </done>
  <what-built>
    - Hero está deployado isoladamente na Vercel preview (URL `.vercel.app` do PR final Phase 3, após merge da copy aprovada na Task 2)
    - `NEXT_PUBLIC_WA_NUMBER=5511922324329` configurado nos 3 ambientes Vercel (verificar antes de testar!)
    - `docs/device-test-checklist.md` + `docs/five-second-test-plan.md` documentam os protocolos
  </what-built>
  <how-to-verify>
    **Sub-checkpoint A — Vercel env confirmation (pré-requisito):**
    1. Logar em vercel.com → Project Likro Landing Page → Settings → Environment Variables
    2. Confirmar `NEXT_PUBLIC_WA_NUMBER` existe em Production, Preview e Development com valor `5511922324329`
    3. Se faltando em algum env: adicionar e re-deploy
    4. Abrir preview URL em `next dev` (browser desktop), DevTools → Console → digitar `document.querySelector('[data-testid="whatsapp-cta"]')?.href || "inspect <a>"` — não deve ter `0000000000` em lugar nenhum do DOM

    **Sub-checkpoint B — Lighthouse mobile (HERO-06 + D-19):**
    1. Chrome DevTools (não localhost; usar Vercel preview URL): Lighthouse → Mobile preset → "Simulated Slow 4G + 4x CPU slowdown"
    2. Categoria: Performance (Accessibility/Best Practices/SEO opcionais)
    3. Run 3x (variabilidade); tirar a mediana
    4. **Pass criteria:**
       - Performance score ≥ 85
       - LCP < 2.5s
       - CLS < 0.1
       - (informativo) TBT < 200ms
    5. Salvar JSON report do Lighthouse no repo: `.planning/phases/03-hero-benchmarked-isolado/lighthouse-mobile.json`
    6. PageSpeed Insights: abrir `pagespeed.web.dev`, colar Vercel preview URL, anotar score Mobile e Core Web Vitals

    **Sub-checkpoint C — Real-device test (CTA-04 + HERO-04):**
    Seguir `docs/device-test-checklist.md` completo:
    1. iPhone Safari direto: hero CTA + header CTA abrem WhatsApp
    2. iPhone Instagram IAB: idem (aceitável intermediária `wa.me` page se "Continue to chat" funcionar)
    3. Android Chrome direto: idem
    4. Android Instagram IAB: idem
    5. Confirmar mensagens pré-preenchidas:
       - Hero CTA → `WHATSAPP_MESSAGES.hero` aparece na text box do WhatsApp
       - Header CTA → `WHATSAPP_MESSAGES.header` aparece (texto diferente do hero)

    **Sub-checkpoint D — 5-second test (HERO-07):**
    Seguir `docs/five-second-test-plan.md`:
    1. Lenny manda Vercel preview URL pra 3 pessoas via WhatsApp com o prompt literal
    2. Coletar 3 respostas literais
    3. Avaliar cada resposta nas 3 dimensões (categoria, vertical, CTA)

    **Sub-checkpoint E — Registrar tudo em `03-VERIFICATION.md`:**

    Criar `.planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md` com a estrutura:

    ```markdown
    # Phase 3 — Verification (manual checklist conforme D-19)

    **Vercel preview URL:** `<URL>`
    **Data da verificação:** YYYY-MM-DD
    **Verifier:** Lenny

    ## Vercel env vars (pré-requisito)

    - [x/✗] `NEXT_PUBLIC_WA_NUMBER` em Production = `5511922324329`
    - [x/✗] `NEXT_PUBLIC_WA_NUMBER` em Preview = `5511922324329`
    - [x/✗] `NEXT_PUBLIC_WA_NUMBER` em Development = `5511922324329`

    ## Lighthouse mobile (HERO-06)

    | Métrica | Target | Run 1 | Run 2 | Run 3 | Mediana | Pass? |
    |---------|--------|-------|-------|-------|---------|-------|
    | Performance | ≥ 85 | _ | _ | _ | _ | _ |
    | LCP | < 2.5s | _ | _ | _ | _ | _ |
    | CLS | < 0.1 | _ | _ | _ | _ | _ |
    | TBT (informativo) | — | _ | _ | _ | _ | — |

    **LCP element identificado pelo Lighthouse:** (mockup `<Image>` OU `<h1>`)
    **Lighthouse report:** [lighthouse-mobile.json](./lighthouse-mobile.json)

    ## PageSpeed Insights

    **URL:** `pagespeed.web.dev/report?url=<encoded vercel url>`
    **Score Mobile:** _
    **Score Desktop:** _
    **Notes:** (qualquer regression flag)

    ## Real-device test (CTA-04, HERO-04)

    | Device | Browser/IAB | CTA testado | WhatsApp app abriu? | Mensagem correta? | Notas |
    |--------|-------------|-------------|---------------------|-------------------|-------|
    | iPhone | Safari direto | Hero primary | _ | _ | _ |
    | iPhone | Safari direto | Header secondary | _ | _ | _ |
    | iPhone | Instagram IAB | Hero primary | _ | _ | _ |
    | iPhone | Instagram IAB | Header secondary | _ | _ | _ |
    | Android | Chrome direto | Hero primary | _ | _ | _ |
    | Android | Chrome direto | Header secondary | _ | _ | _ |
    | Android | Instagram IAB | Hero primary | _ | _ | _ |
    | Android | Instagram IAB | Header secondary | _ | _ | _ |

    ## 5-second test (HERO-07)

    | Pessoa | Relação | Resposta literal | Categoria? | Vertical (clínica)? | CTA (WhatsApp)? | Veredicto |
    |--------|---------|------------------|------------|---------------------|-----------------|-----------|
    | 1 | _ | "_" | ✓/✗ | ✓/✗ | ✓/✗ | _ |
    | 2 | _ | "_" | ✓/✗ | ✓/✗ | ✓/✗ | _ |
    | 3 | _ | "_" | ✓/✗ | ✓/✗ | ✓/✗ | _ |

    **Resultado final:** pass / soft pass / fail

    ## Copy approval (COPY-04)

    - **Variante aprovada:** v1 / v2 / v3
    - **PR link:** `https://github.com/.../pull/XX`
    - **Data merge:** YYYY-MM-DD

    ## Veredicto Phase 3

    - [_] Lighthouse mobile pass (Performance ≥ 85, LCP < 2.5s, CLS < 0.1)
    - [_] Real-device pass obrigatório (Safari iOS direto + Chrome Android direto)
    - [_] Real-device IAB Instagram desejável (iOS + Android)
    - [_] 5-second test pass (≥2 acertos)
    - [_] Copy aprovada por Lenny via PR
    - [_] STATE.md atualizado (Phase 3 complete)

    **Status Phase 3:** complete / blocked-on-<item>
    ```

    Preencher TODOS os campos com valores reais (não placeholders). Anexar lighthouse-mobile.json se possível.
  </how-to-verify>
  <resume-signal>Após preencher `03-VERIFICATION.md` com todos os 4 sub-checkpoints aprovados (ou soft pass documentado), sinalizar "Phase 3 verified, prosseguir Task 4 STATE.md update"</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Atualizar STATE.md e ROADMAP.md — Phase 3 complete, desbloquear Phase 4</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/STATE.md (estado atual — verificar progress e accumulated context)
    - .planning/ROADMAP.md §"Progress" + §"Phase 3" (atualizar status)
    - .planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md (criado/preenchido na Task 3 — confirmar pass antes de marcar complete)
  </read_first>
  <action>
    **NOTA:** Só executar esta task APÓS Task 3 ter veredicto "complete" em `03-VERIFICATION.md`. Se algum sub-checkpoint falhou (Lighthouse LCP > 2.5s, real-device fail, 5-second fail < 2 acertos), NÃO marcar complete — voltar ao Plan 02 ou abrir issue.

    **Edição 1 — `.planning/STATE.md`:**

    Bloco frontmatter:
    ```yaml
    progress:
      total_phases: 7
      completed_phases: 3
      total_plans: 13   # 4 + 6 + 3 = 13
      completed_plans: 13
      percent: 100
    ```

    "Current Position":
    ```markdown
    **Milestone:** v1 (initial launch — clínicas vertical)
    **Phase:** 4 of 7 (narrative sections)
    **Plan:** Not started
    **Status:** Ready to plan
    ```

    "Performance Metrics" — preencher com valores reais do Lighthouse:
    ```markdown
    | Lighthouse Performance (mobile) | ≥ 85 | <valor do mediana> |
    | LCP mobile | < 2.5s | <valor mediana> |
    | CLS | < 0.1 | <valor mediana> |
    ```

    "Open Todos / Pendências":
    - Remover bullet "🟡 PENDENTE — Antes da Phase 5: Webhook target" PERMANECE (não muda nesta plan)
    - Confirmar que "Número WhatsApp Likro" já foi removido pelo Plan 01

    "Session Continuity":
    ```markdown
    **Last session work:** Phase 3 (Hero benchmarked isolado) closed. 3 plans mergeados:
    - 03-01: env vars + WhatsAppLocation "header" + grep tests Wave 0 + mockup asset
    - 03-02: Hero + Header + content/hero.ts (3 variantes) + page.tsx
    - 03-03: docs (copy-review, device-test, five-second-test) + PR template + 03-VERIFICATION.md preenchido (Lighthouse mobile LCP <valor>, real-device iPhone+Android pass, 5-second test <pass/soft pass>)
    Variante de copy aprovada: <v1/v2/v3 escolhida por Lenny>. Vercel preview URL: <url>.

    **Next session:**
    1. Run `/gsd-ui-phase 4` ou `/gsd-discuss-phase 4` para gather context da Phase 4 (Narrative Sections — Pain → Bridge → Product → HowItWorks → Proof)
    2. Pendências ainda abertas: autorização Dolce Home (Phase 4 — antes da Proof entrar em dev), webhook target form (Phase 5)
    3. Convenção copy-review estabelecida em docs/copy-review.md — reutilizar nas 5 seções narrativas
    ```

    "State invariants" — adicionar 1 invariant nova:
    ```markdown
    - Convenção copy-review-via-PR estabelecida em Phase 3; toda nova seção em `src/content/*.ts` segue o template (3 variantes, gate antes do merge)
    ```

    **Edição 2 — `.planning/ROADMAP.md`:**

    Localizar §"Phase 3" e mudar status:
    ```markdown
    - [x] **Phase 3: Hero (benchmarked isolado)** — Hero deployado sozinho na Vercel com LCP < 2.5s mobile verificado, copy aprovada por Lenny, helper WhatsApp validado em devices reais. (completed YYYY-MM-DD)
    ```

    Localizar §"Phase 3" detalhes e adicionar:
    ```markdown
    **Plans:** 3 plans (3 waves)
    Plans:
    - [x] 03-01-PLAN.md — env vars + WhatsAppLocation extension + grep tests Wave 0 + mockup asset
    - [x] 03-02-PLAN.md — Hero + Header + content/hero.ts + page.tsx
    - [x] 03-03-PLAN.md — docs/copy-review + PR template + Vercel benchmark + real-device + 5-second
    ```

    Tabela "Progress":
    ```markdown
    | 3. Hero (benchmarked isolado) | 3/3 | Complete | YYYY-MM-DD |
    ```

    Atualizar header se aplicável:
    ```markdown
    **Total v1 requirements:** 99
    **Coverage:** 99/99 (100%)
    **Phases completed:** 3 of 7
    ```

    Traceability table — marcar HERO-01..07, COPY-01, COPY-04, CTA-04 como Done:
    ```markdown
    | HERO-01 | Phase 3 | Done |
    | HERO-02 | Phase 3 | Done |
    | HERO-03 | Phase 3 | Done |
    | HERO-04 | Phase 3 | Done |
    | HERO-05 | Phase 3 | Done |
    | HERO-06 | Phase 3 | Done |
    | HERO-07 | Phase 3 | Done |
    | COPY-01 | Phase 3 | Done |
    | COPY-04 | Phase 3 | Done |
    | CTA-04  | Phase 3 | Done |
    ```

    `Last updated` na primeira linha do roadmap (se houver) — atualizar pra data do merge.

    Mitigation: NÃO criar inconsistência (ex: marcar HERO-06 done sem ter Lighthouse pass real registrado). Se algum requirement do conjunto teve soft pass ou fail no 03-VERIFICATION.md, marcar `Partial` ou `Pending` em vez de `Done` e adicionar nota.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/STATE.md','utf8'); if(!s.includes('Phase 3') || !s.includes('completed_phases: 3')) throw new Error('STATE.md not updated'); const r=require('fs').readFileSync('.planning/ROADMAP.md','utf8'); if(!r.includes('[x] **Phase 3:')) throw new Error('ROADMAP Phase 3 not marked done'); console.log('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter `completed_phases: 3` e `total_plans: 13` e `completed_plans: 13`
    - `.planning/STATE.md` "Current Position" diz "Phase: 4 of 7" e "Status: Ready to plan"
    - `.planning/STATE.md` "Session Continuity" referencia explicitamente os 3 plans da Phase 3 e a variante de copy aprovada
    - `.planning/STATE.md` "Performance Metrics" tem valores numéricos reais nas linhas LCP/CLS/Performance (não placeholders `—`)
    - `.planning/ROADMAP.md` Phase 3 marcada `[x]` com data
    - `.planning/ROADMAP.md` Phase 3 lista os 3 plans com `[x]`
    - `.planning/ROADMAP.md` traceability table tem `HERO-01..07`, `COPY-01`, `COPY-04`, `CTA-04` com status `Done`
    - Phase 4 NÃO está marcada `[x]` (continua pending — só Phase 3 fechou)
  </acceptance_criteria>
  <done>
    Phase 3 oficialmente fechada. STATE.md reflete realidade do merge. Roadmap traceability consistente. Próxima sessão (Phase 4) abre com contexto limpo e os 10 requirements da Phase 3 corretamente flagged como Done.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documentation surface | Docs (copy-review, device-test, five-second) são markdown estático — sem runtime; risk surface mínimo |
| PR template → reviewer | PR template injeta seção "Copy review" — reviewer (Lenny) é trust-source autoritativo do conteúdo da copy |
| Vercel preview URL → real-device | Real-device test confirma WhatsApp app foreground; intermediária IAB Instagram é UX subóptima conhecida, não vulnerabilidade |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-02 | Tampering | `wa.me` URL no real-device | mitigate | Real-device checklist (`docs/device-test-checklist.md`) confirma que iOS Universal Link / Android Intent resolvem `wa.me/<phone>` direto pro app WhatsApp, sem `web.whatsapp.com` interstitial. Esta plan apenas valida (não modifica) o helper `buildWhatsAppUrl` de Phase 1. |
| T-3-04 | Spoofing | iOS IAB redirector | accept | Instagram in-app browser pode mostrar página intermediária `wa.me` com botão "Continue to chat" em alguns devices iOS — comportamento do IAB Instagram, não do site Likro. URL final permanece canônica `wa.me/<phone>?text=...`. Documentado como aceitável em `docs/device-test-checklist.md` §"Pass criteria". Sem ação de mitigação aqui — risco residual aceito. |
</threat_model>

<verification>
**Comandos após Task 1 (criação dos docs):**

1. `ls docs/copy-review.md docs/device-test-checklist.md docs/five-second-test-plan.md .github/PULL_REQUEST_TEMPLATE.md` — todos existem
2. `wc -l docs/*.md` — confirma min_lines (60/40/30)
3. `grep -l "copy-review" .github/PULL_REQUEST_TEMPLATE.md` — link presente

**Após Task 2 (Lenny approval, MANUAL):**

4. PR do Plan 02 mergeado com decisão de copy registrada como commit (comentário "LGTM v1/v2/v3" OU commit alterando o `HERO_COPY = ...` line OU edit inline)

**Após Task 3 (benchmarks manuais):**

5. `.planning/phases/03-hero-benchmarked-isolado/03-VERIFICATION.md` existe e tem todos os campos preenchidos com valores reais
6. Lighthouse JSON report anexado em `.planning/phases/03-hero-benchmarked-isolado/lighthouse-mobile.json` (opcional mas recomendado)
7. Veredicto explícito "complete" ou "soft pass" registrado no fim do VERIFICATION.md

**Após Task 4 (state update):**

8. `grep "completed_phases: 3" .planning/STATE.md` retorna 1 match
9. `grep "[x] \*\*Phase 3:" .planning/ROADMAP.md` retorna 1 match
10. Git diff dos 2 arquivos mostra apenas atualizações de progress (não destrutivas)
</verification>

<success_criteria>
1. 4 docs operacionais existem (copy-review, device-test, five-second, PR template) e formam a convenção reutilizável pra Phase 4.
2. Lenny aprovou (ou editou) explicitamente uma variante de copy via PR — git history serve como record permanente da decisão.
3. Vercel preview Lighthouse mobile registra Performance ≥ 85, LCP < 2.5s, CLS < 0.1 — 3 runs com mediana documentada em `03-VERIFICATION.md`.
4. WhatsApp deep link real-device pass: Safari iOS direto + Chrome Android direto abrem app WhatsApp em ambos os CTAs (hero + header) com mensagem PT-BR correta. Instagram IAB pass desejável (com intermediária aceitável).
5. 5-second test pass: ≥2 das 3 respostas mencionam categoria (plataforma/CRM/operação) + vertical (clínica/estética) + CTA (WhatsApp).
6. `03-VERIFICATION.md` populado com valores reais (não placeholders) — auditável post-merge.
7. STATE.md + ROADMAP.md refletem Phase 3 complete; Phase 4 está unlocked com contexto limpo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-hero-benchmarked-isolado/03-03-SUMMARY.md` documentando:
- Os 4 docs criados (paths + min_lines confirmados)
- Variante de copy aprovada por Lenny (v1/v2/v3) + link do PR de Plan 02 mergeado
- Resumo numérico do Lighthouse mobile (Performance, LCP, CLS mediana)
- Resumo do real-device test (4 cenários × 2 CTAs = 8 testes; quantos pass / quantos com intermediária IAB)
- Resumo do 5-second test (3 respostas resumidas + veredicto)
- Vercel preview URL final
- Confirmação STATE.md + ROADMAP.md atualizados
- Convenção operacional pra Phase 4: PR template em `.github/`, `docs/copy-review.md` é o source-of-truth
</output>
