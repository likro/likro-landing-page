---
phase: 08-hero-travessia-a-travessia-da-luz-milestone-v2-0
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - src/app/preview/_components/LightField.tsx
  - src/app/preview/_components/Travessia.tsx
  - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-VALIDATION.md
autonomous: false
requirements: [TACC-01, TACC-02, TVER-01, TVER-02]

must_haves:
  truths:
    - "TVER-01 (5 quadros): 5 screenshots em progress {0,.25,.5,.75,1} são 5 momentos distintos da MESMA jornada — monotônicos (frio→quente, disperso→contido), nada volta ao começo"
    - "TVER-02 (retrospecto): Lenny no browser real sente que percorreu distância emocional/visual; chegada parece conquistada"
    - "prefers-reduced-motion MATA o optic flow (fonte de enjoo), mantém profundidade ESTÁTICA + atmosfera, e apresenta caos→ordem como antes/depois — narrativa intacta, sem movimento vestibular"
    - "Ladder de degradação em runtime (count→DPR→ruído→estático) + Foco de Expansão central estável + parallax ≤0.5 + sem expansão+rotação+pan simultâneos"
  artifacts:
    - path: "src/app/preview/_components/LightField.tsx"
      provides: "Caminho reduced-motion (optic flow OFF, profundidade estática, caos→ordem antes/depois) + ladder de degradação"
      contains: "reduced"
    - path: ".planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-VALIDATION.md"
      provides: "Harness dos 5 quadros (passos Playwright determinísticos) + checklist objetivo TPRF/TACC/TBND + 5 screenshots anexados"
      contains: "5 quadros"
  key_links:
    - from: "useDeviceTier() === 'reduced'"
      to: "versão estática-premium (sem optic flow)"
      via: "branch reduced no LightField e Travessia"
      pattern: "reduced"
    - from: "harness Playwright"
      to: "TVER-01"
      via: "scroll determinístico a progress {0,.25,.5,.75,1} → 5 screenshots distintos"
      pattern: "0\\.25|0\\.5|0\\.75"
---

<objective>
Travar **acessibilidade/segurança vestibular** (reduced-motion + ladder de degradação) e **provar a aceitação**: o harness `[BLOCKING]` dos 5 quadros (TVER-01) e o gate humano do retrospecto (TVER-02). Esta é a fase de verificação — o produto perceptual encontra a régua.

Purpose: `prefers-reduced-motion` precisa entregar a história caos→ordem SEM o optic flow (fonte de enjoo) — narrativa intacta como antes/depois (TACC-01). E a fase só fecha quando os 5 quadros são distintos e o Lenny aprova no browser real.
Output: caminho reduced-motion estático-premium nos dois componentes + ladder de degradação + `08-VALIDATION.md` com os 5 screenshots e o checklist objetivo + checkpoint humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-RESEARCH.md
@.planning/research/HERO-V2-RESEARCH.md
@src/app/preview/_components/LightField.tsx
@src/app/preview/_components/Travessia.tsx
@src/hooks/use-device-tier.ts

<interfaces>
<!-- Engine (plano 01) + narrativa (02) + atmosfera/copy (03) já prontos. -->
<!-- useDeviceTier() retorna 'reduced' quando prefers-reduced-motion OU conexão lenta. -->
<!-- LightField já recebe { progress, active }; o loop lê progressRef. -->
<!-- Validação: NÃO há Playwright instalado como dep — usar o Playwright MCP (mcp__plugin_playwright_playwright__*)
     para o harness, e documentar os passos determinísticos em 08-VALIDATION.md.
     Scroll determinístico a partir do rect do palco:
     scrollY = sectionTop + targetProgress * (sectionHeight - innerHeight). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: prefers-reduced-motion estático-premium + ladder de degradação</name>
  <read_first>
    - src/app/preview/_components/LightField.tsx (engine + narrativa dos planos 01/02 — elevar o branch reduced)
    - src/app/preview/_components/Travessia.tsx (atmosfera/copy do plano 03 — confirmar branch reduced)
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md (bloco "Acessibilidade & segurança vestibular")
    - .planning/research/HERO-V2-RESEARCH.md §7 (reduced-motion: mata optic flow, mantém profundidade estática + atmosfera, caos→ordem como antes/depois)
    - src/hooks/use-device-tier.ts
  </read_first>
  <action>
    **reduced-motion no LightField (TACC-01) — autorar o estático-premium:** quando `tier === "reduced"`:
    - MATAR o optic flow: NÃO aplicar o `zShift`/dolly por progress; sem fluxo radial animado, sem micro-shimmer no tempo. (O optic flow é a fonte de enjoo.)
    - MANTER profundidade ESTÁTICA: oclusão por baldes + tamanho relativo + perspectiva atmosférica continuam (são seguros, não se movem).
    - Apresentar caos→ordem como **antes/depois**: renderizar o estado ORDENADO por padrão (ex.: desenhar com progress fixo ~0.92), OU um fade de opacidade ≤200ms entre o quadro de caos e o de ordem — NÃO uma viagem. Narrativa intacta, zero movimento vestibular.
    - Sem RAF macro autônomo no reduced (1 quadro estático + redraw só em resize), como o código atual já faz.

    **TACC-02 (confirmar invariantes):** Foco de Expansão central e estável (já no plano 01); fluxo acoplado ao scroll, NUNCA autoplay; parallax ≤0.5 (já); NUNCA expansão+rotação+pan simultâneos — garantir que não há rotação global do campo somada ao optic flow.

    **Ladder de degradação em runtime (TPRF-03 reforço):** medir o tempo de frame (média móvel simples sobre N frames); se acima de um teto (~22–24ms ≈ <45fps sustentado), rebaixar em ordem: (1) reduzir `count` (descartar partículas), depois (2) reduzir `dpr`, depois (3) reduzir resolução do ruído (passo maior), por fim (4) cair pra estático. Implementar ao menos os 2 primeiros degraus (count→DPR) de forma real; documentar os degraus 3–4 como fallback.

    Confirmar também no `Travessia.tsx` que o branch `reduced` da atmosfera/copy entrega valores estáticos (sem y/opacity animados na copy; atmosfera em estado quente/ordenado).

    Não quebrar tsc nem brand-lock; roxo via rgba(124,58,237,...).
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx tsc --noEmit && npx vitest run tests/brand-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep em `src/app/preview/_components/LightField.tsx`: o branch `reduced` desenha o estado ordenado estático (ex.: `drawFrame(0.92` ou equivalente) e NÃO entra no loop de optic flow (sem `requestAnimationFrame` macro quando reduced — só redraw em resize).
    - grep: contém uma medida de tempo de frame e um rebaixamento de `count`/`dpr` (ladder — `performance.now()` diff e ajuste de count/dpr).
    - grep: ausência de rotação global do campo combinada ao flow (sem um `rotate`/`globalAngle` aplicado a todas as partículas por frame junto do zShift).
    - grep em `Travessia.tsx`: o branch `reduced` da copy usa `style={reduced ? undefined : ...}` (estado final imediato).
    - grep: zero `#7c3aed`/`#6d28d9` literais em ambos.
    - `npx tsc --noEmit` sem erros; `tests/brand-lock.test.ts` passa.
  </acceptance_criteria>
  <done>Em reduced-motion: optic flow OFF, profundidade estática + atmosfera mantidas, caos→ordem como antes/depois — narrativa intacta, sem enjoo. Ladder count→DPR funciona em runtime. tsc + brand-lock verdes.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING]: Harness dos 5 quadros + checklist objetivo (08-VALIDATION.md)</name>
  <read_first>
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-RESEARCH.md (Validation Architecture — TVER-01/02, TPRF, TACC, TBND)
    - .planning/REQUIREMENTS.md (seção Milestone v2.0 — TVER-01, TVER-02)
    - src/app/preview/_components/Travessia.tsx (a fórmula do progress manual = base do scroll determinístico do harness)
  </read_first>
  <action>
    Pipeline padrão do Lenny PRIMEIRO: code review (skill `requesting-code-review`) sobre o diff dos planos 01–04; corrigir achados reais antes de seguir.

    Subir o dev server em background gerenciado (`npm run dev` — portas 3000–3010 podem estar ocupadas; checar a porta efetiva). NÃO usar subshell `&` que morre.

    **Harness dos 5 quadros (TVER-01) via Playwright MCP:** navegar `/preview`; para cada `targetProgress ∈ {0, 0.25, 0.5, 0.75, 1}`, calcular o scroll determinístico a partir do rect do palco — `scrollY = sectionTop + targetProgress * (sectionHeight - innerHeight)` — aplicar (`window.scrollTo`), aguardar estabilização (alguns frames), e capturar 1 screenshot. Salvar os 5 como `uat-08-frame-00.png`, `uat-08-frame-25.png`, `uat-08-frame-50.png`, `uat-08-frame-75.png`, `uat-08-frame-100.png` na raiz do projeto (padrão dos UATs anteriores).

    **Critério objetivo dos 5 quadros:** confirmar que são visualmente DISTINTOS e MONOTÔNICOS (frio→quente, disperso→contido, roxo aparecendo só no fim); nenhum repete outro; nada volta ao começo. Anexar/referenciar os 5 no 08-VALIDATION.md.

    **Checklist objetivo no 08-VALIDATION.md:**
    - TPRF: code-read confirma 1 RAF de desenho + canvas pós-hidratação + progress sem React state por frame + DPR≤1.5 + zero ctx.filter/shadowBlur por frame. Lighthouse mobile na rota `/preview` (se a rota gated dificultar, rodar contra a build local de dev e anotar): LCP não regride vs ~2.3s, CLS=0.
    - TACC: toggle `prefers-reduced-motion` (emulado no Playwright/DevTools) → optic flow some, versão estática-premium aparece, história caos→ordem legível como antes/depois; console sem erros JS.
    - TBND: `tests/brand-lock.test.ts` passa; grep confirma sem WebGL (`getContext('webgl'`) e sem `<video` autoplay; nota de que `/preview` 404 em produção (gate herdado).

    Criar `08-VALIDATION.md` com: resumo, os 5 screenshots (caminhos), o checklist acima preenchido com PASS/observações, e a seção "TVER-02 — gate humano (pendente Lenny)".
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx vitest run tests/brand-lock.test.ts && node -e "const fs=require('fs');const p='.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-VALIDATION.md';const t=fs.readFileSync(p,'utf8');if(!/5 quadros/.test(t)||!/0\.25/.test(t)){process.exit(1)}console.log('VALIDATION ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Existem 5 arquivos `uat-08-frame-00.png` … `uat-08-frame-100.png` na raiz do projeto.
    - `08-VALIDATION.md` existe, contém a string "5 quadros", referencia os 5 screenshots, e tem o checklist TPRF/TACC/TBND com marcações PASS/observação.
    - grep na codebase: zero `getContext("webgl"` / `getContext('webgl'` e zero `<video` com `autoplay` (TBND-02).
    - `tests/brand-lock.test.ts` passa.
    - Code review (skill) rodado sobre o diff; achados reais corrigidos.
  </acceptance_criteria>
  <done>Os 5 quadros capturados e julgados distintos/monotônicos; 08-VALIDATION.md com checklist objetivo TPRF/TACC/TBND preenchido + seção do gate humano. Code review limpo, console sem erros.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [CHECKPOINT]: Lenny valida a travessia no browser real (TVER-01 final + TVER-02)</name>
  <action>Tarefa de checkpoint (sem código): apresentar a travessia ao Lenny no browser real e os 5 screenshots de 08-VALIDATION.md; coletar o veredito dos 2 testes de aceite (TVER-01 + TVER-02). Não modifica arquivos.</action>
  <what-built>
    A travessia completa em `/preview`: campo de luz pseudo-3D atravessável (optic flow + profundidade), 5 momentos caos→ordem por target-lerp, arco de escala (aberto→íntimo), atmosfera frio→quente, hero exit de vetores opostos, roxo escasso na chegada, reduced-motion estático-premium. Os 5 screenshots e o checklist objetivo estão em `08-VALIDATION.md`.
  </what-built>
  <how-to-verify>
    1. Subir `npm run dev` (a porta efetiva está no terminal — 3000–3010 podem estar ocupadas) e abrir `/preview` no browser real.
    2. **TVER-01 (5 quadros):** rolar devagar do topo ao fim. Confirmar que percebe 5 momentos distintos e reconhecíveis da MESMA jornada (caos frio/disperso → entrada → travessia/convergência → quase-ordem → ordem conquistada); nada volta ao começo. Comparar com os 5 screenshots em `08-VALIDATION.md`.
    3. **TVER-02 (retrospecto):** ao chegar no fim, olhar mentalmente pra trás — sentiu que ATRAVESSOU um espaço (não viu um efeito de fora)? O estado final parece CONQUISTADO (limpo, contido, quente, roxo no auge da escassez), claramente diferente do começo?
    4. Mobile: abrir no celular (ou DevTools mobile) — a travessia recebe tratamento premium, fluida, sem jank.
    5. reduced-motion: ativar "Reduzir movimento" no OS — confirmar que o optic flow some e a história caos→ordem ainda se lê como antes/depois, sem enjoo.
  </how-to-verify>
  <resume-signal>Digite "aprovado" se a travessia passa nos 2 testes de aceite, ou descreva o que ainda não entrega a sensação de jornada percorrida (vira insumo pra gap-closure).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→render | Client-only canvas + atmosfera; sem inputs/dados/PII novos. O único output externo é o `WhatsAppCta` existente (helper `wa.me/` validado + env). |
| dev-gate | `/preview` 404 em produção via `assertDevAccess()` (herdado). Harness roda só em dev. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-08 | Denial of Service | ladder de degradação / RAF | mitigate | Ladder rebaixa count→DPR sob carga; pause offscreen/oculto (plano 01) intacto; reduced-motion = 1 quadro estático. |
| T-08-09 | Information Disclosure | 08-VALIDATION.md / screenshots | accept | Artefatos de planning locais, sem PII (canvas abstrato, sem dados de lead). Baixo risco. |
| T-08-10 | Tampering | nenhum input/PII | accept | Client-only; sem novo attack surface. Baixo risco. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `tests/brand-lock.test.ts` passa.
- 5 screenshots `uat-08-frame-*.png` gerados; `08-VALIDATION.md` com checklist objetivo TPRF/TACC/TBND.
- Toggle reduced-motion: optic flow some, estático-premium aparece, caos→ordem legível como antes/depois, console limpo.
- Grep: sem WebGL, sem `<video autoplay`.
- Checkpoint humano: Lenny aprova os 2 testes de aceite no browser real.
</verification>

<success_criteria>
- TVER-01: 5 quadros distintos/monotônicos confirmados (screenshots + Lenny).
- TVER-02: Lenny sente jornada percorrida + chegada conquistada no browser real.
- TACC-01/02: reduced-motion entrega a história sem optic flow; invariantes vestibulares respeitados; ladder de degradação ativo.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-04-SUMMARY.md`
</output>
