---
phase: 08-hero-travessia-a-travessia-da-luz-milestone-v2-0
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/preview/_components/Travessia.tsx
autonomous: true
requirements: [TRV-06, TRV-08, TRV-09, TBND-01]
user_setup: []

must_haves:
  truths:
    - "Atmosfera evolui contínua e monotônica: frio/tensão → quente/calma, acoplada ao scroll; nada volta ao começo"
    - "Hero exit de vetores opostos: no 1º scroll o campo recua/afunda enquanto a headline sobe e dissolve — entrega o usuário na travessia"
    - "Copy vive só no topo e some cedo conforme a travessia assume o palco"
    - "Brand-lock: roxo escasso só acento; fundo dark TINGIDO (roxo-navy, nunca preto puro — anti-banding); Inter com ênfase em itálico da mesma família; film grain mata banding"
  artifacts:
    - path: "src/app/preview/_components/Travessia.tsx"
      provides: "Camadas de atmosfera (vinheta fria recua, vinheta de tensão fecha-no-caos-abre-na-calma, bloom quente cresce, banho quente, vinheta de enquadramento, film grain) + hero exit + copy editorial"
      contains: "useTransform"
  key_links:
    - from: "progress (MotionValue)"
      to: "camadas de atmosfera + copy + hero exit"
      via: "useTransform sobre progress, monotônico"
      pattern: "useTransform\\(progress"
    - from: "hero exit"
      to: "entrega na travessia"
      via: "headline y↑+opacity→0 enquanto campo/atmosfera recua (vetores opostos)"
      pattern: "copyY|copyOpacity"
---

<objective>
Sobre o palco do plano 01, vestir a **atmosfera evolutiva** (frio/tensão→quente/calma, monotônica), o **hero exit de vetores opostos** e a **copy editorial só no topo** — a outra metade da sensação de jornada e o que torna a travessia premium (luz com fonte, escuros tingidos, grain).

Purpose: a atmosfera acima dos objetos é o que vende "premium" (08-RESEARCH §1.2); o hero exit é o gesto que entrega o usuário na travessia (Cairn, §4); a copy só no topo valida a experiência visual pura antes de amplificar (08-CONTEXT).
Output: `Travessia.tsx` com camadas de atmosfera acopladas ao progress, grain overlay, hero exit e copy que some cedo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-RESEARCH.md
@.planning/research/HERO-V2-RESEARCH.md
@src/app/preview/_components/Travessia.tsx
@src/content/hero.ts

<interfaces>
<!-- Este plano ESTENDE o Travessia criado no plano 01 (palco + progress manual + lifecycle). -->
<!-- progress: MotionValue<number> já existe; consumir via useTransform. NÃO mexer no rAF de progress nem no mount/pause. -->

De src/content/hero.ts:
```typescript
export const HERO_COPY = {
  h1: string; sub: string;
  ctaPrimary: { label: string };
  ctaSecondary: { label: string; href: string };
  trust: string;
  // ...
};
```
De src/components/ui/whatsapp-cta.tsx: `<WhatsAppCta variant="primary" location="hero">{label}</WhatsAppCta>`
De src/components/ui/container.tsx: `<Container>`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Atmosfera evolutiva monotônica + escuros tingidos + film grain</name>
  <read_first>
    - src/app/preview/_components/Travessia.tsx (palco do plano 01 — as camadas de atmosfera placeholder já existem; ELEVAR pros 5 beats e adicionar vinheta de enquadramento + grain)
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md (bloco "Atmosfera" + "Arco de escala" vinheta de enquadramento)
    - .planning/research/HERO-V2-RESEARCH.md §5 (luz numa sala escura: glows com fonte, vinheta, grain 3–7%, escuros tingidos roxo-navy, nunca #000)
    - src/lib/brand-tokens.ts (cores canônicas — referência; NÃO importar hex pro JSX)
  </read_first>
  <action>
    Esculpir as camadas de atmosfera consumindo `progress` via `useTransform`, todas MONOTÔNICAS (TRV-06 — nada volta ao começo):
    - **Vinheta fria no topo (recua):** `coldOpacity = useTransform(progress, [0, 0.55], [0.85, 0])` — radial-gradient frio (rgba azul-navy) que some.
    - **Vinheta de tensão (fecha no caos, abre na calma):** `tensionVignette = useTransform(progress, [0, 0.62], [0.55, 0.1])` — bordas escuras fechadas no começo, abrem.
    - **Bloom quente central (cresce):** `warmBloom = useTransform(progress, [0.28, 1], [0, 0.62])` — glow com FONTE/direção (não decoração), centrado no Foco de Expansão; pode tocar rgba(124,58,237,...) discreto no halo.
    - **Banho quente de ambiente (no fim):** `warmWash = useTransform(progress, [0.5, 1], [0, 0.4])`.
    - **Vinheta de ENQUADRAMENTO (TRV-04, arco de escala — abre no meio, fecha no fim):** nova camada cuja abertura cresce até ~0.5 e fecha até 1.0 — `useTransform(progress, [0, 0.5, 1], [valFechado, valAberto, valFechadoIntimo])` — acompanha o footprint contraindo do plano 02 (enquadramento íntimo no fim).

    **Escuros tingidos (TBND-01 — NUNCA #000):** o fundo base do palco/page usa surface dark tingido de roxo-navy (já há `bg-surface-darker`; confirmar que é tingido, não preto puro). Gradients usam rgba navy/roxo muito escuro, nunca `#000`/`rgb(0,0,0)` chapado.

    **Film grain (TBND-01 — mata banding, 3–7%):** adicionar uma camada overlay de grain estático com `mix-blend-mode: overlay` (ou `soft-light`) e opacidade ~0.04–0.06. Implementar como um pequeno data-URI SVG `feTurbulence` ou um tile de ruído repetido (`background-image` repeat) — SEM custo por frame, é estático.

    Manter o remate inferior (handoff). Todas as camadas `aria-hidden` e `pointer-events-none`. Em reduced-motion, valores estáticos (refinado no plano 04 — aqui só não quebrar o caminho `reduced ? ... : motionValue` já presente).
    Brand-lock: roxo só acento; NÃO usar hex `#7c3aed`/`#6d28d9` literais (use rgba(124,58,237,...)).
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx vitest run tests/brand-lock.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - grep em `src/app/preview/_components/Travessia.tsx`: contém `useTransform(progress` para coldOpacity, tensionVignette, warmBloom e a vinheta de enquadramento (≥4 camadas atmosféricas dirigidas por progress).
    - grep: contém uma camada de grain (`mix-blend` + `feTurbulence` OU `background-image` de ruído) com opacidade entre 0.03 e 0.07.
    - grep: NÃO contém `rgb(0,0,0)` nem `#000` nem `#000000` chapado em background (escuros tingidos).
    - grep: zero `#7c3aed`/`#6d28d9` literais; roxo via `rgba(124,58,237`.
    - `npx tsc --noEmit` sem erros; `tests/brand-lock.test.ts` passa.
  </acceptance_criteria>
  <done>Atmosfera evolui monotônica frio/tensão→quente/calma com vinheta de enquadramento (íntimo no fim), escuros tingidos (sem preto puro) e film grain estático. tsc + brand-lock verdes.</done>
</task>

<task type="auto">
  <name>Task 2: Hero exit de vetores opostos + copy editorial só no topo</name>
  <read_first>
    - src/app/preview/_components/Travessia.tsx (com a atmosfera da task 1)
    - .planning/research/HERO-V2-RESEARCH.md §4.5 (hero exit de vetores opostos: campo recua / headline sobe+dissolve)
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md (bloco "Copy")
    - src/content/hero.ts (HERO_COPY — reusar)
  </read_first>
  <action>
    **Copy só no topo, some cedo (TRV-09):** manter o bloco de copy (headline Inter + sub + `WhatsAppCta location="hero"` + CTA secundário anchor). Headline com ênfase em itálico da MESMA família Inter (`<em className="italic ...">sua clínica.</em>` — já é o padrão; SEM serif — TBND-01). Reusar `HERO_COPY.sub`, `HERO_COPY.ctaPrimary.label`, `HERO_COPY.ctaSecondary.label/href`. `H1_LEAD`/`H1_EMPHASIS` inline permitidos (protótipo /preview).

    **Hero exit de VETORES OPOSTOS (TRV-08):** no primeiro scroll, a headline SOBE e DISSOLVE enquanto o campo/atmosfera RECUA/AFUNDA — vetores opostos que entregam o usuário na travessia:
    - Copy sobe + some: `copyY = useTransform(progress, [0, 0.3], [0, -56])`, `copyOpacity = useTransform(progress, [0, 0.12, 0.24], [1, 0.6, 0])` (some CEDO — antes do progress 0.25).
    - Campo recua (vetor oposto): aplicar um leve recuo/afundamento ao bloco visual do palco no início — `fieldRecede = useTransform(progress, [0, 0.25], [0, valorPositivoY])` OU um leve `scale` decrescente da camada de atmosfera/campo, de modo que enquanto a copy sobe (-y) o campo desce/afunda (+y ou scale↓). Os dois vetores opostos devem ser perceptíveis no 1º scroll.

    A copy some cedo (~0.24) pra travessia pura assumir o palco (validar visual sem texto — 08-CONTEXT).
    Em reduced-motion: copy entra/fica no estado final imediatamente (sem y/opacity animados) — manter o `style={reduced ? undefined : {...}}` já presente.
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx tsc --noEmit && npx vitest run tests/brand-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep em `src/app/preview/_components/Travessia.tsx`: contém `copyY` e `copyOpacity` com `useTransform(progress` (copy sobe+dissolve); o range de opacity chega a 0 até ~0.24 (some cedo).
    - grep: contém um vetor de recuo do campo (`fieldRecede` OU um `useTransform(progress` aplicado em `y`/`scale` da camada de campo/atmosfera com sinal OPOSTO ao da copy).
    - grep: usa `HERO_COPY.sub`, `HERO_COPY.ctaPrimary` e `<WhatsAppCta` com `location="hero"`.
    - grep: a ênfase é `italic` (mesma família Inter; sem `font-serif`).
    - `npx tsc --noEmit` sem erros; `tests/brand-lock.test.ts` passa.
  </acceptance_criteria>
  <done>No 1º scroll a headline sobe e dissolve enquanto o campo recua (vetores opostos); a copy some cedo (~0.24) e o resto é luz pura. Copy reusa HERO_COPY; itálico Inter. tsc + brand-lock verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→render | Client-only atmosfera/copy; o único output externo é o `WhatsAppCta` existente (helper `wa.me/` validado + env). Sem inputs/dados/PII novos. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | WhatsAppCta reusado | mitigate | CTA usa helper `buildWhatsAppUrl` (proíbe web.whatsapp.com, valida formato) + `NEXT_PUBLIC_WA_NUMBER`; este plano só reusa o componente, não constrói URL. |
| T-08-07 | Information Disclosure | grain data-URI / gradients | accept | Strings estáticas client-side, sem dados; nenhum vazamento. Baixo risco. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `tests/brand-lock.test.ts` passa.
- Visual (Playwright MCP): rolar `/preview` — atmosfera esquenta monotônica; no 1º scroll headline sobe+dissolve e campo recua (vetores opostos); copy some ~0.24; sem banding (grain). Console sem erros.
</verification>

<success_criteria>
- Atmosfera frio/tensão→quente/calma monotônica + vinheta de enquadramento + escuros tingidos + grain.
- Hero exit de vetores opostos entrega o usuário; copy só no topo, some cedo.
- Brand-lock: roxo escasso acento, Inter itálico, sem preto puro. tsc + brand-lock verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-03-SUMMARY.md`
</output>
