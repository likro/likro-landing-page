---
phase: 08-hero-travessia-a-travessia-da-luz-milestone-v2-0
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/app/preview/_components/LightField.tsx
autonomous: true
requirements: [TRV-02, TRV-04, TRV-05, TRV-07]
user_setup: []

must_haves:
  truths:
    - "A travessia percorre 5 momentos distintos e reconhecíveis no progress (~0 caos · ~.25 entrada · ~.5 travessia/convergência · ~.75 quase-ordem · ~1 ordem conquistada)"
    - "UMA matéria única evolui por morph compartilhado (target-lerp por partícula com easeInOutCubic) — NUNCA crossfade, nunca coleção de efeitos, nunca slideshow"
    - "Arco de escala aberto→envolvente→íntimo: contração do footprint do estado ordenado + dolly do optic flow"
    - "Chegada conquistada: estado final limpo/contido, claramente diferente do começo, com roxo no auge da escassez (só partículas próximas/resolvidas acendem)"
  artifacts:
    - path: "src/app/preview/_components/LightField.tsx"
      provides: "target-lerp caos→ordem (targetX/Y/cor por partícula), envelope de ruído 1→0, 5 beats, contração de footprint, roxo escasso intensificando na chegada"
      contains: "easeInOutCubic"
  key_links:
    - from: "LightField drawFrame"
      to: "estado ordenado (targets)"
      via: "pos = lerp(caosPos, targetPos, easeInOutCubic(progress)) com amplitude de ruído 1→0"
      pattern: "easeInOutCubic"
    - from: "progress"
      to: "5 beats reconhecíveis"
      via: "parâmetros (temperatura, footprint, acendimento roxo) mapeados monotonicamente no progress"
      pattern: "progress"
---

<objective>
Sobre o engine pseudo-3D do plano 01, esculpir a NARRATIVA: caos→ordem como **morph de matéria compartilhada** (target-lerp, nunca crossfade), os **5 momentos distintos**, o **arco de escala** (aberto→envolvente→íntimo) e a **chegada conquistada** com roxo escasso intensificando.

Purpose: o teste dos 5 quadros (TVER-01) e o retrospecto (TVER-02) exigem que a MESMA matéria condense numa ordem reconhecível, sem slideshow. Continuidade do "um espaço" morre num dissolve (08-RESEARCH §6) — por isso target-lerp.
Output: `LightField.tsx` agora conta a jornada caos→ordem em 5 beats com escala fechando no fim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md
@.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-RESEARCH.md
@.planning/research/HERO-V2-RESEARCH.md
@src/app/preview/_components/LightField.tsx

<interfaces>
<!-- Este plano ESTENDE o LightField criado no plano 01. -->
<!-- Engine já provê: partículas com z, projeção focal/(focal+z), optic flow (zShift por progress), -->
<!-- baldes de profundidade, atlas assado, 1 RAF lendo progressRef. -->
<!-- Este plano adiciona ao modelo de partícula: targetX/targetY/targetZ + cor-alvo, e no drawFrame o lerp+beats. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Target-lerp caos→ordem — UMA matéria condensando (sem crossfade)</name>
  <read_first>
    - src/app/preview/_components/LightField.tsx (engine do plano 01 — adicionar targets ao modelo e o lerp ao drawFrame; NÃO recriar a projeção/optic-flow)
    - .planning/research/HERO-V2-RESEARCH.md §6 (morph de matéria compartilhada / target-lerp / ruído envelope 1→0 / estado ordenado abstrato)
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md (bloco "Continuidade — caos → ordem")
  </read_first>
  <action>
    Estender o modelo de partícula com o **estado ordenado** (alvo): `targetAngle`/`targetRadius` (ou `targetX`/`targetY` em frações do canvas) + `targetZ` (mais raso/contido) + flag `accent` (já existe). O estado ordenado = arranjo limpo/calmo (faixas de fluxo alinhadas / lattice / sugestão de estrutura), **ABSTRATO — sem cards de UI, dashboards, chat** (08-CONTEXT). Distribuir alvos em ~5 faixas (BANDS) centradas numa região contida (ver footprint na task 2).

    **Morph por target-lerp (TRV-05 — NUNCA crossfade):** no drawFrame, com `e = easeInOutCubic(progress)`:
    ```
    function easeInOutCubic(x){ return x<0.5 ? 4*x*x*x : 1 - Math.pow(-2*x+2,3)/2; }
    const px = lerp(chaosPos, targetPos, e);   // a MESMA partícula migra
    const noiseAmp = (1 - e) * BASE_AMP;        // envelope do ruído 1→0: o alvo vence no fim
    ```
    A poeira **condensa** na forma ordenada — uma matéria só, sem dois layers cruzando opacidade. Manter o ruído orgânico via sin/cos em camadas (já no engine; SEM nova dep simplex — o contrato permite o fallback sin/cos). Amplitude do ruído com envelope `1→0` do caos pra ordem.

    **Temperatura/brilho monotônicos:** cor lerpa frio→quente e brilho sobe com `progress` (perspectiva da chegada). Reusar a paleta assada do plano 01.

    Garantir que `chaosPos` (disperso, pode vazar a viewport no começo) e `targetPos` (contido) coexistem na MESMA partícula — nada é recriado entre beats.
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx tsc --noEmit && npx vitest run tests/brand-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep em `src/app/preview/_components/LightField.tsx`: contém `easeInOutCubic` e a chamada `lerp(` com o easing aplicado (`easeInOutCubic(`...).
    - grep: contém um termo de alvo por partícula (`target` em `targetAngle`/`targetRadius`/`targetX`).
    - grep: contém o envelope de ruído decrescente (`1 - e` ou `(1 - ` multiplicando a amplitude).
    - grep: NÃO contém `globalAlpha` usado pra crossfade de dois campos (não há segundo conjunto de partículas espelhando o primeiro) — uma matéria só.
    - grep: zero ocorrências literais de "card"/"dashboard"/"chat" como geometria (estado ordenado é abstrato).
    - `npx tsc --noEmit` sem erros; `tests/brand-lock.test.ts` passa.
  </acceptance_criteria>
  <done>A mesma matéria condensa do caos disperso pra uma ordem contida via target-lerp easeInOutCubic, ruído com envelope 1→0. Sem crossfade. tsc + brand-lock verdes.</done>
</task>

<task type="auto">
  <name>Task 2: 5 momentos + arco de escala + chegada conquistada (roxo escasso)</name>
  <read_first>
    - src/app/preview/_components/LightField.tsx (com o target-lerp da task 1)
    - .planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-CONTEXT.md (bloco "Os 5 momentos" + "Arco de escala")
    - .planning/research/HERO-V2-RESEARCH.md §8 (os 5 quadros decididos) + §0 (deslocamento/escala)
  </read_first>
  <action>
    **Os 5 momentos no progress (TRV-02 — devem ser reconhecíveis e distintos):** mapear parâmetros monotônicos de modo que os cortes ~0 / ~.25 / ~.5 / ~.75 / ~1 leiam como 5 quadros diferentes:
    1. `~0.00` caos frio/distante/disperso: footprint AMPLO (partículas espalhadas, podem vazar bordas), cor fria, ruído alto, alpha baixo (dim), z grande (longe).
    2. `~0.25` entrada: `z` começa a cair (optic flow do plano 01 já faz isso) → expansão radial perceptível; direção emerge.
    3. `~0.50` travessia/convergência: scale-through nas bordas (partículas próximas crescem e SAEM pelas bordas — "passei por elas"); poeira lerpando pros alvos; esquenta; PRIMEIRAS partículas próximas acendem roxo.
    4. `~0.75` quase-ordem: estrutura tomando forma (faixas alinhadas); oclusão + atmosférica dão volume; calor/clareza subindo.
    5. `~1.00` ordem conquistada: assentada, quente, limpa, CONTIDA (footprint mínimo central), roxo no auge da escassez.

    **Arco de escala aberto→envolvente→íntimo (TRV-04):** combinar (a) dolly do optic flow (já), (b) **contração do footprint**: um fator `footprint = lerp(1.0, 0.42, easeInOutCubic(progress))` que multiplica `targetRadius`/dispersão — o estado ordenado ocupa uma região central contida no fim, vs. tela toda no começo. (A vinheta de enquadramento que abre-no-meio-fecha-no-fim é do plano 03 — atmosfera.)

    **Chegada conquistada + roxo escasso (TRV-07):** o acendimento roxo das partículas `accent` é gated por (i) proximidade (z pequeno / scale grande) E (ii) `progress` alto — `const purpleGain = clamp((progress - 0.5) / 0.5, 0, 1) * nearness;`. Só uma fração mínima de partículas (`accent` raro, definido no plano 01) e só perto da chegada. A escassez é o payoff. Cor do roxo via rgba(124,58,237,...) (permitido; NÃO hex literal `#7c3aed`).

    Tudo continua scrubbed e contínuo (sem `if (beat===2)` por cena — os beats são REGIÕES do gradiente de parâmetros, não switches).
  </action>
  <verify>
    <automated>cd "C:/Users/lenny/Desktop/Likro/landing_page" && npx tsc --noEmit && npx vitest run tests/brand-lock.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - grep em `src/app/preview/_components/LightField.tsx`: contém `footprint` (contração do estado ordenado, arco de escala).
    - grep: contém um gate de roxo combinando progress alto + proximidade (`progress - 0.5` ou equivalente E um termo de `scale`/`nearness`).
    - grep: zero `if (` que ramifique a renderização por número-de-beat (beats são regiões contínuas, não switches) — confirmar ausência de `switch` sobre beats e de `=== 1`/`=== 2` indexando cenas.
    - grep: zero `#7c3aed`/`#6d28d9` literais; roxo via `rgba(124,58,237`.
    - `npx tsc --noEmit` sem erros; `tests/brand-lock.test.ts` passa.
  </acceptance_criteria>
  <done>Rolando `/preview`, os pontos ~0/.25/.5/.75/1 leem como 5 momentos distintos; o footprint contrai (íntimo no fim); o roxo só acende perto+tarde (escassez = payoff). tsc + brand-lock verdes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→render | Mesma do plano 01: client-only canvas, sem inputs/dados/PII novos. Apenas parâmetros visuais derivados do progress. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Denial of Service | target-lerp + 5-beat math no loop | mitigate | Matemática O(n) por frame (sem alocação por partícula no loop; targets pré-computados no mount). Pause do plano 01 (`active`) intacto. |
| T-08-05 | Tampering | nenhum input/PII | accept | Client-only canvas; sem novo attack surface. Baixo risco. |
</threat_model>

<verification>
- `npx tsc --noEmit` limpo; `tests/brand-lock.test.ts` passa.
- Visual (Playwright MCP): rolar `/preview` e capturar ~0/.25/.5/.75/1 — 5 estados visivelmente distintos, monotônicos (frio→quente, disperso→contido), nada volta ao começo; roxo só no fim. Console sem erros.
- Confirmar via code-read: uma matéria só (sem crossfade), beats como regiões contínuas.
</verification>

<success_criteria>
- 5 momentos reconhecíveis e distintos no progress; morph caos→ordem por target-lerp (sem crossfade).
- Arco de escala fecha (footprint contrai) → chegada íntima e conquistada.
- Roxo escasso intensificando só na chegada. tsc + brand-lock verdes.
</success_criteria>

<output>
Após completar, criar `.planning/phases/08-hero-travessia-a-travessia-da-luz-milestone-v2-0/08-02-SUMMARY.md`
</output>
