---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 01
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - src/content/pain.ts
  - tests/content/pain.test.ts
  - tests/sections/pain-invariants.test.ts
  - src/sections/Pain/index.tsx
  - src/sections/Pain/PainBackground.tsx
  - src/sections/Pain/PainCardConstellation.tsx
  - src/sections/Pain/PainCard.tsx
  - src/sections/Pain/PainStatement.tsx
  - src/app/globals.css
autonomous: false
requirements:
  - NARR-01
  - NARR-06
  - NARR-07
  - COPY-02
  - COPY-03
threats:
  - T-4-04
  - T-4-05
must_haves:
  truths:
    - "Pain section renderiza headline + sub + constelação de 4 pseudo-cards (Instagram DM / WhatsApp / Planilha de agenda / Notas soltas) + statement-line de síntese"
    - "Background é DARK (bg-surface-darker) com gradiente sutil + grid em opacity 0.015 (pattern HeroBackground adaptado)"
    - "Os 4 pseudo-cards entram com stagger 0/100/200/300ms via animation-delay quando seção entra viewport (useInView), depois ficam estáticos (Pain quer 'parada', não vida — RESEARCH §218)"
    - "PAIN_COPY contém palavra 'clínica' OU 'paciente' OU 'atendimento' na headline OU sub (COPY-03 vertical specificity)"
    - "Statement-line é frase forte tipo 'Quando a operação depende de ferramentas espalhadas, o paciente sente primeiro' (D-11 phrasing reference)"
    - "Headline NÃO termina em interrogação (D-12 — afirmação, não pergunta)"
    - "Zero CTA WhatsApp na Pain (D-29 default + RESEARCH §214)"
    - "Mobile reflow: constelação não quebra em viewport 375px — rotações reduzidas (±2deg vs ±5deg desktop) via CSS media query OU stack 2x2 simplificado"
    - "Lenny aprovou variante de copy via PR comment ou edit (D-17 cadência)"
  artifacts:
    - path: "src/content/pain.ts"
      provides: "PAIN_COPY com 3 variantes v1/v2/v3 satisfies Record"
      contains: "PAIN_COPY_VARIANTS"
      min_lines: 80
    - path: "tests/content/pain.test.ts"
      provides: "Contract tests: shape, clínica/paciente, anti-IA, no Dolce Home, no interrogação na headline"
      min_lines: 50
    - path: "tests/sections/pain-invariants.test.ts"
      provides: "Grep gates: zero motion.*, zero @/components/motion import, zero vh, zero hardcoded PT-BR, zero priority"
      min_lines: 80
    - path: "src/sections/Pain/index.tsx"
      provides: "Pain orchestrator RSC com PainBackground + headline + PainCardConstellation + PainStatement"
      contains: "PainCardConstellation"
    - path: "src/sections/Pain/PainBackground.tsx"
      provides: "RSC bg dark com gradient sutil + grid opacity 0.015"
    - path: "src/sections/Pain/PainCardConstellation.tsx"
      provides: "'use client' wrapper consumindo useInView; 4 PainCard com stagger"
      contains: "useInView"
    - path: "src/sections/Pain/PainCard.tsx"
      provides: "RSC pseudo-card primitive (Instagram DM unanswered | WhatsApp espalhado | Planilha agenda | Notas soltas)"
    - path: "src/sections/Pain/PainStatement.tsx"
      provides: "'use client' synthesis-line consumindo useInView"
      contains: "useInView"
    - path: "src/app/globals.css"
      provides: "Keyframes Pain (se diferentes de hero-* existentes) — provavelmente reuso de hero-card-rise"
  key_links:
    - from: "src/sections/Pain/PainCardConstellation.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView hook for stagger trigger"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/Pain/PainCardConstellation.tsx"
      to: "src/sections/Pain/PainCard.tsx"
      via: "4-card map with animation-delay stagger"
      pattern: "PainCard"
    - from: "src/sections/Pain/index.tsx"
      to: "src/content/pain.ts"
      via: "PAIN_COPY import"
      pattern: 'from "@/content/pain"'
    - from: "src/app/page.tsx"
      to: "src/sections/Pain/index.tsx"
      via: "<Pain /> render (already wired by Plan 04-00)"
      pattern: "<Pain"
---

<objective>
Implementar a seção Pain — primeira seção dark abaixo do Hero — visualizando fragmentação operacional sistêmica via constelação CSS-only de 4 pseudo-cards (Instagram DM sem resposta / WhatsApp espalhado em múltiplos celulares / Planilha de agenda / Notas soltas), encerrando em uma linha de síntese editorial (D-11). Tom editorial calmo (D-10): "constatação", não "acusação". A constelação entra com stagger quando seção entra viewport (useInView do Plan 04-00); cards ficam estáticos depois (Pain quer parada, não vida — RESEARCH §218).

Purpose: NARR-01 satisfeito visualmente (cenário caótico reconhecível pelo dono da clínica), COPY-02/COPY-03 satisfeitos copy-side (zero buzzwords + verticalização clínica explícita), NARR-06 satisfeito via zero imports `motion/react` ou `@/components/motion/`, NARR-07 satisfeito via Tailwind breakpoints + CSS media queries (sem `useDeviceTier`).

Output: 1 copy module com 3 variantes + 4 componentes de seção + 2 test files + (opcional) keyframes em globals.css. PR ABRE 3 variantes, Lenny aprova UMA, executor ajusta `PAIN_COPY = PAIN_COPY_VARIANTS.vN` e merge — gate de copy é parte do done.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-00-SUMMARY.md
@.planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md
@docs/copy-review.md

<interfaces>
<!-- Interfaces de Plan 04-00 + Phase 3 que esta plan consome. Não explorar codebase. -->

From src/hooks/use-in-view.ts (Plan 04-00):
```typescript
export interface UseInViewOptions { threshold?: number; rootMargin?: string; once?: boolean }
export function useInView<T extends Element = HTMLDivElement>(opts?: UseInViewOptions): [React.RefObject<T | null>, boolean];
```

From src/components/ui/container.tsx (Phase 1):
```typescript
export function Container(props: { as?: "div"|"section"|"main"|"article"; className?: string; children: React.ReactNode }): JSX.Element;
```

From src/content/glossary.ts (Plan 04-00):
```typescript
export const CLINICA_GLOSSARY: { atendimento, agendamento, paciente, retorno, followUp, recepcao, equipeAtendimento, lead, estetica, dermatologia, harmonizacao, odontologia, bemEstar, whatsapp, instagram, inboxMulticanal, distribuicaoAutomatica, agendaIntegrada }
```

From src/app/globals.css (Phase 3 — reuso, NÃO modificar):
- Keyframe `hero-card-rise` (transform translateY 12px → 0, opacity 0 → 1; 0.6s ease-premium-out both)
- CSS class `.hero-card-rise` aplica essa animation
- Surface tokens: bg-surface-darker, text-text-on-dark-primary, text-text-on-dark-secondary, text-text-on-dark-muted
- Global reduced-motion rule (L99-107) já zera todas animations

From tests/sections/hero-invariants.test.ts (Phase 3 — pattern a replicar):
- Helper `walk(dir)` que retorna [] para dir inexistente
- Helper `readLines(file)`
- Helper `stripCopyAllowlist(line)` removendo aria-label/alt/etc
- Regex motionRegex = /\bmotion\.(div|h1|...)\b/g
- Regex vhRegex = /\b(?:min-h|h|max-h)-\[?100vh\]?\b|\bh-screen\b|\bmin-h-screen\b|\bmax-h-screen\b/g
- Regex banned phrases = /\b(desbloqueie|potencialize|transforme sua|próximo nível|...)\b/gi

</interfaces>

<copy_guidance>
RESEARCH §190-220 + CONTEXT §D-09..D-12:

Direção tonal (D-10): editorial calmo, B2B sério, premium. Sem agressão. Postura de autoridade calma, não confronto.

Estrutura sugerida (D-11):
- Headline (afirmação, não pergunta D-12): tom "constatação".
  Exemplo de referência: "Sua operação está espalhada." OU "O atendimento da clínica vive em ferramentas separadas."
- Sub-headline: aterrissa em ferramentas concretas (Instagram + WhatsApp + planilha + notas).
- Constelação: 4 cards representam as ferramentas separadas. Copy de cada card = label canal + frase curta concreta.
- Statement-line de síntese: frase forte tipo "Quando a operação depende de ferramentas espalhadas, o paciente sente primeiro." (D-11 exato exemplo do Lenny — pode ser variante exata ou variação fiel).

3 variantes contrastantes (não micro-ajustes):
- v1: Headline de fragmentação direta ("Sua operação está espalhada"). Sub aterrissa em 4 canais. Statement: linha do Lenny.
- v2: Headline de consequência operacional ("O paciente espera. A operação demora a responder."). Sub explica fragmentação. Statement: variação focada no paciente.
- v3: Headline de constatação editorial ("Quatro lugares. Nenhum em sincronia."). Sub lista os 4 lugares. Statement: "A clínica perde lead onde a operação não fala consigo mesma." ou similar.

Card content (4 cards — copy de cada):
1. Instagram DM unanswered — label "Instagram · DM", title nome de potencial paciente, row text "[mensagem real estilo 'Vi o vídeo da Dra...']", meta "há 2h sem resposta" (timestamp envelhecido).
2. WhatsApp espalhado — label "WhatsApp · Celular Camila", title "3 conversas paradas", rows pendentes ("aguardando 2ª mensagem", "follow-up perdido").
3. Planilha de agenda — label "Planilha · Recepção", title "Slot 14h não confirmado", row text "Marina — falar amanhã" (anotação a mão).
4. Notas soltas — label "Notas · Caderno", title "Retorno em 30 dias", row text strikethrough "Maria 14/04" + row pendente "Carla 28/04 ?".

Cada variante apresenta as MESMAS 4 categorias de card; só varia o tom da headline + sub + statement. Card titles/rows são consistentes entre variantes (são fatos da operação, não voz).
</copy_guidance>

<visual_guidance>
RESEARCH §194-242:

Composição vertical:
- Container default max-w-7xl. Padding py-24 lg:py-32.
- Headline centered top, max-w-3xl, text-balance.
- Sub max-w-2xl mt-6.
- Constelação centered, marginTop ~64-80px. Altura ~360px desktop, ~320px mobile.
- Statement-line embaixo da constelação, mt-16 lg:mt-20, max-w-3xl centered, text size base/lg, text-text-on-dark-secondary, italic ou regular pesado leve. Frase com aspas duplas (curly: "").

Constelação dos 4 pseudo-cards (CSS-only, sem motion lib):
- Positioning: `relative`, cada card absolute com `left-1/2 top-X` + transform translate + rotate.
- Card 1 (Instagram DM): top-left, rotate(-4deg), z-10, opacity-90 (mais sutil porque "mais antigo, mais esquecido")
- Card 2 (WhatsApp): top-right, rotate(3deg), z-20
- Card 3 (Planilha): bottom-left, rotate(2deg), z-20
- Card 4 (Notas): bottom-right, rotate(-3deg), z-10
- Spread horizontal aproximado: ±180px desktop, ±100px sm, ±60px mobile (similar pattern HeroCardStack)
- Mobile (< 640px): reduzir rotações para ±2deg e spread para ±60px; OU stack vertical 2x2 grid se rotação absolute não couber

Card visual (PainCard.tsx) — herda HeroCard pattern:
- bg-surface-card (#FBFCFD) — D-08 tokens existentes
- border border-neutral-200/70 ring-1 ring-inset ring-white/80
- shadow drop deep + soft 2 camadas (igual HeroCardStack:48)
- radius 14px (cards principais — vide HeroCardStack pattern)
- w-[180px] sm:w-[240px] (escala ~70% do HeroCard, vide RESEARCH §654)
- padding 3.5 sm:4
- Inside: header com lucide icon + label uppercase tracking-[0.12em] + meta font-mono à direita; title 14-15px; rows com lucide check/circle ou strikethrough texto.

PainBackground.tsx (RSC, CSS-only):
- Reaproveitar pattern HeroBackground mas SEM o haze pulse central (Pain não tem foco visual único).
- Camadas: L1 base atmospheric gradient (similar Hero L1), L2 radial sutil (sem roxo — pain não tem accent), L4 grid técnico (mesmo opacity 0.015, mas SEM mask radial — pain quer "constelação espalhada", não foco central), L6 vinheta inferior pra transição com Bridge.
- Importante: vinheta inferior do Pain pode TERMINAR em uma cor light (gradient para #fafaf9 = bg-surface-light de Bridge) pra transição cromática D-15.

Animação:
- Constelação: cada card ganha `animation-delay: ${i * 100}ms` inline style. Quando `useInView` dispara, classe muda de `opacity-0` para `hero-card-rise` (reusa keyframe existente).
- Statement-line: mesmo padrão, mas com delay maior (e.g. 500ms — entra DEPOIS dos cards).
- Cards NÃO têm `hero-card-float-*` (Pain quer estático — D-11/D-12 tom de constatação parada; RESEARCH §218).
- Reduced motion: globals.css L99-107 já zera tudo globalmente.

Zero CTA WhatsApp na Pain (D-29 default). Zero priority Image (NARR + landing global rule).
</visual_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — pain-invariants + pain copy contract tests (plant before code)</name>
  <files>tests/sections/pain-invariants.test.ts, tests/content/pain.test.ts</files>
  <read_first>
    - tests/sections/hero-invariants.test.ts (full file — replicate structure verbatim, adapt regex paths to Pain)
    - tests/content/hero.test.ts (full file — replicate, adapt to PAIN_COPY shape)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md (lines 543-575 — per-section test pattern)
  </read_first>
  <behavior>
    pain-invariants.test.ts gates (mirror Hero pattern):
    - Test 1: Zero `motion.*` JSX in `src/sections/Pain/**/*.tsx` (regex same as Hero).
    - Test 2: Zero `import ... from '@/components/motion/'` OR `from 'motion/react'` OR `from 'framer-motion'` in `src/sections/Pain/**/*.tsx`. NEW invariant per NARR-06 reinterpretation (RESEARCH §483-492).
    - Test 3: Zero `vh` height utilities — only `dvh`/`svh` allowed (regex same as Hero).
    - Test 4: Zero `priority` JSX prop in `src/sections/Pain/**/*.tsx`.
    - Test 5: Zero PT-BR hard-coded sentence-like strings in JSX of `src/sections/Pain/**/*.tsx` (regex same as Hero — copy lives in src/content/pain.ts).
    - Test 6: Helper sanity — `walk(nonexistent)` returns `[]`.

    pain.test.ts copy contracts:
    - Test 1: PAIN_COPY shape — `h2`, `sub`, `statement`, `cards` (array of 4 with shape {kind, label, title, rows, meta}).
    - Test 2: PAIN_COPY.h2 OR PAIN_COPY.sub contains `/cl[ií]nica|paciente|atendimento|agendamento/i` (verticalization — COPY-03).
    - Test 3: PAIN_COPY.h2 does NOT end in `?` (D-12 — afirmação não pergunta). Regex check.
    - Test 4: Anti-IA banned phrases regex matches nothing in joined copy (h2 + sub + statement + cards content). Same BANNED_PHRASES regex as hero test.
    - Test 5: Zero mention of "Dolce Home" anywhere in PAIN_COPY (D-27 carried over).
    - Test 6: PAIN_COPY.cards has exactly 4 entries.
    - Test 7: PAIN_COPY_VARIANTS exports v1, v2, v3 (all three with same shape — `satisfies Record<string, PainCopy>`).
  </behavior>
  <action>
Step 1 — Create `tests/sections/pain-invariants.test.ts`. Copy structure from `tests/sections/hero-invariants.test.ts` literally. Replace:
- `HERO_DIR` → `PAIN_DIR = path.resolve(SRC_DIR, "sections/Pain")`
- Remove `HEADER_FILE` reference.
- Remove `HERO_MOCKUP_FILE` reference and Test 3 (`priority` count exactly 1) — replace with the simpler "zero priority in PAIN_DIR" check.
- Remove `HERO_COPY_FILE` reference (banned-phrases content test moves to pain.test.ts).
- Add new Test 2 (zero motion lib imports) with regex `/from\s+['"](?:framer-motion|motion\/react|@\/components\/motion)/`.

Required header comment block:
```ts
/**
 * Phase 4 / Plan 04-01 — Pain section invariants (Wave 0 grep gates).
 *
 * Replicates the Hero invariant pattern (tests/sections/hero-invariants.test.ts)
 * adapted for src/sections/Pain/. Adds NEW invariant test 2: zero imports of
 * motion lib (framer-motion / motion/react / @/components/motion/) — NARR-06
 * reinterpretation per 04-RESEARCH.md §483-492.
 *
 * Tests skip gracefully when src/sections/Pain/ doesn't exist yet (walk returns
 * []). Tests start enforcing as soon as Plan 04-01 Task 2+ create the files.
 */
```

Step 2 — Create `tests/content/pain.test.ts`. Copy structure from `tests/content/hero.test.ts`. Adapt:
- Import `PAIN_COPY` and `PAIN_COPY_VARIANTS` from `@/content/pain`.
- Each test wrapped in `it.skipIf(!fs.existsSync(path.resolve(__dirname, "../../src/content/pain.ts")))` for graceful skipping until Task 2 lands the copy module. (Alternative: leave as `it(...)` and accept this file failing until Task 2 — explicit is fine since they ship in same commit. Choose ONE approach and apply consistently.)
- BANNED_PHRASES regex identical to Hero test.
- New: Test 3 verifies `!/\?\s*$/.test(PAIN_COPY.h2)` (D-12).
- New: Test 6 `expect(PAIN_COPY.cards).toHaveLength(4)`.
- New: Test 7 `expect(Object.keys(PAIN_COPY_VARIANTS).sort()).toEqual(["v1", "v2", "v3"])`.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/pain-invariants.test.ts tests/content/pain.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/sections/pain-invariants.test.ts` exists, has >= 6 `it(...)` blocks.
    - `grep -c "from\\\\s\\+\\['\\\"\\](?:framer-motion|motion/react|@/components/motion)" tests/sections/pain-invariants.test.ts` returns >= 1 (motion-import gate present).
    - File `tests/content/pain.test.ts` exists, has >= 7 `it(...)` blocks.
    - `grep -c "PAIN_COPY_VARIANTS" tests/content/pain.test.ts` returns >= 1.
    - `npx vitest run tests/sections/pain-invariants.test.ts` exits 0 (all tests pass — they default-pass when Pain dir is empty, since walk returns []).
    - `npx vitest run tests/content/pain.test.ts` — either exits 0 with all tests skipped (skipIf path) OR exits non-zero with informative "Cannot find module '@/content/pain'" (acceptable if Task 2 ships in same commit).
  </acceptance_criteria>
  <done>Pain Wave 0 gates planted; they fail loudly the moment Task 2 violates an invariant in Pain/.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PAIN_COPY module (3 variants) + Pain section components</name>
  <files>src/content/pain.ts, src/sections/Pain/index.tsx, src/sections/Pain/PainBackground.tsx, src/sections/Pain/PainCardConstellation.tsx, src/sections/Pain/PainCard.tsx, src/sections/Pain/PainStatement.tsx</files>
  <read_first>
    - src/content/hero.ts (HERO_COPY shape + 3-variant comment header pattern)
    - src/sections/Hero/HeroBackground.tsx (full file — adapt to Pain dark-with-vignette-to-light-bottom)
    - src/sections/Hero/HeroCardStack.tsx (full file — adapt absolute positioning + rotation pattern; remove float animations)
    - src/sections/Hero/index.tsx (RSC orchestrator pattern)
    - src/sections/Pain/index.tsx (Plan 04-00 scaffold — overwrite)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-09..D-12 + <specifics> phrasing examples)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md (lines 190-242 — Pain construction)
  </read_first>
  <behavior>
    PAIN_COPY shape:
    ```ts
    export type PainCardRow = { type: "text" | "check" | "pending" | "strikethrough"; content: string };
    export type PainCard = {
      kind: "instagram" | "whatsapp" | "spreadsheet" | "notes";
      label: string;
      title: string;
      rows: ReadonlyArray<PainCardRow>;
      meta?: string;
    };
    export type PainCopy = {
      h2: string;
      sub: string;
      statement: string;
      cards: readonly [PainCard, PainCard, PainCard, PainCard];
    };
    export const PAIN_COPY_VARIANTS: Record<"v1" | "v2" | "v3", PainCopy> satisfying type;
    export const PAIN_COPY: PainCopy = PAIN_COPY_VARIANTS.v1; // provisional, Lenny changes via PR
    ```

    Components behavior:
    - `Pain` (index.tsx, RSC): renders `<section bg-surface-darker py-24 lg:py-32 aria-labelledby="pain-headline">` with `<PainBackground />` + `<Container>` containing h2/sub + `<PainCardConstellation />` + `<PainStatement />`. h2 uses `id="pain-headline"`, renders `PAIN_COPY.h2` (NOT inline string).
    - `PainBackground` (RSC): static dark layered gradient + grid + bottom vignette fading toward bg-surface-light (for transition to Bridge per D-15).
    - `PainCardConstellation` ("use client"): consumes `useInView({ threshold: 0.3 })` from `@/hooks/use-in-view`. Renders 4 `PainCard`s absolutely positioned. Each card has inline `style={{ animationDelay: \`${i * 100}ms\` }}` and conditional className `inView ? "hero-card-rise" : "opacity-0"`.
    - `PainCard` (RSC, no motion): receives `card: PainCard` prop, renders styled article with lucide icon per kind (Instagram | MessageCircle | Sheet | NotebookPen).
    - `PainStatement` ("use client"): consumes own `useInView` with delayed reveal (own threshold + render with `inView` class).
  </behavior>
  <action>
Step 1 — Create `src/content/pain.ts` with 3 contrasting variants per `<copy_guidance>` above. Header comment must document each variant's direction:

```ts
/**
 * Phase 4 / Plan 04-01 — Pain copy module.
 *
 * 3 variantes contrastantes (D-17 cadência). Lenny aprova UMA via PR comment
 * 'LGTM v?' ou edit inline; depois desta plan, PAIN_COPY = PAIN_COPY_VARIANTS.v?
 * fica fixado e demais variantes archived no git history.
 *
 * v1 — Fragmentação direta:
 *   h2: "Sua operação está espalhada."
 *   Tom: constatação calma B2B (D-10/D-12).
 *
 * v2 — Consequência operacional ao paciente:
 *   h2: "O paciente espera. A operação demora a responder."
 *   Tom: ângulo humano dentro da postura B2B.
 *
 * v3 — Editorial contado em cifras de lugares:
 *   h2: "Quatro lugares. Nenhum em sincronia."
 *   Tom: editorial seco, Linear/Stripe.
 *
 * COPY-02: zero buzzwords anti-IA.
 * COPY-03: vertical clínica/paciente/atendimento aparece em h2 OU sub.
 * D-12: h2 NÃO termina em interrogação — afirmação.
 * D-27: zero menção a Dolce Home.
 */
```

Each variant must:
- Use one of the headline directions above (or close variation).
- Have `sub` aterrissando em 4 canais concretos (Instagram + WhatsApp + planilha + notas/caderno).
- Have `statement` em frase forte single-line (D-11 reference: "Quando a operação depende de ferramentas espalhadas, o paciente sente primeiro." — v1 deve usar esta exata frase ou variação muito próxima).
- Share the SAME 4 `cards` objects (cards are facts of the operation, not voice — vary only in headline/sub/statement).

Card content (consistent across variants):
```ts
cards: [
  {
    kind: "instagram",
    label: "Instagram · DM",
    title: "@marina_souza",
    rows: [{ type: "text", content: '"Oi, vi o vídeo da Dra. Camila — queria saber preço de harmonização."' }],
    meta: "há 2h sem resposta",
  },
  {
    kind: "whatsapp",
    label: "WhatsApp · Camila",
    title: "3 conversas paradas",
    rows: [
      { type: "pending", content: "Aguardando 2ª mensagem" },
      { type: "pending", content: "Follow-up de retorno" },
    ],
  },
  {
    kind: "spreadsheet",
    label: "Planilha · Recepção",
    title: "Slot 14h não confirmado",
    rows: [{ type: "text", content: "Marina — falar amanhã" }],
    meta: "atualizado a mão",
  },
  {
    kind: "notes",
    label: "Notas · Caderno",
    title: "Retorno em 30 dias",
    rows: [
      { type: "strikethrough", content: "Maria 14/04" },
      { type: "text", content: "Carla 28/04 ?" },
    ],
  },
] as const,
```

Step 2 — Implement `src/sections/Pain/PainBackground.tsx` (RSC, CSS-only). Adapt HeroBackground pattern:

```tsx
/**
 * PainBackground — dark atmosphere com transição cromática para light no fim.
 *
 * Razão da vinheta inferior LIGHT: D-15 — Pain (DARK) → Bridge (LIGHT) precisa de
 * transição visual; o final do Pain "dissolve" em direção ao surface-light de Bridge.
 *
 * MOTION-08: zero animation; pure CSS gradient layers. RSC.
 */
export function PainBackground() {
  return (
    <div aria-hidden="true" className="pointer-events-none absolute inset-0 -z-10">
      {/* L1 — base atmospheric, blue-black mais denso que Hero (Pain quer "peso") */}
      <div className="absolute inset-0" style={{ background: "linear-gradient(180deg, rgba(10,15,26,1) 0%, rgba(14,20,34,1) 60%, rgba(10,15,26,1) 100%)" }} />
      {/* L2 — grid técnico opacity 0.015, SEM mask radial (constelação espalhada, não foco) */}
      <div className="absolute inset-0" style={{
        backgroundImage: "linear-gradient(to right, rgba(255,255,255,0.015) 1px, transparent 1px), linear-gradient(to bottom, rgba(255,255,255,0.015) 1px, transparent 1px)",
        backgroundSize: "64px 64px",
      }} />
      {/* L3 — vinheta inferior fade-to-light para transição com Bridge (D-15) */}
      <div className="absolute inset-x-0 bottom-0 h-1/3" style={{ background: "linear-gradient(to bottom, transparent, rgba(250,250,249,0.0) 40%, rgba(250,250,249,0.95) 100%)" }} />
    </div>
  );
}
```

Step 3 — Implement `src/sections/Pain/PainCard.tsx` (RSC). Adapt HeroCard visual pattern (article + bg-surface-card + 2-layer shadow + ring-1 inset). Use lucide icons keyed by `card.kind`:
- instagram → `Instagram` (rose-400 muted — Pain não tem vida)
- whatsapp → `MessageCircle` (neutral-500)
- spreadsheet → `Table2` (neutral-500)
- notes → `NotebookPen` (neutral-500)

Card width `w-[180px] sm:w-[240px]`, padding `p-3.5 sm:p-4`, radius 14px (`rounded-[14px]`). Header: icon + label uppercase tracking-[0.12em] text-[10.5px] text-neutral-500 + meta font-mono right-aligned. Title `mt-3 text-[14px] font-semibold leading-[1.3] tracking-[-0.01em] text-neutral-900`. Rows with strikethrough variant using `line-through text-neutral-400`, pending using `Circle` icon, text default.

Step 4 — Implement `src/sections/Pain/PainCardConstellation.tsx` (`"use client"`):

```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import { PainCard } from "./PainCard";
import { PAIN_COPY } from "@/content/pain";

const POSITIONS = [
  // Card 1 — Instagram, top-left
  "left-1/2 top-0 z-10 opacity-90 [transform:translateX(calc(-50%-60px))_rotate(-4deg)] sm:[transform:translateX(calc(-50%-140px))_rotate(-4deg)] lg:[transform:translateX(calc(-50%-200px))_rotate(-4deg)]",
  // Card 2 — WhatsApp, top-right
  "left-1/2 top-4 z-20 [transform:translateX(calc(-50%+60px))_rotate(3deg)] sm:[transform:translateX(calc(-50%+140px))_rotate(3deg)] lg:[transform:translateX(calc(-50%+200px))_rotate(3deg)]",
  // Card 3 — Spreadsheet, bottom-left
  "left-1/2 top-44 z-20 [transform:translateX(calc(-50%-50px))_rotate(2deg)] sm:[transform:translateX(calc(-50%-130px))_rotate(2deg)] lg:[transform:translateX(calc(-50%-180px))_rotate(2deg)]",
  // Card 4 — Notes, bottom-right
  "left-1/2 top-48 z-10 [transform:translateX(calc(-50%+50px))_rotate(-3deg)] sm:[transform:translateX(calc(-50%+130px))_rotate(-3deg)] lg:[transform:translateX(calc(-50%+180px))_rotate(-3deg)]",
] as const;

export function PainCardConstellation() {
  const [ref, inView] = useInView<HTMLDivElement>({ threshold: 0.3 });
  return (
    <div ref={ref} aria-hidden="true" className="relative mx-auto mt-16 h-[360px] w-full max-w-4xl sm:h-[380px] lg:h-[420px]">
      {PAIN_COPY.cards.map((card, i) => (
        <div key={card.kind} className={`absolute ${POSITIONS[i]}`}>
          <div
            className={inView ? "hero-card-rise" : "opacity-0"}
            style={{ animationDelay: `${i * 100}ms` }}
          >
            <PainCard card={card} />
          </div>
        </div>
      ))}
    </div>
  );
}
```

Step 5 — Implement `src/sections/Pain/PainStatement.tsx` (`"use client"`):

```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import { PAIN_COPY } from "@/content/pain";

export function PainStatement() {
  const [ref, inView] = useInView<HTMLParagraphElement>({ threshold: 0.5 });
  return (
    <p
      ref={ref}
      className={`mx-auto mt-16 max-w-3xl text-balance text-center text-base leading-[1.6] text-text-on-dark-secondary sm:mt-20 sm:text-lg ${inView ? "hero-card-rise" : "opacity-0"}`}
      style={{ animationDelay: "500ms" }}
    >
      {PAIN_COPY.statement}
    </p>
  );
}
```

Step 6 — Overwrite `src/sections/Pain/index.tsx` (RSC orchestrator):

```tsx
/**
 * Pain — Phase 4 § Pain section (NARR-01).
 *
 * D-05/D-09: dark, fragmentação operacional sistêmica.
 * D-10/D-11/D-12: editorial calmo, headline afirmação não pergunta, statement-line de síntese.
 * NARR-06 reinterpretado: zero motion.* JSX, zero import @/components/motion — animations CSS-only via useInView + keyframe hero-card-rise.
 * D-29: zero CTA WhatsApp dentro da Pain.
 */
import { Container } from "@/components/ui/container";
import { PAIN_COPY } from "@/content/pain";
import { PainBackground } from "./PainBackground";
import { PainCardConstellation } from "./PainCardConstellation";
import { PainStatement } from "./PainStatement";

export function Pain() {
  return (
    <section
      id="pain"
      aria-labelledby="pain-headline"
      className="relative isolate overflow-hidden bg-surface-darker py-24 lg:py-32"
    >
      <PainBackground />
      <Container className="relative z-10">
        <h2
          id="pain-headline"
          className="mx-auto max-w-3xl text-balance text-center text-3xl font-semibold leading-[1.1] tracking-[-0.02em] text-text-on-dark-primary sm:text-4xl lg:text-[2.75rem]"
        >
          {PAIN_COPY.h2}
        </h2>
        <p className="mx-auto mt-6 max-w-2xl text-balance text-center text-base leading-[1.6] text-text-on-dark-secondary sm:text-lg">
          {PAIN_COPY.sub}
        </p>
        <PainCardConstellation />
        <PainStatement />
      </Container>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>npx vitest run tests/sections/pain-invariants.test.ts tests/content/pain.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - File `src/content/pain.ts` exists; `grep -c "PAIN_COPY_VARIANTS" src/content/pain.ts` returns >= 2 (declaration + export).
    - `grep -c "v1: {" src/content/pain.ts` returns >= 1 (v1 variant).
    - `grep -c "v2: {" src/content/pain.ts` returns >= 1.
    - `grep -c "v3: {" src/content/pain.ts` returns >= 1.
    - `grep -E "@marina_souza|harmonização" src/content/pain.ts` matches (card content verbatim from action spec).
    - `grep -c "Dolce Home" src/content/pain.ts` returns 0.
    - `grep -cE "(desbloqueie|potencialize|transforme sua|próximo nível)" src/content/pain.ts` returns 0.
    - `grep -c "use client" src/sections/Pain/PainCardConstellation.tsx` returns 1.
    - `grep -c "use client" src/sections/Pain/PainStatement.tsx` returns 1.
    - `grep -c "use client" src/sections/Pain/PainCard.tsx` returns 0 (RSC).
    - `grep -c "use client" src/sections/Pain/PainBackground.tsx` returns 0 (RSC).
    - `grep -cE "from\\s+['\"]motion|from\\s+['\"]framer-motion|from\\s+['\"]@/components/motion" src/sections/Pain/` (any file) returns 0.
    - `grep -c "useInView" src/sections/Pain/PainCardConstellation.tsx` returns >= 1.
    - `grep -c "useInView" src/sections/Pain/PainStatement.tsx` returns >= 1.
    - `grep -c "hero-card-rise" src/sections/Pain/PainCardConstellation.tsx` returns >= 1 (reuse Phase 3 keyframe).
    - PAIN_COPY.h2 não termina em `?` (D-12 — covered by `tests/content/pain.test.ts` Test 3).
    - `npx vitest run tests/sections/pain-invariants.test.ts` exits 0 — all 6 tests pass.
    - `npx vitest run tests/content/pain.test.ts` exits 0 — all 7 tests pass.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Pain section ships with copy module (3 variants) + 5 components + tests green. Section renders in page.tsx in correct narrative position with dark→light vignette transition baked in. Ready for copy-review PR.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Pain copy review PR + Playwright smoke (D-17 cadência)</name>
  <files>src/content/pain.ts (variante final aprovada substitui PAIN_COPY = PAIN_COPY_VARIANTS.vN)</files>
  <action>Lenny revisa as 3 variantes em src/content/pain.ts via PR. Executor aguarda LGTM e ajusta a variante ativa. Pipeline pré-merge: code-review skill + Playwright MCP per CLAUDE.md user instructions.</action>
  <verify>
    <automated>npx vitest run tests/content/pain.test.ts tests/sections/pain-invariants.test.ts</automated>
  </verify>
  <done>Lenny approved a copy variant via PR comment or inline edit; executor merged with PAIN_COPY pointing to approved variant; Playwright screenshots attached to PR description; CI green.</done>
  <what-built>
    - 3 variantes contrastantes de copy em `src/content/pain.ts` (v1/v2/v3 satisfies Record).
    - Pain section visualmente completa: dark background, constelação de 4 pseudo-cards com stagger, statement-line de síntese.
    - Transição cromática Pain DARK → Bridge LIGHT via vinheta inferior da PainBackground (D-15).
    - Zero CTA WhatsApp (D-29). Zero motion lib import (NARR-06 reinterpretado).
  </what-built>
  <how-to-verify>
1. Pull this branch, run `npm install` (no new deps expected), `npm run dev`.
2. Open `http://localhost:3000`, scroll past Hero → Pain entra com stagger dos 4 cards.
3. Lenny revisa as 3 variantes em `src/content/pain.ts`:
   - Comenta `LGTM v1` (ou v2/v3) no PR OU edita inline pra ajustar a variante escolhida.
   - Verifica tom: editorial calmo, B2B sério, sem agressão. Statement-line lê "constatação parada".
4. Executor (Claude) ajusta `PAIN_COPY = PAIN_COPY_VARIANTS.vN` para a aprovada e re-pusha.
5. Pipeline pré-merge (D-00 user CLAUDE.md):
   - **Code review skill** sobre diff.
   - **Playwright MCP**: abrir `http://localhost:3000`, scrollar, screenshot da Pain section em desktop (1280x800) e mobile (375x812). Verificar:
     - Constelação não corta cards em 375px.
     - Stagger visível dos 4 cards.
     - Transição Pain→Bridge (vinheta fade) visualmente coerente.
     - Console JS sem erros.
     - `prefers-reduced-motion: reduce` (DevTools rendering panel): cards entram em estado final sem animation.
6. Aprovado → merge.
  </how-to-verify>
  <resume-signal>Lenny aprova variante via comment "LGTM v?" ou edit; Playwright smoke screenshots anexados ao PR; merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser DOM | useInView + IntersectionObserver in Pain client islands |
| copy→DOM | PAIN_COPY strings rendered via React (no dangerouslySetInnerHTML); HTML entities in copy (curly quotes, en-dash) escape via React default |
| filesystem→test | pain-invariants.test.ts reads src/sections/Pain at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-04 | Information Disclosure | PAIN_COPY accidentally includes real client name (e.g., "Dolce Home") | mitigate | tests/content/pain.test.ts Test 5 grep-rejects /dolce home/i; same pattern Hero copy test. |
| T-4-05 | Tampering | Future contributor inlines a PT-BR string in Pain JSX, bypassing src/content/pain.ts | mitigate | tests/sections/pain-invariants.test.ts Test 5 detects long PT-BR text or ≥2 accented vowel sequences in JSX of Pain dir; same regex pattern Hero invariant uses. |
| T-4-06 | Tampering | Future contributor imports motion lib in Pain (reverting NARR-06 reinterpretation) | mitigate | tests/sections/pain-invariants.test.ts Test 2 grep-rejects `from "motion/react"`, `from "framer-motion"`, `from "@/components/motion/"`. |
</threat_model>

<verification>
- `npx vitest run tests/sections/pain-invariants.test.ts tests/content/pain.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- `npx next build` exits 0.
- Visual (Playwright via Task 3): Pain section renders, constelação staggers in viewport, dark→light vignette transitions to Bridge.
</verification>

<success_criteria>
- NARR-01 satisfied: 4 pseudo-cards render fragmentation (Instagram DM unanswered / WhatsApp paradas / Planilha não confirmada / Notas com retorno strikethrough).
- NARR-06 satisfied: zero motion.* JSX, zero motion lib imports in Pain/.
- NARR-07 satisfied: mobile reflow via Tailwind breakpoints (rotações reduzidas em sm:, w-[180px] mobile vs w-[240px] desktop).
- COPY-02 satisfied: zero banned phrases (grep test green).
- COPY-03 satisfied: clínica/paciente/atendimento aparece em h2 OR sub (grep test green).
- D-12: h2 não termina em `?` (test green).
- D-29: zero CTA WhatsApp na Pain (manual inspection + zero `<WhatsAppCta>` import in Pain/).
- Lenny aprovou copy variant via PR (Task 3 unlock).
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-01-SUMMARY.md` documenting:
- Variante final aprovada (v1/v2/v3) + razão
- Card visual primitives e como reusam HeroCard pattern
- PainBackground vignette-to-light pattern (D-15 cromatic transition)
- Stagger timing chosen (0/100/200/300ms cards + 500ms statement)
- Screenshots Playwright (desktop + mobile)
</output>
</content>
</invoke>