---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 04
type: execute
wave: 5
depends_on: ["04-00", "04-03"]
files_modified:
  - src/content/how-it-works.ts
  - tests/content/how-it-works.test.ts
  - tests/sections/how-it-works-invariants.test.ts
  - src/sections/HowItWorks/index.tsx
  - src/sections/HowItWorks/HowItWorksHeader.tsx
  - src/sections/HowItWorks/HowItWorksStep.tsx
  - src/sections/HowItWorks/HowItWorksMiniMockup.tsx
autonomous: false
requirements:
  - NARR-04
  - NARR-06
  - NARR-07
  - COPY-02
  - COPY-03
threats:
  - T-4-12
must_haves:
  truths:
    - "HowItWorks renderiza 4 passos timeline vertical (D-21/D-22): Lead chega / Operação distribui / Atendente conversa / Paciente agendado"
    - "Cada passo: número grande à esquerda + headline curta + descrição 1-2 linhas + mini-mockup CSS à direita"
    - "Linha conectora vertical sutil entre passos no desktop (border-l border-neutral-200 OR pseudo-element); some no mobile (flex-col stack)"
    - "Surface LIGHT NEUTRAL (bg-surface-lighter #fff), tom explicativo simplificador (D-23) — NÃO compete visualmente com Hero ou Product"
    - "Números dos steps usam accent-primary roxo (#7C3AED) em font-mono grande (text-6xl lg:text-7xl) — único accent visual da seção, micro-elemento autorizado"
    - "Mini-mockups por step: (1) notification card chegando / (2) routing pill + seta / (3) 2 balões de conversa / (4) calendar slot confirmado"
    - "Cada step usa useInView pra fade-up stagger quando entra viewport"
    - "HOW_COPY contém 'clínica' OU 'paciente' OU 'lead' em headers (COPY-03)"
    - "Mobile: stack flex-col (número em cima, conteúdo abaixo, sem linha conectora)"
    - "Zero CTA WhatsApp (D-29)"
    - "Lenny aprovou variante de copy via PR (D-17)"
  artifacts:
    - path: "src/content/how-it-works.ts"
      provides: "HOW_COPY 3 variantes — header (h2/sub) + 4 steps com {number, headline, description, mockupKind}"
      contains: "HOW_COPY_VARIANTS"
      min_lines: 100
    - path: "tests/content/how-it-works.test.ts"
      provides: "Contract tests: shape, 4 steps tuple, anti-IA, no Dolce Home, 3 variants"
    - path: "tests/sections/how-it-works-invariants.test.ts"
      provides: "Standard 6 grep gates"
    - path: "src/sections/HowItWorks/index.tsx"
      provides: "Orchestrator RSC bg-surface-lighter + HowItWorksHeader + 4× HowItWorksStep"
      contains: "HowItWorksStep"
    - path: "src/sections/HowItWorks/HowItWorksHeader.tsx"
      provides: "RSC h2 + sub"
    - path: "src/sections/HowItWorks/HowItWorksStep.tsx"
      provides: "'use client' single step com useInView; recebe step props + index"
      contains: "useInView"
    - path: "src/sections/HowItWorks/HowItWorksMiniMockup.tsx"
      provides: "RSC switch por mockupKind: notification | routing | conversation | calendar-slot"
  key_links:
    - from: "src/sections/HowItWorks/HowItWorksStep.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView per step"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/HowItWorks/index.tsx"
      to: "src/content/how-it-works.ts"
      via: "HOW_COPY import"
      pattern: 'from "@/content/how-it-works"'
---

<objective>
Implementar HowItWorks — seção LIGHT neutral com 4 passos em timeline vertical mostrando o fluxo real do lead na clínica (D-21/D-22): Lead chega (Instagram/WhatsApp/Facebook) → Operação distribui (atribuição automática) → Atendente conversa (centralizado, contexto carregado) → Paciente agendado (com retorno e follow-up automático). Tom explicativo simplificador (D-23) — NÃO compete com Hero ou Product. Surface light neutral, números grandes em accent roxo (único accent visual autorizado em micro-elemento), mini-mockups discretos.

Purpose: NARR-04 satisfeito via staggered reveal vertical (horizontal scroll = stretch goal v1.1 per REQUIREMENTS). NARR-06 satisfeito (zero motion lib). NARR-07 satisfeito (mobile = flex-col stack, sem linha conectora). COPY-03 satisfeito (lead/paciente/clínica em headers).

Output: 1 copy module com 3 variantes + 4 components + 2 tests. Cada step renderiza com useInView trigger; cross-step stagger via animation-delay incremental.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-03-SUMMARY.md

<interfaces>
From src/hooks/use-in-view.ts: useInView (already used in Plans 01-03 — same API).

From src/app/globals.css:
- hero-card-rise (fade-up entrance reuse)
- hero-live-pulse (notification dot in step 1 mini-mockup)
- Surface: bg-surface-lighter (#fff for neutral light), text-text-primary, text-text-secondary, text-text-muted
- Accent: text-accent-primary (#7C3AED) — for number labels (micro-element D-20 accent rule)

From PRODUCT_COPY/PRODUCT_SECONDARIES mini-mockup pattern (Plan 04-03):
- routing-pill, calendar-grid patterns to reuse aesthetic conventions; HowItWorks creates 4 new mockup kinds inspired by these but distinct.

</interfaces>

<copy_guidance>
CONTEXT D-21/D-22 + RESEARCH §330-380:

Structure:
```ts
export type HowMockupKind = "notification" | "routing" | "conversation" | "calendar-slot";
export type HowStep = {
  number: string; // "01" | "02" | "03" | "04" — formatado com leading zero
  headline: string;
  description: string;
  mockupKind: HowMockupKind;
};
export type HowCopy = {
  header: { h2: string; sub: string };
  steps: readonly [HowStep, HowStep, HowStep, HowStep];
};
```

4 steps (D-22 sequence — consistent across variants):

step 1: Lead chega
  mockupKind: "notification"
  Concept: "Instagram/WhatsApp/Facebook captação multicanal"

step 2: Operação distribui
  mockupKind: "routing"
  Concept: "atribuição automática pro atendente certo"

step 3: Atendente conversa
  mockupKind: "conversation"
  Concept: "centralizado, com contexto, em tempo real"

step 4: Paciente agendado
  mockupKind: "calendar-slot"
  Concept: "com retorno e follow-up automático"

3 variantes contrastantes:

v1 — Headers operacionais simples:
  header.h2: "Do primeiro 'oi' ao agendamento, sem trocas de aplicativo."
  header.sub: "O caminho que cada lead percorre dentro do sistema."
  step 1 headline: "Lead entra por qualquer canal."
  step 1 description: "Instagram, WhatsApp, Facebook — tudo vai pra mesma caixa de entrada."
  step 2 headline: "A operação distribui automaticamente."
  step 2 description: "Cada conversa vai pra atendente certa pelo canal, especialidade ou idioma."
  step 3 headline: "Atendente conversa em uma tela só."
  step 3 description: "Com histórico do paciente e contexto do canal já carregados."
  step 4 headline: "Paciente agendado, retorno marcado."
  step 4 description: "Confirmação automática via WhatsApp, follow-up programado."

v2 — Tom Linear/Stripe editorial:
  header.h2: "O lead percorre quatro pontos. Você acompanha em um."
  header.sub: "Como uma conversa vira agendamento dentro da Likro."
  step 1 headline: "Captação."
  step 1 description: "Instagram, WhatsApp, Facebook entram em uma fila só."
  step 2 headline: "Distribuição."
  step 2 description: "A operação atribui o lead à atendente certa em segundos."
  step 3 headline: "Atendimento."
  step 3 description: "A conversa acontece com contexto do paciente já carregado."
  step 4 headline: "Agendamento."
  step 4 description: "Slot confirmado, retorno marcado, follow-up programado."

v3 — Verbos no presente (manual operacional):
  header.h2: "Como o paciente entra na clínica pela tela."
  header.sub: "Quatro passos do canal ao agendamento — sem ninguém trocar de aplicativo."
  step 1 headline: "O lead aparece."
  step 1 description: "DM no Instagram, mensagem no WhatsApp, contato no Facebook — tudo na mesma fila."
  step 2 headline: "A operação distribui."
  step 2 description: "A lead vai pra atendente certa pelo canal, especialidade ou disponibilidade."
  step 3 headline: "A equipe conversa."
  step 3 description: "Com o histórico do paciente e o contexto do canal já carregados na tela."
  step 4 headline: "O paciente agenda."
  step 4 description: "Slot confirmado na conversa, retorno automático marcado para 30 dias."

PROIBIDO em todas:
- "jornada do cliente" (anti-IA banned)
- "transforme sua clínica" (anti-IA banned)
- Numerologia mística ("4 pilares essenciais")
- Promessas ("garante 100% de retenção" etc — D-27 carry-over)

Validation extras (em how-it-works.test.ts):
- step number values: ["01", "02", "03", "04"] exatamente.
- Cada step.headline length entre 5 e 60 chars (não micro, não parágrafo).
- Cada step.description length entre 30 e 200 chars.
- step[*].mockupKind valores são exatamente ["notification", "routing", "conversation", "calendar-slot"] em ordem.
</copy_guidance>

<visual_guidance>
CONTEXT D-21/D-23 + RESEARCH §330-380:

Section orchestrator (HowItWorks/index.tsx):
- bg-surface-lighter (#fff — neutral light D-23).
- py-24 lg:py-32.
- Container max-w-5xl (menor que default 7xl — HowItWorks é mais editorial/manual).

HowItWorksHeader (RSC):
- h2 size="section" id="how-headline" text-text-primary text-balance max-w-3xl centered.
- sub mt-5 max-w-2xl text-text-secondary text-base sm:text-lg.

HowItWorksStep ("use client"):
- Receives props: `step: HowStep`, `index: number`, `isLast: boolean`.
- useInView({ threshold: 0.3 }).
- Layout DESKTOP (lg:): `flex flex-row items-start gap-12`. Left col: number (w-24 lg:w-32). Right col: content + mini-mockup flex flex-col gap-6 OR side-by-side `lg:flex-row` content vs mockup.
  - Decision: vertical timeline = each step row is `flex flex-row` with left = number, right = `flex flex-col lg:flex-row gap-6`. Content (headline + description) takes left 2/3 of right area, mini-mockup takes right 1/3.
- Layout MOBILE: `flex flex-col gap-4` — number on top, content below, mini-mockup further below.
- Connector line DESKTOP: `before:` pseudo OR explicit `<div className="absolute left-12 top-32 bottom-0 w-px bg-neutral-200">` between steps. Render conditionally (NOT on last step).
- Step padding: py-12 lg:py-16 (large vertical spacing — calm pace, D-23 simplificador).
- Number style: font-mono text-6xl lg:text-7xl text-accent-primary leading-none tracking-tight. THIS is the only purple accent in the section — micro-element D-20 rule satisfied.
- Headline: text-xl lg:text-2xl font-semibold text-text-primary tracking-tight mt-1 (alignment com number baseline).
- Description: text-base text-text-secondary leading-[1.6] mt-2 max-w-md.
- inView class: `inView ? "hero-card-rise" : "opacity-0"` applied to outer row.

HowItWorksMiniMockup (RSC) — switch on mockupKind:
- "notification": Card pequeno w-[180px] lg:w-[200px] h-[80px], bg-surface-card border + radius-[12px], conteúdo = Instagram icon + "@marina_souza" + "há 12s" + dot verde hero-live-pulse.
- "routing": Pill horizontal w-fit, bg-neutral-50 border-neutral-200 px-3 py-2 rounded-full, conteúdo "Marina" + ArrowRight (text-violet-500) + "Dra. Camila".
- "conversation": 2 balões empilhados (bg-emerald-50 right-aligned para atendente / bg-neutral-50 left-aligned para paciente), cada balão w-fit max-w-[180px] px-3 py-2 rounded-[12px] com tail. Texto micro estilo "Oi, posso ajudar?" / "Quero saber preço".
- "calendar-slot": Card w-[180px] lg:w-[200px], bg-surface-card border radius-[12px] p-3, conteúdo = grid 3-col com 1 slot destacado (bg-emerald-50 border-emerald-200 text-emerald-700) "Ter 14:30" + 5 slots neutros.

Aspect: cada mini-mockup é COMPACTO (~80-100px height — menor que Product secundárias mini-mockups que são ~80-100px também; HowItWorks mini-mockups precisam dar EVIDÊNCIA, não roubar atenção D-23).

Mobile reflow:
- Steps: flex-col stack, sem connector line, padding y-8.
- Number: text-5xl (vs text-7xl desktop).
- Mini-mockup: full-width below content, height ~80px.

PROIBIDO:
- next/image em qualquer step.
- Horizontal scroll (v1.1 stretch goal).
- Roxo accent fora dos números dos steps (números = único accent visual D-20).
</visual_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — how-it-works invariants + copy contract tests</name>
  <files>tests/sections/how-it-works-invariants.test.ts, tests/content/how-it-works.test.ts</files>
  <read_first>
    - tests/sections/product-invariants.test.ts (Plan 04-03 pattern — clone)
    - tests/content/product.test.ts (Plan 04-03 — adapt to HOW_COPY shape)
  </read_first>
  <behavior>
    how-it-works-invariants.test.ts: 6 gates standard, pointing at `src/sections/HowItWorks/`.

    how-it-works.test.ts contracts:
    - Test 1: Shape — `header.{h2,sub}`, `steps: tuple 4` with `{number, headline, description, mockupKind}`.
    - Test 2: VARIANTS has v1, v2, v3.
    - Test 3: `steps[*].number` exactly `["01","02","03","04"]`.
    - Test 4: `steps[*].mockupKind` exactly `["notification","routing","conversation","calendar-slot"]`.
    - Test 5: Each `step.headline.length` in [5, 60].
    - Test 6: Each `step.description.length` in [30, 200].
    - Test 7: Joined corpus contains `/cl[ií]nica|paciente|atendimento|agendamento|lead/i` somewhere.
    - Test 8: Anti-IA banned phrases regex green.
    - Test 9: Zero `/jornada do cliente|transforme sua/i`.
    - Test 10: No Dolce Home.
  </behavior>
  <action>
Step 1 — Clone product-invariants.test.ts → how-it-works-invariants.test.ts, swap Product → HowItWorks, productdir → src/sections/HowItWorks.

Step 2 — Create how-it-works.test.ts implementing 10 tests above. `it.skipIf(!fs.existsSync(...))` for graceful skip.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/how-it-works-invariants.test.ts tests/content/how-it-works.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist, invariants has >= 6 `it()`, contracts has >= 10 `it()`.
    - `grep -c "01.*02.*03.*04" tests/content/how-it-works.test.ts` returns >= 1 (number tuple gate).
    - `grep -c "notification.*routing.*conversation.*calendar-slot" tests/content/how-it-works.test.ts` returns >= 1.
    - `npx vitest run` on both files exits 0.
  </acceptance_criteria>
  <done>HowItWorks Wave 0 gates planted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: HOW_COPY (3 variants) + HowItWorks components</name>
  <files>src/content/how-it-works.ts, src/sections/HowItWorks/index.tsx, src/sections/HowItWorks/HowItWorksHeader.tsx, src/sections/HowItWorks/HowItWorksStep.tsx, src/sections/HowItWorks/HowItWorksMiniMockup.tsx</files>
  <read_first>
    - src/content/product.ts (Plan 04-03 — 3-variant pattern)
    - src/sections/Product/ProductSecondaryCard.tsx (mini-mockup switch pattern)
    - src/sections/Product/ProductSecondaryGrid.tsx (useInView stagger pattern)
    - src/sections/HowItWorks/index.tsx (Plan 04-00 scaffold — overwrite)
  </read_first>
  <action>
Step 1 — Create `src/content/how-it-works.ts` with 3 variants per `<copy_guidance>` above. Use verbatim copy from guidance. Types as defined in copy_guidance.

Step 2 — `src/sections/HowItWorks/HowItWorksHeader.tsx`:
```tsx
import { HOW_COPY } from "@/content/how-it-works";

export function HowItWorksHeader() {
  return (
    <header className="mx-auto max-w-3xl text-center">
      <h2
        id="how-headline"
        className="text-balance text-3xl font-semibold leading-[1.1] tracking-[-0.02em] text-text-primary sm:text-4xl lg:text-[2.5rem]"
      >
        {HOW_COPY.header.h2}
      </h2>
      <p className="mx-auto mt-5 max-w-2xl text-balance text-base leading-[1.6] text-text-secondary sm:text-lg">
        {HOW_COPY.header.sub}
      </p>
    </header>
  );
}
```

Step 3 — `src/sections/HowItWorks/HowItWorksMiniMockup.tsx` (RSC, switch by mockupKind). Each kind renders a small compositional card with lucide icons:

```tsx
import { ArrowRight, CalendarCheck2, Instagram } from "lucide-react";
import type { HowStep } from "@/content/how-it-works";

interface Props {
  kind: HowStep["mockupKind"];
}

export function HowItWorksMiniMockup({ kind }: Props) {
  if (kind === "notification") {
    return (
      <div className="w-[200px] rounded-[12px] border border-neutral-200/70 bg-surface-card p-3 ring-1 ring-inset ring-white/80 shadow-[0_8px_18px_-10px_rgba(8,12,24,0.12)]">
        <div className="flex items-center gap-2">
          <Instagram className="size-4 text-rose-400" strokeWidth={2.25} />
          <span className="flex-1 text-[12px] font-semibold text-neutral-900">@marina_souza</span>
          <span className="hero-live-pulse inline-block size-[5px] rounded-full bg-emerald-500" />
        </div>
        <div className="mt-1.5 truncate text-[11px] text-neutral-500">novo lead · há 12s</div>
      </div>
    );
  }
  if (kind === "routing") {
    return (
      <div className="inline-flex w-fit items-center gap-1.5 rounded-full border border-neutral-200 bg-neutral-50 px-3 py-2">
        <span className="text-[12px] font-semibold text-neutral-900">Marina</span>
        <ArrowRight className="size-3.5 text-violet-500" strokeWidth={2.5} />
        <span className="text-[12px] font-semibold text-neutral-900">Dra. Camila</span>
      </div>
    );
  }
  if (kind === "conversation") {
    return (
      <div className="flex w-[220px] flex-col gap-1.5">
        <div className="self-start max-w-[160px] rounded-[12px] rounded-bl-[4px] border border-neutral-200 bg-neutral-50 px-3 py-1.5 text-[11px] text-neutral-700">Vi o vídeo da Dra. Camila — queria saber preço.</div>
        <div className="self-end max-w-[160px] rounded-[12px] rounded-br-[4px] border border-emerald-200/70 bg-emerald-50 px-3 py-1.5 text-[11px] text-emerald-800">Oi Marina! Vou te passar agora.</div>
      </div>
    );
  }
  // calendar-slot
  return (
    <div className="grid w-[200px] grid-cols-3 gap-1.5">
      {Array.from({ length: 6 }).map((_, i) => {
        const isSelected = i === 4;
        return (
          <div
            key={i}
            className={`flex h-9 items-center justify-center rounded-md border text-[10.5px] font-mono ${isSelected ? "border-emerald-300 bg-emerald-50 text-emerald-700" : "border-neutral-200 bg-neutral-50 text-neutral-400"}`}
          >
            {isSelected ? "Ter 14:30" : "—"}
          </div>
        );
      })}
    </div>
  );
}
```

Step 4 — `src/sections/HowItWorks/HowItWorksStep.tsx` ("use client"):
```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import type { HowStep } from "@/content/how-it-works";
import { HowItWorksMiniMockup } from "./HowItWorksMiniMockup";

interface Props {
  step: HowStep;
  index: number;
  isLast: boolean;
}

export function HowItWorksStep({ step, index, isLast }: Props) {
  const [ref, inView] = useInView<HTMLDivElement>({ threshold: 0.3 });
  return (
    <div
      ref={ref}
      className={`relative flex flex-col gap-6 py-10 lg:flex-row lg:items-start lg:gap-12 lg:py-14 ${inView ? "hero-card-rise" : "opacity-0"}`}
      style={{ animationDelay: `${index * 80}ms` }}
    >
      <div className="w-24 shrink-0 lg:w-32">
        <div className="font-mono text-5xl leading-none tracking-tight text-accent-primary lg:text-7xl">
          {step.number}
        </div>
      </div>
      <div className="flex flex-1 flex-col gap-6 lg:flex-row lg:items-start lg:gap-10">
        <div className="flex-1">
          <h3 className="text-balance text-xl font-semibold leading-[1.25] tracking-[-0.015em] text-text-primary lg:text-2xl">
            {step.headline}
          </h3>
          <p className="mt-2 max-w-md text-base leading-[1.55] text-text-secondary">
            {step.description}
          </p>
        </div>
        <div className="shrink-0">
          <HowItWorksMiniMockup kind={step.mockupKind} />
        </div>
      </div>
      {/* Vertical connector — desktop only, NOT on last step */}
      {!isLast && (
        <div aria-hidden="true" className="absolute left-12 top-20 hidden h-full w-px bg-neutral-200 lg:block" />
      )}
    </div>
  );
}
```

Step 5 — Overwrite `src/sections/HowItWorks/index.tsx`:
```tsx
/**
 * HowItWorks — Phase 4 § HowItWorks (NARR-04).
 *
 * D-21/D-22: 4 passos timeline vertical, fluxo real do lead.
 * D-23: tom explicativo simplificador, NÃO compete com Hero/Product.
 * D-05: surface LIGHT NEUTRAL (#fff).
 * NARR-06: zero motion lib.
 */
import { Container } from "@/components/ui/container";
import { HOW_COPY } from "@/content/how-it-works";
import { HowItWorksHeader } from "./HowItWorksHeader";
import { HowItWorksStep } from "./HowItWorksStep";

export function HowItWorks() {
  return (
    <section
      id="how-it-works"
      aria-labelledby="how-headline"
      className="relative bg-surface-lighter py-24 lg:py-32"
    >
      <Container className="max-w-5xl">
        <HowItWorksHeader />
        <ol className="mt-16 lg:mt-20">
          {HOW_COPY.steps.map((step, i) => (
            <li key={step.number}>
              <HowItWorksStep step={step} index={i} isLast={i === HOW_COPY.steps.length - 1} />
            </li>
          ))}
        </ol>
      </Container>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/sections/how-it-works-invariants.test.ts tests/content/how-it-works.test.ts && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "HOW_COPY_VARIANTS" src/content/how-it-works.ts` returns >= 2.
    - `grep -c '"01"' src/content/how-it-works.ts` returns >= 3 (v1+v2+v3 all have step number 01).
    - `grep -c '"02"' src/content/how-it-works.ts` returns >= 3.
    - `grep -c '"03"' src/content/how-it-works.ts` returns >= 3.
    - `grep -c '"04"' src/content/how-it-works.ts` returns >= 3.
    - `grep -cE "notification|routing|conversation|calendar-slot" src/content/how-it-works.ts` returns >= 12 (4 kinds × 3 variants).
    - `grep -c "use client" src/sections/HowItWorks/HowItWorksStep.tsx` returns 1.
    - `grep -c "useInView" src/sections/HowItWorks/HowItWorksStep.tsx` returns >= 1.
    - `grep -c "text-accent-primary" src/sections/HowItWorks/HowItWorksStep.tsx` returns >= 1 (number color).
    - `grep -c "bg-surface-lighter" src/sections/HowItWorks/index.tsx` returns >= 1.
    - `grep -cE "from\\s+['\"]motion|from\\s+['\"]framer-motion|from\\s+['\"]@/components/motion" src/sections/HowItWorks/` (recursive) returns 0.
    - `grep -c "WhatsAppCta" src/sections/HowItWorks/` (recursive) returns 0.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/sections/how-it-works-invariants.test.ts tests/content/how-it-works.test.ts` exits 0.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <done>HowItWorks ships com 4 steps timeline + mini-mockups distintos + roxo accent micro-element nos números.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: HowItWorks copy review PR + Playwright timeline smoke</name>
  <files>src/content/how-it-works.ts (variante final aprovada substitui HOW_COPY = HOW_COPY_VARIANTS.vN)</files>
  <action>Lenny revisa as 3 variantes em src/content/how-it-works.ts via PR. Executor aguarda LGTM e ajusta a variante ativa. Pipeline pré-merge: code-review skill + Playwright MCP per CLAUDE.md user instructions.</action>
  <verify>
    <automated>npx vitest run tests/content/how-it-works.test.ts tests/sections/how-it-works-invariants.test.ts</automated>
  </verify>
  <done>Lenny approved a copy variant via PR comment or inline edit; executor merged with HOW_COPY pointing to approved variant; Playwright screenshots attached to PR description; CI green.</done>
  <what-built>
    - 3 variantes em `src/content/how-it-works.ts` (v1 simples / v2 Linear/Stripe / v3 verbos no presente).
    - HowItWorks LIGHT neutral, 4 passos timeline vertical, números grandes em accent roxo, mini-mockups distintos por step, connector line vertical entre steps no desktop.
  </what-built>
  <how-to-verify>
1. `npm run dev` → scroll até HowItWorks.
2. Verificar:
   - 4 steps em ordem 01→04, números grandes em roxo (font-mono).
   - Cada step entra com hero-card-rise stagger 80ms.
   - Mini-mockup distinto por step (notification card / routing pill / conversation balões / calendar slot).
   - Linha conectora vertical entre steps no desktop; some no mobile.
   - Tom explicativo, NÃO compete visualmente com Product.
3. Lenny revisa 3 variantes em `src/content/how-it-works.ts` → LGTM v?.
4. Pipeline pré-merge: code review + Playwright (desktop + mobile screenshots + reduced-motion test).
5. Executor ajusta `HOW_COPY = HOW_COPY_VARIANTS.vN` e merge.
  </how-to-verify>
  <resume-signal>Lenny aprova variante; Playwright screenshots anexados; merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser DOM | useInView in HowItWorksStep (per-step) |
| copy→DOM | HOW_COPY rendered via React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-12 | Tampering | Future contributor inflates HowItWorks beyond 4 steps OR removes step numbers OR introduces horizontal scroll (V1.1 stretch only) | mitigate | tests/content/how-it-works.test.ts Tests 3-4 strictly assert 4-step tuple + mockupKind sequence; per-step number values locked to 01-04. |
</threat_model>

<verification>
- `npx vitest run tests/sections/how-it-works-invariants.test.ts tests/content/how-it-works.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- `npx next build` exits 0.
- Visual: 4 steps render in vertical timeline; connector line visible desktop, absent mobile; mini-mockups distinct.
</verification>

<success_criteria>
- NARR-04 satisfied (staggered reveal vertical, horizontal scroll deferred to v1.1).
- NARR-06 + NARR-07 satisfied.
- COPY-02 + COPY-03 satisfied.
- D-23 tom explicativo simplificador (visual review via Task 3).
- Lenny approved copy via PR.
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-04-SUMMARY.md`:
- Variante final aprovada
- Timeline screenshot desktop + mobile
- 4 mini-mockup kinds rendered correctly
- Tom verdict do Lenny (não compete com Product)
</output>
</content>
</invoke>