---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 03
type: execute
wave: 4
depends_on: ["04-00", "04-02"]
files_modified:
  - src/content/product.ts
  - tests/content/product.test.ts
  - tests/sections/product-invariants.test.ts
  - src/sections/Product/index.tsx
  - src/sections/Product/ProductHeader.tsx
  - src/sections/Product/ProductHeroFeature.tsx
  - src/sections/Product/ProductHeroFeatureMockup.tsx
  - src/sections/Product/ProductSecondaryGrid.tsx
  - src/sections/Product/ProductSecondaryCard.tsx
autonomous: false
requirements:
  - NARR-03
  - NARR-06
  - NARR-07
  - NARR-08
  - COPY-02
  - COPY-03
threats:
  - T-4-09
  - T-4-10
  - T-4-11
must_haves:
  truths:
    - "Product section renderiza header (h2 + sub) + 1 hero feature 'Atendimento multicanal' full-width (mockup + copy) + grid de 3 secundárias em row (Distribuição automática / Follow-up e retorno / Agenda operacional) — D-16/D-17/D-18/D-19 reinterpretação NARR-03"
    - "Surface LIGHT off-white (bg-surface-light), seção mais limpa visualmente da landing (D-20)"
    - "Hero feature mockup central: 1 inbox-style card grande (representando caixa de entrada multicanal) + 2 overlay cards menores em camadas — pattern HeroCardStack ampliado"
    - "Mockup inclui MICRO-LINHA implícita de Agentes IA (NARR-08 + D-20.1): texto sutil tipo 'atribuído automaticamente · sugestão de IA aceita' DENTRO do mockup — não banner, não seção dedicada"
    - "Sem buzzwords cyberpunk/hype de IA em nenhum lugar do Product (D-20.1: Linear/Stripe/Vercel tom, NÃO Anthropic/OpenAI marketing)"
    - "3 secundárias têm mini-mockup CSS compacto cada (mini-pill 'Lead → Atendente' / mini-timeline 3 pontos / mini-grid 3x2 slots) — RESEARCH §323"
    - "Zero checklist 'feature ✓ feature ✓' (D-19 — mata percepção premium)"
    - "Hero feature mockup tem 2 overlays com hero-card-float-a/c (RESEARCH §302 — operação viva sutil)"
    - "PRODUCT_COPY contém 'clínica' OU 'paciente' OU 'atendimento' na h2/sub OU em pelo menos 1 secundária (COPY-03)"
    - "Zero menção a 'Relatórios' (D-16 reinterpretação — Relatórios out v1)"
    - "Zero menção a 'CRM' como pilar standalone (D-16 — CRM implícito nas secundárias via Distribuição + Follow-up)"
    - "Zero CTA WhatsApp (D-29)"
    - "Mobile: hero feature stack vertical (texto cima + mockup baixo, mockup com rotações reduzidas); secundárias grid-cols-1 stacked"
    - "Lenny aprovou variante de copy via PR (D-17)"
  artifacts:
    - path: "src/content/product.ts"
      provides: "PRODUCT_COPY 3 variantes — header (h2/sub) + feature hero (eyebrow/title/description + mockup line IA) + 3 secundárias"
      contains: "PRODUCT_COPY_VARIANTS"
      min_lines: 150
    - path: "tests/content/product.test.ts"
      provides: "Contract tests: shape, clínica, anti-IA, no Dolce Home, no Relatórios, no CRM-as-pillar, 3 variantes"
      min_lines: 60
    - path: "tests/sections/product-invariants.test.ts"
      provides: "Standard 6 grep gates + bonus: zero 'IA'/'AI' em cyberpunk styling (no neon glow, no purple gradient backgrounds em mockup)"
    - path: "src/sections/Product/index.tsx"
      provides: "Orchestrator RSC: ProductHeader + ProductHeroFeature + ProductSecondaryGrid"
      contains: "ProductHeroFeature"
    - path: "src/sections/Product/ProductHeader.tsx"
      provides: "RSC h2 + sub"
    - path: "src/sections/Product/ProductHeroFeature.tsx"
      provides: "'use client' wrapper consumindo useInView; renders 2-col grid (copy left, mockup right) com stack mobile"
      contains: "useInView"
    - path: "src/sections/Product/ProductHeroFeatureMockup.tsx"
      provides: "RSC composição central inbox + 2 overlays float; inclui linha IA implícita"
    - path: "src/sections/Product/ProductSecondaryGrid.tsx"
      provides: "'use client' grid 3-col com stagger via useInView"
      contains: "useInView"
    - path: "src/sections/Product/ProductSecondaryCard.tsx"
      provides: "RSC card primitive — eyebrow + title + description + mini-mockup CSS"
  key_links:
    - from: "src/sections/Product/ProductHeroFeature.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView trigger"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/Product/ProductSecondaryGrid.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView stagger"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/Product/index.tsx"
      to: "src/content/product.ts"
      via: "PRODUCT_COPY import"
      pattern: 'from "@/content/product"'
---

<objective>
Implementar Product — seção LIGHT off-white com mais peso visual da landing (D-20). Estrutura reinterpretada (D-16): 1 hero feature "Atendimento multicanal" full-width + 3 secundárias em row (Distribuição automática / Follow-up e retorno / Agenda operacional). Hero feature mockup é a composição central (inbox-style card grande + 2 overlays float) — pattern HeroCardStack escalado pra single-feature hero.

NARR-08 (Agentes IA) está implícito no mockup via micro-linha sutil ("atribuído automaticamente · sugestão de IA aceita" OU similar — D-20.1). NÃO é seção dedicada. NÃO tem buzzwords cyberpunk. Tom Linear/Stripe/Vercel/Arc/Apple.

Purpose: NARR-03 reinterpretado satisfeito (4 pilares ROADMAP virou 1 hero + 3 secundárias per D-16). NARR-08 satisfeito via D-20.1 implicit-AI-line. COPY-03 satisfeito (clínica/paciente/atendimento). NARR-06/07 satisfeitos.

Output: Maior plan da Phase 4 — 9 arquivos / 4 tasks. **Scope justified by complexity weight per CONTEXT D-20** (Product = "pressure-high" section, mais limpa visualmente da landing; Task 3 well-isolated as ProductHeroFeature + Mockup; splitting would add dependency overhead without clear benefit). Estruturalmente: 1 copy module + 1 orchestrator + 5 sub-components + 2 tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-00-SUMMARY.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-01-SUMMARY.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-02-SUMMARY.md

<interfaces>
From src/hooks/use-in-view.ts (Plan 04-00):
```typescript
export function useInView<T extends Element = HTMLDivElement>(opts?: { threshold?: number; rootMargin?: string; once?: boolean }): [React.RefObject<T | null>, boolean];
```

From src/components/ui/container.tsx: Container with max-w-7xl default; can override via className.

From src/components/ui/headline.tsx: Headline `<h2>` size="section" provides Tailwind sizing (text-3xl sm:text-4xl lg:text-5xl font-bold tracking-tight); use className override for tracking/leading refinement.

From src/app/globals.css (Phase 3 — reuso, NÃO modificar):
- hero-card-rise (entrance, 0.6s)
- hero-card-float-a (7s, -4px peak) — overlay 1
- hero-card-float-c (8s, -5px peak) — overlay 2
- hero-live-pulse (dot 2s loop)
- Surface tokens light: bg-surface-light, bg-surface-card (#FBFCFD), bg-surface-card-strong (#fff)
- Text light: text-text-primary, text-text-secondary, text-text-muted

From src/sections/Hero/HeroCardStack.tsx (Phase 3 — visual reference):
- Card: w-[210px] sm:w-[300px] for hero stack
- For Product feature hero mockup central: scale UP to w-[280px] sm:w-[420px] lg:w-[480px] (RESEARCH §655: ~150% of HeroCard width)
- Overlays in Product: w-[160px] sm:w-[200px] (RESEARCH §655: ~60% of HeroCard)

</interfaces>

<copy_guidance>
CONTEXT D-16..D-20.1 + RESEARCH §270-328:

Structure:
```ts
export type ProductCopy = {
  header: { h2: string; sub: string };
  feature: {
    eyebrow: string; // pequeno label uppercase tracking-wide acima do title
    title: string;
    description: string; // 1-2 frases descrevendo o que a feature É operacionalmente
    mockup: {
      inboxRows: ReadonlyArray<{ channel: "instagram" | "whatsapp" | "facebook"; from: string; preview: string; meta?: string; status?: "new" | "assigned" | "responded" }>;
      iaLine: string; // micro-linha implícita IA (D-20.1)
    };
  };
  secondaries: readonly [
    { eyebrow: string; title: string; description: string; mockupKind: "routing-pill" },
    { eyebrow: string; title: string; description: string; mockupKind: "timeline-3pts" },
    { eyebrow: string; title: string; description: string; mockupKind: "calendar-grid" },
  ];
};
```

3 variantes contrastantes:

v1 — Operacional concreto:
  header.h2: "A operação do atendimento, em uma única camada."
  header.sub: "Lead do Instagram, conversa no WhatsApp, agenda da recepção — tudo na mesma tela."
  feature.title: "Atendimento multicanal."
  feature.description: "A equipe responde tudo de um lugar só. WhatsApp, Instagram e Facebook em uma caixa de entrada compartilhada — com contexto do paciente já carregado."
  feature.mockup.iaLine: "Atribuído automaticamente · sugestão de resposta aceita"
  secondaries: ver bloco abaixo.

v2 — Identidade categórica:
  header.h2: "A camada operacional da clínica de estética."
  header.sub: "O canal, o atendimento e a agenda em sincronia — sem o atendente trocar de aplicativo."
  feature.title: "Caixa de entrada multicanal."
  feature.description: "Cada lead entra por um canal diferente — Instagram, WhatsApp, Facebook. Aqui ele entra em uma fila só."
  feature.mockup.iaLine: "Roteamento automático · contexto do paciente carregado"
  secondaries: idem v1 com tom mais editorial.

v3 — Editorial seco:
  header.h2: "Um lugar pro atendimento da clínica viver."
  header.sub: "Multicanal, integrado à agenda, com a equipe na mesma página."
  feature.title: "Atendimento multicanal."
  feature.description: "Instagram, WhatsApp, Facebook — uma caixa de entrada. Distribuição automática pra equipe certa, histórico do paciente sempre por perto."
  feature.mockup.iaLine: "Distribuído pela IA · pronto pra resposta"
  secondaries: idem.

Inbox rows mockup (consistent across variants — fatos da operação):
```ts
inboxRows: [
  { channel: "instagram", from: "@marina_souza", preview: "Vi o vídeo da Dra. Camila — queria saber preço.", meta: "há 12s", status: "new" },
  { channel: "whatsapp", from: "Carla Mendes", preview: "Posso remarcar pra terça?", meta: "há 1min", status: "assigned" },
  { channel: "facebook", from: "Lucas Pereira", preview: "Obrigado! Confirmado pra amanhã 14h.", meta: "há 3min", status: "responded" },
]
```

Secundárias (consistent — variar apenas tom da description):

s1: Distribuição automática
  eyebrow: "DISTRIBUIÇÃO"
  title: "Cada lead com o atendente certo."
  description: "Roteamento automático por canal, idioma, especialidade — sem fila travada."
  mockupKind: "routing-pill" (mini-mockup: pill "Marina → Dra. Camila" com seta lucide)

s2: Follow-up e retorno
  eyebrow: "FOLLOW-UP"
  title: "Ninguém esquece de voltar a falar."
  description: "Conversas que ficaram em aberto viram lembrete; retornos do paciente viram agendamento."
  mockupKind: "timeline-3pts" (mini-mockup: 3 pontos conectados horizontal — 1ª mensagem · 1ª resposta · agendado)

s3: Agenda operacional
  eyebrow: "AGENDA"
  title: "A agenda na mesma tela do atendimento."
  description: "Slots disponíveis na hora, agendamento direto da conversa, confirmação automática via WhatsApp."
  mockupKind: "calendar-grid" (mini-mockup: grid 3x2 representando slots, 2 ocupados em accent neutro, 4 vagos)

PROIBIDO em todas as 3 variantes:
- Palavra "Relatórios" (D-16 — out v1)
- "CRM" como pilar standalone (pode aparecer como verbo "centraliza CRM" no descritivo, mas NÃO como title de seção/secundária)
- "IA" "agente IA" "inteligência artificial" em title/eyebrow — só em `iaLine` do mockup (D-20.1)
- Checklist visual estilo "✓ Multicanal ✓ Automação" (D-19)
- Cyberpunk vocabulary: "neural", "máquina", "robô", "automação inteligente que aprende"
</copy_guidance>

<visual_guidance>
RESEARCH §270-328:

ProductHeader (RSC):
- h2 size="section" (text-3xl sm:text-4xl lg:text-5xl), `id="product-headline"`, text-text-primary, text-balance, max-w-3xl centered.
- sub max-w-2xl mt-5 text-base sm:text-lg text-text-secondary.

ProductHeroFeature ("use client"):
- 2-col grid desktop (`lg:grid-cols-[2fr_3fr] gap-12`), stack mobile (`grid-cols-1 gap-10`).
- Left col: eyebrow uppercase tracking-[0.18em] text-[11px] text-text-muted (or text-accent-primary tipográfico minimal D-20 accent rule) → title text-2xl sm:text-3xl lg:text-4xl text-text-primary tracking-tight → description text-base text-text-secondary leading-[1.6] max-w-md.
- Right col: `<ProductHeroFeatureMockup />`.
- Container marginTop após ProductHeader: mt-20 lg:mt-24.

ProductHeroFeatureMockup (RSC, no client hook — float animations are CSS-only autonomous):
- Central inbox card: w-[280px] sm:w-[420px] lg:w-[480px], bg-surface-card-strong (#fff — variar surface dos overlays #FBFCFD pra hierarquia D-20), rounded-[14px], shadow 2-layer LIGHT (`shadow-[0_20px_40px_-12px_rgba(8,12,24,0.15),0_4px_12px_-4px_rgba(8,12,24,0.08)]`), border border-neutral-200/70, ring-1 ring-inset ring-white.
- Card header: title "Atendimentos" + label "Caixa de entrada multicanal" + live-pulse dot verde "3 novos".
- Card body: render 3 inbox rows from PRODUCT_COPY.feature.mockup.inboxRows. Each row: icon canal (Instagram pink-500 muted, WhatsApp emerald-600 muted, Facebook blue-500 muted), from (font-semibold), preview (truncate), meta (font-mono right), status badge sutil.
- iaLine: rendered abaixo da última row inbox, com separator border-top, em pattern de "system event" — text-[11px] uppercase tracking-[0.12em] text-text-muted + ícone Sparkles ou similar lucide muito sutil. Exemplo: `· Atribuído automaticamente · sugestão de resposta aceita ·`
- Overlays (2): position absolute em cima do central card.
  - Overlay 1: top-right, w-[160px] sm:w-[200px], rotate 4deg, bg-surface-card (#FBFCFD), conteúdo = mini-confirmação ("Carla Mendes · remarcado · Ter 14:30"). Class `hero-card-float-a`.
  - Overlay 2: bottom-left, w-[140px] sm:w-[180px], rotate -3deg, conteúdo = mini-routing ("Marina → Dra. Camila"). Class `hero-card-float-c`.
- ZERO purple gradient backgrounds. ZERO neon glow. ZERO 3D perspective. ZERO browser chrome.

ProductSecondaryGrid ("use client"):
- `useInView({ threshold: 0.2 })`.
- Grid `grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8` mt-24 lg:mt-32.
- 3 ProductSecondaryCard com inline `style={{ animationDelay: \`${i * 100}ms\` }}` + className `inView ? "hero-card-rise" : "opacity-0"`.

ProductSecondaryCard (RSC):
- bg-surface-card-strong, rounded-[14px], border border-neutral-200/70, ring-1 ring-inset ring-white, shadow LIGHT (`shadow-[0_8px_24px_-12px_rgba(8,12,24,0.1)]`), p-6 lg:p-7.
- eyebrow uppercase tracking-[0.18em] text-[11px] text-text-muted (variação OK: text-accent-primary se a discreção apertar — micro-elemento, vide D-20 accent rule).
- title text-lg lg:text-xl font-semibold text-text-primary mt-3 tracking-tight.
- description text-sm text-text-secondary leading-[1.5] mt-2.
- mini-mockup mt-5, altura fixa h-[80px] lg:h-[100px], switch por `mockupKind`:
  - "routing-pill": pill com texto + lucide ArrowRight, fundo neutral-50, border neutral-200, padding 2-3.
  - "timeline-3pts": flex row de 3 dots (size-2 rounded-full) conectados por hr horizontal; primeiro dot bg-emerald-500 + hero-live-pulse, segundo bg-neutral-400, terceiro bg-neutral-300; labels mini abaixo em font-mono.
  - "calendar-grid": grid grid-cols-3 grid-rows-2 gap-1 com 6 cells; 2 com bg-neutral-200 (ocupado), 4 com bg-neutral-50 + border-neutral-100 (vago); labels horários micro.

Section orchestrator (Product/index.tsx):
- bg-surface-light, py-28 lg:py-36 (maior que Pain/Bridge — Product é o "miolo" D-20).
- Container max-w-7xl.

Mobile reflow:
- Hero feature stack vertical: copy primeiro, mockup central diminui pra w-[280px], overlays rotação reduzida ±2deg.
- Secondary grid: grid-cols-1 vertical stack.
- Padding section py-20 mobile.

PROIBIDO:
- `priority` em qualquer `<Image>` (não usa next/image — tudo CSS).
- bg-accent-primary em qualquer surface grande.
- "Cyberpunk" iaLine styling: SEM glow roxo gigante, SEM neon ring, SEM gradient saturado. iaLine é micro-texto editorial em text-muted — invisível-quase, visível-só-se-olhar.
</visual_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — product invariants + product copy contract tests</name>
  <files>tests/sections/product-invariants.test.ts, tests/content/product.test.ts</files>
  <read_first>
    - tests/sections/pain-invariants.test.ts (Plan 04-01 pattern — clone)
    - tests/content/pain.test.ts (Plan 04-01)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-16/D-20.1 prohibitions)
  </read_first>
  <behavior>
    product-invariants.test.ts: 6 standard gates (motion JSX, motion-lib import, vh, priority, hardcoded PT-BR, walk-empty sanity) pointing at `src/sections/Product/`.

    product.test.ts contracts:
    - Test 1: Shape — `header.{h2,sub}`, `feature.{eyebrow,title,description,mockup.{inboxRows,iaLine}}`, `secondaries: readonly tuple 3` with `{eyebrow,title,description,mockupKind}`.
    - Test 2: VARIANTS has v1, v2, v3.
    - Test 3: At least one of `h2/sub/feature.description/secondaries[*].description` contains `/cl[ií]nica|paciente|atendimento|agendamento/i`.
    - Test 4: Anti-IA banned phrases (standard regex) — joined corpus.
    - Test 5: Zero `/relat[óo]rios?/i` in PRODUCT_COPY corpus (D-16 — Relatórios out).
    - Test 6: Zero `\bCRM\b` standalone in `header.h2`, `feature.title`, or any `secondaries[*].title` (D-16 — CRM not a pillar). Allowed in descriptions only.
    - Test 7: Zero `/intelig[êe]ncia artificial|agente de ia|chatbot|neural|m[áa]quina que aprende/i` in PRODUCT_COPY corpus (D-20.1 cyberpunk/hype gate).
    - Test 8: `feature.mockup.iaLine` exists and contains either "automaticamente", "automático", "IA", or "sugestão" (D-20.1 — micro-line presence required).
    - Test 9: Zero Dolce Home.
    - Test 10: `feature.title` contains "multicanal" OR "atendimento" (D-17 — feature hero é "Atendimento multicanal").
    - Test 11: `secondaries[0].title` mentions routing concept (regex: `/distribui|rotear|atribu/i`).
    - Test 12: `secondaries[1].title` mentions follow-up concept (regex: `/follow|retornar|voltar|relembr|esquec/i`).
    - Test 13: `secondaries[2].title` mentions agenda concept (regex: `/agenda|agendamento|hor[áa]rio|slot/i`).
  </behavior>
  <action>
Step 1 — `tests/sections/product-invariants.test.ts`: clone of pain-invariants with Pain→Product. 6 tests.

Step 2 — `tests/content/product.test.ts`. Header:
```ts
/**
 * Phase 4 / Plan 04-03 — Product copy contracts.
 *
 * Gates específicos:
 * - D-16 reinterpretation: zero "Relatórios", zero "CRM" como title de pillar.
 * - D-20.1: zero cyberpunk vocabulary (neural, máquina, agente de IA cyberpunk);
 *   iaLine no mockup é a única menção implícita de IA, deve conter "automaticamente"
 *   ou "IA" ou "sugestão" / "automático".
 * - D-17: feature.title contém "multicanal" OU "atendimento" — feature hero é "Atendimento multicanal".
 */
```

Implement 13 tests as defined. Use `it.skipIf(!fs.existsSync(...))` until Task 2 lands copy.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/product-invariants.test.ts tests/content/product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sections/product-invariants.test.ts` exists, >= 6 `it(...)` blocks.
    - `tests/content/product.test.ts` exists, >= 13 `it(...)` blocks.
    - `grep -cE "relat[óo]rios?|CRM" tests/content/product.test.ts` returns >= 2 (D-16 gates present).
    - `grep -cE "intelig[êe]ncia artificial|neural|m[áa]quina que aprende" tests/content/product.test.ts` returns >= 1 (D-20.1 gate present).
    - `grep -c "multicanal" tests/content/product.test.ts` returns >= 1 (D-17 gate).
    - `npx vitest run tests/sections/product-invariants.test.ts` exits 0.
    - `npx vitest run tests/content/product.test.ts` exits 0 (skip or "module not found" — both acceptable pre-Task-2).
  </acceptance_criteria>
  <done>Product Wave 0 gates planted with full D-16/D-17/D-20.1 enforcement.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PRODUCT_COPY (3 variants) + Product orchestrator + ProductHeader + ProductSecondaryGrid/Card</name>
  <files>src/content/product.ts, src/sections/Product/index.tsx, src/sections/Product/ProductHeader.tsx, src/sections/Product/ProductSecondaryGrid.tsx, src/sections/Product/ProductSecondaryCard.tsx</files>
  <read_first>
    - src/content/pain.ts (3-variant header pattern)
    - src/sections/Pain/index.tsx (RSC orchestrator pattern)
    - src/sections/Pain/PainCardConstellation.tsx (useInView + map + stagger pattern)
    - src/sections/Product/index.tsx (Plan 04-00 scaffold — overwrite)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-16..D-20)
  </read_first>
  <action>
Step 1 — Create `src/content/product.ts` with 3 variants per `<copy_guidance>`. Header comment documents v1/v2/v3 directions explicitly with the verbatim copy from guidance above.

Types section verbatim:
```ts
export type ProductInboxRow = {
  channel: "instagram" | "whatsapp" | "facebook";
  from: string;
  preview: string;
  meta?: string;
  status?: "new" | "assigned" | "responded";
};
export type ProductSecondaryMockupKind = "routing-pill" | "timeline-3pts" | "calendar-grid";
export type ProductSecondary = {
  eyebrow: string;
  title: string;
  description: string;
  mockupKind: ProductSecondaryMockupKind;
};
export type ProductCopy = {
  header: { h2: string; sub: string };
  feature: {
    eyebrow: string;
    title: string;
    description: string;
    mockup: {
      cardTitle: string;
      cardLabel: string;
      newCountBadge: string;
      inboxRows: ReadonlyArray<ProductInboxRow>;
      iaLine: string;
      overlayRouting: { from: string; to: string };
      overlayConfirm: { name: string; slot: string };
    };
  };
  secondaries: readonly [ProductSecondary, ProductSecondary, ProductSecondary];
};
export const PRODUCT_COPY_VARIANTS = { v1: {...}, v2: {...}, v3: {...} } as const satisfies Record<"v1"|"v2"|"v3", ProductCopy>;
export const PRODUCT_COPY: ProductCopy = PRODUCT_COPY_VARIANTS.v1;
```

Each variant supplies ALL fields. inboxRows + overlay objects are CONSISTENT across variants (fatos da operação). Vary: header.h2, header.sub, feature.title, feature.description, feature.mockup.iaLine, secondaries[*].title/description (eyebrow consistent: "DISTRIBUIÇÃO" / "FOLLOW-UP" / "AGENDA").

Step 2 — `src/sections/Product/ProductHeader.tsx`:
```tsx
import { PRODUCT_COPY } from "@/content/product";

export function ProductHeader() {
  return (
    <header className="mx-auto max-w-3xl text-center">
      <h2
        id="product-headline"
        className="text-balance text-3xl font-semibold leading-[1.1] tracking-[-0.02em] text-text-primary sm:text-4xl lg:text-[2.75rem]"
      >
        {PRODUCT_COPY.header.h2}
      </h2>
      <p className="mx-auto mt-5 max-w-2xl text-balance text-base leading-[1.6] text-text-secondary sm:text-lg">
        {PRODUCT_COPY.header.sub}
      </p>
    </header>
  );
}
```

Step 3 — `src/sections/Product/ProductSecondaryCard.tsx` (RSC, mini-mockup switch):
- Renders article with border + 2-layer LIGHT shadow + radius 14px.
- eyebrow uppercase tracking-[0.18em] text-[11px] text-text-muted (option: text-accent-primary for activation if visual review apertar).
- title text-lg lg:text-xl font-semibold mt-3.
- description text-sm text-text-secondary leading-[1.5] mt-2.
- mini-mockup absolute or relative below description, h-[80px] lg:h-[100px], switch on `mockupKind`:
  - routing-pill: rounded-full bg-neutral-50 border border-neutral-200 px-3 py-2 inline-flex items-center gap-2 — text "from" ArrowRight text "to". Use real names from PRODUCT_COPY (default: "Marina" → "Dra. Camila").
  - timeline-3pts: flex items-center gap-2 — 3 dots size-2 rounded-full bg-{emerald-500 hero-live-pulse / neutral-400 / neutral-300} connected by `<hr className="flex-1 border-neutral-200" />`. Labels font-mono mini below.
  - calendar-grid: grid grid-cols-3 grid-rows-2 gap-1.5 — 6 cells h-6 rounded-md, 2 with bg-neutral-200 + border-neutral-300 (booked), 4 with bg-neutral-50 + border-neutral-200 (open).

Step 4 — `src/sections/Product/ProductSecondaryGrid.tsx` (`"use client"`):
```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import { PRODUCT_COPY } from "@/content/product";
import { ProductSecondaryCard } from "./ProductSecondaryCard";

export function ProductSecondaryGrid() {
  const [ref, inView] = useInView<HTMLDivElement>({ threshold: 0.2 });
  return (
    <div ref={ref} className="mt-24 grid grid-cols-1 gap-6 lg:mt-32 lg:grid-cols-3 lg:gap-8">
      {PRODUCT_COPY.secondaries.map((card, i) => (
        <div
          key={card.title}
          className={inView ? "hero-card-rise" : "opacity-0"}
          style={{ animationDelay: `${i * 100}ms` }}
        >
          <ProductSecondaryCard card={card} />
        </div>
      ))}
    </div>
  );
}
```

Step 5 — Overwrite `src/sections/Product/index.tsx`:
```tsx
/**
 * Product — Phase 4 § Product (NARR-03 reinterpretado per D-16).
 *
 * D-16: 1 hero feature "Atendimento multicanal" full-width + 3 secundárias em row.
 * D-20: surface light off-white, seção mais limpa visualmente.
 * D-20.1: Agentes IA implícitos no mockup (NARR-08).
 * NARR-06: zero motion lib import.
 */
import { Container } from "@/components/ui/container";
import { ProductHeader } from "./ProductHeader";
import { ProductHeroFeature } from "./ProductHeroFeature";
import { ProductSecondaryGrid } from "./ProductSecondaryGrid";

export function Product() {
  return (
    <section
      id="produto"
      aria-labelledby="product-headline"
      className="relative bg-surface-light py-24 sm:py-28 lg:py-36"
    >
      <Container>
        <ProductHeader />
        <ProductHeroFeature />
        <ProductSecondaryGrid />
      </Container>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/sections/product-invariants.test.ts tests/content/product.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PRODUCT_COPY_VARIANTS" src/content/product.ts` returns >= 2.
    - `grep -c "v1: {" src/content/product.ts` returns >= 1.
    - `grep -c "v2: {" src/content/product.ts` returns >= 1.
    - `grep -c "v3: {" src/content/product.ts` returns >= 1.
    - `grep -c "Atendimento multicanal\\|Caixa de entrada multicanal" src/content/product.ts` returns >= 1.
    - `grep -cE "relat[óo]rios?" src/content/product.ts` returns 0.
    - `grep -cE "DISTRIBUIÇÃO|FOLLOW-UP|AGENDA" src/content/product.ts` returns >= 3.
    - `grep -c "routing-pill\\|timeline-3pts\\|calendar-grid" src/content/product.ts` returns >= 3.
    - `grep -c "@marina_souza" src/content/product.ts` returns >= 1 (inbox row consistent).
    - `grep -cE "intelig[êe]ncia artificial|neural" src/content/product.ts` returns 0.
    - `grep -c "use client" src/sections/Product/ProductSecondaryGrid.tsx` returns 1.
    - `grep -c "use client" src/sections/Product/ProductHeader.tsx` returns 0 (RSC).
    - `grep -c "use client" src/sections/Product/ProductSecondaryCard.tsx` returns 0 (RSC).
    - `grep -cE "from\\s+['\"]motion|from\\s+['\"]framer-motion|from\\s+['\"]@/components/motion" src/sections/Product/` (recursive) returns 0.
    - `grep -c "useInView" src/sections/Product/ProductSecondaryGrid.tsx` returns >= 1.
    - `grep -c "bg-surface-light" src/sections/Product/index.tsx` returns >= 1.
    - `grep -c "WhatsAppCta" src/sections/Product/` (recursive) returns 0 (D-29).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/sections/product-invariants.test.ts` exits 0.
    - `npx vitest run tests/content/product.test.ts` exits 0 — all 13 tests pass.
  </acceptance_criteria>
  <done>Product copy + orchestrator + header + secondary grid+card built. Hero feature still pending — Task 3.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ProductHeroFeature + ProductHeroFeatureMockup (split + central card + 2 overlays + iaLine)</name>
  <files>src/sections/Product/ProductHeroFeature.tsx, src/sections/Product/ProductHeroFeatureMockup.tsx</files>
  <read_first>
    - src/sections/Hero/HeroCardStack.tsx (scale-up pattern for central card + overlay pattern)
    - src/content/product.ts (Task 2 — PRODUCT_COPY shape canonical)
    - src/sections/Product/index.tsx (Task 2 — orchestrator imports this)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md (lines 270-328 + 640-668 mockup composition rules)
  </read_first>
  <behavior>
    ProductHeroFeature ("use client"):
    - 2-col grid `lg:grid-cols-[2fr_3fr]` desktop, `grid-cols-1` mobile, `gap-10 lg:gap-12`.
    - mt-20 lg:mt-24 (separation from header).
    - Left col: eyebrow + title + description (text-aligned left desktop, centered mobile).
    - Right col: ProductHeroFeatureMockup.
    - useInView({ threshold: 0.2 }) — applies hero-card-rise to LEFT col text block on enter (NOT the mockup central card — central card is "operational state final immediately"; only the entrance fade for the text bloc + overlays).
    - HERO-like principle scaled: large central mockup card lacks entrance animation; overlays may animate (overlays are "secondary detail").

    ProductHeroFeatureMockup (RSC, CSS-only animations):
    - Inner relative container.
    - Central card: w-[280px] sm:w-[420px] lg:w-[480px], bg-surface-card-strong (#fff), border + ring + 2-layer LIGHT shadow, radius 14px, padding p-5 sm:p-6.
    - Card header: row with title (PRODUCT_COPY.feature.mockup.cardTitle) + small label (PRODUCT_COPY.feature.mockup.cardLabel uppercase) + right-side new count badge (e.g., "3 novos") with hero-live-pulse dot.
    - Card body: 3 rows from PRODUCT_COPY.feature.mockup.inboxRows. Each row: 
      - icon channel: Instagram (rose-400) | MessageCircle (emerald-600) | Facebook (blue-500)
      - from (font-semibold text-[13px])
      - preview (text-[12.5px] text-text-secondary truncate)
      - meta (font-mono text-[10.5px] text-neutral-400)
      - status badge mini (text-[10px] pill bg-neutral-100 text-neutral-600 OR bg-emerald-50 text-emerald-700 for "new")
    - iaLine (D-20.1 micro-line): rendered after rows with `border-t border-neutral-100 mt-4 pt-3`. Format: `<p className="text-[10.5px] uppercase tracking-[0.12em] text-text-muted flex items-center gap-1.5"><Sparkles className="size-3" /> {PRODUCT_COPY.feature.mockup.iaLine}</p>`. NO neon, NO purple glow, NO gradient — pure muted text-muted neutral.
    - Overlay 1 (routing): absolute top-[-12px] right-[-20px] sm:right-[-40px], w-[160px] sm:w-[200px], rotate(4deg), bg-surface-card (#FBFCFD), p-3, radius 12px, content: "from → to" using lucide ArrowRight. Class `hero-card-float-a`.
    - Overlay 2 (confirmation): absolute bottom-[-16px] left-[-16px] sm:left-[-36px], w-[140px] sm:w-[180px], rotate(-3deg), p-3, content: "name · slot" using lucide CalendarCheck2 (emerald-600). Class `hero-card-float-c`.
    - ZERO purple/accent gradient background. ZERO neon. ZERO 3D rotation. ZERO browser chrome.

    PRODUCT_COPY supplies all text content; no string is hardcoded in JSX.
  </behavior>
  <action>
Step 1 — `src/sections/Product/ProductHeroFeatureMockup.tsx`:

```tsx
/**
 * ProductHeroFeatureMockup — RSC composição CSS-only.
 *
 * Central inbox card + 2 overlay cards (routing + confirmation).
 * Float animations CSS-only via hero-card-float-a/c (já existem em globals.css).
 *
 * D-20.1: iaLine como micro-elemento textual ("system event" style) — NÃO banner,
 * NÃO neon, NÃO glow. Sparkles lucide muito sutil + texto uppercase tracking generous
 * em text-muted. Tom Linear/Stripe, não Anthropic/OpenAI marketing.
 *
 * NARR-06: zero motion lib import; tudo CSS keyframes Phase 3.
 */
import { CalendarCheck2, Facebook, Instagram, MessageCircle, MoveRight, Sparkles } from "lucide-react";
import { PRODUCT_COPY } from "@/content/product";

const CHANNEL_ICONS = {
  instagram: { Icon: Instagram, color: "text-rose-400" },
  whatsapp: { Icon: MessageCircle, color: "text-emerald-600" },
  facebook: { Icon: Facebook, color: "text-blue-500" },
} as const;

export function ProductHeroFeatureMockup() {
  const { mockup } = PRODUCT_COPY.feature;
  return (
    <div aria-hidden="true" className="relative mx-auto w-full max-w-[480px]">
      {/* Central inbox card */}
      <article className="relative z-20 overflow-hidden rounded-[14px] border border-neutral-200/70 bg-surface-card-strong p-5 ring-1 ring-inset ring-white shadow-[0_20px_40px_-12px_rgba(8,12,24,0.15),0_4px_12px_-4px_rgba(8,12,24,0.08)] sm:p-6">
        <header className="flex items-center justify-between gap-3 pb-4 border-b border-neutral-100">
          <div>
            <div className="text-[10.5px] font-medium uppercase tracking-[0.12em] text-neutral-500">{mockup.cardLabel}</div>
            <h3 className="mt-1 text-[15px] font-semibold tracking-[-0.01em] text-neutral-900">{mockup.cardTitle}</h3>
          </div>
          <div className="flex items-center gap-1.5">
            <span aria-hidden="true" className="hero-live-pulse inline-block size-[5px] rounded-full bg-emerald-500" />
            <span className="text-[10.5px] font-medium tracking-tight text-emerald-700">{mockup.newCountBadge}</span>
          </div>
        </header>
        <ul className="mt-4 space-y-3">
          {mockup.inboxRows.map((row, i) => {
            const { Icon, color } = CHANNEL_ICONS[row.channel];
            const statusBadge =
              row.status === "new"
                ? { label: "novo", cls: "bg-emerald-50 text-emerald-700" }
                : row.status === "assigned"
                ? { label: "atribuído", cls: "bg-blue-50 text-blue-700" }
                : { label: "respondido", cls: "bg-neutral-100 text-neutral-600" };
            return (
              <li key={i} className="flex items-start gap-3">
                <Icon className={`mt-0.5 size-4 shrink-0 ${color}`} strokeWidth={2.25} />
                <div className="min-w-0 flex-1">
                  <div className="flex items-baseline justify-between gap-2">
                    <span className="text-[13px] font-semibold tracking-tight text-neutral-900">{row.from}</span>
                    {row.meta && <span className="font-mono text-[10.5px] tabular-nums text-neutral-400">{row.meta}</span>}
                  </div>
                  <p className="mt-0.5 truncate text-[12.5px] leading-[1.4] text-neutral-600">{row.preview}</p>
                </div>
                <span className={`shrink-0 rounded-md px-1.5 py-0.5 text-[10px] font-medium ${statusBadge.cls}`}>{statusBadge.label}</span>
              </li>
            );
          })}
        </ul>
        <p className="mt-4 flex items-center gap-1.5 border-t border-neutral-100 pt-3 text-[10.5px] uppercase tracking-[0.12em] text-text-muted">
          <Sparkles className="size-3 text-neutral-400" strokeWidth={2.25} />
          {mockup.iaLine}
        </p>
      </article>

      {/* Overlay 1 — routing top-right */}
      <div className="hero-card-float-a absolute -top-3 right-[-20px] z-30 w-[160px] rotate-[4deg] sm:right-[-40px] sm:w-[200px]" style={{ animationDelay: "0.6s" }}>
        <div className="rounded-[12px] border border-neutral-200/70 bg-surface-card p-3 ring-1 ring-inset ring-white/80 shadow-[0_12px_24px_-12px_rgba(8,12,24,0.18)]">
          <div className="text-[10px] font-medium uppercase tracking-[0.12em] text-neutral-500">DISTRIBUIÇÃO</div>
          <div className="mt-1.5 flex items-center gap-1.5 text-[12px] font-semibold text-neutral-900">
            <span>{mockup.overlayRouting.from}</span>
            <MoveRight className="size-3.5 text-violet-500" strokeWidth={2.5} />
            <span>{mockup.overlayRouting.to}</span>
          </div>
        </div>
      </div>

      {/* Overlay 2 — confirmation bottom-left */}
      <div className="hero-card-float-c absolute -bottom-4 left-[-16px] z-30 w-[140px] rotate-[-3deg] sm:left-[-36px] sm:w-[180px]" style={{ animationDelay: "1.2s" }}>
        <div className="rounded-[12px] border border-neutral-200/70 bg-surface-card p-3 ring-1 ring-inset ring-white/80 shadow-[0_12px_24px_-12px_rgba(8,12,24,0.18)]">
          <div className="flex items-center gap-1.5 text-[10px] font-medium uppercase tracking-[0.12em] text-neutral-500">
            <CalendarCheck2 className="size-3 text-emerald-600" strokeWidth={2.5} />
            AGENDADO
          </div>
          <div className="mt-1.5 text-[12px] font-semibold text-neutral-900">{mockup.overlayConfirm.name}</div>
          <div className="mt-0.5 font-mono text-[10.5px] tabular-nums text-neutral-500">{mockup.overlayConfirm.slot}</div>
        </div>
      </div>
    </div>
  );
}
```

Step 2 — `src/sections/Product/ProductHeroFeature.tsx`:

```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import { PRODUCT_COPY } from "@/content/product";
import { ProductHeroFeatureMockup } from "./ProductHeroFeatureMockup";

export function ProductHeroFeature() {
  const [ref, inView] = useInView<HTMLDivElement>({ threshold: 0.2 });
  const { feature } = PRODUCT_COPY;
  return (
    <div ref={ref} className="mt-20 grid grid-cols-1 items-center gap-10 lg:mt-24 lg:grid-cols-[2fr_3fr] lg:gap-12">
      <div className={`text-center lg:text-left ${inView ? "hero-card-rise" : "opacity-0"}`}>
        <div className="text-[11px] font-medium uppercase tracking-[0.18em] text-text-muted">{feature.eyebrow}</div>
        <h3 className="mt-3 text-balance text-2xl font-semibold leading-[1.15] tracking-[-0.02em] text-text-primary sm:text-3xl lg:text-4xl">
          {feature.title}
        </h3>
        <p className="mx-auto mt-4 max-w-md text-balance text-base leading-[1.6] text-text-secondary lg:mx-0">
          {feature.description}
        </p>
      </div>
      <div className={`flex justify-center lg:justify-end ${inView ? "hero-card-rise" : "opacity-0"}`} style={{ animationDelay: "150ms" }}>
        <ProductHeroFeatureMockup />
      </div>
    </div>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/sections/product-invariants.test.ts tests/content/product.test.ts && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "use client" src/sections/Product/ProductHeroFeature.tsx` returns 1.
    - `grep -c "use client" src/sections/Product/ProductHeroFeatureMockup.tsx` returns 0 (RSC).
    - `grep -c "useInView" src/sections/Product/ProductHeroFeature.tsx` returns >= 1.
    - `grep -c "PRODUCT_COPY.feature.mockup.iaLine" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1 (D-20.1 wiring).
    - `grep -c "Sparkles" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1.
    - `grep -c "hero-card-float-a" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1.
    - `grep -c "hero-card-float-c" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1.
    - `grep -c "hero-live-pulse" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1.
    - `grep -cE "bg-accent-primary|bg-gradient.*violet|bg-violet-[5-9]" src/sections/Product/ProductHeroFeatureMockup.tsx` returns 0 (zero purple/violet large backgrounds — D-20.1 anti-cyberpunk).
    - `grep -c "<Image" src/sections/Product/` (recursive) returns 0 (no next/image — all CSS).
    - `grep -c "bg-surface-card" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1 (overlays use card surface).
    - `grep -c "bg-surface-card-strong" src/sections/Product/ProductHeroFeatureMockup.tsx` returns >= 1 (central card hierarchy D-20).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/sections/product-invariants.test.ts tests/content/product.test.ts` exits 0.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <done>ProductHeroFeature + mockup ship with central inbox + 2 floating overlays + implicit AI micro-line. Zero buzzwords cyberpunk. D-20.1 satisfeito via micro-line muted (não banner, não seção).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Product copy review PR + Playwright premium-feel smoke</name>
  <files>src/content/product.ts (variante final aprovada substitui PRODUCT_COPY = PRODUCT_COPY_VARIANTS.vN)</files>
  <action>Lenny revisa as 3 variantes em src/content/product.ts via PR. Executor aguarda LGTM e ajusta a variante ativa. Pipeline pré-merge: code-review skill + Playwright MCP per CLAUDE.md user instructions.</action>
  <verify>
    <automated>npx vitest run tests/content/product.test.ts tests/sections/product-invariants.test.ts</automated>
  </verify>
  <done>Lenny approved a copy variant via PR comment or inline edit; executor merged with PRODUCT_COPY pointing to approved variant; Playwright screenshots attached to PR description; CI green.</done>
  <what-built>
    - 3 variantes contrastantes em `src/content/product.ts` (v1 operacional / v2 categórica / v3 editorial seco).
    - Product section LIGHT off-white: ProductHeader + ProductHeroFeature (split + central inbox + 2 overlays float + iaLine implícita IA) + ProductSecondaryGrid (3 cards com mini-mockups distintos).
    - Zero "Relatórios", zero "CRM" como pillar, zero cyberpunk IA hype.
  </what-built>
  <how-to-verify>
1. Pull branch, `npm run dev`.
2. `http://localhost:3000` → scroll até Product. Verificar:
   - Seção lê "mais limpa visualmente da landing" (D-20 — pressão alta no acabamento).
   - Hero feature mockup central renderiza inbox com 3 rows + 2 overlays flutuando sutilmente (hero-card-float-a/c).
   - iaLine ("Atribuído automaticamente · sugestão de resposta aceita" — variante v1) é VISÍVEL mas MUTED — micro-elemento editorial, não banner.
   - Secundárias 3 cards entram em stagger; mini-mockups distintos por kind (routing-pill / timeline-3pts / calendar-grid).
3. Lenny revisa as 3 variantes em `src/content/product.ts`:
   - LGTM v? OR edit inline.
   - **Pressão extra**: "Product section provavelmente vai definir se isso parece software sério ou landing de startup" (CONTEXT.md <specifics>). Lenny avalia tom premium luxury tech editorial (D-00).
4. Pipeline pré-merge:
   - **Code review skill**.
   - **Playwright MCP**: screenshots desktop (1280x800) + mobile (375x812). Verificar:
     - Mobile: hero feature stack vertical, mockup central com w-[280px], overlays não cortam fora do viewport.
     - Console JS sem erros.
     - reduced-motion: overlays não fazem float; estado final imediato.
     - Hover nas 3 secundárias: zero hover effect que dependa de hover (mobile-friendly).
5. Executor ajusta `PRODUCT_COPY = PRODUCT_COPY_VARIANTS.vN` e re-pusha.
6. Merge.
  </how-to-verify>
  <resume-signal>Lenny aprova variante; Playwright screenshots premium-feel anexados; merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser DOM | useInView in ProductHeroFeature + ProductSecondaryGrid |
| copy→DOM | PRODUCT_COPY rendered via React; status badges and channel icons are static lucide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-09 | Tampering | Future contributor adds "Relatórios" pillar back, reverting D-16 reinterpretation | mitigate | tests/content/product.test.ts Test 5 grep-rejects /relat[óo]rios?/i. |
| T-4-10 | Tampering | Future contributor introduces cyberpunk IA banner (neon glow, "AI-powered") reverting D-20.1 | mitigate | tests/content/product.test.ts Test 7 (cyberpunk vocab regex) + product-invariants grep for `bg-violet-[5-9]` / `bg-gradient.*violet` in Product/ JSX (added as Task 3 acceptance). |
| T-4-11 | Information Disclosure | Inbox mockup rows accidentally leak real client names / numbers | accept | Mock rows use fictional names (Marina Souza, Carla Mendes, Lucas Pereira). No real client data. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0.
- `npx vitest run tests/sections/product-invariants.test.ts tests/content/product.test.ts` exits 0.
- `npx next build` exits 0.
- Visual (Playwright): Product premium polish, mockup overlays float subtly, iaLine muted-visible, secundárias mini-mockups distintos.
</verification>

<success_criteria>
- NARR-03 reinterpretado satisfeito (D-16: 1 hero feature + 3 secundárias).
- NARR-08 satisfeito (D-20.1: implicit-AI-line in mockup).
- NARR-06 + NARR-07 satisfied.
- COPY-02 + COPY-03 satisfied.
- D-16 (no Relatórios, no CRM pillar) + D-20.1 (no cyberpunk) gated mecanicamente.
- Lenny approved copy via PR (Task 4 unlock).
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-03-SUMMARY.md` documenting:
- Variante final aprovada
- Hero feature mockup screenshot
- Mini-mockup variants para 3 secundárias (routing-pill / timeline-3pts / calendar-grid)
- iaLine final ("Atribuído automaticamente · sugestão de resposta aceita" ou aprovado)
- Premium-feel verdict do Lenny
</output>
</content>
</invoke>