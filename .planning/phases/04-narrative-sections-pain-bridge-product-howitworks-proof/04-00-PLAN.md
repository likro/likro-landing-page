---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/use-in-view.ts
  - src/content/glossary.ts
  - tests/landing/coherence.test.ts
  - src/sections/Pain/index.tsx
  - src/sections/Bridge/index.tsx
  - src/sections/Product/index.tsx
  - src/sections/HowItWorks/index.tsx
  - src/sections/Proof/index.tsx
  - src/app/page.tsx
  - docs/copy-review.md
autonomous: true
requirements:
  - COPY-05
  - NARR-06
threats:
  - T-4-01
  - T-4-02
must_haves:
  truths:
    - "useInView hook existe, é client-only, retorna [ref, inView] e respeita prefers-reduced-motion (returns true immediately)"
    - "CLINICA_GLOSSARY exporta termos verticais aprovados (paciente, agendamento, retorno, follow-up, estética, dermatologia, harmonização facial, odontologia, bem-estar, WhatsApp, Instagram, caixa de entrada multicanal, distribuição automática, agenda integrada)"
    - "src/app/page.tsx renderiza <Hero /> seguido de <Pain />, <Bridge />, <Product />, <HowItWorks />, <Proof /> nesta ordem exata"
    - "Cada section index.tsx existe como RSC scaffold com <section aria-labelledby> e h2 placeholder (será sobrescrito por plans 01-05)"
    - "tests/landing/coherence.test.ts roda e valida ordem das seções via parse de imports em src/app/page.tsx"
    - "tests/landing/coherence.test.ts valida zero <Image priority> em src/sections/{Pain,Bridge,Product,HowItWorks,Proof}/"
    - "docs/copy-review.md menciona cadência Phase 4 sequencial (Pain → Bridge → Product → HowItWorks → Proof)"
  artifacts:
    - path: "src/hooks/use-in-view.ts"
      provides: "Hook IO-based motion-free, ~30 lines"
      contains: 'IntersectionObserver'
      min_lines: 25
    - path: "src/content/glossary.ts"
      provides: "Termos verticais clínica aprovados (COPY-05)"
      contains: "CLINICA_GLOSSARY"
    - path: "tests/landing/coherence.test.ts"
      provides: "Cross-section ordering + no-priority-image global gate"
      contains: "page.tsx"
      min_lines: 40
    - path: "src/sections/Pain/index.tsx"
      provides: "Pain scaffold RSC <section aria-labelledby='pain-headline'>"
      contains: "pain-headline"
    - path: "src/sections/Bridge/index.tsx"
      provides: "Bridge scaffold RSC <section aria-labelledby='bridge-headline'>"
      contains: "bridge-headline"
    - path: "src/sections/Product/index.tsx"
      provides: "Product scaffold RSC <section aria-labelledby='product-headline'>"
      contains: "product-headline"
    - path: "src/sections/HowItWorks/index.tsx"
      provides: "HowItWorks scaffold RSC <section aria-labelledby='how-headline'>"
      contains: "how-headline"
    - path: "src/sections/Proof/index.tsx"
      provides: "Proof scaffold RSC <section aria-labelledby='proof-headline'>"
      contains: "proof-headline"
    - path: "src/app/page.tsx"
      provides: "Renderiza Header + Hero + Pain + Bridge + Product + HowItWorks + Proof na ordem"
      contains: "<Proof"
    - path: "docs/copy-review.md"
      provides: "Cadência Phase 4 documentada — sequencial Pain → Bridge → Product → HowItWorks → Proof"
      contains: "Phase 4"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/sections/{Pain,Bridge,Product,HowItWorks,Proof}/index.tsx"
      via: "named imports + RSC composition"
      pattern: 'from "@/sections/(Pain|Bridge|Product|HowItWorks|Proof)"'
    - from: "tests/landing/coherence.test.ts"
      to: "src/app/page.tsx"
      via: "fs.readFileSync + regex order check"
      pattern: "Hero[\\s\\S]*Pain[\\s\\S]*Bridge[\\s\\S]*Product[\\s\\S]*HowItWorks[\\s\\S]*Proof"
---

<objective>
Plan 04-00 estabelece a fundação compartilhada de Phase 4: o hook `useInView` motion-free (substituto de `<RevealOnView>` para o escopo Phase 4 inteiro), o glossário vertical canônico (COPY-05), o teste cross-section de coerência (ordering + no-priority guard), os scaffolds RSC mínimos das 5 seções (placeholder h2 + section semantics) e a wiring delas em `src/app/page.tsx` na ordem narrativa correta. Documenta a cadência D-17 aplicada ao Phase 4 (sequencial) em `docs/copy-review.md`.

Purpose: Resolve `Don't Hand-Roll` da research §455-481 (hook IO único centralizado), satisfaz NARR-06 reinterpretado (zero imports de `@/components/motion/` em sections — sections vão consumir `useInView` direto), e estabelece o "loading skeleton" de page.tsx (sections rendered, mas vazias) pra que cada section plan downstream só preencha seu próprio escopo sem precisar tocar em page.tsx novamente. Coherence test cravado HOJE pega regressão (ordem trocada, section removida) na primeira commit.

Output: 1 hook + 1 glossário + 1 coherence test + 5 scaffolds + page.tsx wireado + doc atualizado. Plans 01-05 ficam puramente "preencher uma seção" — orchestrator, sub-componentes, copy, keyframes, testes específicos.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/03-hero-benchmarked-isolado/03-CONTEXT.md

<interfaces>
<!-- Tipos / patterns existentes que esta plan consome. Não explorar codebase. -->

From src/components/ui/container.tsx:
```typescript
export function Container(props: { as?: "div"|"section"|"main"|"article"; className?: string; children: React.ReactNode }): JSX.Element;
// Renders: <Component className="mx-auto w-full max-w-7xl px-4 sm:px-6 lg:px-8 {className}" />
```

From src/components/ui/headline.tsx:
```typescript
export function Headline(props: { as?: "h1"|"h2"|"h3"; size?: "hero"|"section"|"sub"; className?: string; children: React.ReactNode; id?: string }): JSX.Element;
```

From src/app/globals.css (existing tokens — Phase 4 só consome):
- Surface: bg-surface-darker (#0A0F1A), bg-surface-dark (#0E1422), bg-surface-light (#fafaf9), bg-surface-lighter (#fff), bg-surface-card (#FBFCFD)
- Text dark: text-text-on-dark-primary (#F5F7FA), text-text-on-dark-secondary (#B6BDC9), text-text-on-dark-muted (#7F8896)
- Text light: text-text-primary (#0a0a0b), text-text-secondary (#3f3f46), text-text-muted (#71717a)
- Accent: text-accent-primary (#7c3aed) — APENAS em micro-elementos
- Keyframes existentes pra reuso por Plans 01-05: hero-headline-reveal, hero-card-rise, hero-card-float-a/b/c, hero-live-pulse, hero-haze-drift
- Global reduced-motion rule já zera animations (globals.css L99-107)

From src/app/page.tsx (atual):
```tsx
import { Header } from "@/components/layout/Header";
import { Hero } from "@/sections/Hero";

export default function HomePage() {
  return (
    <>
      <Header />
      <main>
        <Hero />
      </main>
    </>
  );
}
```

From docs/copy-review.md (existing — Phase 3 D-17 doc):
- Cadência async via PR seção-a-seção
- 3 variantes em src/content/<sec>.ts (v1/v2/v3 satisfies Record<...>)
- Lenny aprova via "LGTM vN" comment ou edit inline
- Merge unlocks next section

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useInView hook + cross-section coherence test (Wave 0 gates)</name>
  <files>src/hooks/use-in-view.ts, tests/hooks/use-in-view.test.ts, tests/landing/coherence.test.ts</files>
  <read_first>
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md (lines 449-481 — useInView pseudo-API and rationale)
    - src/hooks/use-device-tier.ts (existing hook pattern — but do NOT import motion/react; this new hook is motion-free)
    - tests/sections/hero-invariants.test.ts (Phase 3 grep test pattern using fs + regex, no shell grep — replicate this style)
    - src/app/page.tsx (current — coherence test parses this)
  </read_first>
  <behavior>
    useInView hook (`src/hooks/use-in-view.ts`):
    - Test 1: When `prefers-reduced-motion: reduce` matches, returns `[ref, true]` immediately on first render (no IO observation needed).
    - Test 2: When IO not yet observed (no reduced motion), returns `[ref, false]` initially.
    - Test 3: Hook is client-only — first line of file is `"use client";`.
    - Test 4: Hook signature: `useInView<T extends Element = HTMLDivElement>(opts?: { threshold?: number; rootMargin?: string; once?: boolean }): [React.RefObject<T>, boolean]`.
    - Test 5: With `once: true` (default), once `inView` becomes true it stays true (regression: never flips back to false).
    - Test 6: When ref is unmounted, observer is disconnected (cleanup in useEffect return).

    coherence test (`tests/landing/coherence.test.ts`):
    - Test 1: `src/app/page.tsx` imports — in order — Hero, Pain, Bridge, Product, HowItWorks, Proof (regex on import statements with line-number ordering).
    - Test 2: `src/app/page.tsx` JSX renders — in order — `<Hero`, `<Pain`, `<Bridge`, `<Product`, `<HowItWorks`, `<Proof`. Uses single multiline regex `/&lt;Hero[\s\S]*?&lt;Pain[\s\S]*?&lt;Bridge[\s\S]*?&lt;Product[\s\S]*?&lt;HowItWorks[\s\S]*?&lt;Proof/`.
    - Test 3: Zero `priority` JSX prop in any `.tsx` file under `src/sections/Pain/`, `src/sections/Bridge/`, `src/sections/Product/`, `src/sections/HowItWorks/`, `src/sections/Proof/` (regex `/\bpriority\b(?=\s*(?:\}|\/|>|=\s*\{))/g` minus `priority={false}` allowance — same regex Phase 3 invariant uses).
    - Test 4: Zero `framer-motion`, `motion/react`, or `@/components/motion/` imports in any `.tsx` under the 5 section dirs (NARR-06 reinterpretation per research §483-492). Regex: `/from\s+['"](?:framer-motion|motion\/react|@\/components\/motion)/`.
    - Test 5: Helper `walk()` returns `[]` for nonexistent directory (sanity check that the empty-tree case doesn't silently green-test).
  </behavior>
  <action>
Step 1 — Create `src/hooks/use-in-view.ts`:

```tsx
"use client";

import { useEffect, useRef, useState } from "react";

/**
 * Phase 4 — Hook IO-based motion-free.
 *
 * Substitui o uso de @/components/motion/RevealOnView em sections de Phase 4
 * (NARR-06 reinterpretation — vide 04-RESEARCH.md §483-492).
 *
 * Comportamento:
 * - prefers-reduced-motion: reduce → retorna [ref, true] imediatamente (estado final, sem motion).
 * - Sem reduced motion → IntersectionObserver com threshold default 0.2 e rootMargin "0px 0px -10% 0px".
 * - opts.once (default true): uma vez inView, nunca volta para false.
 * - SSR-safe: window/IntersectionObserver não acessado durante render server.
 */
export interface UseInViewOptions {
  threshold?: number;
  rootMargin?: string;
  once?: boolean;
}

export function useInView<T extends Element = HTMLDivElement>(
  opts: UseInViewOptions = {},
): [React.RefObject<T | null>, boolean] {
  const { threshold = 0.2, rootMargin = "0px 0px -10% 0px", once = true } = opts;
  const ref = useRef<T | null>(null);
  const [inView, setInView] = useState(false);

  useEffect(() => {
    // Reduced motion: skip IO entirely, return final state.
    if (typeof window !== "undefined" && window.matchMedia?.("(prefers-reduced-motion: reduce)").matches) {
      setInView(true);
      return;
    }
    const node = ref.current;
    if (!node || typeof IntersectionObserver === "undefined") return;

    const observer = new IntersectionObserver(
      (entries) => {
        const entry = entries[0];
        if (!entry) return;
        if (entry.isIntersecting) {
          setInView(true);
          if (once) observer.disconnect();
        } else if (!once) {
          setInView(false);
        }
      },
      { threshold, rootMargin },
    );

    observer.observe(node);
    return () => observer.disconnect();
  }, [threshold, rootMargin, once]);

  return [ref, inView];
}
```

Step 2 — Create `tests/hooks/use-in-view.test.ts` using vitest + @testing-library/react. Mock `matchMedia` and `IntersectionObserver` in setup. Cover the 6 behaviors above.

Required test imports (verbatim):
```typescript
import { describe, expect, it, vi, beforeEach, afterEach } from "vitest";
import { renderHook, act } from "@testing-library/react";
import { useInView } from "@/hooks/use-in-view";
```

Mock pattern (verbatim — set up in beforeEach):
```typescript
const matchMediaMock = vi.fn().mockImplementation((q: string) => ({
  matches: false, // override per-test
  media: q, addListener: vi.fn(), removeListener: vi.fn(),
  addEventListener: vi.fn(), removeEventListener: vi.fn(), dispatchEvent: vi.fn(),
}));
Object.defineProperty(window, "matchMedia", { writable: true, value: matchMediaMock });

class IOMock {
  observe = vi.fn();
  disconnect = vi.fn();
  unobserve = vi.fn();
  takeRecords = vi.fn();
  constructor(public cb: IntersectionObserverCallback) {}
  trigger(isIntersecting: boolean) { this.cb([{ isIntersecting } as IntersectionObserverEntry], this as any); }
}
const ioInstances: IOMock[] = [];
(globalThis as any).IntersectionObserver = vi.fn((cb: IntersectionObserverCallback) => {
  const inst = new IOMock(cb); ioInstances.push(inst); return inst;
});
```

Step 3 — Create `tests/landing/coherence.test.ts`. Replicate the Node-puro fs+regex pattern from `tests/sections/hero-invariants.test.ts` (no shell grep). Tests as defined in `<behavior>` above.

Required test file skeleton (verbatim header — keep stylistic consistency with hero-invariants.test.ts):
```typescript
import { describe, expect, it } from "vitest";
import fs from "node:fs";
import path from "node:path";

const SRC_DIR = path.resolve(__dirname, "../../src");
const PAGE_FILE = path.resolve(SRC_DIR, "app/page.tsx");
const SECTION_DIRS = ["Pain", "Bridge", "Product", "HowItWorks", "Proof"].map((s) =>
  path.resolve(SRC_DIR, "sections", s),
);
```

The coherence test must SKIP gracefully when `src/sections/Pain/` etc. don't exist yet (returning `[]` from walk) — same pattern Phase 3 uses (vide hero-invariants.test.ts:48-50). Test 1 (import order) MUST run only if all 5 section dirs exist; otherwise `it.skipIf(...)`. Tests 3-4 (zero priority / zero motion imports) run unconditionally — they default-pass when dirs are empty.
  </action>
  <verify>
    <automated>npx vitest run tests/hooks/use-in-view.test.ts tests/landing/coherence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File `src/hooks/use-in-view.ts` exists, line 1 is exactly `"use client";`.
    - `grep -c "IntersectionObserver" src/hooks/use-in-view.ts` returns >= 1.
    - `grep -c "matchMedia" src/hooks/use-in-view.ts` returns >= 1.
    - `grep -c 'from "motion' src/hooks/use-in-view.ts` returns 0 (motion-free).
    - File `tests/hooks/use-in-view.test.ts` exists, contains 6 `it(...)` blocks (one per Test 1-6 above).
    - File `tests/landing/coherence.test.ts` exists, contains tests covering: import order, JSX render order, zero priority, zero motion imports, walk-empty sanity.
    - `npx vitest run tests/hooks/use-in-view.test.ts` exits 0 with 6 passing tests.
    - `npx vitest run tests/landing/coherence.test.ts` exits 0 (Test 1/2 skip if section dirs absent at this stage; tests 3-5 pass).
  </acceptance_criteria>
  <done>useInView hook lands motion-free; coherence test enforces page.tsx ordering + NARR-06 zero-motion-import + no-priority across the 5 future section dirs starting from this commit.</done>
</task>

<task type="auto">
  <name>Task 2: Glossary, section RSC scaffolds, page.tsx wiring, copy-review cadence doc</name>
  <files>src/content/glossary.ts, src/sections/Pain/index.tsx, src/sections/Bridge/index.tsx, src/sections/Product/index.tsx, src/sections/HowItWorks/index.tsx, src/sections/Proof/index.tsx, src/app/page.tsx, docs/copy-review.md</files>
  <read_first>
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-05 atmosphere sequence, D-08 surface tokens, D-26 Proof categories)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md (lines 600-633 — glossary canonical content; lines 727-794 — folder structure)
    - src/app/page.tsx (current Header + Hero composition pattern)
    - src/sections/Hero/index.tsx (RSC orchestrator pattern: section/aria-labelledby/Container/min-h-svh)
    - docs/copy-review.md (existing Phase 3 cadence doc — append Phase 4 section)
    - src/components/ui/container.tsx, src/components/ui/headline.tsx (primitives to reuse)
  </read_first>
  <action>
Step 1 — Create `src/content/glossary.ts` with the exact canonical content from RESEARCH.md §605-631 (COPY-05). Content must be:

```typescript
/**
 * Glossário vertical Likro — termos aprovados para uso em copy das seções (COPY-05).
 *
 * Esta é a fonte canônica consultada na redação de cada variante de copy
 * (src/content/<sec>.ts). Importação direta NÃO é obrigatória — copy modules
 * podem usar as strings finais diretamente. Este arquivo serve como referência.
 *
 * Novos termos exigem aprovação Lenny via PR.
 */
export const CLINICA_GLOSSARY = {
  atendimento: "atendimento",
  agendamento: "agendamento",
  paciente: "paciente",
  retorno: "retorno",
  followUp: "follow-up",
  recepcao: "recepção",
  equipeAtendimento: "equipe de atendimento",
  lead: "lead",
  estetica: "estética",
  dermatologia: "dermatologia",
  harmonizacao: "harmonização facial",
  odontologia: "odontologia",
  bemEstar: "bem-estar",
  whatsapp: "WhatsApp",
  instagram: "Instagram",
  inboxMulticanal: "caixa de entrada multicanal",
  distribuicaoAutomatica: "distribuição automática",
  agendaIntegrada: "agenda integrada",
} as const;

export type ClinicaGlossaryKey = keyof typeof CLINICA_GLOSSARY;
```

Step 2 — Create 5 section scaffolds (RSC, minimal — these will be overwritten by Plans 01-05 but need to exist NOW for page.tsx to compile and coherence test to pass).

`src/sections/Pain/index.tsx` (DARK surface, scaffold):
```tsx
/**
 * Pain — Phase 4 section scaffold (Plan 04-00). Will be expanded by Plan 04-01.
 *
 * D-05 atmosphere: DARK (continua o peso de Hero, intensifica tensão).
 * D-09: fragmentação operacional sistêmica.
 * Surface: bg-surface-darker.
 */
import { Container } from "@/components/ui/container";

export function Pain() {
  return (
    <section
      id="pain"
      aria-labelledby="pain-headline"
      className="relative isolate overflow-hidden bg-surface-darker py-24 lg:py-32"
    >
      <Container>
        <h2
          id="pain-headline"
          className="text-balance text-3xl font-semibold tracking-tight text-text-on-dark-primary sm:text-4xl lg:text-5xl"
        >
          {/* Plan 04-01 substitui pelo copy real de PAIN_COPY.h2 */}
          Pain scaffold
        </h2>
      </Container>
    </section>
  );
}
```

`src/sections/Bridge/index.tsx` (LIGHT — primeira seção light, statement editorial puro D-13/D-15):
```tsx
/**
 * Bridge — Phase 4 section scaffold (Plan 04-00). Will be expanded by Plan 04-02.
 *
 * D-13/D-15: statement editorial silencioso puro, sem mockup. Transição cromática
 * dark→light entre Pain e Bridge É o efeito cinematográfico.
 * Surface: bg-surface-light.
 */
import { Container } from "@/components/ui/container";

export function Bridge() {
  return (
    <section
      id="bridge"
      aria-labelledby="bridge-headline"
      className="relative bg-surface-light py-24 lg:py-40"
    >
      <Container className="max-w-3xl text-center">
        <h2
          id="bridge-headline"
          className="text-balance text-2xl font-semibold tracking-tight text-text-primary sm:text-3xl lg:text-4xl"
        >
          {/* Plan 04-02 substitui pelo copy real de BRIDGE_COPY.statement */}
          Bridge scaffold
        </h2>
      </Container>
    </section>
  );
}
```

`src/sections/Product/index.tsx` (LIGHT off-white, mais limpa D-20):
```tsx
/**
 * Product — Phase 4 section scaffold (Plan 04-00). Will be expanded by Plan 04-03.
 *
 * D-16: 1 hero feature full-width + 3 secundárias em row.
 * D-20: mais limpa visualmente da landing. Surface off-white.
 * D-20.1: Agentes IA implícitos no mockup do hero feature (NARR-08).
 */
import { Container } from "@/components/ui/container";

export function Product() {
  return (
    <section
      id="produto"
      aria-labelledby="product-headline"
      className="relative bg-surface-light py-24 lg:py-32"
    >
      <Container>
        <h2
          id="product-headline"
          className="text-balance text-3xl font-semibold tracking-tight text-text-primary sm:text-4xl lg:text-5xl"
        >
          {/* Plan 04-03 substitui pelo copy real de PRODUCT_COPY.h2 */}
          Product scaffold
        </h2>
      </Container>
    </section>
  );
}
```

`src/sections/HowItWorks/index.tsx` (LIGHT neutral, timeline vertical D-21/D-22):
```tsx
/**
 * HowItWorks — Phase 4 section scaffold (Plan 04-00). Will be expanded by Plan 04-04.
 *
 * D-21: 4 passos timeline vertical. Surface light neutral.
 * D-22: lead chega → operação distribui → atendente conversa → paciente agendado.
 */
import { Container } from "@/components/ui/container";

export function HowItWorks() {
  return (
    <section
      id="how-it-works"
      aria-labelledby="how-headline"
      className="relative bg-surface-lighter py-24 lg:py-32"
    >
      <Container>
        <h2
          id="how-headline"
          className="text-balance text-3xl font-semibold tracking-tight text-text-primary sm:text-4xl lg:text-5xl"
        >
          {/* Plan 04-04 substitui pelo copy real de HOW_COPY.h2 */}
          HowItWorks scaffold
        </h2>
      </Container>
    </section>
  );
}
```

`src/sections/Proof/index.tsx` (DARK, categorias minimal D-24/D-26/D-28):
```tsx
/**
 * Proof — Phase 4 section scaffold (Plan 04-00). Will be expanded by Plan 04-05.
 *
 * D-24/D-28: institucional silencioso, dark editorial. O silêncio visual vale mais.
 * D-26: categorias verticais em row — Estética · Dermatologia · Harmonização Facial · Odontologia · Bem-estar.
 * D-27: PROIBIDO logos cliente, números fabricados, testimonials, Dolce Home.
 */
import { Container } from "@/components/ui/container";

export function Proof() {
  return (
    <section
      id="proof"
      aria-labelledby="proof-headline"
      className="relative bg-surface-darker py-24 lg:py-32"
    >
      <Container className="max-w-4xl text-center">
        <h2
          id="proof-headline"
          className="text-balance text-2xl font-semibold tracking-tight text-text-on-dark-primary sm:text-3xl lg:text-4xl"
        >
          {/* Plan 04-05 substitui pelo copy real de PROOF_COPY.h2 */}
          Proof scaffold
        </h2>
      </Container>
    </section>
  );
}
```

Step 3 — Update `src/app/page.tsx`. Replace the current body with:

```tsx
import { Header } from "@/components/layout/Header";
import { Hero } from "@/sections/Hero";
import { Pain } from "@/sections/Pain";
import { Bridge } from "@/sections/Bridge";
import { Product } from "@/sections/Product";
import { HowItWorks } from "@/sections/HowItWorks";
import { Proof } from "@/sections/Proof";

export default function HomePage() {
  return (
    <>
      <Header />
      <main>
        <Hero />
        <Pain />
        <Bridge />
        <Product />
        <HowItWorks />
        <Proof />
      </main>
    </>
  );
}
```

Step 4 — Append a Phase 4 section to `docs/copy-review.md`. New section content (append below existing Phase 3 doc, do not modify Phase 3 lines):

```markdown
## Phase 4 — Cadência sequencial (Pain → Bridge → Product → HowItWorks → Proof)

A cadência D-17 aplica-se **por seção e em ordem**. Razão (RESEARCH §585-599):
- D-17 explicitamente diz "copy review é gate, não polish de fim".
- Cada seção aprende da anterior (Pain estabelece tom de fragmentação; Bridge calibra contra Pain; Product calibra contra Bridge; etc).

**Sequência travada:**

1. Plan 04-01 (Pain) → PR com 3 variantes `src/content/pain.ts` → Lenny aprova → merge.
2. Plan 04-02 (Bridge) → idem após Pain merged.
3. Plan 04-03 (Product) → idem após Bridge merged.
4. Plan 04-04 (HowItWorks) → idem após Product merged.
5. Plan 04-05 (Proof) → idem após HowItWorks merged.

**Convenção por seção (idêntica à Phase 3):**
- `src/content/<sec>.ts` exporta `<SEC>_COPY_VARIANTS = { v1, v2, v3 } satisfies Record<string, <Sec>Copy>` e `<SEC>_COPY = <SEC>_COPY_VARIANTS.v1` (provisional ativo).
- 3 variantes contrastantes (não micro-ajustes); cabeçalho do arquivo documenta direção de cada variante.
- Aprovação Lenny via comment `LGTM v1`/`LGTM v2`/`LGTM v3` ou edit inline; após merge, variante aprovada vira `<SEC>_COPY` definitivo, demais ficam archived no git history.

**Anti-IA gate por seção:** test `tests/content/<sec>.test.ts` reproduz regex banned phrases + checa especificidade vertical (`clínica` presente quando aplicável).
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/landing/coherence.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "CLINICA_GLOSSARY" src/content/glossary.ts` returns >= 1.
    - `grep -c 'harmonização facial' src/content/glossary.ts` returns 1 (canonical spelling).
    - `grep -c 'caixa de entrada multicanal' src/content/glossary.ts` returns 1.
    - Each of the 5 files `src/sections/{Pain,Bridge,Product,HowItWorks,Proof}/index.tsx` exists and exports a named function matching the section name (regex `export function (Pain|Bridge|Product|HowItWorks|Proof)\(\)`).
    - `grep -c "aria-labelledby=\"pain-headline\"" src/sections/Pain/index.tsx` returns 1.
    - `grep -c "aria-labelledby=\"bridge-headline\"" src/sections/Bridge/index.tsx` returns 1.
    - `grep -c "aria-labelledby=\"product-headline\"" src/sections/Product/index.tsx` returns 1.
    - `grep -c "aria-labelledby=\"how-headline\"" src/sections/HowItWorks/index.tsx` returns 1.
    - `grep -c "aria-labelledby=\"proof-headline\"" src/sections/Proof/index.tsx` returns 1.
    - Pain section uses `bg-surface-darker`, Bridge uses `bg-surface-light`, Product uses `bg-surface-light`, HowItWorks uses `bg-surface-lighter`, Proof uses `bg-surface-darker` (D-05 atmospheric sequence).
    - `grep -E '<Hero|<Pain|<Bridge|<Product|<HowItWorks|<Proof' src/app/page.tsx | wc -l` returns >= 6.
    - In `src/app/page.tsx`, regex `/<Hero[\s\S]*?<Pain[\s\S]*?<Bridge[\s\S]*?<Product[\s\S]*?<HowItWorks[\s\S]*?<Proof/` matches.
    - `grep -c "Phase 4" docs/copy-review.md` returns >= 1.
    - `grep -c "Plan 04-01" docs/copy-review.md` returns >= 1.
    - `grep -c "Plan 04-02" docs/copy-review.md` returns >= 1.
    - `grep -c "Plan 04-03" docs/copy-review.md` returns >= 1.
    - `grep -c "Plan 04-04" docs/copy-review.md` returns >= 1.
    - `grep -c "Plan 04-05" docs/copy-review.md` returns >= 1.
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/landing/coherence.test.ts` exits 0 — all tests (including import order and JSX render order) now pass since all 5 section files exist.
  </acceptance_criteria>
  <done>Foundation laid: glossary canonical, 5 scaffolds rendering empty h2s in correct atmosphere (dark/light per D-05), page.tsx wireado, copy-review.md documenta cadência sequencial Phase 4, coherence test fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser (DOM API) | IntersectionObserver, matchMedia called from useInView — DOM-only, no user input |
| build→runtime | Section components compile from RSC + client islands; no env vars or external IO in this plan |
| filesystem→test | tests/landing/coherence.test.ts reads src/app/page.tsx + section dirs at test time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | use-in-view hook accidentally reintroduces motion lib | mitigate | tests/landing/coherence.test.ts Test 4 grep-rejects imports of `framer-motion`, `motion/react`, `@/components/motion/` in any section .tsx; hook file itself imports only from `react`. |
| T-4-02 | Denial of Service | useInView allocating observers without cleanup → memory leak | mitigate | Hook returns disconnect in useEffect cleanup; test 6 verifies disconnect called on unmount; `once: true` (default) disconnects on first intersection. |
| T-4-03 | Information Disclosure | Section components leak server-side state to client | accept | All section scaffolds are pure presentational RSC with hard-coded h2 placeholder — no data, no env, no user input. |
</threat_model>

<verification>
- `npx tsc --noEmit` exits 0 (typecheck on whole repo).
- `npx vitest run tests/hooks/use-in-view.test.ts tests/landing/coherence.test.ts` exits 0.
- `npx next build` exits 0 (validates RSC scaffolds + page.tsx wiring compile).
- Manual: open `http://localhost:3000` after `npm run dev`; visually confirm Hero + 5 empty section bands stacked with correct dark/light alternation per D-05.
</verification>

<success_criteria>
- `useInView` hook lands motion-free, IO-based, reduced-motion-aware, with 6 passing unit tests.
- `CLINICA_GLOSSARY` exported with 18 canonical vertical terms (COPY-05 satisfied at infrastructure level).
- 5 section scaffolds exist as RSC, each with correct surface token per D-05 atmosphere sequence.
- `src/app/page.tsx` renders Hero + 5 sections in narrative order; coherence test enforces this from this commit forward.
- `docs/copy-review.md` documents Phase 4 sequential cadence; downstream plans (01-05) reference this doc.
- NARR-06 reinterpretation locked in via grep test (zero motion lib imports in any section).
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-00-SUMMARY.md` documenting:
- Hook API and usage example
- Glossary exports
- Page.tsx render order
- Coherence test scope (what regressions it catches)
- Phase 4 cadence locked in copy-review.md
</output>
</content>
</invoke>