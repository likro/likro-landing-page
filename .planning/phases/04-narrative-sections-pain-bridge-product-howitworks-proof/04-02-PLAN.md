---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 02
type: execute
wave: 3
depends_on: ["04-00", "04-01"]
files_modified:
  - src/content/bridge.ts
  - tests/content/bridge.test.ts
  - tests/sections/bridge-invariants.test.ts
  - src/sections/Bridge/index.tsx
  - src/sections/Bridge/BridgeStatement.tsx
autonomous: false
requirements:
  - NARR-02
  - NARR-06
  - NARR-07
  - COPY-02
threats:
  - T-4-07
must_haves:
  truths:
    - "Bridge section renderiza statement editorial silencioso puro — sem mockup, sem cards, sem CTA (D-13/D-15 reinterpretação NARR-02)"
    - "Surface LIGHT (bg-surface-light) — primeira seção light da landing; transição cromática dark→light entre Pain e Bridge É o efeito cinematográfico (D-15)"
    - "Composição minimalista: max-w-3xl centered, muito padding vertical (py-32 lg:py-40), tipografia centrada peso editorial"
    - "Statement entra com fade-in suave via useInView quando seção entra viewport (clip-path mask reveal estilo hero-headline-reveal OR fade-up suave)"
    - "BRIDGE_COPY contém frase forte 1-2 linhas (D-13). Estilo Linear/Stripe editorial. SEM 'transforme sua', SEM 'potencialize', SEM 'próximo nível' (D-14 + COPY-02)"
    - "Zero mockup, zero ScrollScene (NARR-02 reinterpretado — transição cromática É o efeito cinematográfico, não componente)"
    - "Zero CTA WhatsApp (D-29)"
    - "Mobile reflow via Tailwind: padding y-24 mobile / y-40 desktop, max-w-3xl mantido com px-6 sm:px-8"
    - "Lenny aprovou variante de copy via PR (D-17)"
  artifacts:
    - path: "src/content/bridge.ts"
      provides: "BRIDGE_COPY 3 variantes statement editorial"
      contains: "BRIDGE_COPY_VARIANTS"
      min_lines: 50
    - path: "tests/content/bridge.test.ts"
      provides: "Contract tests: shape, anti-IA explicit (D-14 banned phrases extras), no Dolce Home, 3 variants"
    - path: "tests/sections/bridge-invariants.test.ts"
      provides: "Grep gates: zero motion, zero motion-lib import, zero vh, zero priority, zero hardcoded PT-BR JSX"
    - path: "src/sections/Bridge/index.tsx"
      provides: "Bridge orchestrator RSC, light surface, max-w-3xl centered, renders BridgeStatement"
      contains: "BridgeStatement"
    - path: "src/sections/Bridge/BridgeStatement.tsx"
      provides: "'use client' statement reveal via useInView"
      contains: "useInView"
  key_links:
    - from: "src/sections/Bridge/BridgeStatement.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView for clip-path reveal trigger"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/Bridge/index.tsx"
      to: "src/content/bridge.ts"
      via: "BRIDGE_COPY import"
      pattern: 'from "@/content/bridge"'
---

<objective>
Implementar Bridge — primeira seção LIGHT da landing — como statement editorial silencioso puro (D-13/D-15). A reinterpretação D-15 é central: NARR-02 ROADMAP original previa mockup dashboard surgindo via ScrollScene; o CONTEXT.md confirma que isso foi REINTERPRETADO — a própria transição cromática dark→light entre Pain e Bridge é o efeito cinematográfico. Bridge não tem mockup, não tem cards, não tem CTA. É 1-2 linhas centradas em tipografia editorial, com muito espaço em branco, max-w-3xl centered.

Purpose: NARR-02 satisfeito pela reinterpretação locked (D-15). NARR-06 satisfeito (zero motion imports). COPY-02 satisfeito (gate explícito contra "transforme sua", "potencialize", "próximo nível", "leve sua operação", além das já banidas em todas as sections). D-29 satisfeito (zero CTA).

Output: 1 copy module com 3 variantes editoriais contrastantes + Bridge orchestrator RSC + BridgeStatement client island consumindo useInView pra reveal cinematográfico. Único elemento animado da seção é a entrada do próprio statement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-00-SUMMARY.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-01-SUMMARY.md

<interfaces>
From src/hooks/use-in-view.ts (Plan 04-00):
```typescript
export function useInView<T extends Element = HTMLDivElement>(opts?: { threshold?: number; rootMargin?: string; once?: boolean }): [React.RefObject<T | null>, boolean];
```

From src/app/globals.css (existing, Phase 3 — reuso):
- Keyframe `hero-headline-reveal` (clip-path inset(0 100% 0 0) → inset(0 0 0 0), opacity 0 → 1; 0.8s ease-premium-out both) — class `.hero-headline-reveal` aplica.
- Surface light tokens: bg-surface-light, text-text-primary, text-text-secondary.

From src/components/ui/container.tsx: Container with max-w-7xl default; override via className="max-w-3xl".

</interfaces>

<copy_guidance>
CONTEXT D-13/D-14 + RESEARCH §244-260:

Princípio: statement editorial silencioso, estilo Linear/Stripe. 1-2 linhas, frase forte, SEM promessa milagrosa.

PROIBIDO explicitamente (D-14 + COPY-02 banned phrases regex):
- "transforme sua clínica"
- "potencialize seu atendimento"
- "leve sua operação ao próximo nível"
- "desbloqueie", "revolucione", "empodere"
- "solução inovadora", "jornada do cliente"

3 variantes contrastantes:

v1 — Recusa silenciosa do estado atual (referência D-15 Lenny):
  statement: "Existe um jeito de operar isso sem rodar 4 apps abertos. Sem perder lead. Sem mandar a equipe procurar onde está a conversa."
  Tom: três cortes secos negando o estado atual. Recusa, não promessa.

v2 — Afirmação operacional contida:
  statement: "A operação da clínica em um sistema só. A equipe em uma tela só. O paciente sem repetir a história."
  Tom: tríade de afirmações contidas, Linear-style.

v3 — Frase única editorial:
  statement: "Existe outra forma de operar — e ela cabe em uma tela."
  Tom: single-line peso editorial, ar de manifesto curto.

Notas:
- D-09 D-12 NÃO se aplicam aqui (Bridge é frase, não headline interrogativa). Bridge pode mencionar "clínica" ou "operação" — não obrigatório.
- Cada variante tem campo `eyebrow` opcional? **Não.** Bridge é statement puro; eyebrow adicionaria estrutura demais. Frase única ou tríade nada mais.
</copy_guidance>

<visual_guidance>
RESEARCH §240-260 + CONTEXT D-15:

Composição minimal:
- Section bg-surface-light (off-white #fafaf9). Padding `py-32 lg:py-40` (muito ar).
- Container override `max-w-3xl mx-auto px-6 sm:px-8` (estreito, centrado).
- Statement: text size grande (text-2xl sm:text-3xl lg:text-4xl), peso semibold ou medium (não bold — peso editorial), tracking tight (-0.02em), leading 1.25, text-text-primary, balance.
- Statement com 2-3 sentenças cortadas por `.` → cada sentença em sua própria linha visual (CSS pode separar via `<br />` em JSX OR usar `display: block` no `:not(:last-child)` se for span). Sintaxe limpa: dividir em array de strings no copy module, renderizar `{statements.map(s => <p>{s}</p>)}`.

Alternativa de estrutura no copy:
```ts
export type BridgeCopy = {
  statements: ReadonlyArray<string>; // 1-3 frases
};
```

Animação:
- BridgeStatement client island consome `useInView({ threshold: 0.4 })`.
- Cada `<p>` recebe classe `inView ? "hero-headline-reveal" : "opacity-0"` + `style={{ animationDelay: \`${i * 200}ms\` }}` (stagger entre sentenças).
- Cinematográfico clip-path mask reveal da esquerda → direita (reusa keyframe `hero-headline-reveal`).
- Reduced motion globally já trata.

Único elemento animado da seção. ZERO mockup. ZERO card. ZERO scrollscene component. ZERO CTA. ZERO accent purple (Bridge não tem destaque colorido — é puro neutro editorial).

Mobile: padding reduzido para `py-24`. Tipografia desce para text-2xl. Container max permanece `max-w-3xl` mas px-6 (vs px-8 desktop).
</visual_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — bridge-invariants + bridge copy contract tests</name>
  <files>tests/sections/bridge-invariants.test.ts, tests/content/bridge.test.ts</files>
  <read_first>
    - tests/sections/pain-invariants.test.ts (Plan 04-01 — clone structure, swap Pain → Bridge)
    - tests/content/pain.test.ts (Plan 04-01 — clone, adapt to BRIDGE_COPY shape)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-14 banned phrase additions)
  </read_first>
  <behavior>
    bridge-invariants.test.ts: 6 gates mirroring pain-invariants but pointing at `src/sections/Bridge/` PLUS a structural test verifying that `src/sections/Bridge/BridgeStatement.tsx` renders BRIDGE_COPY.statements inside an `<h2>` element with `id="bridge-headline"` (regex: file contains both `<h2` and `id="bridge-headline"` AND `BRIDGE_COPY.statements`, AND does NOT contain `sr-only` h2 boilerplate). This enforces B4 fix permanently.

    bridge.test.ts copy contracts:
    - Test 1: BRIDGE_COPY shape — `statements: ReadonlyArray<string>` length 1-3.
    - Test 2: BRIDGE_COPY_VARIANTS has v1, v2, v3 keys.
    - Test 3: Anti-IA banned phrases — uses SAME regex as Pain/Hero PLUS extra D-14 specific patterns: `/transforme sua|potencialize|próximo nível|leve sua? .+ a outro patamar|solução inovadora|revolucione|empodere/i` — checks against joined `statements`.
    - Test 4: No mention of Dolce Home.
    - Test 5: No mention of "WhatsApp" inline in statements (Bridge não é canal-specific, statement é abstração).
    - Test 6: Each statement string length between 20 and 200 chars (frase forte, não micro-fragment, não parágrafo).
  </behavior>
  <action>
Step 1 — Create `tests/sections/bridge-invariants.test.ts` by literal clone of `tests/sections/pain-invariants.test.ts`. Replace all `Pain` → `Bridge` and `pain` → `bridge` identifiers. Path constant: `BRIDGE_DIR = path.resolve(SRC_DIR, "sections/Bridge")`. Keep 6 tests.

Step 2 — Create `tests/content/bridge.test.ts`. Header comment:
```ts
/**
 * Phase 4 / Plan 04-02 — Bridge copy contracts.
 *
 * Bridge é statement editorial. Não tem h2/sub/cards. Forma: `statements: string[]`.
 * Gate extra D-14: além das banned phrases padrão, rejeita também "transforme sua",
 * "leve a outro patamar", e variações de promessa milagrosa.
 */
```

Required regex (verbatim):
```ts
const BANNED_PHRASES = /(desbloqueie|desbloque|potencialize|potencia|transforme sua|próximo nível|proximo nivel|solução inovadora|solucao inovadora|jornada do cliente|do início ao fim|do inicio ao fim|feito para você|feito para voce|leve seu? .+ a outro patamar|revolucione|empodere)/i;
const D14_EXTRA = /(potencialize|transforme sua|próximo nível|solução inovadora|revolucione|empodere|leve.{1,30}outro patamar)/i;
```

Tests as defined in `<behavior>`. Each test uses `it.skipIf(!fs.existsSync(...))` for graceful skip until Task 2 lands copy.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/bridge-invariants.test.ts tests/content/bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `tests/sections/bridge-invariants.test.ts` exists, has >= 6 `it(...)` blocks, all referencing `BRIDGE_DIR` or `sections/Bridge`.
    - `tests/content/bridge.test.ts` exists, has >= 6 `it(...)` blocks.
    - `grep -c "BRIDGE_COPY_VARIANTS" tests/content/bridge.test.ts` returns >= 1.
    - `grep -cE "transforme sua|potencialize|próximo nível" tests/content/bridge.test.ts` returns >= 1 (D-14 extra gate present).
    - `npx vitest run tests/sections/bridge-invariants.test.ts` exits 0.
    - `npx vitest run tests/content/bridge.test.ts` exits 0 (skips while Bridge copy not yet present, OR errors with "Cannot find module" — both acceptable in same commit).
  </acceptance_criteria>
  <done>Bridge Wave 0 gates planted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BRIDGE_COPY (3 variants) + Bridge components</name>
  <files>src/content/bridge.ts, src/sections/Bridge/index.tsx, src/sections/Bridge/BridgeStatement.tsx</files>
  <read_first>
    - src/content/pain.ts (Plan 04-01 — convention for 3-variant header comment)
    - src/sections/Pain/PainStatement.tsx (Plan 04-01 — pattern for useInView + hero-headline-reveal)
    - src/sections/Bridge/index.tsx (Plan 04-00 scaffold — overwrite)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-13/D-14/D-15 + <specifics> Bridge phrasing example)
  </read_first>
  <action>
Step 1 — Create `src/content/bridge.ts`:

```ts
/**
 * Phase 4 / Plan 04-02 — Bridge copy module.
 *
 * Bridge = statement editorial silencioso puro (D-13/D-15). NÃO tem h2/sub/cards.
 * Forma: `statements: readonly string[]` — 1-3 frases cortadas por `.`.
 *
 * v1 — Recusa silenciosa em tríade (referência D-15 Lenny):
 *   "Existe um jeito de operar isso sem rodar 4 apps abertos."
 *   "Sem perder lead. Sem mandar a equipe procurar onde está a conversa."
 *
 * v2 — Afirmação operacional contida em tríade:
 *   "A operação da clínica em um sistema só."
 *   "A equipe em uma tela só."
 *   "O paciente sem repetir a história."
 *
 * v3 — Single-line editorial:
 *   "Existe outra forma de operar — e ela cabe em uma tela."
 *
 * COPY-02 + D-14: zero "transforme sua", "potencialize", "próximo nível", "revolucione".
 * D-27: zero menção a Dolce Home.
 */
export type BridgeCopy = {
  statements: ReadonlyArray<string>;
};

export const BRIDGE_COPY_VARIANTS = {
  v1: {
    statements: [
      "Existe um jeito de operar isso sem rodar 4 apps abertos.",
      "Sem perder lead. Sem mandar a equipe procurar onde está a conversa.",
    ],
  },
  v2: {
    statements: [
      "A operação da clínica em um sistema só.",
      "A equipe em uma tela só.",
      "O paciente sem repetir a história.",
    ],
  },
  v3: {
    statements: ["Existe outra forma de operar — e ela cabe em uma tela."],
  },
} as const satisfies Record<"v1" | "v2" | "v3", BridgeCopy>;

export const BRIDGE_COPY: BridgeCopy = BRIDGE_COPY_VARIANTS.v1;
```

Step 2 — Create `src/sections/Bridge/BridgeStatement.tsx`:

```tsx
"use client";
import { Fragment } from "react";
import { useInView } from "@/hooks/use-in-view";
import { BRIDGE_COPY } from "@/content/bridge";

/**
 * BridgeStatement — único elemento animado de Bridge.
 *
 * O statement editorial É o h2 real (id="bridge-headline"). Não há heading sintético
 * sr-only — a frase forte é o heading da seção (D-13 statement editorial silencioso
 * + SEO: heading real contém copy real, não boilerplate).
 *
 * Quando há tríade de statements (v1 ou v2), são concatenados dentro do mesmo <h2>
 * separados por <br /> (cada frase em uma linha visual, mas semanticamente um único
 * heading). Stagger de animation-delay aplicado a cada frase via <span> wrapper.
 *
 * NARR-06 reinterpretado: zero motion lib import; CSS-only via useInView + keyframe Phase 3.
 */
export function BridgeStatement() {
  const [ref, inView] = useInView<HTMLHeadingElement>({ threshold: 0.4 });
  return (
    <h2
      ref={ref}
      id="bridge-headline"
      className="font-sans text-3xl sm:text-4xl lg:text-5xl font-medium leading-[1.15] tracking-[-0.02em] text-text-primary max-w-3xl mx-auto text-center text-balance"
    >
      {BRIDGE_COPY.statements.map((s, i) => (
        <Fragment key={i}>
          <span
            className={`inline-block ${inView ? "hero-headline-reveal" : "opacity-0"}`}
            style={{ animationDelay: `${i * 200}ms` }}
          >
            {s}
          </span>
          {i < BRIDGE_COPY.statements.length - 1 && <br />}
        </Fragment>
      ))}
    </h2>
  );
}
```

Step 3 — Overwrite `src/sections/Bridge/index.tsx`:

```tsx
/**
 * Bridge — Phase 4 § Bridge (NARR-02 reinterpretado).
 *
 * D-13/D-15: statement editorial silencioso PURO. Sem mockup, sem cards, sem CTA.
 * A transição cromática Pain DARK → Bridge LIGHT É o efeito cinematográfico.
 * Primeira seção LIGHT da landing.
 *
 * NARR-06: zero motion lib import; única animação é o reveal do statement via useInView.
 * D-29: zero CTA.
 *
 * SEO/a11y: o próprio statement É o h2 visível (id="bridge-headline").
 * NÃO usar h2 sr-only sintético — a frase editorial real é o heading.
 * BridgeStatement renderiza o h2 internamente.
 */
import { Container } from "@/components/ui/container";
import { BridgeStatement } from "./BridgeStatement";

export function Bridge() {
  return (
    <section
      id="bridge"
      aria-labelledby="bridge-headline"
      className="relative bg-surface-light py-24 sm:py-32 lg:py-40"
    >
      <Container className="max-w-3xl text-center">
        <BridgeStatement />
      </Container>
    </section>
  );
}
```

NOTA: Bridge usa o próprio statement editorial como h2 visível (id="bridge-headline"). NÃO há h2 sr-only sintético — a frase real É o heading (melhora SEO + evita boilerplate heading que contradiz D-13). BridgeStatement renderiza um único `<h2>` contendo todos os statements concatenados (com `<br />` entre frases quando há tríade).
  </action>
  <verify>
    <automated>npx vitest run tests/sections/bridge-invariants.test.ts tests/content/bridge.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "BRIDGE_COPY_VARIANTS" src/content/bridge.ts` returns >= 2.
    - `grep -c "v1: {" src/content/bridge.ts` returns >= 1.
    - `grep -c "v2: {" src/content/bridge.ts` returns >= 1.
    - `grep -c "v3: {" src/content/bridge.ts` returns >= 1.
    - `grep -E "Existe um jeito de operar isso sem rodar 4 apps abertos" src/content/bridge.ts` matches (v1 verbatim Lenny phrase).
    - `grep -cE "transforme sua|potencialize|próximo nível" src/content/bridge.ts` returns 0.
    - `grep -c "Dolce Home" src/content/bridge.ts` returns 0.
    - `grep -c "use client" src/sections/Bridge/BridgeStatement.tsx` returns 1.
    - `grep -c "useInView" src/sections/Bridge/BridgeStatement.tsx` returns >= 1.
    - `grep -c "hero-headline-reveal" src/sections/Bridge/BridgeStatement.tsx` returns >= 1.
    - `grep -c "<h2" src/sections/Bridge/BridgeStatement.tsx` returns >= 1 (statement IS the h2).
    - `grep -c 'id="bridge-headline"' src/sections/Bridge/BridgeStatement.tsx` returns 1.
    - `grep -c "sr-only" src/sections/Bridge/index.tsx` returns 0 (no synthetic boilerplate heading — B4 fix).
    - `grep -c "BRIDGE_COPY.statements" src/sections/Bridge/BridgeStatement.tsx` returns >= 1.
    - `grep -cE "from\\s+['\"]motion|from\\s+['\"]framer-motion|from\\s+['\"]@/components/motion" src/sections/Bridge/` (any file) returns 0.
    - `grep -c "bg-surface-light" src/sections/Bridge/index.tsx` returns >= 1.
    - `grep -c "WhatsAppCta" src/sections/Bridge/` (any file) returns 0 (D-29).
    - `grep -c "max-w-3xl" src/sections/Bridge/index.tsx` returns >= 1.
    - `npx vitest run tests/sections/bridge-invariants.test.ts tests/content/bridge.test.ts` exits 0 — all tests pass.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>Bridge ships as statement-only LIGHT section, single client island consumes useInView + reused Phase 3 keyframe. Transição cromática Pain→Bridge funcional (Pain vinheta fade-to-light alinha com bg-surface-light de Bridge).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Bridge copy review PR + Playwright cromatic transition smoke</name>
  <files>src/content/bridge.ts (variante final aprovada substitui BRIDGE_COPY = BRIDGE_COPY_VARIANTS.vN)</files>
  <action>Lenny revisa as 3 variantes em src/content/bridge.ts via PR. Executor aguarda LGTM e ajusta a variante ativa. Pipeline pré-merge: code-review skill + Playwright MCP per CLAUDE.md user instructions.</action>
  <verify>
    <automated>npx vitest run tests/content/bridge.test.ts tests/sections/bridge-invariants.test.ts</automated>
  </verify>
  <done>Lenny approved a copy variant via PR comment or inline edit; executor merged with BRIDGE_COPY pointing to approved variant; Playwright screenshots attached to PR description; CI green.</done>
  <what-built>
    - 3 variantes editoriais de statement em `src/content/bridge.ts`.
    - Bridge section LIGHT minimalista: max-w-3xl centered, 1-3 frases em tipografia editorial, fade-in via clip-path mask reveal.
    - Transição cromática Pain DARK → Bridge LIGHT visível (vinheta inferior de Pain alinha com bg-surface-light de Bridge).
  </what-built>
  <how-to-verify>
1. Pull branch, `npm run dev`.
2. `http://localhost:3000` → scroll Hero → Pain → **transição cromática** para Bridge: o fade dark→light do fim do Pain alinha visualmente com o início do Bridge (sem corte abrupto, sem barra de cor diferente entre eles).
3. Bridge entra: statement em clip-path mask reveal (cada frase staggers 200ms).
4. Lenny revisa 3 variantes em `src/content/bridge.ts`:
   - v1 (recusa em tríade, referência Lenny D-15)
   - v2 (afirmação contida em tríade)
   - v3 (single-line editorial)
   - Aprova via `LGTM v?` comment OR edit inline.
5. Pipeline pré-merge:
   - **Code review skill**.
   - **Playwright MCP**: screenshot da transição Pain→Bridge em desktop + mobile. Reduced-motion test: ativar `prefers-reduced-motion: reduce` no DevTools → statement entra em estado final sem animation.
6. Executor ajusta `BRIDGE_COPY = BRIDGE_COPY_VARIANTS.vN` e re-pusha.
7. Merge.
  </how-to-verify>
  <resume-signal>Lenny aprova variante; Playwright screenshots da transição cromática anexados; merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser DOM | useInView in BridgeStatement |
| copy→DOM | BRIDGE_COPY rendered via React, no innerHTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-07 | Information Disclosure | BRIDGE_COPY mentions client name or fabricated stat | mitigate | tests/content/bridge.test.ts Tests 4-5 grep-reject /dolce home/i; D-14/COPY-02 regex catches buzzwords. |
| T-4-08 | Tampering | Future contributor adds mockup component to Bridge (re-introducing NARR-02 original) | mitigate | tests/sections/bridge-invariants.test.ts Tests 4-5 reject priority Image + hardcoded JSX PT-BR strings; visual review via Task 3 catches structural drift. |
</threat_model>

<verification>
- `npx vitest run tests/sections/bridge-invariants.test.ts tests/content/bridge.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- `npx next build` exits 0.
- Visual: cromatic transition Pain→Bridge seamless (Pain vinheta fade-to-light meets Bridge bg-surface-light without color discontinuity).
</verification>

<success_criteria>
- NARR-02 satisfied via D-15 reinterpretation locked: cromatic transition IS the cinematic effect; zero mockup, zero ScrollScene component.
- NARR-06 satisfied: zero motion lib imports.
- COPY-02 + D-14 satisfied: zero banned phrases (extra-strict regex green).
- D-29: zero CTA.
- Lenny approved copy via PR (Task 3 unlock).
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-02-SUMMARY.md`:
- Variante final aprovada
- Statement structure (single line vs tríade)
- Playwright screenshots da transição cromática
- Confirmation: D-15 reinterpretation in production
</output>
</content>
</invoke>