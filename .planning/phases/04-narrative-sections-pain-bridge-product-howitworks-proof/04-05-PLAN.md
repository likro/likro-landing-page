---
phase: 04-narrative-sections-pain-bridge-product-howitworks-proof
plan: 05
type: execute
wave: 6
depends_on: ["04-00", "04-04"]
files_modified:
  - src/content/proof.ts
  - tests/content/proof.test.ts
  - tests/sections/proof-invariants.test.ts
  - src/sections/Proof/index.tsx
  - src/sections/Proof/ProofBackground.tsx
  - src/sections/Proof/ProofCategories.tsx
autonomous: false
requirements:
  - NARR-05
  - NARR-06
  - NARR-07
  - COPY-02
  - COPY-05
  - COPY-06
threats:
  - T-4-13
  - T-4-14
must_haves:
  truths:
    - "Proof section renderiza eyebrow (uppercase mini) + headline editorial + row de 5 categorias verticais minimal — D-24/D-26/D-28"
    - "Categorias verticais EXATAS: Estética · Dermatologia · Harmonização Facial · Odontologia · Bem-estar (D-26) separadas por dot `·` OU lucide icons MUITO sutis"
    - "Surface DARK editorial (bg-surface-darker), tom institucional silencioso (D-28)"
    - "Zero logo de cliente, zero números fabricados, zero testimonials, zero badges 'trusted by', zero 'as seen in' (D-27)"
    - "Zero menção a Dolce Home (D-27 + STATE.md decisão 2026-05-18)"
    - "Zero claim sem fonte: zero regex /\\+\\d+\\s*(clínicas|leads|mensagens)|\\d+%/ em PROOF_COPY (COPY-06)"
    - "Headline candidate: 'Em operação em clínicas que dependem de atendimento de alto volume.' OU 'Infraestrutura operacional para clínicas de estética e dermatologia.' (D-25 — Claude escolhe melhor entre variantes)"
    - "Categorias entram com fade sequential (stagger 100ms) via useInView quando seção entra viewport"
    - "Mobile: categorias flex-wrap justify-center, dots `·` ainda separam (D-26)"
    - "Zero CTA WhatsApp (D-29 default + Phase 5 cuida do CTA final)"
    - "Lenny aprovou variante de copy via PR (D-17)"
  artifacts:
    - path: "src/content/proof.ts"
      provides: "PROOF_COPY 3 variantes — {eyebrow, headline, categories: tuple 5}"
      contains: "PROOF_COPY_VARIANTS"
      min_lines: 60
    - path: "tests/content/proof.test.ts"
      provides: "Contract tests + EXTRA D-27 gates: zero Dolce Home, zero stat numbers, zero 'trusted by' lookalikes"
      min_lines: 60
    - path: "tests/sections/proof-invariants.test.ts"
      provides: "Standard 6 grep gates + EXTRA: zero next/image + zero Image priority + zero logo file imports"
    - path: "src/sections/Proof/index.tsx"
      provides: "RSC orchestrator bg-surface-darker + ProofBackground + eyebrow/headline + ProofCategories"
      contains: "ProofCategories"
    - path: "src/sections/Proof/ProofBackground.tsx"
      provides: "RSC dark austere — gradient sutil, grid opacity baixíssima, SEM glow, SEM accent"
    - path: "src/sections/Proof/ProofCategories.tsx"
      provides: "'use client' row de 5 categorias com stagger via useInView"
      contains: "useInView"
  key_links:
    - from: "src/sections/Proof/ProofCategories.tsx"
      to: "src/hooks/use-in-view.ts"
      via: "useInView stagger trigger"
      pattern: 'from "@/hooks/use-in-view"'
    - from: "src/sections/Proof/index.tsx"
      to: "src/content/proof.ts"
      via: "PROOF_COPY import"
      pattern: 'from "@/content/proof"'
---

<objective>
Implementar Proof — última seção da Phase 4, dark editorial institucional silencioso. Comunica credibilidade pelo refinamento visual + tom contido (D-28) — NÃO por logos, números, testimonials. Direção locked (D-24/D-26): categorias verticais minimal em row, separadas por dot `·`. Cinco categorias exatas: Estética / Dermatologia / Harmonização Facial / Odontologia / Bem-estar (D-26).

Dolce Home NÃO está autorizado (STATE.md 2026-05-18 decisão final do Lenny). Proof segue caminho genérico sem nome específico de cliente.

Purpose: NARR-05 satisfeito via categorias verticais minimal + tom institucional silencioso (D-24/D-28); copy genérico sem citar cliente (Dolce Home explicitamente excluído). COPY-06 satisfeito (zero claims numéricos sem fonte — gate mecânico via regex). COPY-05 satisfeito (categorias referenciam glossário CLINICA_GLOSSARY do Plan 04-00). NARR-06/07 satisfeitos.

Output: 1 copy module com 3 variantes + 3 components + 2 tests. Componente mais leve da Phase 4 — Proof é silêncio editorial.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-RESEARCH.md
@.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-04-SUMMARY.md
@.planning/STATE.md

<interfaces>
From src/hooks/use-in-view.ts: same API used by Plans 01-04.

From src/content/glossary.ts (Plan 04-00):
```typescript
export const CLINICA_GLOSSARY = {
  estetica: "estética",
  dermatologia: "dermatologia",
  harmonizacao: "harmonização facial",
  odontologia: "odontologia",
  bemEstar: "bem-estar",
  // ...
} as const;
```

From src/app/globals.css:
- hero-card-rise (reuse for category stagger entrance)
- Surface dark: bg-surface-darker, text-text-on-dark-primary, text-text-on-dark-secondary, text-text-on-dark-muted
- Border on dark: border-border-on-dark-subtle, border-border-on-dark-default

From src/sections/Hero/HeroBackground.tsx: dark layered gradient pattern (Proof reaproveita simplified — sem haze, sem accent).

</interfaces>

<copy_guidance>
CONTEXT D-24..D-28 + STATE.md 2026-05-18 Dolce Home decision:

Structure:
```ts
export type ProofCopy = {
  eyebrow: string; // ex: "EM OPERAÇÃO" uppercase tracking-generous
  headline: string;
  categories: readonly [string, string, string, string, string]; // EXATAS D-26
};
```

3 variantes contrastantes (D-25 candidate headlines + variations):

v1 — Categorias verticais minimal:
  eyebrow: "EM OPERAÇÃO"
  headline: "Infraestrutura operacional para clínicas de estética e dermatologia."
  categories: ["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"]

v2 — Volume operacional silencioso:
  eyebrow: "JÁ EM USO"
  headline: "Em operação em clínicas que dependem de atendimento de alto volume."
  categories: ["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"]

v3 — Constatação editorial:
  eyebrow: "INFRAESTRUTURA"
  headline: "A camada operacional já roda em clínicas reais — todos os dias."
  categories: ["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"]

NOTA crítica: AS CATEGORIAS SÃO IDÊNTICAS NAS 3 VARIANTES (D-26 locked — não negociar a lista). Só eyebrow + headline variam.

PROIBIDO em todas (D-27 + STATE.md):
- "Dolce Home" (caso especial — autorização explicitamente negada 2026-05-18)
- Nome de QUALQUER cliente específico
- Números fabricados ("+500 clínicas", "+10mil leads", "98% satisfação", "milhares de mensagens/mês")
- Quotes/testimonials (com ou sem atribuição)
- "Trusted by", "as seen in", "in partnership with", "powered by"
- Badges visuais que sugiram social proof não-existente
- "Líder de mercado", "referência", "primeiro/melhor/único"
- "Em parceria com Likro" / "Likro Partner" / etc

PERMITIDO:
- "Em operação", "já em uso", "infraestrutura", "atendimento de alto volume", "clínicas reais"
- Categorias verticais (D-26 list locked)
</copy_guidance>

<visual_guidance>
CONTEXT D-24/D-26/D-28 + RESEARCH §390-420:

Section orchestrator (Proof/index.tsx):
- bg-surface-darker DARK (D-05 atmosphere — Proof fecha a landing em peso editorial autoridade).
- py-24 lg:py-32.
- Container max-w-4xl centered (D-26 row de categorias merece espaço respiro mas não 7xl — D-28 "silêncio visual vale mais").

ProofBackground (RSC, austere):
- Reaproveita HeroBackground L1 (base atmospheric gradient) + L4 (grid técnico opacity 0.015) somente — SEM L2/L3 radial accent purple/blue, SEM haze (Proof é silêncio, não foco visual).
- Vinheta superior leve pra costurar com HowItWorks (light) → Proof (dark): top gradient `linear-gradient(to bottom, rgba(255,255,255,0.0) 0%, rgba(10,15,26,1) 60%)`.

Header bloc:
- eyebrow: uppercase tracking-[0.22em] text-[11px] text-text-on-dark-muted, centered.
- headline mt-4 max-w-3xl mx-auto: text-balance text-2xl sm:text-3xl lg:text-4xl font-semibold leading-[1.2] tracking-[-0.02em] text-text-on-dark-primary, centered.
- id="proof-headline" on h2.

ProofCategories ("use client"):
- useInView({ threshold: 0.4 }).
- Container `mt-12 lg:mt-16`.
- Layout: `flex flex-wrap items-center justify-center gap-4 lg:gap-8`.
- For each category in PROOF_COPY.categories: render `<span>` with class `inView ? "hero-card-rise" : "opacity-0"` + inline `style={{ animationDelay: \`${i * 100}ms\` }}`. Text-[14px] lg:text-[15px] font-medium tracking-tight text-text-on-dark-secondary.
- Between categories: render `<span aria-hidden="true" class="text-text-on-dark-muted">·</span>` separator (NOT after the last). Mobile flex-wrap means separators wrap naturally; that's acceptable.
- ZERO icons by default (D-26 — "pode ter ícones lucide sutis OU só texto separado por dot"; we choose TEXT-ONLY for v1, mais editorial).
- ZERO accent roxo (Proof não tem accent visual — D-28 silêncio).

PROIBIDO:
- next/image em qualquer ponto.
- Logo strip.
- Badges / pills coloridos.
- Quotes em itálico.
- Numbers display (`+500` / `98%`).
</visual_guidance>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — proof invariants + proof copy contract tests (extra D-27 gates)</name>
  <files>tests/sections/proof-invariants.test.ts, tests/content/proof.test.ts</files>
  <read_first>
    - tests/sections/how-it-works-invariants.test.ts (Plan 04-04 — clone)
    - tests/content/how-it-works.test.ts (Plan 04-04 — adapt)
    - .planning/STATE.md (Dolce Home decision lines 78-83)
    - .planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-CONTEXT.md (D-27 prohibitions)
  </read_first>
  <behavior>
    proof-invariants.test.ts: 6 standard gates + 2 EXTRA gates:
    - Test 7 (EXTRA): Zero `<Image` JSX in `src/sections/Proof/**/*.tsx` (no next/image — categorias são texto puro, D-26).
    - Test 8 (EXTRA): Zero `from "@/logos"` or any logo asset import (D-27 zero logos).

    proof.test.ts contracts:
    - Test 1: Shape — `{eyebrow, headline, categories: tuple 5}`.
    - Test 2: VARIANTS has v1, v2, v3.
    - Test 3: ALL THREE variants have categories EXACTLY `["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"]` (D-26 locked).
    - Test 4: Anti-IA banned phrases regex green.
    - Test 5: Zero `/dolce\s*home/i` in any variant (STATE.md decision 2026-05-18).
    - Test 6: Zero stat numbers — regex `/\+\d+|\d+%|\d{2,}\s*(clínicas|leads|mensagens|atendimentos)/i` returns no matches in joined corpus (COPY-06 / D-27).
    - Test 7: Zero `/trusted by|as seen in|em parceria|líder de mercado|referência (do|de)|primeir[oa] no|melhor/i` (D-27 buzzword anti-social-proof).
    - Test 8: Zero testimonial pattern — joined corpus does not match regex `/[“”].+?[“”]\s*[—-]\s*\S+/` (quoted text + em-dash + attribution = fake testimonial). Editorial scare-quotes are allowed.
    - Test 9: Each variant eyebrow is uppercase format (regex `/^[A-ZÀ-Ú\s]+$/`) length 6-20.
    - Test 10: Each variant headline length in [40, 150] (frase forte, não micro).
  </behavior>
  <action>
Step 1 — `tests/sections/proof-invariants.test.ts`: clone how-it-works-invariants with Proof swap. Add Tests 7 & 8 (extra).

Step 2 — `tests/content/proof.test.ts`. Header documents D-27 + STATE.md extra gates:
```ts
/**
 * Phase 4 / Plan 04-05 — Proof copy contracts.
 *
 * Gates EXTRA (D-27 + STATE.md 2026-05-18):
 * - Zero Dolce Home (autorização explicitamente negada — STATE.md).
 * - Zero stat numbers (regex /\+\d+|\d+%|\d{2,}\s*(clínicas|leads|...)/i).
 * - Zero testimonials/quotes.
 * - Zero "trusted by", "em parceria", "líder de mercado", "referência (do/de)".
 * - Categorias verticais TRAVADAS: ["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"] (D-26).
 */
```

Implement 10 tests as defined.
  </action>
  <verify>
    <automated>npx vitest run tests/sections/proof-invariants.test.ts tests/content/proof.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both test files exist.
    - `proof-invariants.test.ts` has >= 8 `it()` (6 standard + 2 extra).
    - `proof.test.ts` has >= 10 `it()` blocks.
    - `grep -c "dolce" tests/content/proof.test.ts` returns >= 1 (case-insensitive gate present).
    - `grep -cE "\\\\+\\\\d|\\\\d%" tests/content/proof.test.ts` returns >= 1 (stat number gate).
    - `grep -c "trusted by" tests/content/proof.test.ts` returns >= 1.
    - `grep -c "Estética.*Dermatologia.*Harmonização Facial.*Odontologia.*Bem-estar" tests/content/proof.test.ts` returns >= 1 (locked list reference).
    - `npx vitest run` on both exits 0.
  </acceptance_criteria>
  <done>Proof Wave 0 gates planted — D-27 + STATE.md mechanically enforced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PROOF_COPY (3 variants) + Proof components</name>
  <files>src/content/proof.ts, src/sections/Proof/index.tsx, src/sections/Proof/ProofBackground.tsx, src/sections/Proof/ProofCategories.tsx</files>
  <read_first>
    - src/content/how-it-works.ts (Plan 04-04 — 3-variant pattern)
    - src/sections/Hero/HeroBackground.tsx (austere dark pattern to adapt)
    - src/sections/Proof/index.tsx (Plan 04-00 scaffold — overwrite)
  </read_first>
  <action>
Step 1 — Create `src/content/proof.ts`:

```ts
/**
 * Phase 4 / Plan 04-05 — Proof copy module.
 *
 * D-24/D-26/D-28: institucional silencioso dark editorial.
 * D-27 + STATE.md 2026-05-18: zero Dolce Home, zero logos, zero números fabricados,
 * zero testimonials, zero "trusted by"-likes. Credibilidade vem do tom contido.
 *
 * v1 — Categorias verticais minimal (D-25 candidato a):
 *   headline: "Infraestrutura operacional para clínicas de estética e dermatologia."
 *
 * v2 — Volume operacional silencioso (D-25 candidato b):
 *   headline: "Em operação em clínicas que dependem de atendimento de alto volume."
 *
 * v3 — Constatação editorial:
 *   headline: "A camada operacional já roda em clínicas reais — todos os dias."
 *
 * Categorias TRAVADAS (D-26): ["Estética", "Dermatologia", "Harmonização Facial", "Odontologia", "Bem-estar"]
 * — idênticas nas 3 variantes.
 *
 * COPY-05: categorias referenciam termos do glossário CLINICA_GLOSSARY (Plan 04-00).
 * COPY-06: zero claims numéricos sem fonte.
 */
export type ProofCopy = {
  eyebrow: string;
  headline: string;
  categories: readonly [string, string, string, string, string];
};

const CATEGORIES: ProofCopy["categories"] = [
  "Estética",
  "Dermatologia",
  "Harmonização Facial",
  "Odontologia",
  "Bem-estar",
];

export const PROOF_COPY_VARIANTS = {
  v1: {
    eyebrow: "EM OPERAÇÃO",
    headline: "Infraestrutura operacional para clínicas de estética e dermatologia.",
    categories: CATEGORIES,
  },
  v2: {
    eyebrow: "JÁ EM USO",
    headline: "Em operação em clínicas que dependem de atendimento de alto volume.",
    categories: CATEGORIES,
  },
  v3: {
    eyebrow: "INFRAESTRUTURA",
    headline: "A camada operacional já roda em clínicas reais — todos os dias.",
    categories: CATEGORIES,
  },
} as const satisfies Record<"v1" | "v2" | "v3", ProofCopy>;

export const PROOF_COPY: ProofCopy = PROOF_COPY_VARIANTS.v1;
```

Step 2 — `src/sections/Proof/ProofBackground.tsx`:

```tsx
/**
 * ProofBackground — dark editorial AUSTERE.
 *
 * D-28: silêncio visual vale mais que exagero. SEM accent roxo, SEM haze, SEM
 * radial glow. Apenas base gradient + grid quase imperceptível + transição
 * top que costura HowItWorks (light) → Proof (dark) sem corte abrupto.
 *
 * MOTION-08: zero animation; CSS layered backgrounds. RSC.
 */
export function ProofBackground() {
  return (
    <div aria-hidden="true" className="pointer-events-none absolute inset-0 -z-10">
      {/* L1 — top transition fade do light HowItWorks pro dark Proof */}
      <div className="absolute inset-x-0 top-0 h-1/3" style={{ background: "linear-gradient(to bottom, rgba(255,255,255,0.0) 0%, rgba(10,15,26,1) 100%)" }} />
      {/* L2 — base atmospheric gradient blue-black */}
      <div className="absolute inset-0" style={{ background: "linear-gradient(180deg, rgba(10,15,26,1) 0%, rgba(14,20,34,1) 50%, rgba(10,15,26,1) 100%)" }} />
      {/* L3 — grid técnico opacity 0.012 (mais baixo que Pain — austere) */}
      <div className="absolute inset-0" style={{
        backgroundImage: "linear-gradient(to right, rgba(255,255,255,0.012) 1px, transparent 1px), linear-gradient(to bottom, rgba(255,255,255,0.012) 1px, transparent 1px)",
        backgroundSize: "72px 72px",
      }} />
    </div>
  );
}
```

Step 3 — `src/sections/Proof/ProofCategories.tsx`:

```tsx
"use client";
import { useInView } from "@/hooks/use-in-view";
import { PROOF_COPY } from "@/content/proof";

/**
 * ProofCategories — D-26 row verticais minimal.
 *
 * Stagger 100ms via useInView. Separados por dot `·` text-text-on-dark-muted.
 * Mobile: flex-wrap mantém visual de "row" (D-26 + RESEARCH §535).
 *
 * NARR-06: CSS-only via useInView + keyframe Phase 3.
 */
export function ProofCategories() {
  const [ref, inView] = useInView<HTMLUListElement>({ threshold: 0.4 });
  return (
    <ul
      ref={ref}
      className="mt-12 flex flex-wrap items-center justify-center gap-x-4 gap-y-3 lg:mt-16 lg:gap-x-8"
    >
      {PROOF_COPY.categories.map((cat, i) => (
        <li key={cat} className="flex items-center gap-3 lg:gap-6">
          <span
            className={`text-[14px] font-medium tracking-tight text-text-on-dark-secondary lg:text-[15px] ${inView ? "hero-card-rise" : "opacity-0"}`}
            style={{ animationDelay: `${i * 100}ms` }}
          >
            {cat}
          </span>
          {i < PROOF_COPY.categories.length - 1 && (
            <span aria-hidden="true" className="text-text-on-dark-muted">·</span>
          )}
        </li>
      ))}
    </ul>
  );
}
```

Step 4 — Overwrite `src/sections/Proof/index.tsx`:

```tsx
/**
 * Proof — Phase 4 § Proof (NARR-05).
 *
 * D-24/D-26/D-28: dark institucional silencioso, categorias verticais minimal.
 * D-27 + STATE.md 2026-05-18: zero Dolce Home, zero logos, zero números fabricados.
 * D-05: surface DARK — fecha a landing em peso autoridade.
 * NARR-06: zero motion lib.
 */
import { Container } from "@/components/ui/container";
import { PROOF_COPY } from "@/content/proof";
import { ProofBackground } from "./ProofBackground";
import { ProofCategories } from "./ProofCategories";

export function Proof() {
  return (
    <section
      id="proof"
      aria-labelledby="proof-headline"
      className="relative isolate overflow-hidden bg-surface-darker py-24 lg:py-32"
    >
      <ProofBackground />
      <Container className="relative z-10 max-w-4xl text-center">
        <div className="text-[11px] font-medium uppercase tracking-[0.22em] text-text-on-dark-muted">
          {PROOF_COPY.eyebrow}
        </div>
        <h2
          id="proof-headline"
          className="mx-auto mt-4 max-w-3xl text-balance text-2xl font-semibold leading-[1.2] tracking-[-0.02em] text-text-on-dark-primary sm:text-3xl lg:text-4xl"
        >
          {PROOF_COPY.headline}
        </h2>
        <ProofCategories />
      </Container>
    </section>
  );
}
```
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run tests/sections/proof-invariants.test.ts tests/content/proof.test.ts && npx next build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "PROOF_COPY_VARIANTS" src/content/proof.ts` returns >= 2.
    - `grep -c "Dolce" src/content/proof.ts` returns 0 (case-sensitive — STATE.md).
    - `grep -cE "\\+\\d|\\d%|\\d{2,}\\s+(clínicas|leads|mensagens)" src/content/proof.ts` returns 0 (no stat numbers).
    - `grep -c "Estética" src/content/proof.ts` returns >= 1.
    - `grep -c "Dermatologia" src/content/proof.ts` returns >= 1.
    - `grep -c "Harmonização Facial" src/content/proof.ts` returns >= 1.
    - `grep -c "Odontologia" src/content/proof.ts` returns >= 1.
    - `grep -c "Bem-estar" src/content/proof.ts` returns >= 1.
    - `grep -cE "trusted by|as seen in|em parceria|líder de mercado" src/content/proof.ts` returns 0.
    - `grep -c "use client" src/sections/Proof/ProofCategories.tsx` returns 1.
    - `grep -c "use client" src/sections/Proof/ProofBackground.tsx` returns 0 (RSC).
    - `grep -c "useInView" src/sections/Proof/ProofCategories.tsx` returns >= 1.
    - `grep -c "hero-card-rise" src/sections/Proof/ProofCategories.tsx` returns >= 1.
    - `grep -c "bg-surface-darker" src/sections/Proof/index.tsx` returns >= 1.
    - `grep -c "<Image" src/sections/Proof/` (recursive) returns 0.
    - `grep -cE "bg-accent-primary|accent-glow" src/sections/Proof/` (recursive) returns 0 (zero accent — D-28).
    - `grep -cE "from\\s+['\"]motion|from\\s+['\"]framer-motion|from\\s+['\"]@/components/motion" src/sections/Proof/` returns 0.
    - `grep -c "WhatsAppCta" src/sections/Proof/` returns 0 (D-29).
    - `npx tsc --noEmit` exits 0.
    - `npx vitest run tests/sections/proof-invariants.test.ts tests/content/proof.test.ts` exits 0.
    - `npx next build` exits 0.
  </acceptance_criteria>
  <done>Proof ships com dark institucional silencioso, 5 categorias verticais minimal locked D-26, zero D-27 violations, zero Dolce Home. Phase 4 visualmente completa após este task.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Proof copy review PR + Playwright full-landing smoke (Phase 4 close)</name>
  <files>src/content/proof.ts (variante final aprovada substitui PROOF_COPY = PROOF_COPY_VARIANTS.vN)</files>
  <action>Lenny revisa as 3 variantes em src/content/proof.ts via PR. Executor aguarda LGTM e ajusta a variante ativa. Pipeline pré-merge: code-review skill + Playwright MCP per CLAUDE.md user instructions.</action>
  <verify>
    <automated>npx vitest run tests/content/proof.test.ts tests/sections/proof-invariants.test.ts</automated>
  </verify>
  <done>Lenny approved a copy variant via PR comment or inline edit; executor merged with PROOF_COPY pointing to approved variant; Playwright screenshots attached to PR description; CI green.</done>
  <what-built>
    - 3 variantes em `src/content/proof.ts` (v1 categorias minimal / v2 volume silencioso / v3 constatação editorial).
    - Proof DARK editorial: eyebrow + headline + row de 5 categorias verticais.
    - Background austere — sem accent, sem glow, sem haze. Transição top costura HowItWorks light → Proof dark sem corte.
    - **Phase 4 fechada após merge**: Hero (dark) → Pain (dark) → Bridge (light) → Product (light off-white) → HowItWorks (light neutral) → Proof (dark) sequência atmosférica D-05 em produção.
  </what-built>
  <how-to-verify>
1. `npm run dev` → scroll full landing top→bottom.
2. Verificar a sequência D-05 visualmente:
   - Hero dark → Pain dark (continuidade peso) → Bridge **transição cromática** dark→light (corte editorial D-15) → Product light off-white (mais limpa) → HowItWorks light neutral (explicativo, NÃO compete) → Proof **transição cromática** light→dark (fade top do ProofBackground) e fecha em autoridade silenciosa.
   - Em Proof: eyebrow uppercase tracking-generoso, headline editorial, 5 categorias com stagger 100ms separadas por `·`.
3. Lenny revisa 3 variantes em `src/content/proof.ts`:
   - LGTM v? OR edit inline.
   - **Tom check**: D-28 silêncio editorial — Proof tem que parecer "essa infraestrutura já está sendo usada", NÃO "olha quantos clientes temos".
4. Pipeline pré-merge:
   - **Code review skill**.
   - **Playwright MCP**: screenshots full-page desktop + mobile + screenshot específica das duas transições cromáticas (Pain→Bridge e HowItWorks→Proof). Reduced-motion test em toda a página.
   - **5-second test** opcional (3 pessoas sem contexto descrevem em 5s do início Hero ao fim Proof: "isso é Likro, é pra clínicas, parece sério/premium").
5. Executor ajusta `PROOF_COPY = PROOF_COPY_VARIANTS.vN` e re-pusha.
6. Merge → **Phase 4 está fechada**. Próxima Phase é Phase 5 (Form + Footer + Floating + CTAs distribuídos).
  </how-to-verify>
  <resume-signal>Lenny aprova variante; Playwright full-landing screenshots anexados; Phase 4 fechada via merge.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→browser DOM | useInView in ProofCategories |
| copy→DOM | PROOF_COPY rendered via React |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-13 | Information Disclosure | PROOF_COPY accidentally mentions Dolce Home (autorização negada STATE.md 2026-05-18) | mitigate | tests/content/proof.test.ts Test 5 grep-rejects /dolce\s*home/i. |
| T-4-14 | Tampering | Future contributor adds fabricated stat ("+500 clínicas", "98% retenção") OR logo strip | mitigate | tests/content/proof.test.ts Test 6 (stat number regex) + Test 7 (trusted-by lookalikes) + proof-invariants Test 7-8 (zero <Image>, zero logo imports). |
| T-4-15 | Repudiation | Lenny later authorizes Dolce Home and contributor needs to opt-in | accept | Documented in 04-05-SUMMARY.md "Reavaliar em milestone posterior se autorização mudar" — process change requires updating STATE.md + this gate. |
</threat_model>

<verification>
- `npx vitest run tests/sections/proof-invariants.test.ts tests/content/proof.test.ts` exits 0.
- `npx tsc --noEmit` exits 0.
- `npx next build` exits 0.
- Visual full-landing: D-05 atmosphere sequence preserved end-to-end via Playwright screenshots.
</verification>

<success_criteria>
- NARR-05 satisfied via institutional silence (D-24/D-28) — categories minimal, zero fabricated proof.
- NARR-06 + NARR-07 satisfied.
- COPY-02 + COPY-05 (categorias usam termos do glossário) + COPY-06 (zero claims) satisfied.
- D-27 + STATE.md 2026-05-18 (zero Dolce Home) satisfied mecanicamente.
- D-26 categorias travadas em 3 variantes.
- Lenny approved copy via PR.
- **Phase 4 fechada** — 5 seções narrativas em produção, sequência atmosférica D-05 preservada.
</success_criteria>

<output>
After completion, create `.planning/phases/04-narrative-sections-pain-bridge-product-howitworks-proof/04-05-SUMMARY.md`:
- Variante final aprovada
- Phase 4 wrap-up: lista todas as 5 sections em produção
- Playwright full-landing screenshots (desktop + mobile)
- Confirmação D-05 atmosphere sequence preservada
- Hand-off para Phase 5 (Form + Footer + Floating + CTAs distribuídos)
</output>
</content>
</invoke>